---
title: Controllability-Aware Adversarial Examples Against LLM-Based Network Traffic Classifiers
title_zh: 针对基于大语言模型的网络流量分类器的具备可控性感知的对抗样本攻击
authors: Zhenpeng Li
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07739v1"
tags: ["query:q3"]
score: 8.4
relevance_score: 9.0
quality_score: 8.5
reliability_score: 8.0
practicality_score: 7.5
evidence: 基于LLM的网络流量分类与对抗鲁棒性
tldr: 本文研究受现实攻击约束下，LLM 网络流量分类器的对抗鲁棒性问题。作者提出可控性意识黑盒迁移攻击框架，将流量特征划分为直接可控、间接可控和不可控类别，仅扰动攻击者可操作特征。通过五个 IDS 数据集、七个 LLM、两类传统模型及超过50万对抗样本实验发现，LLM 存在显著迁移攻击风险，但依赖数据集和模型类型。论文提出的问题具有安全应用价值，实验规模较大，值得进一步细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07739v1/fig-001.webp\", \"caption\": \"TABLE VI ML TARGET ASR UNDER BLACK-BOX TRANSFER USING AN XGBOOST SURROGATE, REPORTED SEPARATELY FOR TREE AND NEURAL TARGETS.\", \"page\": 7, \"index\": 1, \"width\": 466, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2607.07739v1/fig-002.webp\", \"caption\": \"TABLE V LLM ATTACK SUCCESS RATES (MEAN ± STD ACROSS ε AND SEEDS). ONLY VALID CELLS WITH CLEAN MACRO-F1 ≥ 0.45 ARE REPORTED; BOLD INDICATES THE HIGHEST ASR PER ROW.\", \"page\": 7, \"index\": 2, \"width\": 922, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2607.07739v1/fig-003.webp\", \"caption\": \"Fig. 1. Controllability-aware black-box transfer evaluation framework. Flow features are partitioned into DC/IC/UC groups; only DC features are perturbed on a shared XGBoost surrogate and transferred to LLM and ML IDS targets under identical attacker constraints.\", \"page\": 3, \"index\": 3, \"width\": 883, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2607.07739v1/fig-004.webp\", \"caption\": \"TABLE II FEATURE CONTROLLABILITY TAXONOMY ACROSS DATASETS. | · | DENOTES COUNT; REPRESENTATIVE FEATURES SHOWN FOR EACH CATEGORY.\", \"page\": 3, \"index\": 4, \"width\": 520, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2607.07739v1/fig-005.webp\", \"caption\": \"TABLE IX MULTI-SURROGATE TRANSFER ASR TO LLM TARGETS (FD-PGD, MEAN ACROSS ε ∈ {0.05, 0.15, 0.30}, 2 SEEDS). ‡HIKARI USES DC = 25 TAXONOMY.\", \"page\": 9, \"index\": 5, \"width\": 535, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2607.07739v1/fig-006.webp\", \"caption\": \"TABLE III DATASET CHARACTERISTICS AND CONTROLLABILITY PARTITIONS. THE TABLE DEFINES THE FEATURE SPACES UNDER WHICH EACH TRANSFER EXPERIMENT IS EVALUATED; IT DOES NOT BY ITSELF IMPLY RELATIVE VULNERABILITY ACROSS DATASETS.\", \"page\": 5, \"index\": 6, \"width\": 529, \"height\": 344}]"
motivation: 在攻击者只能控制部分网络流量特征的现实条件下，基于 LLM 的入侵检测分类器是否比传统机器学习模型更容易受到黑盒迁移对抗攻击。
method: 构建基于特征可控性划分的黑盒迁移攻击框架，仅修改直接可控特征，并比较不同攻击算法对 LLM 与传统 IDS 模型的跨架构迁移效果。
result: 论文在五个 IDS 基准数据集、七个 LLM 目标和两个传统模型上进行了超过50万次对抗测试，并报告27个有效 LLM 配置。结果显示，相比平均 ML 基线，LLM 在全部五个数据集上的攻击成功率更高，同时梯度和 NES 方法比贪心扰动具有更稳定的迁移能力。
conclusion: 精读建议优先关注第三节的可控性攻击框架定义、第五节实验设计以及第六节结果分析，因为这些部分决定论文贡献是否成立。还应重点检查数据集划分、LLM 架构设置和攻击约束是否足以支持其安全结论。
key_findings: [提出 DC/IC/UC 三类网络流量特征可控性划分，使对抗攻击更符合真实攻击者能力。, LLM 对抗迁移脆弱性明显高于平均传统 ML 基线，但不同数据集间差异较大。, 梯度型和分数型攻击跨架构迁移效果优于贪心坐标攻击，并通过多代理验证降低模型依赖疑虑。]
limitations: 正文指出完整协议级流量有效性约束未覆盖；实验主要基于已有 IDS 数据集和模拟迁移攻击，现实部署环境验证仍不足。
---

## 1. 问题与背景
本文研究基于大语言模型（LLM）的网络流量分类器在真实攻击约束下的对抗鲁棒性问题，重点关注攻击者只能修改部分网络流量特征时，LLM-IDS 是否仍容易受到规避攻击。随着 LLM 被用于网络入侵检测和安全运营场景，其面对黑盒迁移攻击的安全性成为实际部署中的关键问题。已有传统 IDS 对抗研究往往在不符合现实约束的特征空间中进行扰动，无法准确反映攻击者可操作的攻击能力。

## 2. 核心思路 / 方法
论文提出一种面向可控性的黑盒迁移攻击框架，根据网络通信语义将流量特征划分为直接可控（DC）、间接可控（IC）和不可控（UC）三类，并仅允许对 DC 特征进行扰动。该方法利用共享 XGBoost 替代模型生成有限差分 PGD、贪心坐标攻击和 NES 等对抗样本，再迁移攻击七种 LLM 目标模型以及传统机器学习分类器。相比传统任意修改特征的攻击方式，该框架通过约束扰动范围提高了攻击现实性，并系统比较了不同架构之间的迁移攻击差异。

## 3. 结果与结论
实验覆盖五个 IDS 数据集、27 个有效 LLM 配置以及超过 50 万个对抗样本，结果表明 LLM 的迁移攻击脆弱性较高，但受数据集和比较对象影响明显。相较平均机器学习基线，LLM 在五个数据集上均表现出更高攻击成功率（ASR），而与 LightGBM 比较时，不同数据集呈现不同结果。论文进一步发现，基于梯度和分数估计的方法（PGD、NES）在所有 LLM 配置中均比贪心坐标扰动具有更强的跨架构迁移能力。

## 4. 局限与适用边界
论文虽然保证了特征约束违反率为 0%，但未实现完整的网络协议级流量重放验证，因此仍不能完全证明生成样本在真实网络环境中的可执行性。研究主要适用于基于流量特征序列化输入的 LLM 入侵检测分类场景，其结论可能不直接适用于其他类型的安全任务或不同输入形式的 LLM 应用。

## Abstract
Large language models (LLMs) are increasingly explored as network intrusion detection classifiers, but their adversarial robustness under realistic attacker constraints remains unclear. We present a controllability-aware black-box transfer framework for LLM-based network traffic classifiers. The framework partitions flow features into directly controllable (DC), indirectly controllable (IC), and uncontrollable (UC) groups according to network communication semantics, then restricts perturbations to DC features while freezing IC/UC features. Using a shared XGBoost surrogate, we generate finite-difference PGD, greedy coordinate-wise, and NES adversarial examples and transfer them to seven LLM targets and two conventional ML targets across five IDS benchmarks from 1999 to 2022. Across 27 valid LLM configurations and over 500,000 adversarial examples, we find that LLM transfer vulnerability is substantial but dataset- and comparator-dependent. Compared with LightGBM, LLMs are more vulnerable on RT-IoT2022 and CIC-IDS-2018, comparable on NSL-KDD and UNSW-NB15, and less vulnerable on HIKARI-2021; compared with the averaged ML baseline, LLMs show higher ASR on all five datasets. We further observe a consistent cross-architecture transfer hierarchy: gradient- and score-based perturbations transfer more effectively than greedy perturbations across all 27 LLM cells and 9/10 ML cells. Cross-surrogate validation with tree, neural, and linear surrogates yields similar LLM ASR, reducing evidence that the findings are XGBoost-specific. Constraint violation rate is 0\% by construction.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文研究了**基于大语言模型（LLM）的网络流量分类器在现实攻击约束下的对抗鲁棒性问题**，提出一种可控性感知（Controllability-Aware）的黑盒迁移攻击框架，将网络流量特征划分为直接可控（DC）、间接可控（IC）和不可控（UC）三类，仅允许攻击者修改实际可操作的 DC 特征。  
通过五个 IDS 数据集、七个 LLM 目标模型和两个传统 ML 模型的超过 50 万次对抗测试，论文发现 LLM-IDS 存在显著迁移攻击风险，但风险受到数据集和比较基线影响；梯度型与分数型攻击比贪心攻击具有更强跨架构迁移能力。

**关键词：** LLM 网络流量分类｜对抗样本｜黑盒迁移攻击｜特征可控性｜入侵检测系统｜跨架构鲁棒性

---

# 2. 核心故事与贡献

## 2.1 问题与动机

### 研究背景

近年来，大语言模型开始被用于网络入侵检测任务，将传统数值流量特征转换为自然语言描述输入 LLM 完成分类。然而，已有研究主要关注检测准确率，对于：

- LLM-IDS 是否容易受到对抗攻击；
- 攻击者只能控制部分网络流量特征时攻击是否仍有效；
- LLM 与传统 ML IDS 的攻击脆弱性是否存在差异；

仍缺少系统研究。

论文指出，已有 IDS 对抗攻击存在两个核心问题：

1. **攻击约束不现实**

传统攻击通常直接修改任意 feature-space 向量，但真实攻击者无法控制：

- 远端服务器响应；
- 网络路径延迟；
- 监控系统统计变量。

因此，攻击结果可能高估真实威胁。

2. **缺少 LLM 与传统模型的统一比较**

此前工作通常研究：

- ML → ML；
- LLM 检测能力；

但没有回答：

> LLM-based IDS 是否比传统 IDS 更容易受到现实约束下的黑盒迁移攻击？

---

## 2.2 核心洞察

论文核心观点：

> 对抗风险评估必须首先定义攻击者能控制什么，再生成攻击样本。

作者提出：

- Directly Controllable（DC）：攻击者可直接修改；
- Indirectly Controllable（IC）：攻击者可通过行为间接影响；
- Uncontrollable（UC）：攻击者无法控制。

攻击只作用于 DC 特征，使攻击更接近真实网络环境。

论文进一步发现：

- LLM 的迁移攻击风险不是固定高或低，而是依赖：
  - 数据集；
  - 模型能力；
  - 比较基线。

例如：

- 相比平均 ML baseline：
  - LLM 在五个数据集上均具有更高 ASR；
  - dataset-level Wilcoxon p = 0.031；
  - Cohen’s d = 1.21。

见正文 Introduction。

---

## 2.3 主要贡献

### Contribution 1：可控性感知攻击框架

提出 DC/IC/UC 网络流量特征分类体系。

特点：

- 攻击前确定攻击能力；
- 只扰动 DC 特征；
- 保证 Constraint Violation Rate（CVR）=0%。

见正文 Section III。

---

### Contribution 2：跨架构 LLM vs ML 鲁棒性比较

实验覆盖：

- 5 个 IDS 数据集；
- 7 个 LLM；
- 2 类传统 ML 分类器；
- 27 个有效 LLM evaluation cells；
- 超过 500,000 个 adversarial examples。

见正文 Introduction / Section V。

---

### Contribution 3：发现跨架构迁移规律

论文发现：

- PGD；
- NES；

比 greedy coordinate attack 更容易跨架构迁移。

该规律：

- 覆盖全部 27 个 LLM cells；
- 覆盖 9/10 个 ML cells。

见正文 Introduction / Section VI。

---

# 3. 相关工作与定位

| 工作 | 攻击约束 | LLM目标 | 黑盒迁移 | 多数据集 | 问题空间有效性 |
|-|-|-|-|-|-|
| Pierazzi et al. [5] | 部分讨论 feature/problem space | ❌ | ❌ | ❌ | ✅ |
| Apruzzese et al. [3] | 强调攻击者能力限制 | ❌ | ❌ | ❌ | 部分 |
| Han et al. [4] | 实际流量修改 | ❌ | 部分 | 部分 | ✅ |
| Ferrag et al. [1] | LLM IDS | ✅ | ❌ | 部分 | ❌ |
| Xu et al. [2] | LLM 流量分类 | ✅ | ❌ | ❌ | ❌ |
| 本文 | ✅ DC/IC/UC taxonomy | ✅ | ✅ | ✅ 五数据集 | 部分 |

见正文 Table I。

---

## 定位总结

相比已有工作：

### 相比传统 IDS 对抗研究

优势：

- 不再允许任意 feature perturbation；
- 引入攻击能力模型。

不足：

- 未达到完整 packet-level replay。

---

### 相比 LLM IDS 工作

优势：

- 首次系统分析 LLM-IDS adversarial transfer。

不足：

- 主要针对 flow feature serialization 场景。

---

# 4. 方法详解

## 4.1 整体框架

论文提出：

**Controllability-Aware Black-box Transfer Framework**

流程：

```
Network Flow Features
          |
          v
 DC / IC / UC Feature Partition
          |
          v
 Modify only DC Features
          |
          v
 XGBoost Surrogate Attack Model
          |
          |
 --------------------------------
 |              |               |
PGD          NES          Greedy
 |
 v
Transfer to:
LLM IDS + ML IDS
```

见 Figure 1。

---

# 4.2 特征可控性划分

给定流量特征：

\[
x \in R^d
\]

划分：

\[
I_{DC}\cup I_{IC}\cup I_{UC}
=
\{1,...,d\}
\]

三个集合互斥。

---

## Directly Controllable (DC)

攻击者直接决定。

例如：

- source bytes；
- packet count；
- sender TCP flags。

---

## Indirectly Controllable (IC)

攻击者可通过持续行为影响。

例如：

- connection rate；
- service ratio。

---

## Uncontrollable (UC)

攻击者无法修改。

例如：

- destination bytes；
- response time；
- RTT。

---

# 4.3 对抗攻击生成

攻击基于共享 XGBoost surrogate。

采用三类攻击：

## FD-PGD

有限差分估计梯度：

\[
\nabla_x f(x)
\approx
\frac{f(x+\delta)-f(x-\delta)}
{2\delta}
\]

特点：

- 梯度方向优化；
- 高迁移能力。

---

## NES

通过随机搜索估计梯度。

特点：

- score-based black-box attack；
- 不依赖目标模型梯度。

---

## Greedy Coordinate Attack

逐个修改 feature：

- 根据局部收益选择扰动方向。

---

# 4.4 迁移攻击设置

攻击：

\[
Surrogate \rightarrow Target
\]

目标：

- 7 个 LLM；
- LightGBM；
- DNN-IDS。

保持：

- 相同攻击预算；
- 相同 DC 约束。

---

# 5. 实验分析

## 5.1 实验设置

| 项目 | 设置 |
|-|-|
| 数据集 | NSL-KDD、UNSW-NB15、CIC-IDS-2018、HIKARI-2021、RT-IoT2022 |
| LLM | 7 个 |
| ML | LightGBM + DNN IDS |
| 攻击数量 | >500,000 |
| surrogate | XGBoost |
| 攻击方法 | FD-PGD、NES、Greedy |

见正文 Section V。

---

# 5.2 LLM 攻击成功率

主要发现：

## LLM 相比平均 ML baseline 更脆弱

结果：

- 五个数据集全部：

\[
ASR_{LLM}>ASR_{ML}
\]

统计：

- Wilcoxon：

\[
p=0.031
\]

- Cohen’s d：

\[
1.21
\]

见 Introduction。

---

# 5.3 LLM 与 LightGBM 对比

| Dataset | 结果 |
|-|-|
| RT-IoT2022 | LLM 更容易攻击（约7.2×） |
| CIC-IDS-2018 | LLM 更容易攻击（约1.4×） |
| NSL-KDD | 接近 |
| UNSW-NB15 | 接近 |
| HIKARI-2021 | LightGBM 更脆弱（1.43×） |

说明：

> LLM 脆弱性具有 dataset-dependent 特征。

见 Introduction / Table V。

---

# 5.4 攻击方法比较

| 方法 | 迁移能力 |
|-|-|
| FD-PGD | 高 |
| NES | 高 |
| Greedy | 较低 |

发现：

- 全部 27 个 LLM cells 中：
  - PGD/NES > Greedy。

- ML：
  - 9/10 cells 满足。

见 Section VI。

---

# 5.5 多 surrogate 验证

论文进一步测试：

- Tree surrogate；
- Neural surrogate；
- Linear surrogate。

结果：

不同 surrogate 得到类似 LLM ASR。

说明：

> 结论不是 XGBoost 特有。

见 Table IX。

---

# 6. 局限性与风险

## 6.1 假设约束

论文只保证：

- feature controllability；

没有保证：

- 完整协议合法性；
- packet-level replay。

因此：

生成样本仍可能无法直接部署到真实网络。

---

## 6.2 应用边界

方法主要适用于：

- flow feature → text → LLM classifier。

不一定适用于：

- 原始 packet LLM；
- 日志分析 LLM；
- SOC agent 类型系统。

---

## 6.3 潜在风险

攻击结果说明：

LLM IDS 在面对黑盒迁移攻击时存在安全风险。

但：

- 实际攻击成本；
- 在线部署环境；
- 防御策略；

仍需进一步验证。

---

# 7. 复现与后续问题

## 7.1 复现重点

### 1. DC/IC/UC 标注规则

关键：

- 如何判断 feature 是否属于攻击者可控范围。

需要严格复现 Table II。

---

### 2. LLM 输入格式

需要确认：

- flow feature 如何序列化；
- prompt template；
- LLM inference 设置。

正文未完全展开。

[需验证]

---

### 3. 攻击预算设置

需要复现：

- ε 设置；
- seed；
- query 数量。

见 Section V。

---

## 7.2 后续研究方向

### 方向1：真实网络级验证

从 feature attack 推进：

- packet modification；
- traffic replay；
- online evaluation。

---

### 方向2：LLM IDS 防御机制

研究：

- adversarial training；
- prompt defense；
- feature normalization；
- uncertainty detection。

---

# 8. 直接证据与待验证项

## 直接证据

1. **可控性攻击框架**

> 特征被划分为 DC、IC、UC，并只扰动 DC 特征。

见正文 Section III，Figure 1。

---

2. **大规模实验规模**

> 覆盖五个 IDS 数据集、七个 LLM、两个 ML 分类器，共 27 个有效 LLM 配置和超过 500,000 个攻击样本。

见正文 Introduction / Section V。

---

3. **LLM 高攻击成功率**

> 相比平均 ML baseline，LLM 在五个数据集均具有更高 ASR。

统计：

- Wilcoxon p=0.031；
- Cohen’s d=1.21。

见正文 Introduction。

---

4. **攻击迁移规律**

> PGD 和 NES 在所有 27 个 LLM cells 中均比 greedy 具有更高迁移效果。

见正文 Section VI。

---

5. **约束满足情况**

> Constraint violation rate = 0%。

见 Abstract。

---

## [需验证]

- [需验证] 七个 LLM 的具体模型名称及参数配置，正文摘要未完整列出。
- [需验证] LLM prompt 模板、tokenization 方法及推理参数。
- [需验证] DC/IC/UC 划分是否适用于真实生产网络流量。
- [需验证] 攻击样本是否能够通过真实协议栈重新生成有效网络流。

（完）
