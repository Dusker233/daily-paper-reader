---
title: Detecting Malicious Agent Skills in the Wild using Attention
title_zh: 基于注意力机制的真实环境恶意智能体技能检测
authors: "Bacem Etteib, Daniele Lunghi, Tégawendé F. Bissyandé"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23416v1"
tags: ["query:q9"]
score: 6.8
relevance_score: 6.5
quality_score: 7.5
reliability_score: 7.5
practicality_score: 6.5
evidence: LLM智能体安全与恶意技能检测
tldr: "提出针对LLM agent技能市场中恶意技能检测问题，设计Locate-and-Judge两阶段注意力定位+LLM判别框架，通过注意力对结构片段进行风险定位，再由轻量筛选后的关键片段交给强判别模型分析，实现低成本全市场扫描。在13.4万技能实测中发现131个确认恶意技能（约83%精度），相较全量扫描显著降低约2.8倍token成本，同时保持较高检出能力，整体具有较强工程落地价值，值得进一步精读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23416v1/fig-001.webp\", \"caption\": \"Figure 3. Mean judge-input tokens per skill. LOCATE-AND-JUDGE sends 2.84× fewer input tokens than reading each skill in full.\", \"page\": 6, \"index\": 1, \"width\": 408, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2606.23416v1/fig-002.webp\", \"caption\": \"Figure 5. Cost–accuracy on the reviewed sample. Attention selection dominates random spans at matched cost; the knee is K=3.\", \"page\": 7, \"index\": 2, \"width\": 458, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2606.23416v1/fig-003.webp\", \"caption\": \"Figure 1. Overview of LOCATE-AND-JUDGE. A deterministic parser splits the skill into structural spans, a small reader LLM ranks them by instructionfollowing attention and keeps the top K, and a zero-shot LLM judge scores only the retained spans. The skill is flagged when the maximum judge score exceeds the calibrated threshold τ . The cheap locator runs on every skill; the expensive judge runs on K spans only.\", \"page\": 4, \"index\": 3, \"width\": 986, \"height\": 214}]"
motivation: 如何在大规模LLM agent技能市场中，以低成本高精度检测隐藏的恶意技能代码或指令。
method: 利用注意力信号做结构化span定位筛选高风险片段，再交由LLM judge进行精细恶意判别的两阶段检测框架。
result: "在约13.4万真实技能扫描中检出131个恶意技能（83%经人工确认），并在对比中优于regex与关键词及部分现有检测器，同时token成本约降低2.8倍。"
conclusion: 建议优先阅读方法部分Locate-and-Judge两阶段设计与attention locator机制，其次看大规模实证扫描章节以理解真实市场效果与误报来源。
key_findings: [两阶段架构显著降低检测成本，同时保持接近全量扫描的检出能力。, attention可作为技能内部恶意指令定位信号，但最终召回受judge能力限制。, 在真实市场扫描中发现大量隐藏恶意技能且多数规避现有检测器。]
limitations: 注意力信号在复杂技能中可能弱化，且整体召回上限受LLM judge能力限制，仍存在漏检风险。
---

## 1. 问题与背景  
本文研究的是在大型语言模型智能体（LLM agents）中，通过“skills（技能包）”执行第三方指令所带来的安全风险，尤其是恶意技能在市场环境中的传播与利用问题。由于skills本质上是可加载的自然语言指令集合，并且运行时拥有用户权限，因此可能被用于数据外泄、代理劫持或供应链持久化攻击。现有prompt injection防御方法依赖“指令与数据分离”的假设，但在skills场景中该假设被打破，使得传统方法失效。该问题的重要性在于技能市场正在成为新的攻击面，且攻击隐蔽性强、规模化传播能力高。

## 2. 核心思路 / 方法  
论文提出Locate-and-Judge两阶段检测框架，用于在大规模技能市场中识别恶意技能。第一阶段locator使用轻量模型基于“instruction-following attention”对技能内部结构片段进行打分，筛选出最可能包含异常指令的top-K spans。第二阶段judge对这些高关注片段进行精细审查，从而判断是否存在恶意行为，实现“先定位、后判断”的分层处理。该方法的关键创新在于利用注意力作为近似攻击信号，并通过结构化筛选显著降低昂贵LLM审查成本，使全市场扫描成为可能，同时相比全量扫描或关键词规则方法在成本与效果之间取得更优平衡。

## 3. 结果与结论  
实验表明，在Skill-Inject基准与真实市场数据上，该方法优于全量LLM扫描、逐段检测以及关键词/正则基线方法，在精度与成本之间取得更优折中。在对约13.4万条真实技能的扫描中，模型标记并人工确认131个恶意技能，其中包括82个隐蔽恶意技能，这些样本多数未被现有检测工具发现。整体精度约为83%，且在成本上相比逐技能全量判断减少约2.8倍token消耗。结论认为该框架能够在低成本下实现市场级别的恶意技能发现，并且主要性能瓶颈来自judge而非定位模块。

## 4. 局限与适用边界  
该方法的召回上限主要受judge能力限制，即使locator能够成功定位可疑片段，最终仍可能因判断误差导致漏检或误判。该方法依赖“attention可作为攻击信号”的假设，在某些攻击隐蔽性更强或注意力分布异常的情况下可能失效。从现有文本无法确认其在不同模型架构或非LLM agent系统中的泛化边界。

## Abstract
LLM agents increasingly load skills, file-based packages of natural-language instructions written by third parties and distributed through marketplaces, that execute with the user's privileges. A single malicious skill can exfiltrate data, hijack the agent, or persist as a supply-chain foothold, which turns the skill marketplace into a new attack surface for agentic systems. Prompt-injection defenses do not carry over to this setting. They rely on a boundary between trusted instructions and untrusted data, whereas a skill is itself a body of instructions, so an injected command sits among many legitimate ones and inherits their authority. We present Locate-and-Judge, a two-stage detector designed for this regime. A lightweight locator scores the structural spans of a skill by the instruction-following attention each span draws and retains only the top-K. A judge then examines the retained spans in detail. Concentrating the costly judgment on a few high-attention spans lets the detector audit an entire marketplace instead of a sample. Compared to direct LLM-based scanning, this approach offers an order-of-magnitude cost reduction, dramatically increasing its scalability at a small cost to recall, and it dominates keyword and regex baselines at comparable expense. Deployed at marketplace scale and at negligible cost, Locate-and-Judge flags skills with high precision, the majority of which we manually confirmed as malicious, surfacing dozens of live malicious skills, including several disguised as benign functionality and many that SkillSpector and Cisco Skill Scanner fail to detect. We release the resulting labeled dataset.