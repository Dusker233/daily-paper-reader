---
title: A Low-Latency Semantic State Estimator using Latent Predictive Learning for Dynamic Network Monitoring and Orchestration
title_zh: 用于动态网络监控与编排的基于潜在预测学习的低延迟语义状态估计器
authors: "Hari Madhukumar, Haiyuan Li, Xiaolan Liu, Andy Corston-Petrie, Dimitra Simeonidou"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08869v1"
tags: ["query:q7"]
score: 7.6
relevance_score: 7.5
quality_score: 7.5
reliability_score: 7.5
practicality_score: 8.0
evidence: 网络遥测语义化实时监控
tldr: "本文提出一种用于动态网络监控与编排的低延迟语义状态估计器LPSE，通过潜在预测学习与JEPA式表征，将可变节点拓扑映射为固定维语义状态，并以语义码本替代自回归LLM输出，实现毫秒级闭环推理。在Kubernetes真实集群实验中达到82.42%语义准确率，同时相比4B模型实现约41倍更低延迟与15倍更小内存，并在部分场景接近120B LLM但延迟降低数百倍，整体具有较强工程落地价值，值得继续精读其系统设计与实验部分。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08869v1/fig-001.webp\", \"caption\": \"Fig. 1. Pipeline for dynamic Kubernetes monitoring that includes multi-node metric ingestion, temporal latent encoding, question-conditioned latent fusion, codebook-based semantic answering, and confidence-aware output selection.\", \"page\": 3, \"index\": 1, \"width\": 1079, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2606.08869v1/fig-002.webp\", \"caption\": \"Fig. 2. Training dynamics (a, b) and end-of-run accuracy and compute (c, d).\", \"page\": 5, \"index\": 2, \"width\": 510, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2606.08869v1/fig-003.webp\", \"caption\": \"TABLE I CROSS-SYSTEM COMPARISON ON THE 100-QUESTION BENCHMARK.\", \"page\": 6, \"index\": 3, \"width\": 1086, \"height\": 225}]"
motivation: 如何在节点数量与监控意图动态变化的云边网络中，实现低延迟、可泛化的语义级状态估计以支撑闭环编排。
method: 通过节点身份驱动的slot routing将变长拓扑编码为固定语义空间，并结合JEPA式潜在预测模型与监控问题条件化输入，从语义码本中直接预测结果以避免自回归生成。
result: "在多节点Kubernetes真实集群测试中达到82.42%语义预测准确率，同时推理延迟约为4B LLM的1/41，并在对比120B LLM时实现约364倍更低平均延迟。"
conclusion: 建议重点阅读系统模型与方法设计部分，尤其是节点slot表示与语义码本机制，其决定模型如何处理拓扑变化；实验部分用于验证低延迟与多查询能力是否成立。
key_findings: [提出基于slot routing的拓扑不变表示，使节点增删或重排不影响模型输入结构。, 语义码本替代生成式LLM输出，实现固定成本的一步推理并显著降低延迟。, 在真实Kubernetes环境中兼顾较高语义准确率与显著优于LLM的推理效率。]
limitations: 实验规模较受限，语义输出依赖预定义码本，泛化到更复杂跨域网络场景仍不明确。
---

## 1. 问题与背景  
该研究关注动态云边网络环境中的闭环监控与编排问题，核心挑战在于如何对实时遥测数据进行“语义级理解”而不仅是原始指标汇总。由于网络拓扑中的节点数量与结构会随服务扩展、迁移和故障恢复不断变化，同时监控查询也具有多样性与实时性，传统方法难以同时满足适应性与毫秒级延迟要求。现有规则系统、轻量预测模型以及基于大模型的方法分别在灵活性、结构适应性或延迟成本上存在明显瓶颈，因此需要一种能够在动态拓扑下稳定运行的低延迟语义状态估计框架。

## 2. 核心思路 / 方法  
论文提出一种基于潜在预测学习的语义状态估计器（LPSE），通过在潜在空间中进行预测而非依赖自回归文本生成，实现固定成本的一次性推理。方法将可变数量的节点遥测数据映射为拓扑自适应的时序表示，并通过基于节点身份的slot routing机制构建置换不变的固定维度表示，从而支持节点增删与重排。模型将监控问题与时序遥测联合建模，并从一个固定语义codebook中输出离散答案，以避免大语言模型式的生成延迟。同时，该设计保持输入空间稳定，使系统可以在闭环控制中以低开销运行并适配多类型查询。

## 3. 结果与结论  
实验在多节点 Kubernetes 集群上验证，模型在语义预测任务中达到约 82.42% 的准确率，同时相比可部署的 4B 规模 LLM 推理延迟降低约 41 倍，并减少约 15 倍内存占用。在更大模型对比中，其在接近 120B LLM 的性能水平（约 11% 差距）下实现约 364 倍更低延迟，表明其适合嵌入多智能体编排系统以满足严格控制循环预算要求。整体结论是，该方法在保证语义可解释性的同时显著提升了实时网络监控的效率。

## 4. 局限与适用边界  
模型在查询重述场景下的鲁棒性较弱，重写查询时性能下降至约 39%，说明冻结的句子编码器仍存在明显瓶颈。同时，slot routing 将输入规模固定为 16 个节点，导致更大规模集群需要重新训练扩展容量。由于监督信号依赖人工阈值规则且预测时间范围为 K=1，长时序趋势推理能力受到限制。

## Abstract
Closed-loop network monitoring and orchestration increasingly require semantic interpretations of live telemetry beyond raw counter collection. However, dynamic cloud-edge environments change both the active node set and the monitoring query at runtime, while control loops demand bounded millisecond-scale responses. We introduce a latent predictive state estimator (LPSE) for dynamic network monitoring and orchestration, built on latent predictive learning over streaming telemetry. The framework converts variable-cardinality node telemetry into topology-adaptive temporal representations, fuses them with monitoring questions, and returns bounded answers from a semantic codebook instead of autoregressive text generation. This design enables fixed-cost, single-pass inference while preserving semantic interpretability. By operating on permutation-invariant, slot-routed node representations keyed by stable identity, the model maintains a fixed input space and generalizes to node addition, removal, and reordering without retraining. Experimental results on a multi-node Kubernetes cluster show semantic prediction accuracy of 82.42% at approximately 41$\times$ lower mean inference latency and 15$\times$ smaller memory footprint compared with a deployable 4B LLM endpoint.