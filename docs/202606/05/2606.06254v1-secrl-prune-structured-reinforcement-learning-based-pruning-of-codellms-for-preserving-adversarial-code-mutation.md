---
title: "SecRL-Prune: Structured Reinforcement Learning-Based Pruning of CodeLLMs for Preserving Adversarial Code Mutation"
title_zh: SecRL-Prune：基于强化学习的结构化剪枝用于保留CodeLLMs的对抗性代码变异能力
authors: "Parsa Memarzadehsaghezi, Pooria Madani, Khalil El-Khatib"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06254v1"
tags: ["query:profile-1"]
score: 7.9
relevance_score: 8.0
quality_score: 8.0
reliability_score: 7.5
practicality_score: 7.0
evidence: 使用强化学习对CodeLLMs进行安全裁剪
tldr: "本文提出 SecRL-Prune，用强化学习驱动的结构化剪枝压缩 CodeLLM，并通过教师-学生 KL 奖励与教师输出缓存来降低资源开销、尽量保留代码变异能力。作者在 HumanEval 与真实恶意样本上验证，发现 10–30% 剪枝后模型仍能保持较高 pass@k 与 var@k，且 20% 剪枝模型生成的变体可显著规避多种检测。若你关心“压缩后的代码大模型是否仍具攻击性”，这篇值得继续细读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06254v1/fig-001.webp\", \"caption\": \"Figure 4: Preservation trends on HumanEval under SecRL-Prune vs. PruneNet.\", \"page\": 6, \"index\": 1, \"width\": 898, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-002.webp\", \"caption\": \"Table 2: HumanEval mutation diversity (var@k) for three CodeLLMs under different structured pruning methods. “Pres.” is\", \"page\": 7, \"index\": 2, \"width\": 1056, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-003.webp\", \"caption\": \"Figure 5: Peak GPU memory usage during pruning-policy\", \"page\": 8, \"index\": 3, \"width\": 506, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-004.webp\", \"caption\": \"Figure 6: Experimental framework for evaluating detection\", \"page\": 8, \"index\": 4, \"width\": 424, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of SecRL-Prune Using a calibration\", \"page\": 4, \"index\": 5, \"width\": 506, \"height\": 334}]"
motivation: 在压缩 CodeLLM 时，如何尽量保留其功能保持且语法多样的代码变异能力，并评估这种能力在恶意代码场景下是否仍然危险。
method: 提出一种面向 CodeLLM 结构化剪枝的强化学习框架 SecRL-Prune，仅对 MLP/FFN 通道做层级剪枝，并以教师-学生 KL 约束作为奖励信号，同时缓存教师 top-P 输出以降低训练时显存占用。
result: "作者在 HumanEval 上用 pass@k 和 var@k 评估三种 7B CodeLLM 于 10–30% 压缩下的表现，结果显示 SecRL-Prune 在激进剪枝时比近期结构化剪枝基线更能保持正确性与代码多样性。案例研究中，20% 剪枝模型生成的语义保持变体可显著降低真实恶意样本被 62+ 检测引擎识别的概率。"
conclusion: "若继续精读，优先看方法部分的 RL 奖励设计、剪枝策略与缓存机制，以及实验部分的 pass@k/var@k 定义和对比基线；这些内容决定它是否真能“压缩而不失去恶意代码变异能力”。案例研究章节也值得看，用于判断安全影响是否具有现实威胁性。"
key_findings: ["10–30% 压缩下，SecRL-Prune 更好保留代码生成正确性与变体多样性。", 缓存教师 top-P 预测后，训练时无需教师与学生同时驻留 GPU，降低资源开销。, "20% 剪枝后的模型仍能产生有效语义保持变异，提升规避静态检测的现实风险。"]
limitations: 仅在 7B 级模型与 HumanEval/样本案例上验证，且主要聚焦 FFN 通道剪枝，泛化性与安全边界仍待确认。
---

## 1. 问题与背景

论文关注一个安全领域的重要问题：当大型代码语言模型（CodeLLM）经过压缩后，是否仍然保留“保持语义不变但改变代码形式”的代码变异能力。该问题与恶意软件的多态化和变形技术密切相关，因为攻击者可能利用 CodeLLM 自动生成大量功能等价但形式不同的恶意代码，以绕过基于签名的检测系统。随着模型压缩技术的发展，如果这种能力能够在小型化模型中保留，那么模型甚至可能部署在资源受限环境中，从而带来新的安全风险。

## 2. 核心思路 / 方法

论文提出 SecRL-Prune，一种面向 CodeLLM 的基于强化学习的结构化剪枝框架，主要对 Transformer 中前馈网络（MLP/FFN）通道进行剪枝。方法从预训练教师模型出发，通过强化学习学习逐层剪枝策略，并以教师模型与学生模型之间的 KL 散度作为奖励信号，使压缩后的模型尽可能保持教师模型行为。为了降低训练成本，作者预先缓存教师模型在校准数据集上的 Top-P 预测结果，后续训练时仅需加载学生模型并与缓存目标比较，从而避免教师和学生模型同时驻留 GPU 显存。相较于已有结构化剪枝方法，其创新点在于将强化学习驱动的层级剪枝决策与基于缓存的教师监督机制结合，在降低资源消耗的同时提升能力保持效果。

## 3. 结果与结论

作者在三个 7B 级 CodeLLM 上进行了 10%～30% 压缩率的实验，并使用 HumanEval 数据集上的 pass@k 衡量代码正确性、var@k 衡量代码多样性。结果表明，在较高压缩率下，SecRL-Prune 相比近期结构化剪枝基线能够更好地保持代码生成正确率和代码变异多样性。进一步的真实恶意软件案例研究显示，由经过 20% 剪枝模型生成的语义保持型代码变异样本能够显著降低检测率。论文据此认为，代码变异能力在经历较大幅度结构化压缩后仍然能够被保留，这使得压缩版 CodeLLM 在网络安全场景中的潜在威胁值得关注。

## 4. 局限与适用边界

从现有文本可确认，实验主要集中于三个 7B 级 CodeLLM、HumanEval 基准以及特定恶意软件案例，因此结论对更大规模模型、其他编程任务或不同压缩策略的泛化能力仍有待验证。论文重点研究结构化剪枝对代码变异能力的影响，对量化、蒸馏等其他压缩路线的系统比较则不属于本文核心范围。

## Abstract
Large code language models (CodeLLMs) can generate and rewrite programs, enabling functionality-preserving code mutation that may be used to create diverse malware variants and evade signature-based detection. A key security question is whether this mutation capability survives model compression, which would make deployment feasible under limited hardware budgets. We propose SecRL-Prune, a structured pruning framework for CodeLLMs that operates on feed-forward (MLP/FFN) channels. Starting from a pretrained teacher, it learns a layer-wise pruning policy with reinforcement learning using a teacher-student KL-divergence reward. To improve efficiency, we cache the teacher's top-P predictions once and compare the pruned student against this compact target, avoiding simultaneous teacher-student residency in GPU memory. We evaluate SecRL-Prune on HumanEval using pass@k for execution correctness and var@k for code diversity across three 7B CodeLLMs at 10-30% compression. SecRL-Prune consistently preserves higher pass@k and var@k than recent structured pruning baselines under aggressive pruning. In a case study on real malware samples, semantics-preserving mutations from 20%-pruned models substantially reduced detections. These results show that code mutation capability can survive significant structured pruning, highlighting the security relevance of compressed CodeLLMs.