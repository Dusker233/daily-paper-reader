---
title: "Caught in the Act(ivation): Toward Pre-Output and Multi-Turn Detection of Credential Exfiltration by LLM Agents"
title_zh: 被抓个正着（活化中）：面向 LLM 代理凭证外泄的输出前与多轮检测
authors: "Kargi Chauhan, Pratibha Revankar"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.04141v1"
tags: ["query:q9"]
score: 6.9
relevance_score: 7.0
quality_score: 7.0
reliability_score: 7.0
practicality_score: 6.5
evidence: 用于凭证外泄检测的LLM代理
tldr: 本论文研究LLM代理在处理敏感凭证时可能被间接提示注入攻击的风险，提出三层防御：预输出激活探测、蜂蜜凭证检测及多轮信息泄露累计监控。在开源模型实验中，激活特征能区分恶意与正常提示，多轮累计监控可捕捉单轮漏检的攻击。研究为凭证外泄防御提供方向，值得安全方向读者精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04141v1/fig-001.webp\", \"caption\": \"Figure 2. CIFT layer analysis on Qwen-7B using readout-position features. Late-layer activation deviations are more predictive than early-layer deviations. We interpret this as evidence of a useful credential-access signal, not as complete mechanistic identification.\", \"page\": 6, \"index\": 1, \"width\": 950, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2606.04141v1/fig-002.webp\", \"caption\": \"Figure 3. Detection F1 across held-out evasion strategies. Text-level detectors degrade under several encodings; CIFT remains stable in this controlled setting because it measures internal features before output rendering.\", \"page\": 6, \"index\": 2, \"width\": 940, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2606.04141v1/fig-003.webp\", \"caption\": \"Figure 4. NIMBUS on a representative synthetic trace. Per-turn leakage scores remain small, while cumulative accounting eventually triggers intervention.\", \"page\": 7, \"index\": 3, \"width\": 949, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2606.04141v1/fig-004.webp\", \"caption\": \"Figure 5. NIMBUS budget sensitivity. Smaller budgets improve detection but may increase false blocks; larger budgets preserve utility but miss short or low-rate leaks.\", \"page\": 7, \"index\": 4, \"width\": 465, \"height\": 331}]"
motivation: 如何在LLM代理中提前检测并防止凭证被多轮对话逐步泄露。
method: 通过预输出激活探针、校准蜂蜜凭证及多轮累计信息流监控实现凭证泄露检测。
result: 实验证明，激活特征可在输出前准确识别凭证访问行为，多轮累计泄露监控能捕捉单轮检测未发现的攻击。
conclusion: 优先阅读方法章节（3-5节）以理解三种防御机制，以及实验与结果部分（6节）分析效果与局限。
key_findings: [激活探针可在输出前识别凭证访问，高准确率且能应对编码变换。, 蜂蜜凭证结合差分隐私建模和顺序校准提高了检测的统计可靠性。, 多轮累计泄露监控能够发现单轮检测漏掉的渐进式攻击。]
limitations: 多轮基准小且为内部合成数据，激活方法需白盒访问，信息估计提供实用信号而非严格上界。
---

## 1. 问题与背景
这篇论文关注的是 LLM agent 在处理“可信密钥”和“不可信外部内容”共处于同一上下文窗口时，如何被间接 prompt injection 诱导泄露凭证的问题。作者指出，生产环境中的 agent 常常同时接收 API key、数据库密码、OAuth token 等敏感信息，以及网页、邮件、检索文档等不可信内容，因此攻击者可以通过嵌入式 payload 引导模型暴露这些凭证。由于现有防御多停留在输入过滤或输出文本监测层面，它们容易被编码、改写、Unicode 变体或跨轮次低速泄露绕过，因此该问题在真实 tool-using agent 场景下非常重要。

## 2. 核心思路 / 方法
论文提出了一个研究原型，作者称之为 Agentic Immune System（AIS），尝试从三个层面监测凭证外泄：其一是在输出 token 生成前，通过 activation probes 直接检测模型是否已经“接触到”凭证相关行为；其二是构造 honeytokens，并结合差分隐私字符模型与 split conformal prediction 做更稳健的蜜罐生成和检测；其三是把多轮外泄看作累积信息流问题，在对话轮次上持续追踪估计的 leakage budget。与只做文本级过滤的方法相比，这套路线强调“预输出监测 + 校准后的 canary 检测 + 时间维度上的泄露累计”，试图补上单轮输出检测无法覆盖的空白。作者也明确说明，activation 方法依赖 white-box access，信息估计器提供的是实践信号而非严格上界，因此更像是探索性的防护框架而非完整部署方案。

## 3. 结果与结论
在受控实验中，开放权重模型上的 activation 特征可以以较高准确率区分 benign prompt 和 credential-seeking prompt，即使面对未见过的编码变换也仍然有效。作者还在一个小型合成多轮套件中发现，累积泄露账本能够检测到那些按单轮看不出来的攻击。总体结论是：凭证外泄防御不应只依赖文本输出过滤，而应结合预输出监控、校准后的 honeytoken 检测和跨轮次的泄露累积建模。

## 4. 局限与适用边界
论文明确承认其多轮基准是 in-house 且规模很小，因此结果目前还是初步证据。activation 方案需要白盒访问，因而不适用于只提供黑盒 API 的模型；同时，信息估计器只是实用信号，并不能构成形式化的泄露上界或证明。

## Abstract
LLM agents often place sensitive credentials in the same context window as untrusted retrieved content, creating a direct path for indirect prompt injection to induce credential exfiltration. We study this failure mode through three complementary defenses. First, we ask whether activation probes can detect credential access before output tokens are emitted. Second, we construct honeytokens from format-specific character models and calibrate detection with split conformal prediction. Third, we treat multi-turn exfiltration as a cumulative information-flow problem and track an estimated leakage budget across conversation turns. In controlled experiments on open-weight models, activation features separate benign and credential-seeking prompts with high accuracy, including under held-out encoding transformations. In a small synthetic multi-turn suite, cumulative accounting detects attacks that per-turn detectors miss. These results are preliminary: the multi-turn benchmark is in-house and small, the activation method requires white-box access, and the information estimator provides a practical signal rather than a formal upper bound. Still, the results suggest that credential-exfiltration defenses should combine pre-output monitoring, calibrated canary detection, and temporal leakage accounting rather than relying only on text-level output filters.