---
title: Decomposing Memorization Reduction in Privacy-Preserving Fine-Tuning of SLMs for CSIRTs
title_zh: 面向CSIRTs的小型语言模型隐私保护微调中记忆化降低机制的分解研究
authors: "Cristhian Kapelinski, Diego Kreutz"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28479v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 8.5
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.5
evidence: 在网络安全CSIRT漏洞扫描数据上进行LLM微调研究
tldr: 本文研究1–3B SLM在CSIRT漏洞数据微调中的记忆化与隐私保护，比较DP-SGD与HMAC伪匿名化，覆盖4模型96适配器与多种提取攻击。结果显示记忆降低主要由优化器更新控制贡献，DP-SGD额外收益有限；HMAC降低标识符暴露但不引入二次泄露，模型F1仅0.19–0.28，整体未达可用水平，值得精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28479v1/fig-001.webp\", \"caption\": \"Table VI [FINE-TUNED MODELS] CROSS-SEED LORA ADAPTER UTILITY ON THE 600-RECORD HELD-OUT, TRAINING-FAITHFUL FORMAT. F1 = F1-MACRO; ACC = TOP-1 ACCURACY; ADJ = ADJACENT ACCURACY (|y − ŷ| ≤ 1 ON THE BUCKET INDEX). ALL VALUES ARE MEANS OVER 3 SEEDS. RANDOM UNIFORM BASELINES: F1≈0.20, ACC=0.250, ADJ=0.625. MAJORITY-CLASS (ALWAYS MEDIUM) BASELINES: F1=0.125, ACC=0.333, ADJ=0.833.\", \"page\": 7, \"index\": 1, \"width\": 572, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2606.28479v1/fig-002.webp\", \"caption\": \"Table I RELATED WORK ALONG SIX DESCRIPTIVE AXES. Approach: MODEL ADAPTATION/TRAINING MODE. Defense: PRIVACY MECHANISM, WITH FORMAL GUARANTEE IN PARENTHESES ((ε, δ)=DP, PRF=PSEUDO-RANDOM FUNCTION). Memo. probe: MEMORIZATION PROTOCOL. “—”=N/A.\", \"page\": 2, \"index\": 2, \"width\": 1076, \"height\": 697}]"
motivation: 在1–3B SLM对CSIRT漏洞数据微调中，DP-SGD与HMAC伪匿名化分别及组合如何影响记忆化泄露与任务性能。
method: 通过多模型LoRA微调对比原始、QLoRA、大批量与DP-SGD(ε=2/8)及HMAC伪匿名化，结合多种提取攻击分解隐私收益来源。
result: "匹配更新对照实验表明66–132%的记忆减少可由优化器更新变化解释，DP-SGD未带来额外下降。HMAC使标识符暴露下降40–61%，slug接近随机基线。"
conclusion: 重点阅读方法设计、matched-update控制实验与攻击评估部分，以理解“记忆减少来源分解”的核心论证。进一步关注DP-SGD与HMAC在同一canary设置下的对照结果。
key_findings: ["记忆化下降主要来自优化器更新变化，可解释66–132%差距。", DP-SGD在ε=2–8范围内未显著带来额外记忆降低。, "HMAC降低40–61%标识符暴露且不引入二次泄露。"]
limitations: 仅限1–3B模型与结构化CSIRT数据，攻击与任务场景可能限制泛化。
---

## 1. 问题与背景  
CSIRTs 在漏洞扫描与事件记录上对语言模型进行微调，但这些数据往往包含 IP、主机名等敏感标识，可能泄露组织内部拓扑结构，从而引发 GDPR 与 LGPD 相关的隐私风险。现有研究表明语言模型在训练与微调过程中存在可被提取的记忆信息，但不同防护手段（如 DP-SGD 与伪匿名化）如何组合仍缺乏系统验证。尤其是在 1–3B 小型语言模型用于本地化安全运维的场景下，这一问题尚未被充分研究。

## 2. 核心思路 / 方法  
论文构建了一个面向 CSIRT 结构化漏洞数据的实证研究框架，对比 raw、QLoRA+大 batch、以及 DP-SGD（ε ∈ {2, 8}）等四种训练范式，并在四个 1–3B SLM 上训练共 96 个 LoRA adapter。数据侧引入 HMAC 伪匿名化方案（AnonShield），并结合 20 个 canary 样本，通过多种抽取攻击（含 dual attack）评估记忆泄露。方法上还引入 matched-update 控制实验，用于区分“优化步数变化”与“DP-SGD 机制本身”的影响，从而分解不同防护机制对记忆降低的贡献。

## 3. 结果与结论  
实验发现，matched-update 控制本身即可解释 66%–132% 的记忆降低效果，说明优化更新规模变化已能复现主要下降趋势，而 DP-SGD 在此基础上几乎不再带来额外降低效果。HMAC 伪匿名化可将原始标识暴露降低约 40%–61%，且 slug 的泄露水平接近随机基线（±0.65 bits）。同时，在所有 96 个适配器与 few-shot 设置下，F1 仅为 0.19–0.28，表明在该训练预算下模型尚未达到可操作的分类性能水平。

## 4. 局限与适用边界  
该研究主要集中在 1–3B 参数规模的小型语言模型以及结构化 CSIRT 漏洞数据上，结论未必能直接推广到更大规模或非结构化语料场景。从现有文本无法确认其在更复杂任务或更强模型上的泛化能力与稳定性。

## Abstract
CSIRTs increasingly fine tune language models on vulnerability scan records, but these records expose internal network topology and create privacy risks under regulations such as GDPR and LGPD. We present the first empirical study of how DP SGD and HMAC pseudonymization interact when fine tuning small language models with 1B to 3B parameters on structured CSIRT data. We evaluate 96 LoRA adapters across four SLMs and four training regimes, including raw fine tuning, QLoRA with large batch training, and DP SGD with epsilon equal to 2 and 8. We also audit memorization using 20 planted canaries, four extraction attacks, and a dual attack targeting HMAC pseudonymized identifiers.   Our results show three main findings. First, matched update controls reproduce the observed reduction in memorization by reducing the number of optimizer updates alone, accounting for 66 percent to 132 percent of the measured effect, with a mean of 100 percent across three seeds and four models. In this setting, DP SGD provides the formal privacy guarantee but does not produce additional measurable reductions in memorization. Second, HMAC pseudonymization removes the original identifiers from the exposure surface, reducing exposure by 40 percent to 61 percent, while pseudonymized identifiers remain close to the expected random baseline and do not become a secondary memorization target. Third, F1 scores remain between 0.19 and 0.28 across all 96 adapters using four shot prompting, indicating that, under the evaluated training budget, 1B to 3B SLMs do not achieve operationally useful performance.

---

## 论文详细总结（自动生成）

## 1. TLDR
本文系统研究 1–3B 小型语言模型在 CSIRT 漏洞扫描数据上的隐私微调问题，重点分解 DP-SGD 与 HMAC 伪匿名化对“记忆化降低”的真实贡献。核心发现是：大部分记忆下降（66%–132%）可由优化器更新变化解释，DP-SGD 本身几乎不再带来额外收益，而 HMAC 仅改变标识符暴露路径但不引入二次泄露。

**Keywords：** Memorization Decomposition / DP-SGD / HMAC Pseudonymization / CSIRT / LoRA / SLM Privacy

---

## 2. 核心故事与贡献

### 2.1 问题与动机
- CSIRT（漏洞扫描/资产记录）数据包含 IP、主机名等敏感拓扑信息，微调 SLM 会导致结构性隐私泄露风险（见 Introduction）。
- 现有研究分别评估 DP-SGD 或 pseudonymization，但**缺乏组合机制下的归因分析**（见 Section I）。
- 尤其在 1–3B 本地部署模型中，这一“隐私-性能-机制归因”缺口尚未被填补。

### 2.2 核心洞察
- “看似 DP 带来的隐私收益”在很多情况下其实来自**优化器更新规模/训练动态变化本身**，而非 DP 噪声机制（见 Introduction / §IV）。
- pseudonymization（HMAC）降低的是“可识别性通道”，但不会成为新的记忆目标（slug 不可被系统性提取）。

### 2.3 主要贡献
- 构建 4 种训练 regime（raw / QLoRA / DP-SGD ε∈{2,8} / HMAC）+ 4 模型 → 96 adapters（见 Abstract / §III）。
- 提出 matched-update control，用于分离“优化更新变化 vs DP机制贡献”。
- 发现：
  - 66%–132% 记忆减少可由 matched-update 解释（见 Abstract / §IV）
  - DP-SGD 无额外增益
  - HMAC 降低 40%–61% 原始标识暴露

---

## 3. 相关工作与定位

### 对比维度

| 方向 | prior work | 本文 |
|------|------------|------|
| Memorization measurement | canary / exposure / MIA（Carlini et al., Mireshghallah et al.） | 同时评估 original + HMAC slug 双通道攻击 |
| DP-SGD | 多用于 BERT / GPT-2 / ≤1.5B（Yu et al., Li et al.） | 1–3B SLM + LoRA + CSIRT结构化数据 |
| pseudonymization | 多为 PII scrub / NLP文本 | HMAC PRF结构化字段保护（AnonShield） |
| 组合机制分析 | 几乎无系统比较 | 首次在同一 canary set 上联合评估 DP + PRF |
| CSIRT应用 | 多为 prompt / 7B+模型 | 本地 1–3B + 微调 + 结构化漏洞数据 |

---

## 4. 方法详解

### 4.1 总体框架
训练对象：
- 4 个 SLM：Gemma 3, Qwen3, Llama 3.2, VaultGemma（1–3B）
- 方法：LoRA / QLoRA + DP-SGD + HMAC preprocessing
- 数据：70,951 CSIRT vulnerability records

---

### 4.2 训练范式（Regimes）
设训练更新为 $\theta_t$：

- Raw fine-tuning：标准 SGD/LoRA
- QLoRA + large batch：控制梯度噪声
- DP-SGD：
  $$ \theta_{t+1} = \theta_t - \eta \cdot (\text{Clip}(g_t) + \mathcal{N}(0, \sigma^2)) $$
- HMAC pseudonymization：
  $$ x_{id} \rightarrow \text{HMAC}_k(x_{id}) $$

---

### 4.3 matched-update control（核心方法）
目的：控制“优化步数/更新量”影响

- 构造对照训练，使 update count 与 DP-SGD 对齐
- 关键：消除 optimizer dynamics bias
- 用于分解：
  - optimization effect
  - DP noise effect

---

### 4.4 记忆化评估
- 20 planted canaries
- 4 种 extraction attack：
  - probabilistic extraction
  - Min-K%++
  - log-prob probing
  - dual attack（original + HMAC slug）

---

## 5. 实验分析

### 5.1 主结果

| 机制 | 记忆变化 | 结论 |
|------|----------|------|
| matched-update | 66%–132% reduction | 主导因素 |
| DP-SGD (ε=2/8) | ≈0 additional gain | 无显著额外收益 |
| HMAC | 40%–61% exposure reduction | 有效降低 identifier leak |
| slug exposure | ±0.65 bits ~ random | 无二次泄露 |

---

### 5.2 任务性能

- F1 = 0.19–0.28（96 adapters）
- baseline random ≈ 0.20
- → 模型基本接近随机分类能力
- 说明：隐私增强条件下 utility 极低

---

### 5.3 关键发现总结
- DP-SGD ≠ 主要减记忆来源
- optimization dynamics ≫ noise injection
- pseudonymization 有效但只影响 input space

---

## 6. 局限性与风险

- **模型规模限制**
  - 仅 1–3B SLM，无法代表大模型行为（见 §III-B）
- **数据域限制**
  - CSIRT结构化漏洞数据，不等价自然语言语料
- **攻击覆盖有限**
  - 仅 20 canaries，可能低估真实泄露面
- **DP参数范围窄**
  - ε ∈ {2,8}，未探索更广隐私预算空间

---

## 7. 复现与后续问题

### 复现关键点
- matched-update 如何精确对齐 optimizer steps（关键实验设计点）
- DP-SGD + LoRA 的 gradient clipping 实现细节（Opacus相关）
- dual attack 对 HMAC slug 的构造方式

### 后续研究方向
- DP-SGD 与 LoRA 在 ≥7B 模型上的真实交互
- pseudonymization + RAG 系统中的隐私传播问题

---

## 8. 直接证据与待验证项

### 直接证据
- “66%–132% of memorization reduction explained by matched-update controls” —— Abstract / §IV
- “DP-SGD adds (ε, δ) but no further reduction” —— Abstract / Results
- “HMAC reduces exposure 40%–61%” —— Abstract / §V
- “slug exposure within ±0.65 bits of chance floor” —— §V
- “F1 in [0.19, 0.28] across 96 adapters” —— §VI

### 待验证项
- [需验证] matched-update 是否严格控制 total gradient noise equivalence（实现细节未完全展开）
- [需验证] DP-SGD 在更大 ε 范围是否仍无增益
- [需验证] dual attack 对真实 CSIRT 攻击面的代表性

（完）
