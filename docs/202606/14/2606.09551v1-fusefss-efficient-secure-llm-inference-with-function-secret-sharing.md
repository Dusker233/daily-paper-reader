---
title: "FuseFSS: Efficient Secure LLM Inference with Function Secret Sharing"
title_zh: FuseFSS：基于函数秘密共享的高效安全大语言模型推理
authors: "Yuhan Ma, Yong Li, Stefan Schmid"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09551v1"
tags: ["query:q9"]
score: 8.2
relevance_score: 7.8
quality_score: 8.5
reliability_score: 8.5
practicality_score: 8.0
evidence: 基于函数秘密共享的安全LLM推理隐私保护
tldr: "本文提出FuseFSS，用于解决两服务器FSS安全LLM推理中非线性与缩放算子成为性能瓶颈的问题。核心是用统一编译器将固定点算子表达为“区间划分+低阶多项式+谓词位”，运行时仅需两次FSS评估（比较与区间查表）及统一后处理，从而替代逐算子协议设计。在BERT与GPT类模型上实现1.24–1.50倍加速、通信减少9–16%，同时显著降低预处理开销，整体更易扩展与工程化实现，值得进一步精读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09551v1/fig-001.webp\", \"caption\": \"Table 11. Best-effort baseline attempts in our environment (batch size 1).\", \"page\": 15, \"index\": 1, \"width\": 1006, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.09551v1/fig-002.webp\", \"caption\": \"Figure 1. Gate-level activation microbench at L=128.\", \"page\": 14, \"index\": 2, \"width\": 1000, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2606.09551v1/fig-003.webp\", \"caption\": \"Table 5. Seq=128 breakdown of online time.\", \"page\": 13, \"index\": 3, \"width\": 763, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2606.09551v1/fig-004.webp\", \"caption\": \"Table 6. Seq=128 breakdown of online communication.\", \"page\": 13, \"index\": 4, \"width\": 691, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2606.09551v1/fig-005.webp\", \"caption\": \"Table 1. End-to-end two-server inference (sequence length 128, batch size 1).\", \"page\": 8, \"index\": 5, \"width\": 850, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2606.09551v1/fig-006.webp\", \"caption\": \"Table 4. Accuracy under fixed-point semantics, where ∆ = (FuseFSS−PyTorch).\", \"page\": 12, \"index\": 6, \"width\": 463, \"height\": 361}]"
motivation: 如何用统一编译框架替代逐算子协议设计，从而加速基于FSS的安全LLM推理中的固定点非线性计算。
method: 将所有固定点非线性算子抽象为统一规格，并编译为“packed comparison + interval lookup”两次FSS调用加标准MPC后处理。
result: "在BERT与GPT类模型实验中，相比Sigma实现1.24–1.50倍端到端加速，并减少9–16%在线通信。预处理阶段键生成时间降低14–23%，密钥规模减少20–24%。"
conclusion: 建议重点阅读第4节编译器设计与operator specification部分，理解统一表达如何覆盖不同非线性算子；以及第6节实验结果以评估工程收益与适用范围。
key_findings: [统一编译模型可替代逐算子FSS协议设计，降低系统复杂度。, 两次FSS操作即可覆盖比较与区间选择，减少专用协议开销。, 在Transformer推理中实现显著加速与通信/预处理开销下降。]
limitations: 依赖固定点算子建模与预定义区间规格，对算子表达能力仍有限且存在一定结构约束。
---

## 1. 问题与背景  
随着大语言模型被广泛以云服务形式部署，如何在不泄露用户输入（如提示词、嵌入或医疗文本）的情况下进行推理成为重要问题。两服务器安全推理基于秘密共享与MPC框架，能够保护输入隐私，但在Transformer/LLM场景中仍面临较高开销。现有基于FSS的GPU系统虽然加速了线性层计算，但固定点非线性算子（如激活函数、归一化与重缩放）仍成为主要瓶颈，因为这些算子通常需要复杂的比较与专用协议设计。因此，如何统一建模并高效执行这些非线性算子成为关键挑战。

## 2. 核心思路 / 方法  
论文提出FuseFSS，一种将固定点算子编译为统一执行流程的安全推理编译器，用于替代逐算子协议设计。其核心思想是将每个标量算子表示为“区间划分 + 低阶多项式 + 谓词比特”的规格描述，并通过编译器生成统一的执行结构。运行时仅依赖两次FSS评估：一次打包比较用于生成所有谓词比特，另一次区间查找用于获取当前区间的系数与常数项。随后通过标准MPC后处理（如Beaver乘法与布尔运算）完成最终计算。同时，FuseFSS引入mask-aware设计，使谓词计算与结构形状仅依赖公开规格，从而保证安全性与一致性。

## 3. 结果与结论  
实验结果表明，FuseFSS在保持模型精度不变的情况下，相比SOTA FSS安全推理系统实现了1.24×–1.50×的端到端加速。在通信开销方面，在线通信减少9%–16%，同时预处理阶段的密钥生成时间降低14%–23%。此外，生成的密钥大小也减少20%–24%，在BERT和GPT类模型上均表现稳定提升。整体结果说明统一编译式设计能够有效替代复杂的逐算子协议优化路径。

## 4. 局限与适用边界  
该方法主要针对固定点标量非线性算子设计，其表达能力依赖于区间划分与低阶多项式近似，因此对更复杂算子可能需要额外建模。系统依赖两服务器不串通假设以及预处理MPC框架，在威胁模型扩展时可能需要重新分析安全性。同时，为保证安全性引入的mask-independent shape约束可能限制部分动态结构优化空间。整体来看，其适用范围主要集中在现有Transformer推理中的标准固定点算子流水线场景。

## Abstract
Two-server secure inference allows a client to query a hosted large language model (LLM) without revealing prompts or embeddings. Recent GPU systems based on function secret sharing (FSS) make linear layers efficient, but fixed-point nonlinearities and helper operations remain a bottleneck because each operator is typically implemented as a bespoke protocol with its own comparisons, wrap-around corrections, and preprocessing material. We present FuseFSS, a compiler that replaces per-operator protocol design with a single compilation pipeline. For each scalar fixed-point operator, a compact specification lists its interval partition, low-degree arithmetic pieces, and required predicate bits. The compiler emits two batched FSS evaluations on the public masked value: one packed comparison that returns all predicate bits, and one vector interval lookup that returns the active coefficients and constants. Compared to the current state-of-the-art FSS-based GPU secure inference, FuseFSS preserves accuracy while achieving a $1.24\times$--$1.50\times$ end-to-end speedup and reducing online communication by $9\%$--$16\%$ on BERT and GPT-style models; preprocessing is also lighter, with $14\%$--$23\%$ lower key-generation time and $20\%$--$24\%$ smaller keys.

---

## 论文详细总结（自动生成）

# FuseFSS 精读总结（Structured Deep Dive）

---

## 1. TLDR
FuseFSS提出一种面向两服务器安全LLM推理的统一编译框架，将传统“逐算子协议设计”替换为标准化的固定点算子表示（区间划分 + 低阶多项式 + 谓词位）。系统运行时仅需**两次FSS调用（comparison + interval lookup）+ 标准MPC后处理**即可覆盖非线性计算。

在BERT与GPT类模型上实现：
- **1.24×–1.50×端到端加速**
- **9%–16%通信降低**
- **14%–23%预处理开销降低**

关键词：Secure Inference, Function Secret Sharing, MPC, Transformer, Compiler, Fixed-point Nonlinearity

---

## 2. 核心故事与贡献

### （1）问题与动机
- 两服务器安全推理已能加速线性层（FSS/DPF/DCF），但：
  - 非线性算子（GELU、softmax、rescale、norm）仍是瓶颈
  - 现有系统依赖“逐算子协议设计”
- 导致问题：
  - wrap-around / signedness 处理复杂
  - 每个算子需要独立比较协议
  - preprocessing 与 key generation 重复设计
- 结果：系统难以扩展，工程复杂度高（见 Introduction）

---

### （2）核心洞察
FuseFSS关键观察：
> 固定点非线性算子的“数据依赖结构高度一致”

统一结构：
- 区间选择（predicate bits）
- 区间内低阶多项式
- 少量常数/系数

因此可以抽象为统一格式：
\[
\text{Operator} = (\text{Interval Partition}, \text{Piecewise Polynomial}, \text{Predicate Bits})
\]

---

### （3）主要贡献
- 提出**FuseFSS compiler**
- 将任意标量固定点算子编译为：
  - 1× packed comparison FSS
  - 1× interval lookup FSS
  - + MPC后处理
- mask-aware安全设计：
  - 仅公开 operator shape
  - mask不影响协议结构
- 实验提升：
  - 1.24×–1.50× latency improvement（Section 6）
  - 通信下降9%–16%

---

## 3. 相关工作与定位

| 类别 | 代表工作 | 方法特点 | 局限 | FuseFSS区别 |
|------|----------|----------|------|------------|
| HE-based inference | Gilad-Bachrach et al. | 同态加密 | 延迟高 | 不依赖HE |
| MPC secure inference | Mohassel & Zhang | Beaver triples | nonlinear costly | FuseFSS减少nonlinear cost |
| IRON / BOLT / BumbleBee | Transformer MPC系统 | per-operator protocol | engineering复杂 | FuseFSS统一编译 |
| Sigma (SOTA FSS GPU) | Gupta et al. | FSS + GPU | operator-specific logic | FuseFSS消除operator-specific design |
| SHAFT / SHARK | secure softmax优化 | specialized kernels | 不通用 | FuseFSS generalizes |

---

## 4. 方法详解

### 4.1 Operator Specification（核心抽象）
每个算子定义为：
- 区间划分：\([α_0, α_1, ..., α_m]\)
- 多项式片段：\(P_i(x)\)
- 谓词函数：\(B_i(x)\)

形式：
\[
F(x) = (P_i(x), B_i(x)) \quad \text{if } x \in I_i
\]

---

### 4.2 编译流程（FuseFSS Compiler）

输入：operator spec + fixed-point metadata  
输出：统一执行图

#### Step 1：Packed Comparison FSS
- 输入 masked value: \(\hat{x} = x + r\)
- 输出：
  - 所有 predicate bits XOR shares
- 实现 comparison + sign extraction

---

#### Step 2：Interval Lookup FSS
- 输入：\(\hat{x}\)
- 输出：
  - active interval coefficients \((a,b,...)\)

---

#### Step 3：MPC Post-processing
- Beaver multiplication
- Bit-to-arithmetic conversion
- 组合：
\[
y = a \cdot x + b
\]

---

### 4.3 安全设计（mask-aware compilation）
关键约束：
- mask independent shape
- 不泄露：
  - interval数量
  - comparison结构
- 保证 semi-honest security model

---

### 4.4 统一执行路径
所有算子统一为：

```
x → mask → open ẋ
   → FSS-compare
   → FSS-lookup
   → MPC postprocess
   → output
```

---

## 5. 实验分析

### 5.1 主结果（Section 6, Table 1）
| 指标 | 提升 |
|------|------|
| End-to-end latency | 1.24×–1.50× faster |
| Online communication | ↓ 9%–16% |
| Key generation time | ↓ 14%–23% |
| Key size | ↓ 20%–24% |

---

### 5.2 模型范围
- BERT（Devlin et al.）
- GPT-style models（Radford et al.）

---

### 5.3 消融与系统分析
- Table 5：online time breakdown
- Table 6：communication breakdown
- Figure 1：gate-level microbenchmark
- Table 11：baseline failure/effort comparison

核心发现：
- nonlinear operators dominates runtime
- FuseFSS reduces operator fragmentation

---

### 5.4 关键结论
- 加速主要来自：
  - 消除 per-operator protocol overhead
  - 减少 comparison logic duplication

---

## 6. 局限性与风险

### （1）表达能力限制
- 依赖：
  - 区间划分
  - 低阶多项式近似
- 对复杂函数（如高精度softmax）可能需要更多分段

---

### （2）安全模型限制
- semi-honest two-party assumption
- 不适用于 malicious adversary without extension

---

### （3）结构约束风险
- mask-independent shape 可能限制动态优化
- operator必须“可编译”

---

### （4）工程依赖
- 强依赖 FSS backend 性能
- GPU优化程度影响整体收益

---

## 7. 复现与后续问题

### 复现关键点
- operator spec设计（区间 + poly + predicate）
- packed comparison FSS实现
- interval lookup batching机制

---

### 后续研究方向
- 支持：
  - malicious security model
  - dynamic operator graph
- 自动生成 operator specification（AutoFSS compiler）

---

## 8. 直接证据与待验证项

### 证据1（Section Abstract）
- “1.24×–1.50× speedup”
- “9%–16% communication reduction”

---

### 证据2（Section 1）
- nonlinear operations dominate transformer secure inference cost

---

### 证据3（Section 6, Table 1）
- end-to-end performance improvement on BERT/GPT

---

### 证据4（Section 3）
- mask-independent shape prevents leakage via protocol structure

---

### 证据5（Appendix N）
- ReLU case shows operator → interval + predicate + postprocess decomposition

---

### 待验证项
- [需验证] 不同 Transformer layer 对 interval partition 敏感性具体数值（论文未完全展开）
- [需验证] malicious security extension成本是否可控（未实验）
- [需验证] compiler在更大模型（>7B）上的扩展性数据

---

（完）
