---
title: "Hiding in Plain Floats: Steganographic Carriers for Indirect Prompt and Content Injection"
title_zh: 明面浮点中的隐藏：用于间接提示与内容注入的隐写载体
authors: "Mudit Sinha, Sanika Chavan"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08403v1"
tags: ["query:q9"]
score: 7.9
relevance_score: 7.8
quality_score: 8.0
reliability_score: 8.5
practicality_score: 7.5
evidence: 基于浮点隐写的LLM提示注入攻击
tldr: "论文研究LLM管线中通过结构化浮点参数（IFS与频域编码）隐藏提示注入的隐写载体，在多模型API与1.4万次实验中验证其在强文本分类防御下仍达94.3%攻击成功率，揭示文本审查对结构化数值通道的失效边界，并讨论简单检测可缓解但非通用防护，整体对安全评估与RAG/工具调用管线具有较高参考价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08403v1/fig-001.webp\", \"caption\": \"Figure 1. Attack success rate by carrier × defense across three current production models (n = 14,400). The cell of primary interest is T3 under the main-matrix classifier ensemble (94.3%), compared to 0% for the same payload text in plain form (T0). T2 and T1b-abl show that neither steganographic encoding alone nor fragmented presentation alone is sufficient.\", \"page\": 6, \"index\": 1, \"width\": 492, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2606.08403v1/fig-002.webp\", \"caption\": \"Figure 2. A T3 sample end-to-end, objective 3 (fabricated metadata). Top: the raw upstream input, ∼29 KB of JSON with two 1,044-value float arrays. No English text, no imperatives. TF-IDF and roberta-base both score this below 0.23 against a 0.5 threshold. Bottom: the ∼2 KB reconstructed report the model receives. The pipeline reverses the jitter (nearest-neighbour decode step), unshuffles, un-XORs, and renders the bytes as an xxd-style hex dump inside the analysis report. The model treats the dump as telemetry and propagates the injected [GRADE: PPB CRITICAL FAIL] field into its summary.\", \"page\": 11, \"index\": 2, \"width\": 1017, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2606.08403v1/fig-003.webp\", \"caption\": \"Figure 4. IFS trajectories at n = 964. Left: real Sierpinski (ACF 0.493). Centre-left: naive T3 with visible banding from characterfrequency bias. Centre-right: whitened intermediate (ACF 0.504, matches a real attractor). Right: final stored T3 after whitening, shuffling, and jitter for one illustrative payload (lag-1 ACF 0.035). Table 8 reports the aggregate mean lag-1 ACF after countermeasures across the broader statistical sweep (0.002). The shuffle reduces the original contraction-map autocorrelation under the first-order feature set of §4.2 and simultaneously makes semantic validation detect the current encoding.\", \"page\": 13, \"index\": 3, \"width\": 995, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2606.08403v1/fig-004.webp\", \"caption\": \"Figure 3. Per-scan detection rate for TF-IDF and roberta-base fine-tuned on identical training data. The two agree on every cell: 100% where textual signal exists, 0% on T3. Under the dual-scan posture (flag if either fires), T1b-abl is caught via the raw-input scan alone; T3 is caught by neither.\", \"page\": 13, \"index\": 4, \"width\": 935, \"height\": 394}]"
motivation: 当提示注入信号被隐藏在结构化浮点参数而非显式文本中时，现有文本型防御是否仍然有效。
method: 通过IFS与频域编码将攻击信息嵌入浮点数组等结构化参数，并在重建阶段恢复为模型可见的隐蔽载体实现间接注入。
result: "在14,400次跨模型实验中，该IFS浮点载体在Prompt Guard 2+TF-IDF与RoBERTa防御下仍达到94.3%攻击成功率。补充实验显示简单xxd检测可将该特定实现ASR降至0%，但依赖特征签名。"
conclusion: 建议优先阅读方法部分的两种载体（T2/T3）与重建机制，以及2×2消融实验，用于理解“数据层隐藏 vs 重建层碎片化”的分解贡献。结果与检测边界章节可用于判断该攻击是否具有泛化性与防御意义。
key_findings: [结构化浮点载体可绕过主流文本分类防御实现高ASR。, 数据层隐藏与重建层碎片化共同决定攻击成功率。, 简单格式特征检测可拦截当前实现但不具通用性。]
limitations: 攻击依赖特定结构化管线与重建流程，且存在明显实现级检测特征，泛化性有限。
---

## 1. 问题与背景  
本文研究的是在大语言模型应用管线中，通过“结构化数值载体”绕过传统文本级提示注入防护的隐蔽攻击问题。动机在于现有防御（如分类器、TF-IDF、Llama Guard 等）默认恶意信号以“可见文本”形式存在，但现实系统越来越多引入 JSON、浮点数组、仿真参数等结构化输入。作者指出，当这些数值在进入模型前被“重建为报告文本”时，攻击内容可能在检测阶段不可见但在模型侧已被激活，因此形成新的安全盲区。

## 2. 核心思路 / 方法  
论文提出两类基于浮点结构的隐写载体，用于间接注入提示与内容攻击，其中包括频谱系数编码（T2）以及基于迭代函数系统（IFS）的浮点数组载体（T3）。核心机制是将攻击负载嵌入连续数值（如坐标序列或参数数组）中，并在管线重建阶段恢复成看似普通的结构化报告，从而绕过文本检测器。相较传统 base64 或 unicode 混淆，这种方法在“数据层存储 + 重建层分片”双阶段同时规避检测，使恶意信号在文本视图生成前不可见，从而形成载体级隐写攻击。

## 3. 结果与结论  
在跨三种商业大模型、共 14,400 次真实 API 攻击实验中，IFS 浮点载体在 Prompt Guard 2 + TF-IDF 双层防御下仍达到约 94.3% 的攻击成功率。相同模式在 RoBERTa 检测器上也可复现，说明问题并非单一模型失效，而是载体结构导致的系统性盲区。同时实验显示，简单的 xxd 风格检测或语义校验可以将当前实现的攻击成功率降至 0%，但属于针对特定表示形式的局部修补。

## 4. 局限与适用边界  
该方法依赖特定的“结构化数值被重建为文本再输入模型”的管线设定，因此并不意味着所有结构化输入系统天然脆弱。现有结果也表明，一旦在数据进入重建阶段前进行格式级或语义级验证（如简单模式检测或结构约束），攻击即可被显著削弱。其有效性更偏向于揭示一种系统设计边界，而非通用、不可防御的攻击手法。

## Abstract
Text-centered prompt-injection defenses assume that the malicious signal is visible in one of the inspected text views. We study a reproducible LLM01-style indirect prompt/content-injection failure mode where that assumption breaks: a payload caught in plain English slips past the same detector when it is transported as structured float parameters and reconstructed only as fragmented telemetry. Across 14,400 attacked real-model trials on three commercial LLM APIs from different providers, the IFS-derived float-array carrier preserves 94.3% leakage ASR under the strongest dual-layer text-classifier defense evaluated in the main matrix: a Prompt Guard 2 + TF-IDF ensemble; the same carrier-level pattern also replicates with a fine-tuned roberta-base detector. We emphasize leakage ASR because downstream systems may act on quoted or reproduced markers even when the model refuses, but Strong ASR is the stricter metric for structurally compliant attack success. A 2 x 2 ablation shows that data-layer storage and reconstruction-layer fragmentation defeat different text views and that both are needed to evade both. A simple xxd detector and semantic validation block the current T3 instance, so the contribution is not an undetectable exploit but a measured failure boundary for text-only inspection in structured-input pipelines that expose reconstructed auxiliary channels to an LLM.