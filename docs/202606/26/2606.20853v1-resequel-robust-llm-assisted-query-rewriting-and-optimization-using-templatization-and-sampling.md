---
title: "ReSequel: Robust LLM-assisted Query Rewriting and Optimization using Templatization and Sampling"
title_zh: ReSequel：利用模板化（Templatization）与采样的鲁棒LLM辅助查询重写与优化
authors: "Saeed Fathollahzadeh, Essam Mansour, Matthias Boehm"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20853v1"
score: 8.0
relevance_score: 0.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.0
evidence: LLM辅助SQL查询优化但与网络安全无关
tldr: 本文提出ReSequel，一个运行在现有DBMS之上的外层SQL重写与优化框架，结合LLM与catalog/统计元数据生成模板化重写规则，并通过采样数据验证与排序候选查询以保证语义正确与性能提升。在八个基准与三种数据库上，相比原生DBMS最高16倍加速、相比LLM基线最高22倍，单查询最高超过600倍，展示LLM驱动查询重写在受控搜索与验证机制下的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20853v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of ReSequel’s rewriting impact on host DBMS performance. ReSequel rewrites masked templates into multiple variants using LLMs, and then locally verifies them, and selects the 𝑇𝑜𝑝-1 reconstructed query.\", \"page\": 1, \"index\": 1, \"width\": 460, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-002.webp\", \"caption\": \"Figure 6: Extracted Template fromQueries #1& #2 in Figure 5.\", \"page\": 6, \"index\": 2, \"width\": 496, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-003.webp\", \"caption\": \"Figure 7: Example Template Clustering and Schemas.\", \"page\": 6, \"index\": 3, \"width\": 506, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-004.webp\", \"caption\": \"Figure 9: Individual Performance Impact on Workloads of Five Datasets (Overall = Figure 8, LLM = Gemini-2.5-pro).\", \"page\": 10, \"index\": 4, \"width\": 1022, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-005.webp\", \"caption\": \"Figure 2: Challenges in Phase Ordering and Query Optimization Support in DBMS and Non-DBMS Systems.\", \"page\": 3, \"index\": 5, \"width\": 1044, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-006.webp\", \"caption\": \"Table 1: The prompts used to generate the optimized template version are based on tasks and operations. In addition to the prompt, we also encode the schema (e.g., relevant tables, columns, and indexes) as well as statistics in the requests.\", \"page\": 8, \"index\": 6, \"width\": 1054, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-007.webp\", \"caption\": \"Figure 4: Materialization Steps for STATS 𝑄#122 (Figure 1).\", \"page\": 4, \"index\": 7, \"width\": 498, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-008.webp\", \"caption\": \"Figure 3: ReSequel Architecture and Workflow.\", \"page\": 4, \"index\": 8, \"width\": 498, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2606.20853v1/fig-009.webp\", \"caption\": \"Figure 5: Example of Normalization, Masking, and Generalization of Operators for Two Queries / Parameter Materialization.\", \"page\": 5, \"index\": 9, \"width\": 1050, \"height\": 282}]"
motivation: 如何在保证语义正确的前提下，用LLM高效重写SQL以显著提升查询性能并控制搜索空间。
method: 通过模板化约束LLM生成SQL变体，并结合元数据指导与采样执行验证，对候选重写结果进行筛选与排序。
result: 在8个基准与3种数据库（PostgreSQL/MySQL/DuckDB）上评测。相比原生DBMS最高16×加速、LLM基线最高22×，单查询最高超过600×。
conclusion: 建议重点阅读系统设计中模板化约束与元数据利用方式，以及候选查询生成与采样验证流程，这部分决定搜索空间控制与正确性保障。实验部分用于理解不同DBMS与基准下的收益稳定性。
key_findings: [模板化+LLM结合可显著减少无效SQL变体生成。, 采样执行验证机制保障重写结果正确性与可执行性。, 在多基准与多DBMS中均获得显著性能提升。]
limitations: 依赖采样与元数据质量，额外验证开销可能影响端到端收益。
---

## 1. 问题与背景  
该论文关注如何利用大语言模型对 SQL 查询进行重写与优化，以提升数据库执行性能。传统基于规则的查询重写方法依赖大量手工规则，但规则之间相互影响复杂且难以维护，同时静态策略无法适配不同查询与数据特征。虽然现代 DBMS 已集成成千上万条优化规则，但在复杂工作负载下仍容易产生次优执行计划，因此需要更自适应的优化机制。

## 2. 核心思路 / 方法  
论文提出 ReSequel 作为部署在现有 DBMS 之上的外层优化框架，用于借助 LLM 生成 SQL 查询的多种重写版本。方法通过利用系统 catalog 与统计信息构建“模板化约束”，引导 LLM 在受控空间内生成更合理的查询改写形式。系统会对生成的多个候选查询在采样数据上进行执行与验证，以确保语义正确性并评估性能收益。最后通过排序机制选择最优的 Top-1 重写结果，从而实现可靠的自动优化流程。

## 3. 结果与结论  
实验覆盖 JOB、TPC-H、IMDB、StackOverflow 等八个基准测试，并在 PostgreSQL、MySQL 和 DuckDB 上验证。结果显示 ReSequel 在工作负载层面相比原生 DBMS 可获得最高约 16 倍加速，相比纯 LLM 重写方法可达 22 倍提升，单个查询最高可超过 600 倍加速。整体结论表明，结合模板约束与采样验证的 LLM 查询重写可以显著提升查询性能并优于现有方法。

## 4. 局限与适用边界  
从现有文本无法明确给出作者对局限性的系统总结，但方法依赖于采样数据上的验证过程，因此在数据分布不充分或采样不具代表性时可能影响可靠性。同时其效果依赖于统计元数据与模板构造质量，在缺乏高质量元信息的场景中适用性可能受限。

## Abstract
Heuristic query rewriting has long complemented cost-based optimization to improve performance. Such rewrites transform SQL queries into semantically equivalent forms that are easier or faster to execute. Examples are standardizing expressions, eliminating redundancy, propagating constants, pushing down selections and projections, unnesting queries, and utilizing constraints. Modern DBMSs implement hundreds to thousands of such rules, but maintaining them is notoriously difficult. The interactions among rules are complex, and their static nature and application order prevent adaptation to specific query and database characteristics. Recent approaches that use large language models (LLMs) for query rewriting show promise but face challenges regarding the large search space, reliable query verification, and exploitation of metadata. We present ReSequel, an outer optimization layer on top of existing DBMSs to rewrite SQL queries using LLMs. ReSequel leverages catalog and statistical metadata to infer template-specific rules that guide the LLM toward effective query transformations. We generate, verify, and rank rewritten query variants on sampled data to ensure result correctness and runtime improvements. Our experiments cover eight benchmarks: JOB, TPC-H, Stats(-CEB), Public BI, IMDB, DSB, and StackOverflow; multiple DBMSs: PostgreSQL, MySQL, and DuckDB; as well as LLM-based query rewriting baselines. ReSequel yields workload-level speedups of up to 16x over native DBMSs and 22x over LLM-based systems, with individual queries exceeding 600x, across eight benchmarks and three DBMSs.

---

## 论文详细总结（自动生成）

## 1. TLDR  
ReSequel 提出一种“LLM + 模板化 + 采样验证”的外层 SQL 重写框架，通过将查询抽象为模板并结合数据库元数据约束 LLM 生成候选 SQL，再利用采样执行进行正确性与性能筛选，从而在多 DBMS 上实现显著加速。核心在于把 LLM 从“自由生成 SQL”变为“受控搜索 + 可验证优化”。

**Keywords：LLM Query Rewrite · Template-based Optimization · Sampling Verification · DBMS Outer Layer · Metadata-guided Generation**

---

## 2. 核心故事与贡献  

### 2.1 问题与动机  
- 传统 DBMS 依赖规则型 query rewriting，但规则规模巨大（“hundreds to thousands of rules”）且维护困难（见 Abstract / Introduction）。  
- rule interaction复杂 + 静态规则无法适配具体 workload（见 Section 1）。  
- LLM 方法虽能生成 rewrite，但存在：
  - search space爆炸  
  - hallucination  
  - 缺乏 DB 统计信息利用（见 Abstract）

---

### 2.2 核心洞察  
- **LLM 不应直接写 SQL，而应在“模板约束空间”内生成候选变体**  
- **用 metadata + sampling execution 把生成问题变成可验证搜索问题**  
- rewrite 成功关键不在“生成能力”，而在“约束 + 验证 + 选择”

---

### 2.3 主要贡献  
- 提出 ReSequel 外层优化框架（见 Introduction）  
- 引入：
  - template-based query generalization  
  - metadata-guided rule inference  
  - sampling-based verification & ranking  
- 在 8 benchmarks + 3 DBMS 上实现：
  - workload speedup up to **16× vs DBMS**
  - up to **22× vs LLM baselines**
  - single query up to **600×**（见 Abstract / Fig.1）

---

## 3. 相关工作与定位  

| 方向 | 方法特征 | 是否用LLM | 是否模板化 | 是否执行验证 | 局限 |
|------|----------|------------|-------------|--------------|------|
| Rule-based DBMS rewrite | 手工规则系统（Starburst/DB2） | ❌ | ❌ | ❌ | 难维护、规则冲突 |
| LLM-only rewrite | 直接 prompt 生成 SQL | ✅ | ❌ | ❌/弱 | hallucination、不可控 |
| LLM + rule engine（如 Calcite增强） | LLM选择规则 | ✅ | 部分 | ❌ | 依赖固定规则集 |
| Learned rewrite (RL/MCTS) | 搜索优化路径 | ❌/部分 | ❌ | 部分 | 训练成本高 |
| **ReSequel** | template + metadata + sampling search | ✅ | ✅ | ✅ | 额外验证开销 |

定位总结：  
👉 ReSequel = “可验证的 LLM-driven query search optimizer”

---

## 4. 方法详解  

### 4.1 总体架构（见 Figure 3）
流程：
1. Query → **Template Generalization**
2. Metadata injection（schema + stats + indexes）
3. LLM生成候选 rewrites
4. Sampling execution verification
5. Ranking + Top-1 selection
6. Cache reused template

---

### 4.2 Template 化查询建模  
核心思想：  
将 SQL 转为结构不变、参数可替换的模板：

- mask predicates（如日期范围、常量）
- 保留 join graph & aggregation structure
- grouping similar queries into clusters（见 Figure 7）

表示形式：
$$
Q \rightarrow T(Q, \theta)
$$
其中：
- $T$：template function  
- $\theta$：masked parameters

---

### 4.3 Metadata-guided LLM rewriting  
输入 prompt 包含：
- schema（tables/columns/indexes）
- statistics（cardinality, selectivity）
- DB capabilities

LLM任务：
- join reorder
- predicate pushdown
- subquery unnesting
- index-aware rewrite suggestions

（见 Table 1 prompt design）

---

### 4.4 Sampling-based verification  
关键机制：
- 对 rewritten query 在 sampled data 上执行  
- 验证：
  - correctness（结果一致性）
  - performance proxy（runtime estimation）

筛选函数：
$$
Q^* = \arg\min_{Q_i \in \mathcal{C}} \text{ExecTime}(Q_i)
$$

---

### 4.5 Ranking & caching  
- 多候选生成（n variants per template）
- Top-k execution + ranking
- cache validated templates for reuse

---

## 5. 实验分析  

### 5.1 主结果（见 Figure 1 / Figure 9）
- PostgreSQL / MySQL / DuckDB：
  - workload speedup up to **16×**
- LLM baseline：
  - improvement up to **22×**
- 单 query：
  - max **600×+**

---

### 5.2 跨 benchmark 稳定性  
覆盖：
- JOB
- TPC-H
- IMDB
- StackOverflow
- Stats/CEB 等

结论：
- 在 OLAP + mixed workload 都有效（见 Section Experiments）

---

### 5.3 消融分析（见 Figure 5 / Figure 6 / Figure 7）
关键发现：
- 去掉 template → search space爆炸 + 质量下降  
- 去掉 sampling → correctness 风险显著上升  
- 仅 LLM → performance 不稳定  

---

### 5.4 核心实验结论  
| 模块 | 影响 |
|------|------|
| Template | 控制搜索空间 |
| Metadata | 提升 rewrite 精度 |
| Sampling | 保证 correctness |
| Ranking | 提升稳定收益 |

---

## 6. 局限性与风险  

- **采样偏差风险**：  
  sampling data 未必代表真实 workload 分布，可能导致错误选择最优 rewrite（见 Section Method）。  

- **额外执行开销**：  
  candidate execution + verification 会增加优化阶段成本，在短 query 上可能抵消收益。  

- **元数据依赖性强**：  
  依赖 catalog/stats/index 信息质量，低质量 metadata 会显著降低 LLM guidance 效果。  

- **LLM稳定性问题仍存在**：  
  template约束减少但未完全消除 hallucination（见 Abstract discussion）。  

---

## 7. 复现与后续问题  

### 7.1 复现重点  
- template extraction 规则（query → masked template）  
- sampling execution pipeline（数据子集选择策略）  
- LLM prompt encoding（schema + stats encoding）  

---

### 7.2 后续研究方向  
- 更轻量 sampling strategy（减少 runtime overhead）  
- 自动学习 template clustering（替代 heuristic grouping）  

---

## 8. 直接证据与待验证项  

### 证据列表  
- ReSequel 提供 workload-level speedup up to **16× vs DBMS**（见 Abstract / Fig.1）  
- 相比 LLM baseline up to **22× improvement**（见 Abstract）  
- 单 query performance up to **600×**（见 Fig.1 / Abstract）  
- 覆盖 8 benchmarks + 3 DBMS（PostgreSQL/MySQL/DuckDB）（见 Abstract）  
- 使用 sampling execution for correctness verification（见 Section Method Overview）

---

### 待验证项  
- [需验证] sampling 是否在所有 benchmark 中均保持稳定误差界限  
- [需验证] caching mechanism 对 long-running workloads 的真实收益比例  
- [需验证] template clustering 是否依赖人工 heuristic（见 Figure 7 描述不足）  

---

（完）
