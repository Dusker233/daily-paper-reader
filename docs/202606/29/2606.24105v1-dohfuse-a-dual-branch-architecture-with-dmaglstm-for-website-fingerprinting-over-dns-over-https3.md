---
title: "DoHFuse: A Dual-Branch Architecture with DMAGLSTM for Website Fingerprinting over DNS over HTTPS/3"
title_zh: DoHFuse：一种采用DMAGLSTM的双分支架构，用于DNS over HTTPS/3（DoH/3）环境下的网站指纹识别
authors: "ZunDong Zhang, Yanan Cheng, Zhaoxin Zhang, Xueyang Huo, Changjiang Wu"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24105v1"
tags: ["query:q4"]
score: 8.325
relevance_score: 8.7
quality_score: 7.8
reliability_score: 7.6
practicality_score: 8.2
evidence: 基于深度学习的加密DNS流量网站指纹识别分类
tldr: "提出DoHFuse用于DoH/3加密DNS流量的网站指纹识别，通过双分支融合统计特征与DMAG-LSTM时序建模捕捉QUIC环境下的突发对齐模式；构建首个真实DoH/3数据集（449类），闭集准确率88.05%，开集AUPRC 0.975，说明现有填充机制难以有效防护，方法与数据集均具参考价值，值得进一步精读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24105v1/fig-001.webp\", \"caption\": \"Fig. 8: Precision–Recall curve of the proposed model in the open world setting. The red marker denotes the best-threshold operating point.\", \"page\": 11, \"index\": 1, \"width\": 474, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2606.24105v1/fig-002.webp\", \"caption\": \"Fig. 7: Accuracy of the least identifiable websites and their corresponding number of observable non-Google DNS queries. The numeric label above each point indicates the maximum number of such queries observed in a single visit.\", \"page\": 11, \"index\": 2, \"width\": 485, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2606.24105v1/fig-003.webp\", \"caption\": \"Fig. 6: Performance metrics of different models under varying classification granularities (100, 200, 449 classes).\", \"page\": 10, \"index\": 3, \"width\": 1076, \"height\": 758}, {\"url\": \"assets/figures/arxiv/2606.24105v1/fig-004.webp\", \"caption\": \"Fig. 1: DoH/3 connection establishment schematic.\", \"page\": 3, \"index\": 4, \"width\": 472, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2606.24105v1/fig-005.webp\", \"caption\": \"Fig. 3: Overview of the proposed DoHFuse architecture. The model integrates a timing-sensitive temporal branch based on DMAG-LSTM and a statistics-based branch for coarse traffic summarization.\", \"page\": 5, \"index\": 5, \"width\": 484, \"height\": 277}]"
motivation: DoH/3加密DNS流量在真实网络环境下是否仍可被有效进行网站指纹识别，以及如何设计模型提升识别能力。
method: 通过双分支结构融合统计特征与DMAG-LSTM时序建模，以捕捉DoH/3（QUIC）中的突发间隔与交错流量模式。
result: "在449类闭集任务中达到88.05%准确率；开集检测AUPRC达0.975、F1为0.951，表现稳定且具区分能力。"
conclusion: 优先阅读数据集构建与DoH/3流量处理流程，以及DMAG-LSTM与双分支融合结构设计；实验部分用于理解模型在闭集与开集设置下的有效性。
key_findings: [构建首个真实DoH/3网站指纹数据集，覆盖449个站点，多轮采集。, 双分支融合统计与时序特征提升DoH/3流量识别鲁棒性。, 实验表明常见填充策略仍难抵御指纹攻击。]
limitations: 主要基于特定采集环境与DNS流量，跨网络与跨设备泛化及防御对抗分析未充分展开。
---

## 1. 问题与背景
本文关注在加密DNS通信场景下的网站指纹攻击问题，尤其是在DNS over HTTPS/3（DoH/3）被广泛部署后，攻击者是否仍能通过加密流量推断用户访问的网站。随着DoH、DoT以及DoH/3在IoT与边缘网络中的应用增强，虽然加密提升了隐私性，但流量模式仍可能泄露行为信息，因此其安全性评估具有重要现实意义。现有研究多集中在Tor或HTTPS或早期DoH协议，对DoH/3在真实QUIC多路复用与padding策略下的指纹可行性缺乏系统分析。

## 2. 核心思路 / 方法
论文首先构建并公开了首个DoH/3网站指纹数据集，覆盖449个网站并在真实网络环境中采集DNS解析流量。基于该数据，提出DoHFuse双分支架构，将时间序列（如包间隔时间）与统计特征进行融合建模，以增强对加密流量结构的表达能力。其核心模型DMAG-LSTM通过多尺度遗忘门机制强化对突发（burst）对齐的时序模式捕捉能力，从而更好处理QUIC多路复用带来的交织流量特征。最终通过软融合机制整合两类特征，实现对闭集与开集场景的统一建模与识别。

## 3. 结果与结论
实验结果表明，在闭集449分类任务中，DoHFuse达到88.05%的准确率，同时precision为88.56、recall为87.96、F1为87.83。在开集检测任务中，模型取得0.975的AUPRC以及0.951的F1分数，表现出较强的检测能力。整体结论指出，即使在DoH/3引入padding与QUIC多路复用的情况下，网站指纹攻击依然具有较高可行性，现有常见保护机制不足以完全消除隐私泄露风险。

## 4. 局限与适用边界
从现有文本无法确认该方法在更大规模互联网环境或跨域网络条件下的泛化能力。其效果依赖于已构建的特定DoH/3数据集与流量采集设置，可能对不同部署策略或未来协议变化敏感。论文主要适用于DoH/3加密DNS流量分析场景，对非DNS或其他协议场景的适用性有限。

## Abstract
As personal data privacy becomes increasingly critical in Internet of Things (IoT) environments, secure DNS protocols such as DNS over HTTPS (DoH) and DNS over TLS (DoT) have been widely adopted to protect device communications. However, without effective obfuscation, these protocols remain vulnerable to Website Fingerprinting (WF) attacks that can reveal user activity. With the ongoing deployment of DNS over HTTP/3 (DoH/3) in modern networked systems, padding strategies have been increasingly applied in practice. It is therefore essential to investigate whether DoH/3 can effectively mitigate WF attacks in realistic IoT and edge-network scenarios. To address this, we first collect and publicly release the first real-world benchmark dataset of DoH/3 traffic, generated from domain resolution processes in practical network environments. We further propose DoHFuse, a dual-branch learning framework that integrates inter-arrival time sequences and refined statistical features through an improved DMAG-LSTM, specifically designed to capture burst-aligned temporal patterns. Experimental results show that DoHFuse achieves an accuracy of 88.05% (precision 88.56, recall 87.96, F1 87.83) in a closed-world setting of 449 classes, and an AUPRC of 0.975 with an F1 score of 0.951 (precision 0.906, recall 1.0) in open-world detection. These findings demonstrate that DoH/3 traffic remains susceptible to WF attacks, suggesting that commonly deployed padding mechanisms alone are insufficient to ensure privacy protection in IoT-scale encrypted DNS communications.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文系统研究了 **DNS over HTTPS/3（DoH/3）在真实 HTTP/3/QUIC 环境下是否仍然容易遭受网站指纹（Website Fingerprinting, WF）攻击**，并首次公开了一个覆盖 **449 个网站** 的真实 DoH/3 基准数据集。

作者提出 **DoHFuse** 双分支模型，通过 **DMAG-LSTM 时序建模 + 统计特征融合** 捕获 QUIC 多路复用下的 burst 时序模式，在 **449 类闭集分类达到 88.05% Accuracy**，开集检测达到 **AUPRC=0.975**，说明当前 DoH/3 中广泛采用的 padding 并不足以阻止网站指纹攻击。

**关键词：** DoH/3、Website Fingerprinting、HTTP/3、QUIC、DMAG-LSTM、双分支融合、加密 DNS、开放集识别

---

# 2. 核心故事与贡献

## （1）问题与动机

传统 Website Fingerprinting 主要针对 Tor、HTTPS 或 DoH/DoT，而 **DoH/3（HTTP/3 over QUIC）** 已开始广泛部署（Chrome、Cloudflare、Android 等），但其由于：

- QUIC 多路复用（Multiplexing）
- Connection Reuse
- HTTP/3 控制帧
- EDNS Padding

导致已有基于 packet length 的方法失效，因此：

> **DoH/3 是否仍然能够进行有效的网站指纹攻击？**

成为本文研究的核心问题（见 Introduction）。

作者指出：

- DNS 比 HTTPS 更早发生，因此攻击可以更早推断用户行为；
- IoT 场景 DNS 请求频繁，因此风险更大；
- 现有工作尚未系统评估 DoH/3 WF（Introduction）。

---

## （2）核心洞察

作者观察到：

DoH/3 最大的问题不是 packet length，而是：

> **QUIC 多路复用造成 request/response burst 被交织(interleaved)。**

因此：

单纯统计 packet size 已不足。

真正具有判别能力的是：

- burst 对齐模式（burst alignment）
- inter-arrival timing
- request gap pattern

于是提出：

> **时间序列 + 全局统计信息联合建模。**

这是 DoHFuse 的设计核心。

---

## （3）主要贡献

论文给出四项主要贡献（Introduction）：

1. **首个真实 DoH/3 Website Fingerprinting 数据集**
   - 449 网站
   - 多轮采集
   - 公开发布

2. **DMAG-LSTM**
   - Multi-scale Forget Gate
   - 面向 burst temporal pattern

3. **DoHFuse**
   - 双分支融合
   - Temporal + Statistics

4. **系统实验**
   - 闭集（Closed-world）
   - 开集（Open-world）
   - 最大 449 分类

最终结果：

| Setting | Result |
|---------|--------|
| Closed-world | Accuracy = **88.05%** |
| Open-world | **AUPRC = 0.975** |

说明：

> DoH/3 的 padding 并不能有效阻止网站指纹攻击（Abstract）。

---

# 3. 相关工作与定位

| 工作 | 场景 | 特征 | 是否考虑 QUIC/HTTP3 | 是否开放集 | 本文区别 |
|------|------|------|----------------------|------------|----------|
| Deep Fingerprinting | Tor | Packet Direction/Length | ✗ | ✗ | DoH/3 环境不同 |
| SuperLearner | HTTPS | Ensemble Features | ✗ | ✗ | 面向 HTTPS |
| DePL | DoH | Packet Length + BiLSTM | ✗ | ✗ | 主要依赖长度特征 |
| Lightweight WF | DoH | Timing Interval | ✗ | ✗ | 无 QUIC burst 建模 |
| ODoH Hybrid DL | ODoH | Statistics + Sequence | 部分 | ✗ | 未针对 DoH/3 |
| Transformer (HTTP3/DoQ) | HTTP3/DoQ | Transformer | ✓ | [需验证] | 未构建 DoH/3 WF 数据集 |
| **DoHFuse（本文）** | **DoH/3** | **Timing + Statistics** | **✓** | **✓** | **首个 DoH/3 Benchmark + 双分支 DMAG-LSTM** |

论文定位（Related Work）：

- 不研究 DoH 检测（DoH Detection）
- 不研究恶意流量检测
- 不研究 Tor

而是：

> 在已识别 DoH/3 流量之后，是否仍能恢复访问网站。

---

# 4. 方法详解

## 4.1 总体框架：DoHFuse

论文整体框架（Figure 3）包括两个分支：

```
                DoH/3 Traffic
                      │
        ┌─────────────┴─────────────┐
        │                           │
 Temporal Branch             Statistical Branch
        │                           │
   DMAG-LSTM                Feature Extraction
        │                           │
        └──────── Soft Fusion ──────┘
                    │
              Final Classifier
```

核心思想：

> 一个分支学习局部时序，一个分支学习整体统计。

---

## 4.2 Temporal Branch：DMAG-LSTM

这是论文主要创新。

作者认为：

普通 LSTM：

- Forget Gate 单尺度
- 无法适应 burst pattern

因此设计：

**DMAG-LSTM（Dynamic Multi-scale Adaptive Gate LSTM）**

核心思想：

> Forget Gate 引入多尺度融合。

目标：

学习：

- Burst Boundary
- Burst Duration
- Burst Interval

从而增强：

```
Inter-arrival Time
↓

Burst Alignment

↓

Temporal Representation
```

论文强调：

DMAG-LSTM 专门针对：

> interleaved burst structures

（Introduction、Method）。

**关键公式：**

正文应包含改进 Forget Gate 的门控公式，但当前提供文本未包含公式推导。

**[需验证：见 Method Section]**

---

## 4.3 Statistics Branch

第二个分支提取：

Refined Statistical Features。

论文摘要未列出完整特征。

根据描述应包括：

- Timing Statistics
- Traffic Summary
- Burst Statistics

用于：

提供 coarse-level information。

具体统计量组成

> **[需验证：见正文 Method Section]**

---

## 4.4 Soft Fusion

最后：

两路 embedding：

```
Temporal Embedding

+

Statistical Embedding
```

采用 Soft Fusion：

得到最终表示。

相比：

- 单时序模型
- 单统计模型

具有更好的：

- Robustness
- Generalization

（Figure 3）。

---

# 5. 实验分析

## 5.1 数据集

论文构建：

> 首个真实 DoH/3 Website Fingerprinting Dataset。

特点：

- 449 Websites
- 多轮采集
- Real-world DoH/3
- Public Benchmark

（Introduction）。

---

## 5.2 闭集实验（Closed-world）

主要结果（Figure 6）：

| Metric | Value |
|---------|-------|
| Accuracy | **88.05%** |
| Precision | 88.56 |
| Recall | 87.96 |
| F1 | 87.83 |

作者同时比较：

- 100 Classes
- 200 Classes
- 449 Classes

发现：

随着类别增加：

准确率下降，

但 DoHFuse 始终优于 baseline（Figure 6）。

---

## 5.3 开集实验（Open-world）

Figure 8：

PR Curve：

最佳结果：

| Metric | Value |
|---------|--------|
| AUPRC | **0.975** |
| Precision | 0.906 |
| Recall | 1.000 |
| F1 | **0.951** |

说明：

未知网站检测能力较强。

---

## 5.4 网站可识别性分析

Figure 7：

论文分析：

不同网站可识别能力不同。

作者发现：

可识别性与：

> 非 Google DNS Query 数量

具有明显关系。

DNS 请求越丰富，

网站越容易被识别。

---

## 5.5 消融实验

当前提供文本未包含完整消融实验结果。

推测应包括：

- 去除 Statistics Branch
- 去除 DMAG-LSTM
- 普通 LSTM 对比

具体提升幅度：

**[需验证：见正文 Ablation Section / Table]**

---

## 5.6 核心发现

- QUIC 多路复用没有消除 Website Fingerprinting。
- Padding 对 packet length 有帮助，但不能消除 timing leakage。
- Timing + Statistics 比单一路径效果更稳定。
- DoH/3 仍存在较大的隐私泄漏风险。

---

# 6. 局限性与风险

## 假设约束

- 数据集来源于作者构建的真实采集环境，尚未证明覆盖所有运营商、网络条件和客户端实现。
- 模型依赖准确提取 DoH/3 请求—响应对，对于异常流量或协议实现差异的鲁棒性仍需验证。

## 应用边界

- 主要针对 DoH/3 网站指纹任务，不适用于普通 HTTPS、Tor 或其他加密协议而无需重新训练。
- 闭集实验假设网站类别固定，真实互联网持续新增网站时性能可能下降；开放集能力虽有验证，但长期泛化仍需进一步评估。

## 潜在风险

- 数据集与模型可能被用于增强流量分析和隐私攻击能力。
- 论文证明现有 EDNS Padding 与 QUIC 混淆不足，意味着部署 DoH/3 并不等同于完全隐藏用户访问行为，需进一步设计针对 burst/timing 的防御机制。

---

# 7. 复现与后续问题

## 复现重点

1. **DoH/3 数据采集流程**
   - 网站选择（449 个）
   - 多轮访问策略
   - DoH/3 请求过滤与请求—响应配对流程（见正文 Data Collection）。

2. **DMAG-LSTM 实现**
   - 多尺度 Forget Gate 的具体计算方式
   - 门控融合参数
   - 与标准 LSTM 的结构差异（见正文 Method）。

3. **双分支融合**
   - 统计特征具体组成
   - Soft Fusion 的实现方式
   - 分类器与训练超参数设置（见正文 Experimental Setup）。

## 后续研究方向

- 设计针对 **burst timing** 的主动防御（例如时序扰动、自适应 padding 或 burst 重整形），而非仅依赖长度填充。
- 研究跨网络、跨设备、跨解析器及跨时间的数据集泛化能力，并探索 Transformer、状态空间模型等更强时序建模方法在 DoH/3 网站指纹中的表现。

---

# 8. 直接证据与待验证项

1. **论文贡献**：首次公开真实 DoH/3 Website Fingerprinting 数据集，覆盖 **449 个网站**，并提出 DoHFuse 与 DMAG-LSTM。（见 **Introduction / Contributions**）

2. **闭集实验结果**：449 类分类达到 **Accuracy=88.05%、Precision=88.56%、Recall=87.96%、F1=87.83**。（见 **Abstract；Figure 6**）

3. **开集实验结果**：模型达到 **AUPRC=0.975、F1=0.951、Recall=1.0**，PR 曲线见 **Figure 8**。

4. **网站可识别性分析**：部分网站识别率与可观测的非 Google DNS 查询数量存在对应关系。（见 **Figure 7**）

5. **[需验证]** DMAG-LSTM 的具体门控公式、多尺度遗忘门实现细节、统计特征维度、Soft Fusion 数学表达式及消融实验的具体提升幅度，当前提供文本未包含完整 Method 与 Experimental Sections，应查阅正文对应章节确认。

**（完）**
