---
title: "Securing LLM-Agent Long-Term Memory Against Poisoning: Non-Malleable, Origin-Bound Authority with Machine-Checked Guarantees"
title_zh: 防御LLM-Agent长期记忆免受投毒攻击：具有机器可验证保证的不可篡改、源绑定权限机制
authors: Yedidel Louck
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24322v1"
tags: ["query:q9"]
score: 8.4
relevance_score: 8.5
quality_score: 8.5
reliability_score: 8.5
practicality_score: 7.0
evidence: LLM智能体记忆投毒与安全防护
tldr: "论文研究LLM代理长期记忆被投毒导致跨会话操控的问题，指出现有基于内容或溯源的防护均可被“洗白”攻击绕过。提出非可变的来源绑定权限与协同验证机制TMA-NM，并给出形式化定理与机器验证证明。在多模型与多攻击通道基准中，现有方法最高达68%攻击成功率，而TMA-NM实现0%成功率且不损失功能，具有较强参考价值，适合进一步精读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24322v1/fig-001.webp\", \"caption\": \"TABLE III PER-MODEL CONSEQUENTIAL-ACTION ASR (STATIC / ADAPTIVE / WHITEBOX) AND UTILITY (%). TMA-NM (BOLD) IS 0/0/0 ON EVERY ONE OF THE EIGHT MODELS WITH NO UTILITY LOSS. NOTE THAT PROB DETECT FAILS completely ON GPT-5-CHAT. AUTO-GENERATED FROM RESULTS.JSON .\", \"page\": 6, \"index\": 1, \"width\": 556, \"height\": 719}, {\"url\": \"assets/figures/arxiv/2606.24322v1/fig-002.webp\", \"caption\": \"Fig. 5. Theory↔benchmark correspondence: attacker-action ASR (%, 8 models) for each defense class against each laundering channel. Each malleable defense fails on the channels the separation theorem predicts, and TMA-NM is 0% throughout.\", \"page\": 7, \"index\": 2, \"width\": 465, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2606.24322v1/fig-003.webp\", \"caption\": \"Fig. 3. Ablation (mean over the eight models): removing origin binding restores attacks, removing elevation costs utility, and removing the verdict log affects neither.\", \"page\": 7, \"index\": 3, \"width\": 419, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2606.24322v1/fig-004.webp\", \"caption\": \"Fig. 2. Unified benchmark (pooled, 8 models, Wilson 95% CIs). TMA-NM (green) is 0% on the direct attack and laundering while preserving 100% legit-utility.\", \"page\": 5, \"index\": 4, \"width\": 507, \"height\": 248}]"
motivation: 如何防止LLM代理的长期记忆在跨会话中被投毒并被“洗白”后驱动危险行为。
method: 将记忆权限与不可篡改的真实来源绑定，并通过Sybil抗性协同验证实现仅可信根可提升权限的非可变信息流控制机制TMA-NM。
result: "在跨模型与多攻击通道基准测试中，现有防御最高出现约68%攻击成功率。TMA-NM在所有模型与通道中均达到0%攻击成功率且保持完整合法功能。"
conclusion: 优先阅读威胁模型与形式化定义部分以理解“洗白攻击”与可变性定义，再看TMA-NM构造与定理证明。最后重点关注MEM-INV-Bench实验设置与跨模型结果表。
key_findings: [现有内容/溯源型防御在记忆洗白攻击下结构性失效。, 提出写入时来源绑定与不可变权限传播机制。, TMA-NM在多模型多通道实验中实现零攻击成功率且不损失功能。]
limitations: 依赖形式化威胁模型与构造性假设，真实开放环境适用性与攻击多样性未完全验证。
---

背景

论文研究的是 L## 1. 问题与LM Agent 长期记忆（Long-Term背景

论文研究的是 LLM Memory）中的“记忆投毒（memory poisoning）”问题：攻击者可以在某个会话中植入不可信内容，并在未来 Agent 的长期记忆投毒问题：攻击完全不同的会话中触发高风险行为，例如转账、配置者可以在一个会话中写入不可信内容，并在未来完全修改或数据外泄。作者指出，现有防御大多依赖内容检测、信任评分或来源无关的会话中诱导支付、配置修改或数据外泄等后果性操作。追踪（lineage）来决定记忆是否有权影响行动，但这些依据都可能被攻击者作者指出，现有防御主要依赖内容检测、信任评分或来源谱系（操纵。随着 MemGPT、Mem0 等具备持久记忆能力lineage）追踪，但这些信号都可以被攻击者“清洗”（laundering）成看似可信的的 Agent 系统广泛部署，该问题已成为影响 Agent 安全性的核心风险。

## 2. 核心思路 / 方法

形式。随着 MemGPT、Mem0 等持久化记忆机制论文提出 TMA-NM（Tamper-evident Memory Authority, Non-Malleable），其核心思想是将“行动成为主流能力，这种跨会话、跨时间的攻击面权限（authority）”与记忆的真实来源在写入时进行不可篡改绑定，而已成为 Agent 系统的重要安全隐患。

## 2. 核心思路 / 方法

论文提出 T不是依据内容本身或后续推导关系判断可信度。作者定义了三MA-NM（Tamper-evident Memory Authority, Non-Malleable），核心思想是将“可种 LLM 特有的“洗白（laundering）”渠道：Agent 自我总结（self-summarization）、执行权限”绑定到写入时记录的真实来源，而不是可信工具回显（trusted-tool echo）以及伪造共识（manufactured corroboration），并证明这些渠道能够使不可信来源绑定到可被改写的内容或派生关系。系统把记伪装成可信信息。为此，系统采用非可忆项建模为 `(content, origin, scope, t_write, act_class)`，其中 origin 由可信监控塑性信息流控制（non-malleable IFC），要求不可信来源产生器根据写入通道在写入时确定，并且任何的数据在传播过程中持续保持不可信属性。只有获得至少两个独立可信主体的佐证，或获得与具体从不可信内容派生出的摘要、代理自写笔记或工具回行为绑定的用户重新授权，记忆权限才允许提升，从而防止攻击者通过显都会继承“不可信”权限。作者形式内容改写或伪造证据完成权限升级。论文进一步给出了化了三种 LLM 特有的清洗通道：代理自摘要、可信工具回显和形式化模型，并通过机器验证的定理证明了该机制的必要性与充分性。

## 3. 结果与伪造共识，并通过机器检查的 TLA+ 模型证明：内容结论

论文构建了跨防御机制、跨攻击方式、跨模型的 MEM-INV-Bench 基准，在防御和可变谱系防御在清洗下不健全（T1），写时 8 个前沿模型上评测内容检测、Lineage来源绑定是必要条件（T2），而“不可变来源绑定 + 抗 Sybil 的多方、防护型 IFC 以及 TMA-NM 等方案。实验结果显示，现有防可信佐证提升”足以阻断后果性操作（T3）。御在理论预测的场景下均会失效，洗白攻击成功率最高可达与 MemLineage、CaMeL/Fides 等方法相比，创新 68%，而未防御系统在部分模型上的攻击点在于把权限提升限制为“至少两个独立可信主体确认”或“与具体操作绑定成功率可达到 100%。相比之下，TMA-NM 在所有模型、所有直接攻击和洗白攻击场景下均实现了的用户授权”，而不是允许内容自证合法性。

## 3. 结果与结论

论文构 0% 攻击成功率，同时保持完整合法功能。作者据此得出结论：基于内容或可建了跨防御、跨攻击、跨模型的 MEM-INV-Bench，在 8 个前沿模型上系统评塑来源关系的防御在长期记忆场景下本质上不可靠测。结果显示，现有防御在清洗攻击下出现高，而基于写入时来源绑定的非可塑权限机制能够提供可证明的安全保证。

## 达 68% 的攻击成功率，失败模式与理论预测一致；而 TMA-NM 在直接攻击和三类清4. 局限与适用边界

论文的安全保证依赖关键假设 A1，即系统洗攻击上均达到 0% 攻击成功率，同时保持 100% 合法任务效能够在写入时正确识别并标记真实来源通道；如果可信通道本身被攻用。作者因此得出结论：长期记忆的安全关键不在于破，则来源绑定也会继承错误标签。此外，论文明确将跨 Agent 更强的内容检测，而在于不可变的来源绑定和受限的权限提升机制。

## 4.共享记忆攻击排除在威胁模型之外，该问题被认为属于 Agent 间通信协议安全 局限与适用边界

该方案的保证建立在“写范畴。从现有文本无法确认该方案在大规模生产环境中的性能开销、部署时来源标签正确”的假设之上，即监控器能够根据认证通道复杂度以及长期运维成本。准确标注 user、trusted tool、agent 或 untrusted external；若可信工具通道本身被攻破，标签会被错误地视为可信。论文也明确将跨 Agent 的共享记忆与通信协议安全排除在外，因此其结论主要适用于单 Agent 的长期记忆写入—检索—执行流水线。

## Abstract
LLM agents increasingly rely on persistent long-term memory, which creates a critical vulnerability that we study here: memory poisoning. An adversary can store untrusted content in one session that later steers a consequential action, such as a payment, a setting change, or data exfiltration, in a future session. Existing defenses base a memory item's authority to act on either its content (detection or trust-scoring) or its derivation history (lineage). We show that both signals are malleable. An attacker can launder an untrusted origin through three channels specific to LLM agents: the agent's own summarization, a trusted-tool echo, and manufactured corroboration. Each makes the content look benign and breaks or flips its derivation edge to ``trusted.'' We formalize malleability for the memory write-retrieve-act pipeline and prove a machine-checked separation theorem. No content- or lineage-based defense is sound under laundering (T1), write-time origin binding is necessary (T2), and non-malleable origin-bound authority with Sybil-resistant corroboration-gated elevation is sufficient (T3). Our construction, TMA-NM (Tamper-evident Memory Authority, Non-Malleable), instantiates non-malleable information-flow control (IFC) for LLM-agent memory. A cross-defense, cross-attack, and cross-model benchmark over eight frontier models shows that existing defenses fail exactly where the theory predicts (up to 68% laundering attack-success), while TMA-NM reaches 0% attack success on both direct and laundering attacks across all models and channels, at full legitimate utility. We release the benchmark, harness, and machine-checked TLA+ models to support reproducibility.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文研究 **LLM Agent 长期记忆（Long-Term Memory）中的 Memory Poisoning（记忆投毒）**，指出现有依赖**内容检测（content）**或**来源谱系（lineage）**的防御都可以通过 LLM 特有的“洗白（laundering）”过程绕过，因此从理论上并不健全。

作者提出 **TMA-NM（Tamper-evident Memory Authority, Non-Malleable）**，通过**写入时来源绑定（write-time origin binding）+ 非可塑权限传播（non-malleable propagation）+ Sybil 抗性协同验证提升（corroboration-gated elevation）**构建不可篡改的信息流控制，并给出机器验证（TLA+）的充分性证明。在 8 个主流模型的 MEM-INV-Bench 上，现有方案洗白攻击最高 ASR 达 **68%**，而 TMA-NM 在所有攻击通道均达到 **0% ASR 且保持 100% 合法功能**。

**关键词：** LLM Agent、Long-Term Memory、Memory Poisoning、Information Flow Control、Origin Binding、Formal Verification、TLA+、Security

---

# 2. 核心故事与贡献

## 2.1 问题与动机

长期记忆已经成为现代 Agent（如 MemGPT、Mem0 等）的默认能力，但也引入了新的跨会话攻击面。

论文关注如下攻击链：

> **Write → Retrieve → Act**

攻击者可以：

1. 在 Session A 写入恶意记忆；
2. Agent 后续重新组织或总结该内容；
3. 数天甚至数周后，在 Session B 被检索出来；
4. 驱动真正的危险操作（支付、配置修改、数据外泄等）。

已有工作主要依赖：

- 内容检测（content detector）
- Trust score
- Provenance / Lineage

作者指出：

> **它们判断 authority 的依据都是可被攻击者改变（malleable）的。**

引言给出的实验背景：

- 已有 Memory Poisoning 可实现约 **99.8% injection success**
- 本文 benchmark 中现有防御在 laundering 下最高 **68% ASR**
- 未防御系统部分模型 ASR 可达 **100%**

（见 Introduction、Section I）

---

## 2.2 核心洞察（Core Insight）

论文提出一个非常明确的观点：

> **真正应该保护的不是 Memory Content，而是 Memory Authority。**

作者认为：

Memory 是否有权驱动危险动作，不应该取决于：

- 内容长什么样
- 谁总结出来
- lineage 是否还能追踪

而应永久绑定：

> **Memory 的真实来源（Origin）**

因此：

Authority 必须满足：

- Non-malleable（不可塑）
- Origin-bound（来源绑定）
- Propagation（传播过程中不会降低可信度）

这是全文最核心的新思想。

---

## 2.3 主要贡献

论文给出了五项贡献（Section I）：

| Contribution | 内容 |
|------------|------|
| C1 | 定义 LLM Memory Authority 与三类 laundering |
| C2 | 提出 Separation Theorem（T1/T2/T3），并进行 TLA+ 机器验证 |
| C3 | 构造攻击证明三类已有防御均失败 |
| C4 | 发布 MEM-INV-Bench（跨模型×跨攻击×跨防御） |
| C5 | 开源 benchmark、harness、TLA+ artifact |

论文最大的价值其实不是提出一个新算法，而是：

> **证明现有一整类思路从理论上就是不可能正确（unsound）。**

---

# 3. 相关工作与定位

论文将已有工作分成三类。

| 方法类别 | 判断依据 | 是否可被 Laundering | 本文观点 |
|-----------|----------|--------------------|---------|
| Content Detection | 内容是否恶意 | 是 | Unsound |
| Trust Scoring | 内容可信度 | 是 | Unsound |
| Lineage / Provenance | 来源图 | 是 | Unsound |
| Capability IFC（CaMeL/Fides） | Session 内信息流 | 长期记忆假设过强 | Unsound |
| MemLineage | Provenance | Summarization 后 lineage 丢失 | Unsound |
| **TMA-NM（本文）** | Write-time Origin Binding | 否 | Sound（在模型假设下） |

论文强调：

已有方法的问题不是：

> Detector 不够强

而是：

> 判断依据本身就是可塑（malleable）的。

因此无论 detector 如何调参，都无法解决结构性漏洞。

（见 Appendix A）

---

# 4. 方法详解

## 4.1 Threat Model

Memory Item 定义：

```text
(content,
 origin,
 scope,
 t_write,
 act_class)
```

其中：

```
origin ∈ {
    user,
    trusted tool,
    agent,
    untrusted external
}
```

```
act_class ∈ {
    none,
    inform,
    act
}
```

整个执行流水线：

```
Write
    ↓
Retrieve
    ↓
Act
```

（Section II）

---

## 4.2 三类 Laundering

论文定义了三个 LLM 特有攻击。

### L-a Self Summarization

```
恶意网页
      ↓
LLM 总结
      ↓
Agent 自己写入 Note
```

内容已经完全改变。

Lineage 消失。

---

### L-b Trusted Tool Echo

```
恶意输入
      ↓
Trusted Tool
      ↓
Tool Output
```

由于 Tool 是 Trusted，

很多系统直接相信输出。

实际上只是 Echo。

---

### L-c Manufactured Corroboration

攻击者：

```
Memory A
Memory B
Memory C
```

三条都来自攻击者。

但是：

```
Count >= 2
```

很多系统认为：

> Consensus

于是提升 Trust。

实际上属于：

> Sybil。

（Section IV）

---

## 4.3 TMA-NM 四个机制

论文提出四个模块。

### M1 Write-time Origin Binding

写入时立即绑定：

```
origin
      ↓
authority
```

例如：

```
untrusted
      ↓
act = none
```

以后不能再修改。

---

### M2 Non-malleable Propagation

这是全文核心。

任何 Derived Item：

```
untrusted
      ↓
summary
```

仍然：

```
act = none
```

即：

Authority 不随内容改变。

同样：

```
Tool(untrusted)
```

输出：

```
untrusted
```

不会因为 Tool Trusted 而升级。

---

### M3 Corroboration-Gated Elevation

唯一允许升级 Authority 的方式：

至少满足之一：

- ≥2 Independent Trusted Principals
- Action-bound User Authorization

不能：

```
Memory 自己证明自己可信
```

也不能：

```
多个攻击者副本互相证明
```

这是论文用于抵御 Sybil 的核心设计。

---

### M4 Tamper-evident Verdict Log

所有决策：

```
append-only hash chain
```

记录：

- bind
- retrieve
- act verdict

用于审计。

论文消融实验显示：

删除 Log：

不会影响安全性，

主要提升：

Audit。

（Section III、Figure 3）

---

# 5. 实验分析

## 5.1 Benchmark

MEM-INV-Bench：

三维组合：

```
Defense
×

Attack

×

Model
```

模型包括：

- GPT-5-chat
- GPT-4o-mini
- Claude Opus 4.1
- Claude Sonnet 4.5
- Gemini 2.5 Flash
- Llama-4 Maverick
- DeepSeek Chat
- Qwen3-235B

（Section VI）

---

## 5.2 主实验

论文 Figure 2 / Table III 的主要结果：

| 方法 | Laundering ASR | Utility |
|------|---------------|---------|
| None | 61–84% | 100% |
| Content | 失败 | 部分下降 |
| Lineage | 失败 | 保持 |
| Capability IFC | 失败 | 保持 |
| **TMA-NM** | **0%** | **100%** |

核心观察：

- 现有方法失败模式完全符合理论预测；
- TMA-NM 在所有模型、所有攻击类型上均保持 **0% 攻击成功率**。

（Figure 2、Figure 5、Table III）

---

## 5.3 消融实验

Figure 3：

作者分别删除：

- Origin Binding
- Elevation
- Verdict Log

结果：

| 去掉模块 | 影响 |
|----------|------|
| Origin Binding | 攻击恢复 |
| Elevation | Utility 降低 |
| Verdict Log | 基本无影响 |

说明：

真正决定安全性的：

> Origin Binding + Propagation。

---

## 5.4 核心发现

论文实验与理论高度一致：

1. Content 方法失败；
2. Lineage 方法失败；
3. Capability IFC 失败；
4. 失败位置与 Separation Theorem 一致；
5. TMA-NM 全部通过。

作者称其为：

> Theory ↔ Benchmark Correspondence

（Figure 5）

---

# 6. 局限性与风险

### （1）来源标记假设（A1）

论文假设：

Monitor 能准确识别：

- user
- trusted tool
- agent
- external

如果认证通道被攻破：

Origin Binding 会绑定错误标签。

这是全文最重要的前提。

（Section II）

---

### （2）单 Agent 场景

论文明确：

Cross-Agent Shared Memory

不在研究范围。

Agent Communication Security 需要其它工作解决。

---

### （3）真实部署成本

论文没有详细分析：

- Monitor 性能开销
- Hash Chain 扩展性
- 大规模 Memory Store 部署成本

均为 **[需验证]**。

---

### （4）攻击覆盖范围

实验覆盖：

- Direct
- Sleeper
- Laundering

对于更复杂：

- 多模态 Memory
- 在线协作 Agent
- 长链 Tool Pipeline

正文没有充分验证，属于 **[需验证]**。

---

# 7. 复现与后续问题

## 复现重点

### （1）Origin Binding

必须保证：

```
Write Time
```

绑定真实来源，

不能依赖内容分析。

---

### （2）Propagation Rule

需要实现：

```
Derived Item

inherits

max(untrust)
```

这是 T2/T3 成立的关键。

---

### （3）Corroboration

必须保证：

```
≥2 Independent Trusted Principals
```

而不是：

```
Count >=2
```

否则会重新受到 Sybil Attack。

---

## 后续研究方向

1. 将 TMA-NM 扩展到 **多 Agent / Shared Memory / MCP** 场景，研究跨主体来源绑定与权限传播机制。
2. 探索更细粒度的信息流传播（如结构化数据、字段级 taint、value-level capability token），降低保守传播带来的潜在可用性损失（正文讨论了该扩展方向）。

---

# 8. 直接证据与待验证项

| 证据 | 来源 |
|------|------|
| 三类 laundering（Self-summarization、Trusted-tool Echo、Manufactured Corroboration）定义及其导致内容/谱系失效 | Section I、Section IV |
| Separation Theorem：T1（内容/lineage 不健全）、T2（写时来源绑定必要）、T3（来源绑定+协同提升充分） | Section IV（机器验证 TLA+） |
| TMA-NM 包含四个机制：M1 写时来源绑定、M2 非可塑传播、M3 Sybil 抗性协同提升、M4 Tamper-evident Verdict Log | Section III、Figure 1 |
| Figure 2 / Figure 5 显示：现有防御在 laundering 下最高约 68% ASR，而 TMA-NM 在所有模型与攻击通道上均为 0% ASR，且保持 100% 合法功能 | Figure 2、Figure 5、Table III、Appendix B（Table XI） |
| Appendix A 指出各基线均采用作者认为最有利、最忠实的实现方式，因此失败来自结构性缺陷而非调参不足 | Appendix A |

**[需验证]**

- Hash Chain 在真实生产环境中的性能开销与可扩展性。
- 开放世界复杂工具生态（动态 Tool、第三方插件）下 A1 假设的实际可满足程度。
- 多 Agent 共享记忆、跨组织协同环境中的安全保证是否仍然成立（正文明确列为范围外）。

**（完）**
