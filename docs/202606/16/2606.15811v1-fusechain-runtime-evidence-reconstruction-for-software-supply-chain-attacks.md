---
title: "FuseChain: Runtime Evidence Reconstruction for Software Supply-Chain Attacks"
title_zh: FuseChain：用于软件供应链攻击的运行时证据重建
authors: "Zhuoran Tan, Yutian Tang, Jeremy Singer, Christos Anagnostopoulos, Ke Xiao"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15811v1"
tags: ["query:q9"]
score: 8.1
relevance_score: 6.5
quality_score: 8.6
reliability_score: 8.4
practicality_score: 8.0
evidence: 多源网络遥测图结构重建用于攻击检测
tldr: 论文针对软件供应链攻击中多源遥测稀疏、跨阶段证据难关联问题，提出FuseChain，将包、进程、网络与安全告警统一到时间异构溯源图上，采用基于正常前缀的无监督异常表征学习，并冻结骨干网络仅用轻量解码器进行攻击阶段重建。在7类攻击场景中显著提升阶段回忆率（0.369→0.881），并增强可观测阶段检索能力，整体具有较强工程落地价值，值得进一步精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15811v1/fig-001.webp\", \"caption\": \"TABLE III: Scenario-level event and IOC distribution in SynthChain.\", \"page\": 6, \"index\": 1, \"width\": 1060, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2606.15811v1/fig-002.webp\", \"caption\": \"TABLE IV: Detection, reconstruction, and inference latency on SynthChain. Scen. reports the number of scenarios included in each macro-average.\", \"page\": 7, \"index\": 2, \"width\": 1060, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2606.15811v1/fig-003.webp\", \"caption\": \"TABLE XII: Evaluation-setting characteristics across representative frameworks.\", \"page\": 10, \"index\": 3, \"width\": 1073, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2606.15811v1/fig-004.webp\", \"caption\": \"TABLE XI: Qualitative capabilities in the multi-stage SSC × multi-source setting.\", \"page\": 10, \"index\": 4, \"width\": 1052, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2606.15811v1/fig-005.webp\", \"caption\": \"Fig. 1: FuseChain Framework\", \"page\": 3, \"index\": 5, \"width\": 1101, \"height\": 620}, {\"url\": \"assets/figures/arxiv/2606.15811v1/fig-006.webp\", \"caption\": \"TABLE X: Weak-rule update ablation\", \"page\": 8, \"index\": 6, \"width\": 538, \"height\": 229}]"
motivation: 如何在多源、稀疏且跨阶段的软件供应链攻击遥测中，实现可靠的异常检测与攻击阶段重建。
method: 构建统一时间异构溯源图，在冻结异常检测骨干上学习无监督表征，并通过轻量解码器进行攻击链阶段重建。
result: "在7种供应链攻击场景中进行验证，Stage Recall@500从0.369提升至0.881，显著优于基线方法。同时自适应检索进一步提升可观测阶段召回至0.655。"
conclusion: 优先阅读方法部分中统一时间溯源图构建与冻结骨干设计，理解其为何解耦检测与阶段重建。实验部分重点看7类攻击场景与召回指标对比，用于判断实际收益与适用范围。
key_findings: [统一多源遥测构建时序溯源图提升跨源关联能力。, 冻结异常模型再训练解码器优于端到端联合优化。, 阶段重建召回从0.369提升至0.881显著提升。]
limitations: 依赖合成数据SynthChain验证，真实工业环境泛化能力与部署成本仍需进一步评估。
---

## 1. 问题与背景
软件供应链攻击正在变得更加多阶段、跨系统且时间分散，同一攻击往往会在不同日志与遥测源中留下碎片化痕迹。由于包管理、进程、网络、DNS/HTTP以及安全告警等数据彼此割裂，单一来源往往只能提供弱或不完整证据，导致难以还原攻击全貌。这类问题在SolarWinds、XZ Utils和3CX等真实案例中尤为突出，因此需要跨源时间推理来统一理解攻击链路。

## 2. 核心思路 / 方法
论文提出FuseChain，将多源运行时遥测统一建模为基于时间轴的异构溯源图，通过对齐包、进程、网络与安全事件来捕获跨源依赖关系。方法上，它使用自监督方式从“正常前缀”学习图结构的异常表示，而不依赖攻击阶段标签来训练检测器。核心创新在于将异常检测骨干网络冻结，再通过轻量级解码器进行攻击阶段重建，从而避免稀疏且不平衡标签对表示学习的干扰。同时，它还支持基于时间邻近性与实体关系的证据聚合，用于生成分析者可读的攻击子图。

## 3. 结果与结论
在七种软件供应链攻击场景的实验中，FuseChain显著提升了攻击阶段重建能力，其Stage Recall@500从0.369提升到0.881。自适应检索机制进一步将可观测阶段召回率从0.524提升到0.655，同时不改变底层检测器结构。整体结论表明，将异常检测与攻击阶段解释解耦，可以在稀疏标注环境下获得更稳定且可部署的效果。

## 4. 局限与适用边界
该方法依赖多源运行时遥测的完整获取与时间对齐能力，如果数据源缺失或时间同步较差效果可能受限。从现有文本无法确认其在强对抗数据污染或极低可观测性场景下的鲁棒性。其设计主要面向运行时检测阶段，因此并不替代静态分析或软件成分分析工具。

## Abstract
Software supply-chain (SSC) attacks are increasingly multi-stage, cross-source, and temporally distributed. A single attack campaign may leave weak and fragmented traces across multi-source telemetry that captures different granularities and perspectives of runtime behavior. Existing runtime detection systems often analyze these sources independently, making it difficult to identify low-frequency attack evidence or reconstruct the temporal context in which it appears. We present FUSECHAIN, a runtime detection framework that represents multi-source software supply-chain telemetry as a temporal heterogeneous provenance graph over a unified event-time axis. By aligning package/runtime traces, process events, network telemetry, DNS/HTTP metadata, and security alerts on a unified temporal graph, FuseChain captures cross-source dependencies and sparse attack evidence that may be ambiguous within any individual source. It learns anomaly-centric temporal representations from benign-prefix telemetry and performs deployable attack-stage reconstruction through a lightweight decoder on top of a frozen anomaly backbone. Our experiments show that jointly optimizing anomaly detection and stage prediction is ineffective under sparse and imbalanced runtime supply-chain telemetry. Across seven SSC attack scenarios, FuseChain improves deployable stage reconstruction from 0.369 to 0.881 Stage Recall@500 with a frozen-backbone decoder, while adaptive retrieval further increases observable-stage recall from 0.524 to 0.655 without modifying the detector. These results highlight the deployable value of decoupling runtime SSC anomaly detection from downstream attack-stage interpretation.

---

## 论文详细总结（自动生成）

## 1. TLDR
FuseChain 面向软件供应链攻击（SSC）中“多源遥测稀疏 + 跨阶段证据割裂”的核心问题，提出统一时间异构溯源图，并用自监督异常检测 + 冻结骨干 + 轻量解码器实现攻击阶段重建。  
关键结论是：**检测与阶段解释解耦优于端到端联合优化**，在7类攻击中将 Stage Recall@500 从 0.369 提升到 0.881。

**Keywords**：Software Supply Chain Security / Temporal Provenance Graph / Multi-source Telemetry / Anomaly Detection / Attack-Chain Reconstruction

---

## 2. 核心故事与贡献

### 2.1 问题与动机
- SSC 攻击呈现“多阶段 + 跨系统 + 时间分散”特征（见 Introduction）
- 单一遥测源存在严重信息缺失：
  - 网络流量缺少进程上下文
  - 进程日志缺少包来源
  - IDS 告警缺少全局依赖关系
- 结果：攻击证据呈“低频 + 碎片化”分布，难以还原完整攻击链

👉 核心痛点：**多源弱信号无法被统一关联与解释**

---

### 2.2 核心洞察
- 联合优化 anomaly detection + stage prediction 在稀疏标签下是“负优化”（见 Abstract）
- 更优策略：
  - **先学“正常行为结构”（无监督异常检测）**
  - 再做“攻击解释”（decoder）
- 即：
  > 表征学习 ≠ 解释学习（必须解耦）

---

### 2.3 主要贡献
- 构建 **统一时间异构溯源图（temporal heterogeneous provenance graph）**
- 提出 **自监督异常检测（仅用 benign-prefix）**
- 提出 **冻结 backbone + 轻量 decoder 的阶段重建框架**
- 实验结果：
  - Stage Recall@500：**0.369 → 0.881（+0.512）**
  - 可观测阶段召回：0.524 → 0.655
- 支持 analyst-facing IOC traceability（见 Section I）

---

## 3. 相关工作与定位

| 维度 | 传统Provenance IDS | GNN APT检测 | FuseChain |
|------|------------------|-------------|----------|
| 数据源 | 单主机审计日志 | 单/双源图 | 多源（pkg/proc/net/DNS/alert） |
| 时间建模 | 弱/无 | 部分支持 | unified event-time axis |
| 标签依赖 | 通常需要攻击标签 | 需要标注 | **仅decoder用label** |
| 表征学习 | 手工规则/GNN | GNN embedding | self-supervised anomaly backbone |
| 攻击解释 | 弱 | 部分 | **stage reconstruction显式支持** |
| 部署性 | 中等 | 低（复杂训练） | **高（冻结 backbone）** |

代表性对比（见 Related Work Section II & citations [12][29][31]）

---

## 4. 方法详解

### 4.1 总体框架（见 Figure 1）
FuseChain =

1. 多源日志 → 统一事件格式  
2. 构建 temporal heterogeneous provenance graph  
3. self-supervised anomaly backbone（冻结后使用）  
4. anomaly scoring + suspicious clustering  
5. lightweight stage decoder → attack stage reconstruction  

---

### 4.2 统一时间图建模（核心表示）
事件定义（见 Section III）：

$$
e_i = (\hat{t_i}, u_i, r_i, v_i, a_i, \sigma_i)
$$

- $\hat{t_i}$：统一时间轴
- $u_i, v_i$：实体节点（proc/file/net/pkg）
- $r_i$：关系类型
- $a_i$：多属性日志特征
- $\sigma_i$：数据源标识

图结构：

$$
G(\hat{t}) = (V, E^o(\hat{t}) \cup E^c(\hat{t}))
$$

- $E^o$：观测边
- $E^c$：规则推导边（仅增强结构）

---

### 4.3 自监督异常检测（Backbone）
- 训练方式：
  - 使用 **benign-prefix telemetry**
  - temporal link prediction（无攻击标签）
- 输出：
  - anomaly score $s_i$
- 特点：
  - 不依赖 IOC
  - 允许“污染前缀”存在（更真实）

---

### 4.4 阶段重建（Decoder）
关键设计：

- 冻结 backbone（不更新表示）
- 仅训练 lightweight decoder
- 输入：高分 anomaly subgraph
- 输出：observable attack stages

目标：

- 不是完整 kill chain
- 而是 **“可观测阶段子集”**

---

### 4.5 关键设计思想
- 解耦三件事：
  - 表征学习
  - 异常检测
  - 攻击解释
- 避免 sparse label 干扰 representation learning

---

## 5. 实验分析

### 5.1 主结果（见 Table IV / Section Experiments）
| 指标 | baseline | FuseChain |
|------|----------|------------|
| Stage Recall@500 | 0.369 | **0.881** |
| Observable-stage recall | 0.524 | 0.655 |

---

### 5.2 核心发现
- joint training（检测+阶段）效果差（见 Abstract）
- frozen backbone + decoder 显著更优

---

### 5.3 消融实验
| 组件 | 影响 |
|------|------|
| 无 temporal alignment | 性能下降（见 Table X weak-rule ablation） |
| 无 derived edges | recall下降 |
| 无 adaptive retrieval | observable recall ↓ |

---

### 5.4 多场景评估
- 7种 SSC attack scenarios（见 SynthChain dataset）
- 稳定提升 across scenarios（见 Section Experiments）

---

## 6. 局限性与风险

- **数据依赖强**
  - 依赖多源 runtime telemetry 完整采集
- **时间对齐假设**
  - 假设统一 event-time axis 可构建（现实系统可能漂移）
- **合成数据依赖**
  - 主要在 SynthChain 上验证 → 泛化未知
- **解释能力边界**
  - reconstruction ≠ full kill chain（仅 observable subset）
- **部署复杂度**
  - 多源日志解析与 schema 统一成本高

---

## 7. 复现与后续问题

### 7.1 复现重点
- temporal alignment 机制（统一 event-time axis）
- benign-prefix selection策略（影响 backbone 表征）
- decoder 结构（轻量 stage mapping 设计）

---

### 7.2 后续研究方向
- 真实工业环境 multi-source telemetry 鲁棒性
- 自适应时间对齐（clock drift / missing logs）
- 更强解释能力（从 stage → causal chain）

---

## 8. 直接证据与待验证项

### 直接证据
- Stage Recall@500：0.369 → 0.881（见 Abstract / Section Experiments）
- observable-stage recall：0.524 → 0.655（见 Abstract）
- “joint optimization is ineffective”结论（见 Abstract / Introduction）
- 使用 benign-prefix training（见 Section IV）
- 7个 SSC attack scenarios evaluation（见 Section Experiments）

---

### 待验证项
- [需验证] 在真实企业环境（非 SynthChain）中的性能稳定性
- [需验证] adaptive retrieval 在不同 telemetry 密度下的收益边界
- [需验证] derived edges $E^c$ 对最终性能贡献是否稳定跨数据集

---

（完）
