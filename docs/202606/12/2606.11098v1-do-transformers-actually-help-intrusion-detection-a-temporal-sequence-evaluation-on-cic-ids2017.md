---
title: Do Transformers Actually Help Intrusion Detection? A Temporal Sequence Evaluation on CIC-IDS2017
title_zh: Transformer 是否真的有助于入侵检测？基于 CIC-IDS2017 的时间序列评估
authors: "Zach Moczkodan, Hany Ragab"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11098v1"
tags: ["query:q5"]
score: 8.3
relevance_score: 8.5
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.5
evidence: 基于Transformer的网络流量入侵检测
tldr: 本文将CIC-IDS2017重构为真实时间序列入侵检测任务，在泄漏规避数据划分与不同padding策略下，对Transformer、RNN与传统模型进行系统对比评估。结果显示评测协议与padding设计对性能影响远超模型结构本身，Transformer在真实序列场景下优势不稳定，随机划分会显著高估检测性能，整体值得进一步细读以评估方法论贡献。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11098v1/fig-001.webp\", \"caption\": \"Fig. 1: Static versus temporal intrusion detection. Static models classify one network flow at a time, whereas temporal models exploit ordered flow sequences within a conversation before producing the IDS decision.\", \"page\": 1, \"index\": 1, \"width\": 524, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-002.webp\", \"caption\": \"Fig. 4: Sequence length ablation across all four temporal models, T ∈ {5, 10, 20}. Each bar is the mean macro-F1 across three seeds at the learning rate with the best seedmean for that (T , model) combination; error bars show ±1 std. T=20 is best or within one standard deviation of best for every model.\", \"page\": 6, \"index\": 2, \"width\": 530, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-003.webp\", \"caption\": \"TABLE IV: Comparison to published Transformer-based IDS results on CIC-IDS2017. Numbers verified from each paper’s reported tables; “–” marks metrics the source paper does not report. T is the actual length seen by the recurrent or attention layer, not the CNN feature extractor. Our entries are mean over three seeds.\", \"page\": 6, \"index\": 3, \"width\": 1064, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-004.webp\", \"caption\": \"TABLE V: Sequence-length ablation. Each cell reports the mean ± std macro-F1 across three seeds at the learning rate with the best seed-mean for that (T , model) combination, on the random 80/20 split with repeat-last padding. Best macroF1 per row in bold; T=20 is best for three of the four models and within one standard deviation of best for the 1D-CNN.\", \"page\": 6, \"index\": 4, \"width\": 1064, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-005.webp\", \"caption\": \"Fig. 5: Per-sample inference latency vs. sequence length T on an NVIDIA A100 GPU, mean±std over 20 repetitions of a 128-window batch. Main panel: all four temporal models (Transformer in red; LSTM/GRU/1D-CNN clustered near zero); inset: zoom below 0.2ms. The Transformer grows ∼6× from T=5 to T=50; constant overheads dominate at these sequence lengths so the O(T 2) self-attention term is not yet apparent. LSTM, GRU, and 1D-CNN remain near-constant.\", \"page\": 7, \"index\": 5, \"width\": 511, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-006.webp\", \"caption\": \"Fig. 9: Per-class recall drop (random − group), mean across three seeds. Red cells = recall loss going to the leakagefree split; blue cells = gain. The Bot column drops for every temporal architecture (the Random Forest’s Bot recall actually improves); the two Transformer variants lose recall across more classes than the recurrent and convolutional models do.\", \"page\": 10, \"index\": 6, \"width\": 512, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-007.webp\", \"caption\": \"TABLE I: Preprocessing: columns removed from the model feature set before training. Every dropped column is either an identifier, a timestamp, or the label itself.\", \"page\": 3, \"index\": 7, \"width\": 533, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-008.webp\", \"caption\": \"Fig. 3: Per-class F1 under the random 80/20 split, mean across three seeds. BENIGN and the high-volume DoS classes are detected by every temporal model at F1 ≈ 1.0; Infiltration is essentially undetectable across every architecture (4 training windows); Bot, Web Attack–SQL Injection and Web Attack– XSS show the largest spread among the leading models.\", \"page\": 4, \"index\": 8, \"width\": 515, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-009.webp\", \"caption\": \"Fig. 7: Realistic evaluation, mean ± std over three seeds. Left: macro-F1 under random vs. group split. Right: drop from the random split per model. The Random Forest and the lightweight RNN/CNN models hold within 0.03 of their random-split macro-F1; the CNN–Transformer drops 0.23, almost entirely attributable to the zero-pad+mask protocol rather than the split itself (cf. Table VII) — a sensitivity that the random split alone cannot reveal.\", \"page\": 9, \"index\": 9, \"width\": 1063, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-010.webp\", \"caption\": \"TABLE III: Performance comparison on CIC-IDS2017 under the random 80/20 split, mean±std over three seeds ({2, 42, 123}). Static models observe only the last flow of each window; temporal models process the full T=20 window. Best macro-F1 in bold; rows ordered within each block by macro-F1.\", \"page\": 5, \"index\": 10, \"width\": 806, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2606.11098v1/fig-011.webp\", \"caption\": \"TABLE II: Architecture and training hyperparameters. Static (non-temporal) models consume only the last flow; temporal models consume the full T=20 window. “Subsample” indicates a class-stratified sub-sample of the training set; “Full” indicates the complete 504,211-window training set.\", \"page\": 5, \"index\": 11, \"width\": 1076, \"height\": 262}]"
motivation: 在真实时间序列建模与无数据泄漏评估条件下，Transformer是否真正优于传统与循环神经网络用于入侵检测。
method: 构建基于通信会话的流序列输入，并通过泄漏规避划分与padding消融实验系统评估不同模型的真实时序建模能力。
result: "在真实序列输入下Transformer取得最高macro-F1约0.89，但在zero-pad+mask设置下下降约0.24。其误报率在泄漏规避评估中从0.04%上升到2.7%，增加约67倍。"
conclusion: 建议优先阅读方法部分的数据重构与split设计，以及实验中padding ablation与四种评估设置对比，因为结论主要由评测协议驱动。其次重点看结果章节中Transformer与Random Forest对比，以理解“结构不如协议”的核心论证。
key_findings: [Transformer性能高度依赖padding策略，非结构固有优势。, 泄漏规避划分下随机森林表现更稳定甚至更优。, 传统随机划分会系统性高估模型macro-F1达0.24。]
limitations: 实验主要依赖单一CIC-IDS2017数据集，泛化性与跨数据集验证不足。
---

## 1. 问题与背景  
该研究围绕一个关键问题展开：Transformer 等时序模型在入侵检测任务中的性能提升，是否真的来自有效的序列建模能力。现有大量基于 CIC-IDS2017 的工作报告了接近“完美”的结果，但普遍忽略了真实网络流的时间结构，并且在评估中可能存在数据泄漏与不合理的划分方式。由于入侵检测本质上依赖连续网络行为的演化，这种评估偏差会直接影响对模型能力的判断，因此问题具有很强的现实安全意义。

## 2. 核心思路 / 方法  
论文将 CIC-IDS2017 重构为“会话级时间序列”任务，把原本独立的网络流按 conversation 组织成有序序列，以体现真实网络行为的时序依赖。作者在统一框架下对九种模型（包括传统机器学习、RNN、CNN 以及 Transformer）进行系统对比，并在随机划分、无泄漏划分以及 padding 机制消融等多种评估条件下进行测试。模型结构上包含 CNN-Transformer 等序列建模架构，同时重点控制 padding 与序列长度等容易引入隐性偏差的因素，从而隔离“方法本身”与“评估方式”的影响。

## 3. 结果与结论  
实验结果表明，Transformer 在真正的序列输入（非 padding 干扰）条件下取得最高 macro-F1（0.89），但在 zero-pad + mask 设置下性能显著下降（macro-F1 下降约 0.24），而 LSTM、GRU 和 1D-CNN 表现相对稳定。在无数据泄漏划分下，Random Forest 表现最稳健（提升约 +0.009），同时 Transformer 的误报率从 0.04% 上升到 2.7%，增长约 67 倍。整体结论是：评估方式（尤其是 padding 与数据划分策略）对结果影响甚至超过模型结构本身。

## 4. 局限与适用边界  
研究主要基于 CIC-IDS2017 数据集，其结论在其他真实网络环境中的泛化能力仍需进一步验证。从现有文本来看，模型性能对 padding 策略和数据划分方式高度敏感，这也意味着不同实验设置可能导致截然不同的结论。此外，该数据集本身可能存在标签噪声与构造偏差，因此结果更适合作为方法论评估，而非直接部署性能参考。

## Abstract
Recent deep learning approaches for network intrusion detection increasingly incorporate temporal architectures such as recurrent networks and Transformers, often reporting near-perfect performance on CIC-IDS2017. However, many existing studies neither supply their temporal modules with genuine sequence inputs nor evaluate under realistic, leakage-free conditions, making it unclear whether reported gains arise from true sequence-modeling capability. In this work, we reformulate CIC-IDS2017 as a temporal intrusion-detection task by constructing ordered flow sequences from network conversations and benchmarking nine classical and deep learning architectures under a random split, two leakage-free splits, and a padding-scheme ablation. The central finding is that padding convention, not architecture, determines the Transformer's performance: on genuinely sequential (non-padded) windows the Transformer achieves the highest macro-F1 of any model in the experiment (0.89); under zero-pad+mask evaluation it drops markedly (-0.24 macro-F1), while LSTM, GRU, and 1D-CNN remain stable. Under leakage-free group evaluation the Random Forest is the most robust model (+0.009), while the Transformer's false-alarm rate grows from 0.04% to 2.7%, a 67-fold increase invisible under conventional protocols. These findings demonstrate that evaluation methodology -- specifically padding convention and split protocol -- has a larger effect on reported performance than architectural choice, and that widely used random splits with repeat-last padding can overestimate model robustness by up to 0.24 macro-F1. We advocate leakage-free splits, explicit padding disclosure, and sequence-aware benchmarking as standard practice in future IDS research. Code and implementation details are available at https://github.com/zachmocz/temporal-ids-bench.

---

## 论文详细总结（自动生成）

## 1. TLDR  
本文将 CIC-IDS2017 重新建模为“会话级时间序列入侵检测”，系统评估 Transformer、RNN 与传统模型在**不同数据划分与 padding 机制**下的真实表现。核心发现是：模型优劣高度依赖评测协议（尤其 padding 与是否泄漏），而非结构本身，Transformer 在理想序列输入下表现最好，但在更严格设置中优势显著不稳定。

**Keywords**：Intrusion Detection, Transformer, CIC-IDS2017, Temporal Modeling, Data Leakage, Padding Ablation

---

## 2. 核心故事与贡献  

### • 问题与动机  
- 现有 CIC-IDS2017 上的深度学习 IDS 方法常报告接近 99%+ accuracy，但多数研究忽略时间结构。  
- 论文指出：许多 Transformer IDS（如 CNN–LSTM–Transformer）实际上“**sequence length = 1**”，并未真正建模时间依赖（见 Section II-C）。  
- 动机：验证 Transformer 提升是否来自“真实序列建模”还是“评测与数据处理偏差”。

---

### • 核心洞察  
- **padding 与 split protocol 的影响 > 模型结构影响**  
- Transformer 在不同 padding 下表现差异巨大：  
  - 正确序列输入：macro-F1 ≈ **0.89**  
  - zero-pad+mask：下降 **≈0.24 macro-F1**  
- Random split 会掩盖真实误差，使模型误报率显著低估（0.04% vs 2.7%）。

---

### • 主要贡献  
- 提出会话级 temporal IDS benchmark（conversation-level flow sequences）。  
- 在 4 种评估协议下对 9 种模型进行统一比较。  
- 证明 CIC-IDS2017 上的“Transformer 优势”在多数情况下不可复现或不稳定。  
- 提供证据：random split 会系统性高估性能（见 Abstract / Section III）。

---

## 3. 相关工作与定位  

| 维度 | 传统 IDS (RF/SVM) | RNN/LSTM/GRU | Transformer IDS | 本文 |
|------|------------------|--------------|----------------|------|
| 输入形式 | 单流 tabular | 伪序列或局部窗口 | attention 序列 | **会话级真实序列** |
| 时间建模 | ❌ | 部分 | 表面支持 | **严格 temporal windowing** |
| split方式 | random split | random split | random split | **random + leakage-free group split** |
| padding处理 | 无 | implicit | often ignored | **explicit padding ablation** |
| 是否考虑泄漏 | ❌ | ❌ | ❌ | **✔ 系统分析 leakage effect** |
| 部署一致性 | 弱 | 弱 | 弱 | **强调真实 streaming IDS** |

---

## 4. 方法详解  

### 4.1 数据重构（核心创新）
将 CIC-IDS2017 从独立 flow 转换为会话序列：

- 按 5-tuple 分组：
$$ q = (src\_ip, dst\_ip, src\_port, dst\_port, protocol) $$

- 构建滑动窗口：
$$ X_{q,s} = [x_s, x_{s+1}, ..., x_{s+T-1}] $$  
$$ y_{q,s} = y_{s+T-1} $$

其中 $T=20$。

---

### 4.2 Padding 策略对比
- **repeat-last padding（主流 IDS 用法）**
  - 短会话填充最后一个流
  - 容易引入“伪模式信号”

- **zero-pad + mask**
  - 使用显式 mask 控制 attention
  - 更接近真实 NLP/sequence modeling 标准

---

### 4.3 模型设置
- Static baseline：仅使用 window 最后一条 flow  
- Temporal models：
  - 1D-CNN
  - LSTM / GRU
  - Transformer（+ CLS token + positional encoding）

---

### 4.4 评估协议
- Random split（传统高估场景）  
- Leakage-free group split（按 conversation）  
- Time split（见 Section III-C）  
- Padding ablation（repeat-last vs zero-mask）

---

## 5. 实验分析  

### 5.1 主结果（random split）
- Transformer 达到最高 macro-F1（≈0.89）（见 Table III / Fig. 3）
- 但 Bot / Infiltration 类别表现波动极大（Fig. 3）

---

### 5.2 padding 消融（关键发现）
| 设置 | Transformer macro-F1 |
|------|---------------------|
| repeat-last padding | ↑（最优） |
| zero-pad + mask | ↓约 0.24 |

👉 说明 Transformer 对 padding 极度敏感（见 Table V）

---

### 5.3 leakage-free split
- Random Forest 最稳定（+0.009 提升）  
- Transformer false-alarm rate：
  - 0.04% → 2.7%（↑约 67×）

（见 Fig. 9 / Fig. 7）

---

### 5.4 sequence length ablation
- T=20 最优或接近最优（Fig. 4）  
- 所有模型随 T 增长整体提升但趋于饱和

---

### 5.5 latency分析
- Transformer inference cost 随 T 增长明显上升  
- LSTM/GRU/1D-CNN 几乎恒定（Fig. 5）

---

## 6. 局限性与风险  

- **单数据集依赖（CIC-IDS2017）**：结论可能无法迁移到真实生产网络。  
- **窗口构造假设固定（T=20）**：可能影响模型比较公平性。  
- **padding设计仍可能不完全覆盖真实 streaming 场景**：实际网络可能存在更复杂 session fragmentation。  
- **标签噪声未完全消除影响**：CIC-IDS2017 已知存在构造偏差（见 Engelen et al. 引用）。  

---

## 7. 复现与后续问题  

### 复现重点  
- 5-tuple grouping + timestamp sorting（关键）  
- repeat-last vs zero-pad 两种 padding  
- group split vs random split  
- window label = last-flow label（非常关键）  

---

### 后续研究方向  
- 多数据集验证（UNSW-NB15 / CIC-IoT-2023）  
- streaming online IDS（真实部署延迟约束）  
- 去 padding bias 的 attention 结构设计  

---

## 8. 直接证据与待验证项  

### 证据（来自正文）  
- Transformer 在 non-padded sequence 上 macro-F1 = **0.89**（见 Abstract / Section III）  
- zero-pad+mask 导致 macro-F1 **下降约 0.24**（见 Abstract / Table V）  
- false-alarm rate：0.04% → 2.7%（≈67×）（见 Abstract / Fig. 9）  
- random split vs leakage-free split差异显著（见 Fig. 7）  
- Random Forest 在 group split 下最稳定（+0.009）（见 Abstract / Fig. 7）  

---

### 待验证项  
- [需验证] “CNN–Transformer结构中 sequence length = 1”在所有引用论文中是否普遍成立（论文 II-B 指出但未逐一复现实验）  
- [需验证] padding effect 是否在其他 IDS 数据集同样显著  
- [需验证] 67× false alarm increase 是否对所有 Transformer 变体一致成立（可能依赖具体实现）  
- [需验证] T=20 是否为全局最优窗口长度（可能存在数据集偏置）

---

（完）
