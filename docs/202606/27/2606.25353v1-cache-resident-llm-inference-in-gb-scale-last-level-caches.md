---
title: Cache-Resident LLM Inference in GB-Scale Last-Level Caches
title_zh: GB级最后级缓存中的缓存驻留LLM推理
authors: "Wanning Zhang, Tongzhou Gu, Marco Canini, Ceyu Xu, Jian Weng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25353v1"
score: 8.1
relevance_score: 0.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.5
evidence: LLM推理架构优化，与流量分析无关
tldr: 论文面向具备GB级LLC的新型服务器CPU，研究如何降低LLM推理中的数据搬运与同步开销。提出缓存驻留执行模型，将权重与KV cache分离到不同资源域，并以子算子粒度调度减少同步。实现后在Llama-3/2上获得2.04×–11.51× TPOT加速，分析模型可达13.9×，对CPU端高效推理具有较强参考价值，值得进一步精读系统设计与评估细节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25353v1/fig-001.webp\", \"caption\": \"Figure 5. Comparison between weight–KV colocation and weight–attention decoupling\", \"page\": 6, \"index\": 1, \"width\": 998, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-002.webp\", \"caption\": \"Figure 8. Model-based throughput and TPOT speedups over llama.cpp across context lengths and batch sizes for four models. The speedup is on top of each cell, and the corresponding absolute throughput (tokens/s) or TPOT (ms) is at the bottom.\", \"page\": 11, \"index\": 2, \"width\": 527, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-003.webp\", \"caption\": \"Figure 1. Transformer-based LLM decodes each token by propagating the embeddings across operators.\", \"page\": 2, \"index\": 3, \"width\": 425, \"height\": 691}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-004.webp\", \"caption\": \"Figure 7. Prototyping Hardware Platform\", \"page\": 7, \"index\": 4, \"width\": 474, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-005.webp\", \"caption\": \"Table 2. End-to-end performance of LLaMA-3.2-3B and LLaMA-2-7B at context length 4096.\", \"page\": 10, \"index\": 5, \"width\": 477, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-006.webp\", \"caption\": \"Figure 3.Architectural paradigm for LLM inference system.\", \"page\": 3, \"index\": 6, \"width\": 527, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-007.webp\", \"caption\": \"Figure 9. Effect of weight-attention separation on per-transformer-block latency in Llama-family models. The speedup is on top of each cell, and the absolute time saving per transformer block (𝜇s) is at the bottom.\", \"page\": 12, \"index\": 7, \"width\": 946, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-008.webp\", \"caption\": \"Figure 10. Customized thread pool versus OpenMP in a single-socket, per-transformer-block comparison. The speedup is on top of each cell, and the absolute time saving per transformer block (𝜇s) is at the bottom.\", \"page\": 12, \"index\": 8, \"width\": 516, \"height\": 327}]"
motivation: 如何在GB级最后级缓存（LLC）上组织LLM推理执行，以同时消除权重与KV缓存竞争并降低同步开销，从而提升CPU推理效率。
method: 通过权重-注意力解耦的缓存驻留执行模型，将模型权重与KV cache分离管理，并在子算子级别进行依赖感知调度以减少同步与数据搬运。
result: 在Llama-3.2-3B与Llama-2-7B上端到端实测TPOT提升2.04×–11.51×；在分析模型下不同规模与上下文长度可达最高13.9×加速。
conclusion: 建议优先阅读执行模型设计与权重-注意力解耦部分以理解核心机制，再看子算子调度与运行时实现细节。最后重点看评估与分析模型部分，用于判断加速来源与适用条件。
key_findings: [GB级LLC可支撑权重驻留，但KV cache与权重竞争导致传统pipeline难扩展。, 权重-注意力解耦可缓解缓存资源冲突并提升整体利用率。, 子算子级调度显著降低同步开销，优于传统operator-centric执行。]
limitations: 依赖大容量LLC的特定CPU架构，GPU/通用加速器上的迁移性与收益未充分验证。
---

## 1. 问题与背景  
该论文关注大语言模型（LLM）推理过程中严重的性能瓶颈，其核心问题是数据在存储层级之间频繁搬运，尤其是在权重参数与注意力机制中的 KV cache 同时竞争缓存资源时造成的压力。随着模型规模与上下文长度增加，推理过程中的内存访问与跨层同步开销显著上升。传统流水线并行虽然可以分摊权重存储，但会引入更深的并行深度，从而放大 KV cache 的占用问题，并进一步加剧资源冲突。因此，在具备 GB 级别 last-level cache 的现代 CPU 上，如何实现真正“缓存驻留”的高效推理成为关键问题。

## 2. 核心思路 / 方法  
论文提出了一种面向缓存驻留的 LLM 推理执行模型，将权重相关计算与注意力/KV cache 管理进行解耦，分别放入不同的资源域中，以避免二者在同一缓存层中的竞争。同时，该方法将传统基于算子边界的同步机制放宽为基于子计算依赖的调度方式，从而减少频繁同步带来的开销。在实现上，系统结合多路 CPU 服务器架构，通过局部性优化的数据放置策略与静态运行时调度来提升缓存利用效率。整体上，该方法突破了传统 pipeline parallelism 与 operator-centric 执行模型的限制，使得缓存驻留条件下的计算与通信能够更细粒度地解耦。

## 3. 结果与结论  
实验结果表明，该原型系统在 Llama-3.2-3B 和 Llama-2-7B 配置下，相比同等资源配置的 llama.cpp，在端到端 token 生成延迟（TPOT）上实现了约 2.04× 到 11.51× 的加速。进一步基于分析模型外推，在不同模型规模、上下文长度和 batch size 条件下，TPOT 最多可达到约 13.9× 的提升。结果表明，只要合理组织执行结构，使权重驻留缓存并降低同步开销，通用 CPU 也能够在 LLM 推理中获得显著性能优势。

## 4. 局限与适用边界  
该方法依赖 GB 级 last-level cache 的硬件条件，因此主要适用于具备大缓存的服务器级 CPU 架构，在其他硬件上的可迁移性从现有文本无法完全确认。此外，论文部分性能提升基于分析模型外推，实际极端规模下的表现仍可能存在偏差。最后，权重与 KV cache 的资源解耦虽然提升效率，但也引入固定资源划分的结构性约束，在某些负载分布变化较大的场景中可能不够灵活。

## Abstract
Large language model (LLM) inference is increasingly dominated by data movement across the memory hierarchy. Recent 3D-stacked cache technologies have enabled GB-scale last-level caches in modern server CPUs, making it possible to keep reusable model weights on chip and exploit cache bandwidth and latency. Achieving this regime is not straightforward: deeper pipelining for weight residency increases in-flight requests and KV-cache footprint, while cache-resident operators make operator-boundary synchronization a visible bottleneck.   We present a cache-resident execution model for inference on hierarchical-memory clustered systems. The model separates weight-centric operators from attention and KV-cache management into dedicated resource domains, keeping reusable weights cache-resident while scaling KV capacity independently of pipeline depth. It also relaxes synchronization from operator boundaries to true sub-operator dependencies, reducing coordination overhead in the cache-resident regime.   We instantiate this model on a multi-socket CPU cluster with a weight-attention decoupled architecture, locality-aware placement, and a specialized static runtime. The prototype substantially outperforms equally provisioned llama.cpp. On deployed Llama-3.2-3B and Llama-2-7B configurations, it achieves 2.04x-11.51x speedup on time-per-output-token (TPOT). Under a validated analytical model, it further reaches up to 13.9x TPOT speedup across model sizes, context lengths, and batch sizes. These results show that commodity CPUs with GB-scale last-level caches can support efficient LLM inference when execution is organized around cache residency, decoupled state management, and dependency-aware coordination.

---

## 论文详细总结（自动生成）

## 1. TLDR  
本文面向具备 GB 级 Last-Level Cache（LLC）的现代服务器 CPU，提出一种**缓存驻留（cache-resident）的 LLM 推理执行模型**，通过将**模型权重与 KV cache 解耦**，并引入**子算子粒度调度**来减少同步开销与数据搬运。  

在 Llama-3.2-3B 与 Llama-2-7B 上实现 **2.04×–11.51× TPOT 加速**，分析模型下最高可达 **13.9×**，证明 CPU 端缓存优化可显著提升 LLM 推理性能。  

**关键词**：LLM推理优化 / GB级LLC / KV-cache / 权重-注意力解耦 / 子算子调度 / CPU inference  

---

## 2. 核心故事与贡献  

### 2.1 问题与动机  
- LLM 推理瓶颈主要来自**数据搬运（memory movement）**，而非算力不足（Abstract, Section 1）  
- 在 pipeline parallelism（PP）下：  
  - 深层流水线 → 权重更易驻留缓存  
  - 但会增加 in-flight tokens → **KV cache 线性增长并挤占 LLC**（Section 1）  
- operator-centric 执行模式导致：  
  - 每个算子边界强同步 → cache-resident 场景下同步开销反而成为瓶颈  

---

### 2.2 核心洞察  
- 权重 vs KV cache 的本质差异：  
  - 权重：静态、跨 token 可复用  
  - KV cache：动态、随上下文增长且绑定单序列  
- 因此二者**不应竞争同一缓存资源域**（Section 1–2）  
- transformer 内部存在大量**子结构独立性（attention heads 等）**，可打破算子级同步  

---

### 2.3 主要贡献  
- 提出 **cache-resident execution model**  
- 提出 **weight–attention decoupling（WA separation）**  
- 提出 **sub-operator dependency scheduling（子算子依赖调度）**  
- 在 CPU 多 socket 系统实现原型：  
  - Llama-3.2-3B / Llama-2-7B  
  - TPOT 提升 2.04×–11.51×（Abstract）  

---

## 3. 相关工作与定位  

| 方向 | 代表工作 | 核心思路 | 本文差异 |
|------|----------|----------|----------|
| Pipeline Parallelism | PipeDream [17] | 层级切分模型 | 本文指出 PP 增加 KV cache 压力 |
| Tensor Parallelism | Megatron-LM [27] | 算子内切分 | 本文强调同步成本过高 |
| KV Cache 管理 | PagedAttention [14], vAttention [21] | 内存分页/管理KV | 本文从“执行模型”层面解耦 |
| LLM Serving 系统 | Orca [33], DistServe [34] | 服务调度优化 | 本文聚焦 CPU cache-resident execution |
| CPU LLM 优化 | FlexGen [26], T-MAC [31] | offloading / lookup | 本文强调 on-chip LLC residency |

---

## 4. 方法详解  

## 4.1 总体架构（Cache-Resident Execution Model）
系统核心目标：  
> 让 weight 常驻 LLC，同时 KV cache 独立扩展，不互相竞争

架构分为三层资源域（Section 2.2–3）：  
- Weight domain（权重计算域）  
- Attention/KV domain（状态管理域）  
- Inter-domain scheduler（依赖调度器）

---

## 4.2 Weight–Attention 解耦（WA Separation）
关键思想：  
- 权重：跨 token 共享 → 放入 cache-resident domain  
- KV cache：每序列独立 → 单独管理  

效果：  
- 消除“weight + KV cache 争抢 LLC”的结构性冲突（Section 1）

---

## 4.3 子算子级调度（Sub-operator Scheduling）
传统方式：  
- operator → barrier → operator  

本文改进：  
- 按 attention head / sub-block 切分依赖  
- 仅在**真实 data dependency**处同步（Section 1）

形式化表达（抽象）：  
$$
\text{Sync} = f(\text{true dependency graph}) \ll \text{operator boundary sync}
$$

---

## 4.4 Cache-Resident Pipeline
关键优化：  
- 静态 runtime（static scheduling）  
- locality-aware placement（数据放置优化）  
- multi-socket resource domain mapping（Section 3）

---

## 5. 实验分析  

## 5.1 主结果  

### Table 2（Section 5）  
- Llama-3.2-3B / Llama-2-7B  
- context = 4096  

结果：  
- TPOT speedup：**2.04× – 11.51×**  
- 对比 baseline：llama.cpp  

---

### Figure 8（Section 5）  
- 不同 batch size / context length / model size  
- speedup 可达：  
  - throughput up to **13.9×（model-based extrapolation）**

---

## 5.2 关键发现  

- cache-residency 后瓶颈转移：  
  - 从 memory bandwidth → synchronization overhead（Figure 9）  
- weight–KV separation 带来显著 latency reduction  
- sub-operator scheduling 对小粒度 latency 改善明显  

---

## 5.3 消融（见 Figures 7–10）  
- Figure 7：硬件平台验证  
- Figure 9：WA separation → per-block latency 降低  
- Figure 10：custom thread pool > OpenMP（CPU调度关键）

---

## 6. 局限性与风险  

- **硬件依赖强**：依赖 GB 级 LLC（server CPU 特性）  
- **GPU 泛化性不足**：未验证在 GPU / TPU 上是否同样有效  
- **分析模型外推风险**：13.9× 来自 model-based extrapolation（需验证）  
- **资源静态划分限制灵活性**：WA separation 可能影响动态负载适配  
- **系统复杂度提升**：子算子调度增加 runtime 设计复杂度  

---

## 7. 复现与后续问题  

### 复现关键点  
- LLC residency 的数据放置策略（cache-aware placement）  
- KV cache 与 weight domain 的物理隔离方式  
- sub-operator dependency graph 构建与调度策略  

### 后续研究方向  
- GPU cache hierarchy 下的类似 WA separation  
- 动态 workload 下的 adaptive resource domain resizing  

---

## 8. 直接证据与待验证项  

### 直接证据  

- TPOT 提升 **2.04×–11.51×**（Abstract / Section 5）  
- analytical model 下最高 **13.9× speedup**（Abstract, Figure 8）  
- KV cache 与 weights 在 PP 下发生 cache contention（Section 1）  
- operator boundary synchronization 成为 cache-resident bottleneck（Section 1）  
- weight–attention decoupling 提升 per-block latency（Figure 9）  

---

### 待验证项  
- [需验证] GB-scale LLC 在不同 CPU 架构上的稳定收益  
- [需验证] 13.9× speedup 的模型外推误差范围  
- [需验证] 子算子调度在超大 batch 下的 scalability 上限  
- [需验证] 是否存在 KV cache fragmentation 或一致性开销  

---

（完）
