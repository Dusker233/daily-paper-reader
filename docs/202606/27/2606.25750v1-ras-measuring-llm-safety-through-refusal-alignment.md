---
title: "RAS: Measuring LLM Safety Through Refusal Alignment"
title_zh: RAS：通过拒绝对齐衡量大语言模型安全性
authors: "Chang-Chieh Huang, Yan-Lun Chen, Chia-Mu Yu, Wei-Bin Lee"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25750v1"
tags: ["query:q9"]
score: 6.9
relevance_score: 4.5
quality_score: 8.5
reliability_score: 8.5
practicality_score: 6.0
evidence: 基于LLM内部表征的安全性评估
tldr: 本文提出SafeVec与RAS，从安全参考模型中提取“拒绝方向”，通过目标模型在安全/越狱提示下隐藏状态与该方向的对齐程度，在表征层面量化LLM安全性并映射为0-100评分。实验覆盖Llama、Gemma、Qwen，显示该指标可区分对齐与非对齐/ablated模型，并与攻击成功率强相关，同时比基于LLM裁判的输出评估更高效，适合白盒安全审计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25750v1/fig-001.webp\", \"caption\": \"Figure 5: Layer-wise analysis for Gemma. Figure 6: Layer-wise analysis for Qwen.\", \"page\": 10, \"index\": 1, \"width\": 945, \"height\": 258}]"
motivation: 能否不依赖输出级评测，而通过LLM内部表征来量化其安全与拒绝行为一致性。
method: 从安全参考模型中提取拒绝方向，并用目标模型隐藏状态与该方向的余弦相似度构建RAS安全评分。
result: 实验表明不同安全程度模型在拒绝方向对齐上呈显著分离，且RAS与攻击成功率（ASR）高度负相关。在Llama、Gemma、Qwen三大模型族中均保持一致趋势。
conclusion: 建议重点阅读SafeVec的拒绝方向提取与layer选择方法，以及RAS标定过程，因为这是从表征到评分的关键映射机制。实验部分优先看跨模型族对齐与ASR相关性分析，以验证指标有效性。
key_findings: [拒绝方向对齐程度可清晰区分安全、未对齐与ablated模型。, RAS与攻击成功率ASR呈稳定强负相关关系。, 方法在不同模型家族中经校准后仍保持一致判别能力。]
limitations: 依赖白盒访问隐藏状态且需同族参考模型，适用范围受限于开放权重场景。
---

## 1. 问题与背景  
现有大模型安全评估主要依赖“输出层”方法，即通过危险或越狱提示词检查模型是否生成违规内容，但这种方式成本高且依赖评判模型与固定题库。随着模型快速迭代，这类评测容易过时，并且难以反映模型在内部是否真正“识别危险”。因此，论文关注的问题是：能否在模型生成之前，从内部表征层面衡量安全性。该问题的重要性在于可以降低评测成本并提升对安全机制本质的理解。

## 2. 核心思路 / 方法  
论文提出 SafeVec 方法，用白盒方式从模型内部激活中提取“拒绝方向”，即用安全与不安全提示在参考模型中的隐藏状态差异来构造向量表示。随后在目标模型中计算其在危险与越狱输入下的隐藏状态与该拒绝方向的余弦相似度，用于衡量拒绝对齐程度。方法进一步选择稳定的层区间以减少噪声，并通过不同安全水平的校准模型将相似度映射为 0–100 的 RAS（Refusal Alignment Score）。与传统方法相比，其创新在于不依赖输出文本，而是直接利用表示空间中的安全信号进行评估。

## 3. 结果与结论  
实验在 Llama、Gemma 和 Qwen 等模型族上验证，发现安全模型与不安全或去对齐模型在拒绝方向上的表征差异明显可分。RAS 与攻击成功率（ASR）呈显著相关性，通常 RAS 越高，模型越不容易被越狱攻击成功。结果表明该方法能够跨模型家族稳定区分不同安全水平模型，并且相比基于 judge 的评估具有更高效率。

## 4. 局限与适用边界  
该方法依赖白盒访问模型内部激活，因此不适用于仅提供 API 的闭源模型。其效果还依赖于参考模型与同家族结构假设，跨架构泛化能力在文本中仅部分验证。此外，RAS 需要校准模型参与标定，因此在不同设置下可能存在标尺漂移问题。

## Abstract
Safety evaluation of large language models (LLMs) is commonly performed by querying models with unsafe or jailbreak prompts and judging whether their outputs violate a safety policy. Although useful, output-level evaluation is expensive, sensitive to judge choice, and easily tied to fixed question banks. We propose **SafeVec**, a white-box evaluation procedure that measures safety from internal representations rather than generated answers. **SafeVec** first extracts layer-wise refusal directions from a safety-aligned reference model, then selects stable layer windows where safe and unsafe behaviors are separable, and finally scores a target model by measuring whether its hidden states align with these refusal directions under unsafe and jailbreak prompts. The resulting metric, **RAS** (**R**efusal **A**lignment **S**core), maps representation-level refusal alignment to a calibrated 0-100 safety score. Across `Llama`, `Gemma`, and `Qwen` model families, RAS separates aligned models from uncensored and abliterated variants, tracks output-level attack success rate, and is substantially faster than judge-based evaluation. These results suggest that refusal alignment provides a compact and efficient signal for white-box LLM safety evaluation.