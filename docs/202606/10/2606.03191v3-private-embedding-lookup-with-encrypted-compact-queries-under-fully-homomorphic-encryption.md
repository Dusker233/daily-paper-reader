---
title: Private Embedding Lookup with Encrypted Compact Queries under Fully Homomorphic Encryption
title_zh: 基于全同态加密的加密紧凑查询私有嵌入查找
authors: "Jung Hee Cheon, Daehyun Jang, Jaehee Kang, Hanee Rhee"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03191v3"
tags: ["query:q7"]
score: 7.8
relevance_score: 8.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 使用同态加密的加密嵌入查找
tldr: 本文提出了一种在完全同态加密（FHE）下实现私有嵌入向量查询的新方法，旨在保护用户输入的隐私，同时降低服务端计算成本。通过引入独立向量评估（IVE），避免了高成本的一热编码生成，将向量生成复杂度从 $O(p\log p)$ 降至 $O(p)$。实验显示在GloVe和FastText加密推理任务中，IVE显著提升嵌入查询效率，值得关注进一步精读其实现与性能分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03191v3/fig-001.webp\", \"caption\": \"Figure 1: IVE-PEL protocol.\", \"page\": 7, \"index\": 1, \"width\": 502, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2606.03191v3/fig-002.webp\", \"caption\": \"Table 2: Resource Consumption of Homomorphic Evaluation on the Server-side for IVE, plus one PCMM call.\", \"page\": 7, \"index\": 2, \"width\": 506, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2606.03191v3/fig-003.webp\", \"caption\": \"Table 3: Comparison of IVE-PEL and Kim et al. on three datasets. For all experiments, each token is represented by four subtable indices, and 2 16 tokens are processed simultaneously via CKKS SIMD packing. VecGen, PCMM, and Total report the amortized time (ms) for vector generation, plaintext-ciphertext matrix multiplication, and the overall PEL pipeline, respectively. Depth measures the multiplicative depth consumed during evaluation. IMDB accuracy indicates the embedding quality after compression; the uncompressed baseline accuracies are 0.8570 for GloVe.6B.50d and 0.8857 for GloVe.42B.300d. We do not report IMDB accuracy for GPT-2 because less than 10% of IMDB tokens overlap with its vocabulary. All timings are averaged over 10 runs, except Kim et al. with log 𝑝 = 10, which is averaged over 2 runs due to its high computational cost.\", \"page\": 10, \"index\": 3, \"width\": 752, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2606.03191v3/fig-004.webp\", \"caption\": \"Table 1: Design-space comparison for private embedding lookup. For the FHE-only rows, table compression gives ℓ private lookups over subtables of size 𝑝, followed by summation of the resulting vectors. FastQuery is included as an adjacent 2PC+HE comparison point rather than as a protocol normalized under this FHE-only compressed-table metric. Query size counts the plaintext query representation before encryption and packing. The last row shows the design point pursued in this work.\", \"page\": 3, \"index\": 4, \"width\": 1036, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2606.03191v3/fig-005.webp\", \"caption\": \"Figure 2: IVE-PEL server-side procedure (Emb). Given a ciphertext ct encoding (𝛼 𝑗𝑚 )𝑚∈[𝑠 ] , the server obtains the embedding vectors (𝑀 𝑗𝑚 )𝑚∈[𝑠 ] by applying IVE and PCMM. Each row corresponds to a single CKKS ciphertext. Here, 𝑠 denotes the number of slots (i.e., 𝑠 = 𝑁 /2). For 1 ≤ ℓ ≤ 𝑝/2 and 1 ≤𝑚 ≤ 𝑠, 𝑟ℓ𝑚 and 𝑖ℓ𝑚 denote Re(𝛼 ℓ 𝑗𝑚 ) and Im(𝛼 ℓ 𝑗𝑚 ), respectively.\", \"page\": 8, \"index\": 5, \"width\": 1051, \"height\": 345}]"
motivation: 如何在完全同态加密下高效且隐私安全地进行服务器端嵌入向量查询。
method: 通过独立向量评估（IVE）替代传统一热向量生成，用加密指数的连续幂构建线性独立向量，再通过预计算基变换恢复嵌入向量。
result: "在Enron-Spam数据集上，IVE将向量生成占加密推理总时间的比例从99.6%降至66.3%，整体查找时间提升至原方法的78.4倍。"
conclusion: 优先阅读方法部分（IVE构建与DCT基变换）、实验设置及结果分析，以理解性能提升机制及实用性。
key_findings: [IVE方法将向量生成复杂度从O(p log p)降至O(p)，显著降低计算成本。, "在加密FastText推理中，IVE大幅减少嵌入查询时间占比，从99.6%降至66.3%。", 实现方案在小批量和全批量设置中均优于前沿方法，提升总延迟约2-78倍。]
limitations: 方法依赖CKKS和DCT基，精度可能受加密误差和基变换影响；对非常大表仍有计算与存储开销。
---

## 1. 问题与背景
本文研究如何在完全同态加密（FHE）环境下进行隐私保护的嵌入向量查询。背景是现代机器学习服务中，客户端输入可能包含敏感信息，而嵌入层需要将离散索引映射为向量，这一步若泄露索引会暴露用户隐私。传统一热向量方法在FHE下计算成本高，尤其对于大规模词表和高维向量，服务器端私有查询成为瓶颈。

## 2. 核心思路 / 方法
论文提出了独立向量评估（Independent Vector Evaluation, IVE）方法，将一热向量替换为由单个加密值的连续幂构成的线性无关向量，从而将向量生成的同态运算复杂度从$O(p\log p)$降到$O(p)$。随后通过预计算的基变换（使用正交离散余弦变换）恢复目标嵌入向量，以减轻误差累积。该方法保持嵌入表在服务器端，同时降低了私有嵌入查询的计算负担，相较于现有方法在FHE环境下更高效。

## 3. 结果与结论
实验表明，在Enron-Spam数据集上，使用IVE将向量生成在加密推理中的时间占比从99.6%降至66.3%，并在GloVe.42B.300d表上实现了最多78.4倍的均摊查询加速。总的来说，IVE显著降低了服务器端私有嵌入查询的延迟，使FHE下的嵌入层查询更具实际可行性。

## 4. 局限与适用边界
该方法主要适用于服务器端嵌入查询和大规模词表场景，对于客户端轻量部署或表压缩方法未充分优化的情况，性能提升可能受限。从现有文本无法确认其他具体局限。

## Abstract
Many NLP or recommendation models begin by mapping discrete client inputs to embedding vectors. Since inputs can reveal sensitive information, the embedding step must be protected in privacy-preserving inference. Fully Homomorphic Encryption (FHE) enables inference over encrypted client data, but turns embedding lookup from simple table access into homomorphic computation. To keep the embedding table server-side and avoid transmitting encrypted embedding vectors from the client, we focus on server-side lookup: the client sends only a small encrypted index.   Prior ICML 2024 work first builds a one-hot vector from the encrypted index before multiplying with the embedding table, and this one-hot generation is the dominant cost. One-hot-based methods are expensive in FHE: they construct a p-dimensional selection vector via an equality test for each coordinate, requiring $O(p \log p)$ total homomorphic operations.   Our key observation is that private embedding lookup only requires a linearly independent representation of the encrypted index, not the one-hot basis itself. Building on it, we propose Independent Vector Evaluation (IVE). Instead of constructing a one-hot vector, IVE evaluates a linearly independent vector built from successive powers of a single encrypted value, reducing vector-generation cost to $O(p)$. It then recovers the same embedding vector via a precomputed change of basis, instantiated with an orthogonal Discrete Cosine Transform to mitigate error amplification.   Our implementation shows IVE improves amortized lookup time by up to 78.4x over prior method. We further evaluate its impact on end-to-end encrypted FastText inference, where embedding lookup is a major cost in the shallow model. On Enron-Spam dataset, replacing one-hot generation with IVE reduces the share of vector generation in encrypted inference time from 99.6% to 66.3%.