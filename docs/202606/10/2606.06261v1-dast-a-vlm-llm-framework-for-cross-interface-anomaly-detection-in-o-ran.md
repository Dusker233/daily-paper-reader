---
title: "DAST: A VLM-LLM Framework for Cross-Interface Anomaly Detection in O-RAN"
title_zh: DAST：用于 O-RAN 跨接口异常检测的 VLM-LLM 框架
authors: "Francesco Spinelli, Esteban Municio, Pau Baguer, Gines Garcia-Aviles, Xavier Costa-Perez"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06261v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: VLM-LLM框架检测O-RAN网络异常
tldr: 论文提出 DAST，一个面向 O-RAN 的零样本跨接口异常检测框架，针对开放式、多厂商 O-RAN 环境中难以获取标注数据、攻击模式快速演化以及跨接口级联异常难检测的问题。其核心是 VLM→LLM→VLM 多智能体链路：先视觉分析 KPI 时序，再结合 O-RAN 知识进行语义推理，最后在高分辨率热图上复核异常。实验基于真实 O-RAN 测试床数据，在性能退化场景下达到 0.910 F1 和 0.843 Accuracy，并超过多个 TSAD 基线。若关注 AI for Networks、O-RAN 安全或基础模型运维应用，值得继续细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06261v1/fig-001.webp\", \"caption\": \"Fig. 4: By leveraging O-RAN domain knowledge, DAST is able to realize that latency in F1-u is the actual anomaly, rather than sporadic packet loss in F1-u, which is actually the consequence.\", \"page\": 5, \"index\": 1, \"width\": 520, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2606.06261v1/fig-002.webp\", \"caption\": \"TABLE II: Results per different O-RAN interfaces.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2606.06261v1/fig-003.webp\", \"caption\": \"Fig. 2: O-RAN testbed used for DAST evaluation.\", \"page\": 4, \"index\": 3, \"width\": 509, \"height\": 463}]"
motivation: 如何在缺乏标注数据和固定基线的开放式 O-RAN 环境中，实现跨接口、可解释且能够泛化到未知威胁的异常检测。
method: 将多变量 KPI 转换为视觉表示，由 VLM 进行模式感知、LLM 结合 O-RAN 领域知识进行推理评分，再由第二个 VLM 对疑似异常进行视觉验证，从而实现零样本跨接口异常检测。
result: 作者在真实 O-RAN 测试床采集的数据上评估 DAST，在代表性的性能退化场景下取得 0.910 F1-Score 和 0.843 Accuracy。论文声称其性能优于多种主流时间序列异常检测（TSAD）基线方法。
conclusion: 优先阅读方法部分，重点关注三阶段 VLM→LLM→VLM 协同推理流程及 O-RAN 知识注入机制；其次阅读实验部分，分析测试床设置、性能退化场景构造以及与 TSAD 基线的对比依据。
key_findings: [DAST 无需标注数据、微调或历史正常基线，可直接进行零样本异常检测。, 跨接口推理能够捕获 O-RAN 控制闭环中的级联异常，而非仅监测单一接口。, 多智能体分工机制缓解了单个 LLM/VLM 处理高维 KPI 和复杂网络语义时的能力瓶颈。]
limitations: 实验仅展示性能退化类场景；泛化到更多攻击类型、不同网络规模及线上实时部署开销，从现有文本无法确认。
---

## 1. 问题与背景
本文关注在 O-RAN 架构下跨接口的异常检测问题。随着 6G 网络向分布式计算和虚拟化功能发展，RAN 不再是单一设备，而是多供应商、多接口协作的虚拟化功能集合，这使得 DoS 和性能下降类攻击难以被传统方法检测。传统时序异常检测方法在缺乏标注数据、高维多变量数据环境下表现不佳，跨接口故障传播进一步增加了检测难度，因此需要一种能够零样本、跨接口识别异常的智能方法。

## 2. 核心思路 / 方法
论文提出 DAST 框架，采用 VLM → LLM → VLM 的三阶段多智能体管线，实现跨接口异常检测。首先，视觉语言模型将 O-RAN KPI 时序数据转化为图形进行可视化分析；接着，基于 O-RAN 知识的语言模型对每个接口进行评分和异常推理；最后，第二个视觉模型在高分辨率热图上验证疑似异常。该方法无需标注数据或微调训练，可零样本适配多供应商配置，创新性在于通过多模态、多智能体协作模拟人类网络专家的推理过程，并实现跨接口关联分析。

## 3. 结果与结论
在 O-RAN 测试床的真实网络数据上，DAST 在性能下降场景下达到了 0.910 的 F1-Score 和 0.843 的准确率，优于现有 TSAD 基线方法。实验表明该框架能够有效识别跨接口异常，提供异常时间段、相关接口、操作影响评级及决策推理，为下游根因分析和运维排障提供支持。

## 4. 局限与适用边界
论文的局限主要在于依赖 O-RAN 域知识的更新与维护，同时效果验证基于特定测试床，泛化到所有网络环境和极端攻击类型的表现从现有文本无法确认。该方法适用于多供应商、动态配置的 O-RAN 环境下的零样本异常检测。

## Abstract
O-RAN enables a disaggregated baseband stack with programmable functions that communicate over standardized open interfaces. The same openness that enables multi-vendor composition also expands the attack surface across logically decoupled tiers that make up the compute continuum. Among these threats, Denial-of-Service and performance-degradation attacks, which account for the majority of catalogued O-RAN threats, are particularly difficult to detect. Traditional Time-Series Anomaly Detection (TSAD) methods fail in this new regime where labelled baselines are scarce, threats evolve faster than detectors can be retrained, and the high-dimensional multivariate telemetry overwhelms monolithic inference models. To address these challenges, we present DAST, a zero-shot multi-agent framework for cross-interface anomaly detection in O-RAN that chains a three-stage VLM $\rightarrow$ LLM $\rightarrow$ VLM pipeline. DAST converts multivariate KPI streams into visual representations, scores textual per-interface descriptions against O-RAN domain knowledge, and verifies suspects on high-resolution heatmaps to output the problematic interfaces, the anomalous time intervals, an indicative O-RAN WG11-aligned operational impact rating and the decision rationale. We evaluate DAST on real network traces collected from an O-RAN testbed under representative performance degradation scenarios, achieving 0.910 F1-Score and 0.843 Accuracy, outperforming state-of-the-art TSAD baselines.

---

## 论文详细总结（自动生成）

# 1. TLDR

DAST 提出了一种面向 O-RAN 的**零样本（Zero-Shot）跨接口异常检测框架**，采用 **VLM → LLM → VLM** 三阶段多智能体推理链，将时序 KPI 转换为视觉输入，并结合 O-RAN 领域知识完成异常识别与解释。

相比传统 TSAD（Time-Series Anomaly Detection）依赖标注数据和静态训练集的方法，DAST 能够识别 O-RAN 控制闭环中的跨接口级联异常，在真实 O-RAN 测试床上取得 **0.910 F1-Score** 和 **0.843 Accuracy**，超过多个 TSAD 基线。

**关键词：** O-RAN、异常检测、VLM、LLM、多智能体系统、零样本学习、跨接口推理、网络安全

---

# 2. 核心故事与贡献

## 2.1 问题与动机

O-RAN 的开放化与解耦架构虽然支持多厂商协作，但也显著扩大了攻击面。

论文指出：

- O-RAN 中约 **60% 已识别威胁属于 DoS 或性能退化攻击**（Introduction）。
- 异常可能通过 A1、E2、F1-c、F1-u 等接口传播，形成跨接口级联效应。
- 传统 TSAD 存在三大问题（Section II-B）：
  - **L1：缺乏标注数据**
  - **L2：分布漂移（Distribution Shift）导致模型失效**
  - **L3：单接口检测器无法理解级联传播**

作者认为未来 O-RAN 场景下最稀缺的不是模型能力，而是**可靠基线与可持续更新标签**。

---

## 2.2 核心洞察

作者提出一个重要观点：

> 不应让单个 Foundation Model 直接处理所有 KPI，而应让多个 Foundation Model 分工协作。

具体原因：

- LLM 不擅长高维数值推理；
- KPI 数量过大可能超过上下文窗口；
- 单模型难以同时完成：
  - 时序模式识别；
  - O-RAN 语义理解；
  - 根因定位。

因此借鉴网络专家诊断流程：

1. 先观察 KPI 图像；
2. 结合网络知识分析；
3. 再回到细粒度图像确认。

形成 VLM→LLM→VLM 推理链。

---

## 2.3 主要贡献

### Contribution 1

提出首个面向 O-RAN 的零样本跨接口异常检测多智能体框架 DAST。

（见 Introduction）

### Contribution 2

引入 O-RAN 域知识驱动的语义推理机制。

模型不仅判断异常，还判断：

- 哪个接口异常；
- 哪个时间段异常；
- 对业务影响等级。

### Contribution 3

在真实 O-RAN 测试床验证。

实验结果：

| 指标 | 数值 |
|--------|--------|
| F1 | 0.910 |
| Accuracy | 0.843 |

（Abstract）

---

# 3. 相关工作与定位

## 3.1 与传统 TSAD 的区别

| 维度 | MSCRED [2] | SpotLight [3] | DAST |
|--------|--------|--------|--------|
| 是否需要训练 | 是 | 是 | 否 |
| 是否依赖正常基线 | 是 | 是 | 否 |
| 是否支持零样本 | 否 | 否 | 是 |
| 是否理解 O-RAN 语义 | 否 | 部分 | 是 |
| 是否支持跨接口推理 | 否 | 否 | 是 |
| 是否可解释 | 中 | 高 | 高 |
| 是否使用 Foundation Model | 否 | 否 | 是 |

（Section II-B）

---

## 3.2 与 Foundation Model 异常检测工作的区别

### 现有工作

- 直接把数值输入 LLM（[4]）
- 把时序画图输入 VLM（[5]）
- 多智能体异常检测（[6]）

### DAST 的差异

DAST 额外引入：

- O-RAN 领域知识库
- 跨接口因果推理
- 二次视觉验证机制
- WG11 风险评级输出

因此不仅是检测器，更像一个网络运维诊断代理。

---

# 4. 方法详解

## 4.1 总体架构

见 Figure 1。

整体流程：

```text
KPI Timeseries
      ↓
Stage 1
VLM-1
视觉画像
      ↓
Stage 2
LLM
领域知识推理
      ↓
Stage 3
VLM-2
热图验证
      ↓
异常报告
```

输出：

- 异常接口
- 异常时间区间
- Impact Score
- 推理解释

---

## 4.2 Stage 1：Visual Interface Profiling

输入：

- O-RAN KPI 时间序列

操作：

将 KPI 转换成折线图。

VLM-1 自动提取：

- 指标名称
- 数值范围
- 波动模式
- 趋势变化

输出示例：

```text
A1-Latency-DL
Range: 0~600 ms
Behavior:
Sharp spikes after t=70
```

（Figure 1）

---

## 4.3 Stage 2：Domain-aware Semantic Evaluation

核心创新模块。

输入：

- Stage 1 文本描述
- O-RAN 领域知识

领域知识包括：

| 信息 | 示例 |
|--------|--------|
| 接口类型 | A1 / E2 / F1 |
| 数据类型 | Control / User Plane |
| 协议 | REST / SCTP |
| 正常行为模式 | Sporadic / Continuous |
| 风险等级 | WG11定义 |

LLM 根据知识库进行评分：

```text
Score(A1)=0.9
Score(F1-c)=0.7
```

并推断：

- 是否异常
- 是否可能是根因

---

## 4.4 Stage 3：Targeted Visual Verification

目的：

降低误报。

流程：

1. 仅保留高分候选；
2. 生成高分辨率 Heatmap；
3. VLM-2 精细分析。

输出：

```text
Anomaly:
A1-Latency-DL

Interval:
[75,200]

Impact:
3
```

（Figure 1）

---

## 4.5 跨接口推理机制

论文最重要的设计之一。

作者强调：

单接口异常 ≠ 根因异常。

例如：

- F1-u Packet Loss 增加
- 实际根因是 F1-u Latency 上升

DAST 利用 O-RAN 语义知识进行判断。

Figure 4 显示：

模型能够区分：

- 原因（Cause）
- 结果（Symptom）

而传统 TSAD 往往将二者同时标记为异常。

---

# 5. 实验分析

## 5.1 实验环境

见 Figure 2。

测试床包含：

- O-CU
- O-DU
- O-RU
- RIC
- O-RAN 接口

收集真实 KPI 数据。

（Figure 2）

---

## 5.2 主结果

论文报告：

| 方法 | F1 | Accuracy |
|--------|--------|--------|
| DAST | 0.910 | 0.843 |
| TSAD Baselines | 更低 | 更低 |

（Abstract；实验章节）

作者结论：

DAST 超越多个 SOTA TSAD 方法。

---

## 5.3 接口级结果

见 Table II。

论文进一步统计：

- A1
- E2
- F1-c
- F1-u

各接口检测性能。

主要发现：

- 控制面接口异常较容易识别；
- 存在级联传播时优势更加明显。

具体数值见 Table II。

---

## 5.4 关键发现

### 发现1

领域知识显著提高检测质量。

Figure 4 表明：

DAST 能识别真正异常源，而非仅检测统计异常。

---

### 发现2

多智能体结构缓解上下文窗口问题。

VLM负责视觉压缩：

```text
High-dimensional KPI
        ↓
Text Summary
        ↓
LLM Reasoning
```

避免将大量数值直接输入 LLM。

---

### 发现3

零样本条件下仍保持较高性能。

无需：

- 微调
- 标签
- 正常基线

这是相对于传统 TSAD 的核心优势。

---

# 6. 局限性与风险

## 假设约束

- 假设 KPI 能有效可视化表达异常模式。
- 假设 O-RAN 域知识库足够准确且持续更新。

---

## 应用边界

- 实验主要覆盖性能退化场景。
- 对复杂攻击（数据投毒、控制面欺骗等）效果未验证。
- 对超大规模运营商网络尚未展示结果。

---

## 潜在风险

- Foundation Model 幻觉可能影响异常解释。
- 域知识过时可能导致错误推断。
- 多阶段推理增加时延和计算成本。

---

# 7. 复现与后续问题

## 7.1 复现重点

### 重点1：KPI可视化策略

需确认：

- 时间窗口长度
- 采样频率
- Plot 分辨率

[需验证]

---

### 重点2：O-RAN知识库构建

需明确：

- 知识格式
- Prompt 模板
- WG11 映射规则

[需验证]

---

### 重点3：候选筛选阈值

Stage 2 如何决定：

```text
Score > ?
```

进入 Stage 3。

正文摘要未给出。

[需验证]

---

## 7.2 后续研究方向

### 方向1

扩展到更多攻击类型：

- Signaling Storm
- RIC Attack
- Data Poisoning
- xApp/rApp 恶意行为

---

### 方向2

结合在线 Agentic AI 运维系统。

形成：

```text
Detection
→ Diagnosis
→ Root Cause Analysis
→ Mitigation
```

闭环自治网络。

---

# 8. 直接证据与待验证项

### 证据1

“60% of all identified O-RAN threats correspond to DoS and performance degradation attacks.”

见 Introduction（Section I）。

---

### 证据2

DAST 采用：

```text
VLM → LLM → VLM
```

三阶段推理链。

见 Abstract、Figure 1、Section III。

---

### 证据3

输出内容包括：

- 异常接口
- 异常时间区间
- WG11 对齐影响等级
- 决策解释

见 Abstract、Figure 1。

---

### 证据4

实验结果达到：

- F1 = 0.910
- Accuracy = 0.843

见 Abstract。

---

### 证据5

Figure 4 显示：

DAST 能识别 F1-u latency 为真实异常源，而不是仅把 packet loss 当作异常。

见 Figure 4。

---

### [需验证]

- Table II 各接口具体 Precision/Recall/F1 数值。
- 使用的具体 VLM/LLM 模型版本。
- Prompt 内容与 Agent 间通信格式。
- 实时推理时延与资源开销。
- 与 MSCRED、SpotLight 的完整数值比较。

（完）
