---
title: Towards Retrieving Interaction Spaces for Agentic Search
title_zh: 面向智能代理搜索的交互空间检索
authors: "Shengyao Zhuang, Yuansheng Ni, Hengxin Fun, Jimmy Lin, Xueguang Ma"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.06880v1"
tags: ["query:profile-1"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 用于LLM的代理式搜索检索空间
tldr: "本论文针对智能搜索代理的检索问题，提出了构建“交互空间”的新方法 RISE，通过 BM25 限定可交互的语料子集，并对文档进行索引处理以支持类 shell 工具操作。实验显示，RISE 在 BrowseComp-Plus 数据集上以 GPT-5.4-mini 实现 78% 准确率，同时将每次查询成本降至原来的四分之一，并在百万文档规模下仍保持稳定表现。结果表明该方法在效率和可扩展性上优于传统直接语料交互方法，值得继续精读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06880v1/fig-001.webp\", \"caption\": \"Table 1: Cross-model results on the 100-query BrowseComp-Plus sample with the 100k corpus. Cost is USD per query; within each model tier, bold marks the best accuracy and the lowest cost. Turns is the mean number of model (API) calls per query. no_ans counts queries with no final answer; superscripts indicate the stopping condition: c context length, t turn/API-call cap, w wall-clock cap, and o other non-answer stop. Search, Bash, and Read are mean tool calls per query; for the retrievalagent, Read is get_document. gold_R is mean gold-document recall in the retrieved candidate set and cov_mean is mean gold-document coverage of the agent’s inspected context; both are evidencecoverage diagnostics and are not directly comparable across architectures. The final gpt-5.4 row is a state-of-the-art upper-bound probe, not part of the main cross-architecture comparison.\", \"page\": 6, \"index\": 1, \"width\": 830, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2606.06880v1/fig-002.webp\", \"caption\": \"Table 2: Scaling from 100k to 1M documents on the 100-query BrowseComp-Plus sample. All RISE-family rows at 1M are RISE-BM25 (the boundary-only configuration), since the offline TOC pipeline was not extended to the 900k FineWeb-Edu distractors. Tool-call columns report 1M values with the change from 100k in parentheses. Read* is the read count for RISE-BM25 and the get_document count for the retrieval-agent. DCI is reported at 1M only for gpt-5.4-nano.\", \"page\": 7, \"index\": 2, \"width\": 830, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2606.06880v1/fig-003.webp\", \"caption\": \"Table 3: BM25 top-K ablation for RISE-BM25 on the 100k corpus. K is applied per sub-query. WS size is the mean number of unique files in the accumulated workspace at the end of a query.\", \"page\": 8, \"index\": 3, \"width\": 702, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2606.06880v1/fig-004.webp\", \"caption\": \"Figure 3: Two case-study trajectories (gpt-5.4-mini, 100k corpus). (a) RISE-BM25: an obfuscated question is decomposed into many BM25 sub-queries whose union forms the workspace, after which bash/read verify the answer in the workspace. (b) RISE: the agent reads a document’s frontmatter and line-numbered table of contents, matches the “Acknowledgements” clue to the TOC entry L259–265: Acknowledgements and credits, and jumps to that line range to confirm the answer (reading only the relevant spans rather than the whole document).\", \"page\": 8, \"index\": 4, \"width\": 814, \"height\": 403}, {\"url\": \"assets/figures/arxiv/2606.06880v1/fig-005.webp\", \"caption\": \"Figure 2: RISE workflow. BM25 supplies the boundary (consequence i): candidate files are imported from the full corpus into a per-query interaction space that lives outside the context window. The imported files are TOC-indexed by an offline processing step (consequence ii): each file exposes a line-numbered table of contents so shell tools can land on relevant spans without a full read. The agent then iterates between multi-query search, shell inspection, and targeted reads inside the bounded space before synthesizing a final answer.\", \"page\": 4, \"index\": 5, \"width\": 829, \"height\": 235}]"
motivation: 如何为智能搜索代理设计可扩展且高效的语料交互空间以提升检索效果。
method: 通过 BM25 构建有限边界的交互空间，并对文档进行结构化索引处理，使代理可在该空间内高效使用 shell 样式工具探索语料。
result: "在 BrowseComp-Plus 上，RISE-BM25 在 1M 文档规模下依旧保持 81% 准确率，而传统 DCI 方法在 33/100 查询中失败且准确率降至 60%。"
conclusion: 优先阅读方法部分，理解 RISE 的交互空间构建和文档处理机制；随后查看实验与结果分析，重点关注成本-准确率对比和规模扩展实验。
key_findings: [RISE 通过 BM25 构建交互空间并索引处理文档，实现了在百万文档规模下的高稳定性和低查询成本。, "在 BrowseComp-Plus 数据集上，RISE-BM25 与纯 shell DCI 基线在准确率上相当，但每次查询成本降低约 75%。", 移除文档处理步骤会导致准确率下降 1–4 个百分点，表明文档处理对性能贡献明显。]
limitations: 现有方法主要在固定数据集和模拟环境下验证，对实时 web 或动态语料的适应性尚不明确。
---

## 1. 问题与背景
本研究关注搜索代理（search agents）在信息检索中的检索机制，目前仍沿用非代理检索模式。传统方法依赖检索器返回少量文档片段供模型生成答案，但当代理可以直接与原始语料交互时，现有方法在大规模语料下会面临成本和延迟快速上升的问题，因此需要重新定义检索目标以构建可探索的交互空间。

## 2. 核心思路 / 方法
论文提出RISE（Retrieving Interaction SpacE），通过BM25构建交互空间，并在索引阶段对文档进行处理以支持shell风格导航。与直接语料交互（DCI）相比，RISE不仅提供有界的候选文档集合，还允许代理使用工具高效探索这些文档，从而减少扫描成本并保持模型准确性。

## 3. 结果与结论
实验显示，在BrowseComp-Plus数据集上，RISE在gpt-5.4-mini模型下以约四分之一查询成本实现78%准确率，并在扩展至100万文档时仍保持81%的性能，而DCI在相同规模下准确率降至60%，出现显著失败率。结果表明，构建边界明确且可交互的文档空间可以有效提升大规模代理检索的效率和稳定性。

## 4. 局限与适用边界
RISE依赖BM25进行交互空间构建和文档处理，对于需要复杂语义理解或非结构化信息深度推理的场景，性能可能受限；适用范围主要在大规模文本语料且代理可使用shell工具导航的任务环境。

## Abstract
Retrieval for search agents is still inherited from non-agentic information retrieval: a retriever ranks the corpus and the agent reads a small set of returned documents. Recent direct corpus interaction (DCI) work shows that agents can instead interact with the raw corpus through shell tools such as grep and file reads. But unbounded interaction does not scale: every broad shell command is a scan over the whole corpus, and latency degrades sharply as the corpus grows. We argue that the role of retrieval for agentic search is not just to select documents that fit in the LLM context window, but to construct an interaction space: a bounded subset of the corpus the agent can explore with associated tools. Two design consequences follow. The space needs a boundary supplied by retrieval, and the objects within it should be processed for interaction. As a proof of concept, we propose RISE (Retrieving Interaction SpacE): we use BM25 to construct the interaction space; meanwhile, its documents are processed during indexing for shell-style navigation. On BrowseComp-Plus, RISE matches the pure-shell DCI baseline at 78% accuracy with gpt-5.4-mini at roughly one quarter of the per-query cost. At 1M documents, RISE-BM25 reaches 81% on gpt-5.4-mini, whereas DCI on gpt-5.4-nano degrades to 60% with 33 of 100 wall-clock failures.

---

## 论文详细总结（自动生成）

## 1. TLDR
本论文提出 RISE 方法，通过 BM25 构建智能搜索代理的交互空间，并对文档进行索引处理以支持 shell 样式操作，实现高效可扩展检索。  
在 BrowseComp-Plus 上，RISE-BM25 在百万文档规模下仍保持 81% 准确率，同时将每次查询成本降低至原来的四分之一。  

**关键词**：RISE、交互空间、BM25、搜索代理、BrowseComp-Plus、成本效率  

---

## 2. 核心故事与贡献

- **问题与动机**：传统检索方法仅返回少量文档片段，代理模型在大规模语料下直接交互会导致延迟和成本急剧上升（Section 1，引言）。  
- **核心洞察**：检索不应仅提供上下文窗口内的文档，而是构建“交互空间”，一个代理可通过工具遍历的有限语料子集，同时文档需处理为可导航对象。  
- **主要贡献**：
  1. 提出 RISE（Retrieving Interaction SpacE），结合 BM25 与文档索引处理实现交互空间（Section 3）。  
  2. 在 BrowseComp-Plus 上验证，RISE 在 gpt-5.4-mini 下以四分之一成本达到纯 shell DCI 78% 准确率，并在 1M 文档下仍保持 81% 性能（Figure 1，Section 3）。

---

## 3. 相关工作与定位

| 特征 / 维度                  | 传统 RAG / snippet retrieval | DCI (Direct Corpus Interaction) | PI-SERINI                | RISE (本工作) |
|-------------------------------|------------------------------|--------------------------------|--------------------------|---------------|
| 文档访问方式                  | 上下文窗口片段               | 原始语料 shell 工具            | BM25 限定候选列表       | BM25 限定交互空间 + shell 导航 |
| 可交互性                      | 低（仅读取）                 | 高（全语料可遍历）              | 中（有限列表可翻页）     | 高（边界明确且可重遍历） |
| 可扩展性                      | 高（小片段）                 | 低（全语料扫描成本大）          | 中                       | 高（交互空间限定减少扫描） |
| 文档处理                      | 无                           | 原始文本                        | 无                        | 索引化 TOC + 行号化，支持跳转 |
| 部署验证规模                  | 文档小规模                  | 100k-400k 文档                  | [需验证]                  | 100k-1M 文档，稳定 81% |

---

## 4. 方法详解

### RISE 架构概览
- **目标**：构建可交互的文档空间，使代理高效使用 shell 样式工具。  
- **核心流程**：
  1. **BM25 构建边界**：每次查询形成一个持久的候选文档集合 $W$，可跨子查询累积。
  2. **文档索引处理**：生成每个文档的行号化 TOC，使 shell 工具可以跳转至相关段落而非全文件扫描。
  3. **代理操作循环**：
     - 发送多子查询获取 top-K 文档到 $W$
     - 使用 bash / grep / cat / read 等工具验证证据
     - 聚合结果生成最终答案  

### 关键公式
- 文档集合 $W$ 构建：
$$
W = \bigcup_{q_i \in queries} \text{BM25\_topK}(q_i)
$$
- 代理观察：
$$
\text{Observation} = \text{Top-10 preview of each sub-query in } W
$$

---

## 5. 实验分析

### 主实验结果
| 模型 / 方法      | 100k 文档 | 1M 文档 | 每次查询成本 | 准确率 |
|-----------------|-----------|---------|--------------|--------|
| DCI (pure shell) | 78%       | 60%     | 高           | 见 Figure 1 |
| RISE-BM25       | 78%       | 81%     | 约四分之一   | 见 Figure 1 |

### 消融实验
- 去掉文档处理步骤（仅 RISE-BM25）准确率下降 1–4 个百分点（Section 3，Figure 2–4）。  
- 说明文档索引处理对性能稳定性有显著贡献。

### 核心发现
- 构建边界明确且可交互的空间能大幅降低扫描成本。  
- 在百万文档规模下，RISE-BM25 准确率仍稳定，而 DCI 失败率高（33/100 queries）。  
- 小规模实验表明，top-K 参数选择直接影响工作空间大小和性能（Figure 3）。

---

## 6. 局限性与风险

- **假设约束**：依赖 BM25 构建候选空间，对语义理解复杂问题可能不足。  
- **应用边界**：适用于可使用 shell 工具的文本代理任务，实时 web 或动态语料尚未验证。  
- **潜在风险**：错误索引或 TOC 构建问题可能导致证据遗漏或跳转失败。[需验证]

---

## 7. 复现与后续问题

### 复现重点
1. BM25 索引与 top-K 子查询的累积导入逻辑。  
2. 文档 TOC 行号化及 bash / read 工具的接口实现。  
3. 查询预算管理（输出截断、工具调用上限、子进程超时）。

### 后续研究方向
- 探索更语义化的交互空间构建（dense retrieval / hybrid 方法）。  
- 适配动态 web / 流式文本的实时代理检索场景。

---

## 8. 直接证据与待验证项

- RISE-BM25 在百万文档上仍保持 81% 准确率，DCI 降至 60%，失败 33/100 queries（Figure 1, Section 3）。  
- 去除文档处理步骤，准确率下降 1–4 个百分点（Figure 2–4, Section 3）。  
- BM25 top-K 参数影响工作空间大小及性能（Figure 3, Section 4）。  
- TOC 索引支持行号化跳转，减少全文扫描（Figure 5, Section 3.2）。  
- [需验证] 在实时 web 数据或非结构化大文本上的性能表现。

（完）
