---
title: "Rethinking Side-Channel Analysis: Automated Discovery and Analysis of Side-Channel Leakage with LLM-Assisted Agents"
title_zh: 重新思考侧信道分析：利用大语言模型辅助代理进行侧信道泄露的自动发现与分析
authors: "Zhen Xu, Zihao Wang, Yuhua Sun, XiaoFeng Wang"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17406v1"
tags: ["query:q7"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: LLM辅助侧信道流量分析
tldr: 本文提出SCAgent，用LLM驱动的智能体自动发现并分析系统侧信道泄露风险，突破传统依赖人工指定目标与通道的限制。方法通过代理探索识别敏感事件、基于系统文档+验证机制发现候选侧信道，并用ROCKET+TabPFN实现少样本泄露分析。在iOS上验证，可发现未知应用行为泄露，展示较强泛化能力，具有较高安全研究价值，适合精读评估。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17406v1/fig-001.webp\", \"caption\": \"Fig. 2: Impact of the dataset size collected in SCAgent.\", \"page\": 11, \"index\": 1, \"width\": 477, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-002.webp\", \"caption\": \"Fig. 4: Performance of SCAgent against gaussian noise.\", \"page\": 12, \"index\": 2, \"width\": 477, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-003.webp\", \"caption\": \"Fig. 3: Performance of SCAgent against frequency reduction.\", \"page\": 12, \"index\": 3, \"width\": 477, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-004.webp\", \"caption\": \"Fig. 1: Identified sensitive in-app activities. Sub-figure (a) shows the Trip app, where travel-related options are directly accessible from the home screen (highlighted by red boxes). By identifying clicks on these options, an attacker can infer users’ travel-related activities. Sub-figures (b) and (c) illustrate the TikTok app, where users first tap the “Add” button in (b) and then select a template in (c). The choice of templates may leak sensitive personal information, such as a user’s birthday. Sub-figures (d) and (e) depict the Flo app. In (d), users tap “Log your symptoms” to record health-related events. In the app settings shown in (e), users can enable “Track pregnancy”. Such interactions and records may reveal critical personal states, such as pregnancy.\", \"page\": 9, \"index\": 4, \"width\": 1037, \"height\": 444}]"
motivation: 如何在无需人工预定义目标事件与侧信道的情况下，自动化发现并分析系统中的侧信道泄露风险。
method: 构建SCAgent，将LLM代理用于事件探索与通道生成，并结合文档验证机制与ROCKET+TabPFN少样本学习完成侧信道泄露分析。
result: 在iOS真实系统中实例化后，能够识别未知应用行为泄露，并在标准基准任务与新发现应用行为中表现出有效性与鲁棒性。从现有文本无法确认更细节数值结果。
conclusion: 优先阅读系统设计部分（SCAgent三阶段流程）与验证机制设计，理解如何抑制LLM幻觉；再看实验部分，重点关注iOS实例与新发现侧信道案例。
key_findings: [LLM智能体可用于自动探索并生成潜在敏。]
---

## 1. 问题与背景

论文关注的是如何自动化发现和分析操作系统中的侧信道泄漏问题，尤其是在现代移动平台上，传统依赖人工经验的侧信道研究已经难以覆盖不断增长的复杂系统行为与应用场景。现有工作通常预设攻击目标和已知信道，但现实中哪些用户行为真正敏感、哪些系统接口可能泄漏信息、以及如何在有限数据条件下高效评估泄漏能力，仍缺乏系统化解决方案。作者认为，仅靠人工枚举和手工分析不仅扩展性差，还可能遗漏大量未知的隐私风险，因此需要一种能够自动探索敏感事件、自动发现潜在信道并可规模化分析泄漏的新框架。

## 2. 核心思路 / 方法

论文提出了一个名为 SCAgent 的自动化侧信道分析框架，其核心思路是将“敏感事件发现—候选信道生成—泄漏能力验证”拆分为三个自动化阶段。首先，SCAgent 使用基于 LLM 的语义推理与 agent-driven exploration 自动探索应用中的潜在敏感行为，通过 Android 端 agent 发现跨平台语义事件，再映射到 iOS 进行后续分析。其次，系统利用 LLM 对系统文档和接口行为进行推理，自动提出可能的侧信道，但为了避免 hallucination，作者加入了显式 verifier，对每个候选信道进行语义一致性、攻击模型可行性以及实际区分能力验证，并将验证结果反馈给后续生成过程。最后，在泄漏分析阶段，论文没有为每个 channel-event 组合单独训练模型，而是采用 few-shot learning 路线，通过 ROCKET 提取对时间偏移鲁棒的时序特征，再结合 TabPFN foundation model 完成分类，从而降低数据收集和模型训练成本。相比传统方法，其创新点在于把 LLM agent、自动验证机制和 foundation model 结合进统一的侧信道分析流程，实现了从发现到评估的端到端自动化。

## 3. 结果与结论

论文在 iOS 平台上实现并评估了 SCAgent，重点分析非特权应用可观察到的 OS-level side channels。实验中，作者共发现了 39 个此前未公开的 iOS 测量原语，覆盖 24 个系统接口。在标准任务上，SCAgent 在 100 个应用的 foreground app identification 中达到 98.1% 准确率，在 100 个网站的 website fingerprinting 中达到 96.7%，均超过已有最强基线。即使去除所有此前文献中已知的 iOS 信道，系统仍分别达到 95.0% 和 90.1% 的准确率，说明仅修补已知信道并不能有效消除侧信道风险。论文还展示了对旅行规划、健康记录、用户资料编辑等细粒度应用内行为的自动发现能力，表明 SCAgent 的价值不仅在于提升已有 benchmark 的准确率，更在于发现此前未被研究的新型敏感事件。

## 4. 局限与适用边界

论文当前主要聚焦于 iOS 上“非越狱、非特权应用可观测”的 OS-level side channels，因此其结论未必能够直接推广到硬件侧信道、浏览器内核级攻击或其他平台。系统中的敏感事件发现仍部分依赖 Android agent 与跨平台应用语义映射，这意味着对于仅存在于 iOS 的应用生态，其自动探索能力从现有文本无法确认。此外，论文正文节选中尚未完整展示不同 iOS 版本、设备型号和真实部署环境下的长期稳定性与泛化边界。

## Abstract
Side-channel attacks exploit unintended information leakage from system behavior and continue to pose serious privacy risks in modern platforms. Despite extensive prior work, side-channel analysis remains largely manual and fragmented, typically assuming predefined target events and a fixed set of known channels. As systems and applications grow increasingly complex, several fundamental questions remain unanswered: which user or system events are sensitive in practice, how side channels associated with these events can be systematically discovered without exhaustive manual effort, and how their leakage can be analyzed at scale without prohibitive data collection and model training costs. To address these questions, we present SCAgent, an automated framework for side-channel risk analysis. To identify sensitive targets beyond manually specified events, SCAgent performs agent-driven system exploration guided by LLM-based semantic reasoning. To systematically discover side channels while mitigating the risk of LLM hallucination, it reasons over system documentation and incorporates explicit verification to enforce semantic consistency, threat-model feasibility, and per-channel usability. To enable scalable analysis under limited data, SCAgent adopts a few-shot learning paradigm based on foundation models, avoiding the need to train bespoke models for each channel--event pair. To bridge the gap between raw time-series side-channel signals and tabular foundation models, SCAgent further introduces a time-shift--robust feature extraction layer that enables effective downstream analysis. We instantiate SCAgent on iOS as a first step, focusing on OS-level side channels observable by unprivileged applications. Our evaluation spans standard benchmarks such as foreground app and website fingerprinting, as well as newly identified sensitive in-app activities in popular applications.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文提出 **SCAgent**，一个面向移动平台侧信道风险的自动化分析框架，首次将 **LLM agent + 文档推理 + 显式验证 + foundation model few-shot 分析** 整合进统一流程，实现从“敏感事件发现”到“侧信道验证”的端到端自动化。系统在 iOS 上发现了 **39 个此前未公开的 OS-level measurement primitives**，并在 foreground app identification 与 website fingerprinting 上达到接近甚至超过现有 SOTA 的攻击效果。

SCAgent 的核心价值不只是提升已知 benchmark 的准确率，而是能够自动发现此前难以人工枚举的细粒度敏感行为（如健康记录、旅行规划、模板选择等），说明现代移动系统仍存在大量“未被系统化发现”的侧信道攻击面。

**关键词：** Side-channel analysis、LLM agent、iOS、Few-shot learning、TabPFN、ROCKET、Website fingerprinting、Automated vulnerability discovery

---

# 2. 核心故事与贡献

## 2.1 问题与动机：传统侧信道分析已无法扩展

传统 side-channel research 存在三个根本问题：

1. **目标事件需要人工指定**
   - 过去研究默认攻击者已知敏感行为（如某 App 是否前台运行）。
   - 现实中大量敏感行为是 application-specific 的，无法人工穷举。

2. **侧信道发现高度依赖专家经验**
   - 主要依赖：
     - 手工阅读系统文档
     - API keyword 搜索
     - 特定平台经验
   - 无法覆盖现代 OS 巨大的接口空间。

3. **分析成本过高**
   - 每个 channel-event pair 都要：
     - 采集大量 trace
     - 训练专用模型
   - 难以规模化。

作者明确指出：
> 当前研究仍然是“manual and fragmented”（见 Introduction）。

---

## 2.2 核心洞察：LLM 可以做“假设生成器”，但必须被约束

论文最重要的 insight：

> LLM 不应直接被信任，而应被视为“fallible hypothesis generator”。

因此 SCAgent 采用：

- LLM 负责：
  - 推理系统行为
  - 生成 candidate side-channel
  - 生成攻击假设

- Verifier 负责：
  - 文档一致性检查
  - threat model feasibility
  - distinguishability 测试
  - hallucination filtering

这是本文与“直接用 LLM 做安全分析”的关键差异。

---

## 2.3 主要贡献

### （1）自动化侧信道发现框架

提出完整 pipeline：

```text
Sensitive Event Discovery
        ↓
LLM-based Channel Proposal
        ↓
Verifier-based Filtering
        ↓
Few-shot Leakage Analysis
```

见正文 Section III。

---

### （2）首次将 Foundation Model 引入侧信道 few-shot 分析

采用：

- ROCKET：
  - time-series feature extraction
  - 时间偏移鲁棒

- TabPFN：
  - tabular foundation model
  - few-shot classification

避免：
- 为每个任务训练 bespoke classifier。

这是方法层面的重要创新。

---

### （3）发现大量未知 iOS side-channel primitives

论文报告：

- 发现：
  - **39 个新 measurement primitives**
  - 覆盖：
    - 24 个 system interfaces

见 Introduction / Evaluation。

---

### （4）攻击效果显著

在 100-class benchmark 上：

| Task | Accuracy |
|---|---|
| Foreground app identification | 98.1% |
| Website fingerprinting | 96.7% |

即便移除所有 prior iOS channels：

| Task | Accuracy |
|---|---|
| App identification | 95.0% |
| Website fingerprinting | 90.1% |

见 Introduction。

这说明：

> “修补已知 channel”并不能真正解决问题。

---

# 3. 相关工作与定位

## 3.1 与传统侧信道工作的对比

| 维度 | 传统工作 | SCAgent |
|---|---|---|
| Target event | 人工指定 | Agent 自动探索 |
| Channel discovery | 专家经验/keyword | LLM 推理 + verifier |
| Hallucination handling | 无 | 显式验证机制 |
| Scalability | 每任务单独分析 | Few-shot pipeline |
| Model training | bespoke classifier | TabPFN foundation model |
| Platform focus | 已知 benchmark | 未知敏感行为发现 |
| Threat model | 固定 channel | 自动扩展 attack surface |

---

## 3.2 与 LLM-for-security 工作的区别

论文在 Background 中提到：

- penetration testing
- protocol fuzzing
- CAPTCHA solving
- blockchain analysis

已有工作主要把 LLM 用作：
- automation tool
- reasoning assistant

但本文不同点在于：

| prior LLM security | 本文 |
|---|---|
| 直接生成 exploit | 生成 hypothesis |
| 强依赖 prompt | verifier-constrained |
| 少有 physical validation | 明确 distinguishability test |
| 偏 software logic | 面向 side-channel physics |

---

## 3.3 与传统 ML side-channel analysis 的区别

传统做法：

- CNN
- RNN
- handcrafted feature

缺点：
- 需要大量 labeled trace。

本文：

```text
ROCKET → TabPFN
```

目标：
- 少样本
- 泛化
- 降低训练成本

这是 foundation model 在 side-channel domain 的一次重要尝试。

---

# 4. 方法详解

# 4.1 整体架构

SCAgent 分为三个阶段：

```text
Stage 1:
Sensitive Event Discovery

Stage 2:
Candidate Channel Discovery

Stage 3:
Leakage Analysis
```

见正文 Section III。

---

# 4.2 Stage 1：Sensitive Event Discovery

## 核心思想

利用 mobile agent 自动探索 App。

但由于：
- Android agent 更成熟

因此：

- 在 Android 上：
  - 自动发现 semantic event

- 映射到 iOS：
  - 执行真实 side-channel analysis

作者强调：

> 并未假设 Android system behavior 可迁移到 iOS。

迁移的是：
- high-level user semantics。

---

## 发现的敏感行为示例

包括：

- Trip：
  - 旅行规划点击

- TikTok：
  - 模板选择
  - 生日相关模板

- Flo：
  - 症状记录
  - pregnancy tracking

见 Figure 1。

这是论文最有价值的部分之一：
> 自动发现“细粒度隐私行为”。

---

# 4.3 Stage 2：LLM-assisted Channel Discovery

## 核心流程

LLM 输入：

- 系统文档
- API 描述
- prior side-channel papers
- OS subsystem semantics

生成：

```text
candidate channel hypothesis
```

例如：

- GPU timing
- memory compression latency
- QoS switching
- cache contention

---

## Prompt Engineering

论文给出非常详细的 Prompt：

包括：

### Hunt Prompt

要求：

- Systematic subsystem scan
- Rendering pipeline
- Memory
- Neural Engine
- Audio stack
- GPU scheduling

并要求：

```text
Use Google Search to verify API existence
```

这是非常 agentic 的设计。

---

# 4.4 Verifier：抑制 LLM Hallucination

这是论文核心创新之一。

Verifier 检查：

## （1）Semantic consistency

channel 是否与文档一致。

---

## （2）Threat-model feasibility

是否：
- non-jailbroken
- unprivileged app 可观测。

---

## （3）Usability

channel 是否真的可 distinguish event。

---

## （4）Resource bottleneck analysis

论文提出：

### “Inference via Latency” Rule

即便 API 被 deny：

```text
failure latency itself
```

也可能泄漏信息。

---

### “Resource Bottleneck” Rule

按物理资源分类：

| Resource Type | Example |
|---|---|
| Heavy I/O + IPC | font loading |
| Memory lookup | cache read |
| Syscall context switch | scheduler |
| ALU saturation | compute loop |

目标：
- 去除 redundant channel。

这是非常有启发性的抽象。

---

# 4.5 Stage 3：Few-shot Leakage Analysis

## 问题

side-channel signal：

- multivariate time series
- 存在 temporal misalignment。

---

## 解决方案

### Step 1：ROCKET

作用：

- random convolution kernels
- time-shift robust feature extraction

输出：
- tabular feature vector。

---

### Step 2：TabPFN

使用：
- foundation model
- few-shot classification。

优点：

- 不需要重新训练大型模型
- 小数据可用。

见正文 Method Section。

---

# 5. 实验分析

# 5.1 实验设置

平台：

| Device | OS |
|---|---|
| iPhone 13 | iOS 15.3 |
| iPhone 14 | iOS 16.3 |

另有：

| Device | OS |
|---|---|
| [需验证] | iOS 26.4.2 |

威胁模型：

- 非越狱
- 非特权 App。

---

# 5.2 Main Results

## Foreground App Identification

| Metric | Result |
|---|---|
| #Apps | 100 |
| Accuracy | 98.1% |

比 strongest baseline：
- 高 4%。

---

## Website Fingerprinting

| Metric | Result |
|---|---|
| #Websites | 100 |
| Accuracy | 96.7% |

比 strongest baseline：
- 高 3%。

---

## 去除已知 channel 后

| Task | Accuracy |
|---|---|
| App identification | 95.0% |
| Website fingerprinting | 90.1% |

核心结论：

> 新发现 channel 本身就足够强。

---

# 5.3 Few-shot 能力

Figure 2：

- 分析 dataset size 对性能影响。

论文结论：

- 小样本下仍有效。

说明：
- TabPFN + ROCKET 的组合确实提升 data efficiency。

见 Figure 2。

---

# 5.4 鲁棒性实验

## Frequency Reduction

Figure 3：
- 测试低采样频率。

结果：
- 性能下降有限。

说明：
- leakage 具有稳定性。

---

## Gaussian Noise

Figure 4：
- 添加噪声。

结果：
- 仍保持较高 accuracy。

说明：
- side-channel signal 非脆弱。

---

# 5.5 新发现的敏感行为

Figure 1 展示：

| App | Sensitive Event |
|---|---|
| Trip | 旅行规划 |
| TikTok | 模板选择 |
| Flo | 怀孕追踪 |

这比传统 benchmark 更重要：

> SCAgent 不只是“提高分类率”，而是在发现新的 privacy surface。

---

# 6. 局限性与风险

## 6.1 Android → iOS 语义映射依赖

目前：

- sensitive event discovery
  - 依赖 Android agent。

因此：

- iOS-only app
- iOS-specific workflow

是否有效：
- [需验证]

---

## 6.2 当前主要关注 OS-level side channel

不包括：

- hardware side-channel
- browser microarchitecture
- cache attack

泛化性有限。

---

## 6.3 LLM 本身仍可能带来偏差

尽管 verifier 存在：

- candidate generation
  - 仍依赖 prompt。

可能：
- 漏掉非典型 channel
- 偏向已有 literature pattern。

---

## 6.4 真实部署风险

论文证明：

- 非特权 App 即可推断敏感行为。

意味着：

- App Store sandbox
- existing mitigations

并不足够。

这对移动生态有现实风险。

---

# 7. 复现与后续问题

# 7.1 复现重点

## （1）Prompt + verifier pipeline

复现难点：

- 多轮 agent reasoning
- verifier feedback loop

Prompt engineering 很关键。

---

## （2）ROCKET feature extraction

需要确认：

- kernel 数量
- feature dimension
- normalization strategy

正文节选未完整给出，[需验证]。

---

## （3）Trace collection pipeline

关键包括：

- sampling frequency
- synchronization
- noise filtering

这些直接影响 classification accuracy。

---

# 7.2 后续研究方向

## （1）Cross-platform generalized discovery

未来可探索：

- Android
- HarmonyOS
- browser
- desktop OS

统一 automated side-channel framework。

---

## （2）Automated mitigation synthesis

当前：
- 只能发现 leakage。

未来：
- 可进一步自动生成：
  - API hardening
  - scheduler randomization
  - noise injection policy。

---

# 8. 直接证据与待验证项

## 直接证据

1. 论文明确指出：
   > “side-channel analysis remain largely manual and fragmented”
   见 Introduction Section I。

---

2. 论文报告：
   - 发现 39 个 previously unreported measurement primitives
   - 覆盖 24 system interfaces
   见 Introduction。

---

3. Foreground app identification：
   - 100 apps
   - 98.1% accuracy
   见 Introduction / Evaluation。

---

4. Website fingerprinting：
   - 100 websites
   - 96.7% accuracy
   见 Introduction / Evaluation。

---

5. Figure 1 展示：
   - Trip travel planning
   - TikTok template selection
   - Flo pregnancy tracking
   等细粒度隐私行为。
   见 Figure 1。

---

## 待验证项

1. [需验证]
   TabPFN 的具体配置：
   - context size
   - training budget
   - inference latency。

---

2. [需验证]
   ROCKET feature extraction：
   - kernel 数量
   - pooling strategy。

---

3. [需验证]
   iOS 26.4.2 实验：
   - 数据规模
   - 设备型号
   - 是否与主实验一致。

---

4. [需验证]
   39 个新 channels 的完整 taxonomy 与 API 列表。
   当前节选未包含完整 Appendix。

---

5. [需验证]
   verifier 的自动化程度：
   - 是否仍有人类介入
   - false positive rate
   - rejection statistics。

---

（完）
