---
title: "Rethinking Side-Channel Analysis: Automated Discovery and Analysis of Side-Channel Leakage with LLM-Assisted Agents"
title_zh: 重新思考侧信道分析：利用 LLM 辅助智能体自动发现与分析侧信道泄露
authors: "Zhen Xu, Zihao Wang, Yuhua Sun, XiaoFeng Wang"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17406v1"
tags: ["query:q9"]
score: 8.3
relevance_score: 8.7
quality_score: 8.4
reliability_score: 8.1
practicality_score: 7.8
evidence: 基于LLM的侧信道自动分析与安全研究框架
tldr: 论文提出 SCAgent，一个面向移动系统侧信道风险的自动化分析框架，试图替代传统依赖专家经验、手工指定事件与通道的分析流程。其核心是结合 LLM 驱动的语义探索、基于文档推理的侧信道发现、显式 verifier 过滤幻觉，以及 few-shot foundation model 做低数据量泄漏分析。作者在 iOS 上验证了前台应用识别、网站指纹与新型 App 内活动泄漏等任务，结果显示能够发现此前未知的侧信道。若关注 AI Agent 在系统安全分析中的应用，这篇值得细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17406v1/fig-001.webp\", \"caption\": \"Fig. 2: Impact of the dataset size collected in SCAgent.\", \"page\": 11, \"index\": 1, \"width\": 477, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-002.webp\", \"caption\": \"Fig. 4: Performance of SCAgent against gaussian noise.\", \"page\": 12, \"index\": 2, \"width\": 477, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-003.webp\", \"caption\": \"Fig. 3: Performance of SCAgent against frequency reduction.\", \"page\": 12, \"index\": 3, \"width\": 477, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.17406v1/fig-004.webp\", \"caption\": \"Fig. 1: Identified sensitive in-app activities. Sub-figure (a) shows the Trip app, where travel-related options are directly accessible from the home screen (highlighted by red boxes). By identifying clicks on these options, an attacker can infer users’ travel-related activities. Sub-figures (b) and (c) illustrate the TikTok app, where users first tap the “Add” button in (b) and then select a template in (c). The choice of templates may leak sensitive personal information, such as a user’s birthday. Sub-figures (d) and (e) depict the Flo app. In (d), users tap “Log your symptoms” to record health-related events. In the app settings shown in (e), users can enable “Track pregnancy”. Such interactions and records may reveal critical personal states, such as pregnancy.\", \"page\": 9, \"index\": 4, \"width\": 1037, \"height\": 444}]"
motivation: 如何在现代移动系统中自动发现敏感事件、识别可行侧信道并以低成本完成大规模侧信道泄漏分析。
method: SCAgent 通过 LLM Agent 进行语义化事件探索，再结合文档推理与 verifier 约束生成可信侧信道候选，并利用 ROCKET+TabPFN 的 few-shot 方案完成低数据侧信道分类分析。
result: 论文在 iOS 环境下验证了 foreground app identification、website fingerprinting 以及新发现的 App 内敏感活动泄漏任务，并声称系统能够稳定发现此前未知的侧信道泄漏。其 verifier 还通过 threat-model feasibility、semantic consistency 与 usability filtering 缓解 LLM 幻觉问题。
conclusion: 建议优先阅读方法部分中“LLM hypothesis + verifier”机制，这是论文区别于传统 SCA 的核心贡献；其次看 few-shot leakage analysis 部分，理解其为何选择 ROCKET 与 TabPFN 组合来处理时间序列侧信道。
key_findings: [SCAgent 不再依赖人工预定义敏感事件，而是通过 Agent 自动探索潜在隐私活动。, 作者强调 LLM 只能作为 hypothesis generator，必须通过 verifier 显式过滤幻觉与不可行攻击面。, ROCKET 与 TabPFN 组合可在小样本条件下完成多种侧信道分析，减少为每个任务单独训练模型的成本。]
limitations: 目前仅在 iOS OS-level side channel 上验证，且事件探索依赖 Android Agent 映射，跨平台泛化能力与真实部署成本仍待确认。
---

## 1. 问题与背景

论文关注移动平台中的侧信道分析自动化问题，即如何系统性发现哪些用户行为是敏感的、哪些系统信道会泄露这些行为，以及如何低成本地完成大规模泄露分析。现有研究大多依赖研究者手工指定目标事件和候选信道，因此难以适应现代移动系统中不断扩张的软件复杂度与应用生态。作者指出，即使单个侧信道泄露有限信息，攻击者仍可通过多信道聚合推断用户隐私，因此现有“封堵已知信道”的防御方式可能无法真正降低现实风险。

## 2. 核心思路 / 方法

论文提出了自动化框架 SCAgent，将侧信道分析拆分为“敏感事件发现—候选信道发现—泄露分析”三个阶段。首先，系统利用 LLM 驱动的 agent 自动探索 App 行为，通过语义推理识别可能具有隐私意义的用户活动；由于缺乏成熟的 iOS agent，作者借助 Android 版本 App 的交互语义发现候选事件，再映射到 iOS 进行后续分析。其次，系统利用 LLM 阅读系统文档并生成潜在侧信道假设，但并不直接信任 LLM 输出，而是引入验证器，对每个候选信道进行语义一致性、威胁模型可行性以及区分能力验证，以减少 hallucination 带来的错误推断。最后，在泄露分析阶段，作者没有为每个“事件—信道”组合训练专用模型，而是采用基于 foundation model 的 few-shot 学习范式，并使用 ROCKET 提取具有时间偏移鲁棒性的时序特征，再结合 TabPFN 完成分类，从而降低数据采集与训练成本。

## 3. 结果与结论

作者在 iOS 平台上实例化 SCAgent，重点研究普通沙盒应用可观测的 OS-level 侧信道，并发现了 39 个此前未报告的测量原语，覆盖 24 个系统接口。在标准任务上，系统在 100 个 App 的前台应用识别中达到 98.1% 准确率，在 100 个网站的网站指纹识别中达到 96.7%，均超过已有最强基线。即使排除所有已有 iOS 研究中出现过的侧信道，系统仍能分别达到 95.0% 和 90.1% 的准确率，说明仅修补已知信道并不足以防御未来攻击。论文还进一步展示了系统能够自动发现旅行规划、健康跟踪、个人资料创建等细粒度 App 内行为，这类目标过去通常难以人工枚举。

## 4. 局限与适用边界

论文目前主要聚焦 iOS 平台上的 OS-level 侧信道，且实验对象主要是未越狱设备中的普通应用场景，因此其结论未必能直接推广到硬件级、浏览器级或跨平台环境。事件发现阶段部分依赖 Android 与 iOS 应用之间共享的交互语义，因此对于缺乏跨平台对应关系的应用，其自动化能力可能受限。由于正文被截断，从现有文本无法确认其在真实长期部署、动态系统更新以及更严格防御机制下的稳定性表现。

## Abstract
Side-channel attacks exploit unintended information leakage from system behavior and continue to pose serious privacy risks in modern platforms. Despite extensive prior work, side-channel analysis remains largely manual and fragmented, typically assuming predefined target events and a fixed set of known channels. As systems and applications grow increasingly complex, several fundamental questions remain unanswered: which user or system events are sensitive in practice, how side channels associated with these events can be systematically discovered without exhaustive manual effort, and how their leakage can be analyzed at scale without prohibitive data collection and model training costs. To address these questions, we present SCAgent, an automated framework for side-channel risk analysis. To identify sensitive targets beyond manually specified events, SCAgent performs agent-driven system exploration guided by LLM-based semantic reasoning. To systematically discover side channels while mitigating the risk of LLM hallucination, it reasons over system documentation and incorporates explicit verification to enforce semantic consistency, threat-model feasibility, and per-channel usability. To enable scalable analysis under limited data, SCAgent adopts a few-shot learning paradigm based on foundation models, avoiding the need to train bespoke models for each channel--event pair. To bridge the gap between raw time-series side-channel signals and tabular foundation models, SCAgent further introduces a time-shift--robust feature extraction layer that enables effective downstream analysis. We instantiate SCAgent on iOS as a first step, focusing on OS-level side channels observable by unprivileged applications. Our evaluation spans standard benchmarks such as foreground app and website fingerprinting, as well as newly identified sensitive in-app activities in popular applications.

---

## 论文详细总结（自动生成）

# 1. TLDR

论文提出 SCAgent，一个面向移动系统侧信道分析的自动化框架，试图将传统依赖专家经验的“目标事件选择—侧信道发现—泄露建模”流程系统化。其核心创新在于：使用 LLM Agent 自动探索敏感事件、利用 verifier 约束 LLM hallucination，并结合 ROCKET + TabPFN 在 few-shot 条件下完成可扩展侧信道分类分析。

作者在 iOS 上验证了该框架，发现了 39 个此前未公开的 OS-level measurement primitives，并在前台 App 识别、网站指纹与 App 内敏感活动推断任务上取得较高准确率，说明“封堵已知 API”不足以真正缓解未来侧信道风险。

**关键词：** Side-Channel Analysis、LLM Agent、iOS Security、Few-shot Learning、TabPFN、ROCKET、Website Fingerprinting、Automated Vulnerability Discovery

---

# 2. 核心故事与贡献

## 2.1 问题与动机：传统侧信道分析无法扩展

传统侧信道研究存在三个根本问题：

1. **目标事件依赖人工指定**
   - 过去工作通常预设攻击目标，例如 foreground app identification 或 website fingerprinting。
   - 但现实中真正敏感的行为是动态变化的，例如健康记录、旅行计划、内容创作等，难以靠人工穷举。

2. **侧信道发现高度依赖专家经验**
   - 研究者通常靠手工阅读系统文档、经验猜测或 API keyword 搜索。
   - 随着移动系统复杂化，这种方法难以覆盖庞大的 attack surface。

3. **分析成本不可扩展**
   - 每个「事件 × 信道」组合都训练专用模型，需要大量 trace collection 与 labeling。
   - 当候选信道数量扩大后，成本迅速爆炸。

作者在 Introduction 中明确指出：
> 即使单个信道仅泄露少量信息，攻击者也可以跨多个 noisy channels 聚合证据，从而高置信度恢复敏感行为（Section I）。

---

## 2.2 核心洞察：LLM 可以做“假设生成器”，但不能直接信任

论文最重要的思想并不是“用 LLM 做安全分析”，而是：

> **LLM 只能作为 hypothesis generator，而不是 ground truth provider。**

作者认为：
- LLM 很适合：
  - 阅读系统文档
  - 建立 subsystem 关联
  - 推断潜在 resource contention
  - 生成候选 leakage hypothesis

但直接使用会出现严重 hallucination：
- API 不存在
- Threat model 不成立
- Sandbox 不可访问
- Channel 无 distinguishability

因此论文设计了 verifier：
- semantic consistency check
- threat-model feasibility check
- usability filtering

这是 SCAgent 区别于普通 “LLM-for-security” 工作的核心。

---

## 2.3 主要贡献

### （1）提出自动化侧信道分析框架 SCAgent

将流程拆解为：

```text
Sensitive Event Discovery
        ↓
Channel Hypothesis Generation
        ↓
Verifier-based Filtering
        ↓
Few-shot Leakage Analysis
```

见 Section I。

---

### （2）发现新的 iOS OS-level side channels

作者声称：
- 发现 **39 个此前未报告的 measurement primitives**
- 覆盖 **24 个系统接口**

见 Introduction / Summary。

---

### （3）few-shot leakage analysis

论文没有为每个 channel-task 单独训练深度模型，而是：

```text
Time-series traces
    ↓
ROCKET feature extraction
    ↓
TabPFN classification
```

其目标是：
- 降低 trace collection 成本
- 避免 bespoke model training
- 支持快速扩展到新事件

---

### （4）在标准 benchmark 上取得高准确率

作者报告：

| Task | Accuracy |
|---|---|
| Foreground App Identification | 98.1% |
| Website Fingerprinting | 96.7% |

即使移除 prior iOS papers 中已知 channel：
- App identification 仍达 95.0%
- Website fingerprinting 仍达 90.1%

见 Introduction。

这意味着：
> “修补已知 channel” ≠ “解决侧信道问题”。

---

# 3. 相关工作与定位

## 3.1 与传统侧信道工作的差异

| 维度 | 传统 SCA | SCAgent |
|---|---|---|
| Target event | 人工指定 | Agent 自动探索 |
| Channel discovery | 专家经验 / keyword search | LLM + verifier |
| Hallucination handling | 无 | 显式 verifier |
| Leakage analysis | 每任务训练专用模型 | Few-shot foundation model |
| 数据需求 | 大规模 labeled traces | 小样本 |
| 扩展性 | 低 | 较高 |
| 系统化程度 | fragmented | pipeline 化 |

---

## 3.2 与“LLM for Security”工作的差异

论文引用：
- penetration testing
- protocol fuzzing
- CAPTCHA solving
- blockchain analysis

但指出：
> 过去工作很少系统性研究 LLM 在 side-channel discovery 中的作用（Section II-A）。

SCAgent 的关键区别：

| 方向 | 普通 LLM 安全工具 | SCAgent |
|---|---|---|
| LLM 角色 | 直接分析器 | hypothesis generator |
| 核心风险 | prompt correctness | hallucination |
| 约束机制 | 少 | verifier pipeline |
| 输出形式 | textual result | measurable channel |

---

## 3.3 与经典 Website Fingerprinting 工作的差异

传统 website fingerprinting：
- 关注 packet sequence
- browser cache
- network timing

SCAgent：
- 聚焦 OS-level side channels
- 不依赖网络抓包
- 利用 resource contention 与 subsystem fluctuation

尤其强调：
- GPU
- RenderServer
- CoreAnimation
- ANE
- memory compression
- scheduler QoS

属于更“系统资源层”的 fingerprinting。

---

# 4. 方法详解

# 4.1 总体框架

SCAgent 包含三个核心阶段：

```text
(1) Sensitive Event Discovery
        ↓
(2) Side-channel Discovery
        ↓
(3) Leakage Analysis
```

见 Section I。

---

# 4.2 Sensitive Event Discovery

## 核心思想

使用 LLM Agent 自动探索 App workflow：

```text
App UI
  ↓
Agent interaction
  ↓
Semantic reasoning
  ↓
Sensitive activity candidates
```

例如：
- TikTok 模板选择
- Flo pregnancy tracking
- Trip travel planning

见 Figure 1。

---

## Android → iOS Mapping

由于缺乏成熟 iOS Agent：

- 使用 Android Agent 发现 semantic events
- 再映射到 iOS workflow

作者特别强调：

> 并不假设 Android side-channel behavior transferable to iOS。

只共享：
- user semantics
- workflow structure

这是一个重要工程折中。

---

# 4.3 LLM-assisted Channel Discovery

## Prompt-based reasoning

论文给出的 prompt 非常具有代表性。

其核心思想是：

### （1）从 subsystem 出发，而不是 API 出发

例如：

| Subsystem | 推断目标 |
|---|---|
| GPU / Metal | DOM complexity |
| RenderServer | frame timing |
| ANE | OCR / image inference |
| Memory compression | large-page behavior |
| CoreAudio | autoplay detection |

---

### （2）围绕 resource bottleneck 建模

Verifier 强调：

> 不要按 API 分类，而要按 physical constraint 分类。

例如：

| Vector | Bottleneck |
|---|---|
| UIFont.systemFont | Disk I/O + IPC |
| UIFont.familyNames | Memory lookup |

虽然都属于字体框架：
- 但 resource signature 不同。

---

# 4.4 Verifier：论文最关键模块

这是全文最重要的部分。

Verifier 对每个 channel 执行：

## （1）Semantic Consistency

检查：
- 文档是否支持该机制
- API 是否真实存在

---

## （2）Threat-model Feasibility

检查：
- 是否需要 entitlement
- 是否触发 TCC
- background app 是否可访问
- sandbox 是否允许

---

## （3）Usability Filtering

检查：
- distinguishability
- signal-to-noise ratio
- thermal throttling
- redundancy

---

## （4）Inference-via-Latency Rule

即使：
- access denied
- API blocked

仍检查：

```text
Latency variance
Lock contention
Service busy timing
```

这是很典型的现代 side-channel 思路。

---

# 4.5 Few-shot Leakage Analysis

## 问题

原始 side-channel trace：

- multivariate
- time-series
- temporally misaligned

不能直接输入 tabular foundation model。

---

## ROCKET + TabPFN

论文采用：

```text
Time-series traces
    ↓
ROCKET
    ↓
Shift-robust features
    ↓
TabPFN
```

ROCKET：
- 提取时间偏移鲁棒特征

TabPFN：
- few-shot tabular classification

目标：
- 避免 per-task model training
- 降低数据需求

见 Section I。

---

# 5. 实验分析

# 5.1 主实验结果

## Foreground App Identification

| 指标 | 结果 |
|---|---|
| App 数量 | 100 |
| Accuracy | 98.1% |

优于 strongest baseline：
- +4 percentage points

见 Introduction。

---

## Website Fingerprinting

| 指标 | 结果 |
|---|---|
| Website 数量 | 100 |
| Accuracy | 96.7% |

优于 strongest baseline：
- +3 percentage points

---

# 5.2 排除已知信道后的结果

这是论文最重要实验之一。

作者移除：
> 所有 prior iOS side-channel papers 中已知 channels。

结果：

| Task | Accuracy |
|---|---|
| App Identification | 95.0% |
| Website Fingerprinting | 90.1% |

说明：

```text
Known-channel mitigation
        ≠
Future-channel mitigation
```

这是论文安全意义最强的结论。

---

# 5.3 Sensitive In-app Activity Discovery

Figure 1 展示：

| App | Sensitive Activity |
|---|---|
| Trip | travel planning |
| TikTok | template selection |
| Flo | symptom logging / pregnancy tracking |

这些目标：
- 语义化
- App-specific
- 难以人工枚举

这是 agent-assisted discovery 的价值所在。

---

# 5.4 Few-shot 数据规模实验

Figure 2：
> “Impact of dataset size collected in SCAgent”

说明：
- 少量 trace 已可获得较高准确率
- foundation model 确实降低数据需求

但具体 sample 数量与 scaling curve：
- 当前摘要未给出
- [需验证：见 Figure 2 正文]

---

# 5.5 Robustness 实验

## Frequency Reduction

Figure 3：
- 测试采样频率降低影响

说明系统对低频采样仍具有一定鲁棒性。

---

## Gaussian Noise

Figure 4：
- 加入噪声后性能变化

说明：
- channel 不完全依赖 clean timing
- 存在较稳定 resource signature

---

# 5.6 新版本 iOS 验证

作者额外在 iOS 26.4.2 上测试：

| Task | Accuracy |
|---|---|
| Website Fingerprinting | 96.5% |
| Foreground App ID | 82.0% |

作者说明：
- dataset 较小
- 为 focused validation

但仍说明：
> leakage 并未在新版 iOS 中完全消失。

---

# 6. 局限性与风险

## 6.1 Android → iOS 映射假设

事件探索依赖：
- Android Agent
- cross-platform semantic mapping

但：
- workflow 不一定一致
- iOS UI 行为可能不同

泛化能力有限。

---

## 6.2 仅覆盖 OS-level side channel

论文主要研究：
- unprivileged app observable channels

未覆盖：
- hardware microarchitectural attacks
- browser internals
- kernel-level leakage

因此不能代表全部侧信道空间。

---

## 6.3 LLM hallucination 仍可能残留

虽然 verifier 缓解 hallucination：

但：
- verifier 本身也基于规则与经验
- 可能遗漏 subtle infeasible assumptions

特别是：
- undocumented subsystem behavior
- future OS patches

---

## 6.4 防御意义存在“军备竞赛”风险

论文核心结论实际上意味着：

```text
Attack surface discovery
can be automated
```

这可能导致：
- channel discovery acceleration
- defensive lagging

未来：
- 攻击自动化速度可能超过 patch 周期。

---

# 7. 复现与后续问题

# 7.1 复现重点

## （1）Verifier Pipeline

最关键的是 verifier：

需要复现：
- semantic consistency
- threat-model feasibility
- distinguishability filtering

否则：
- LLM 输出会 rapidly degenerate。

---

## （2）ROCKET Feature Extraction

需要确认：

- kernel 数量
- feature dimension
- temporal alignment 策略

因为这是 few-shot 成功的关键。

[需验证：正文是否给出超参数]

---

## （3）iOS Measurement Primitive Collection

需要重点关注：

- sampling frequency
- foreground/background scheduling
- thermal control
- trace synchronization

这些都会显著影响 leakage quality。

---

# 7.2 后续研究方向

## （1）Cross-platform Automated SCA

未来可扩展：
- Android
- macOS
- browser runtime
- IoT systems

---

## （2）Multi-channel Fusion

当前工作更偏：
- single-channel evaluation

未来可以研究：

```text
Cross-channel evidence aggregation
```

这也是作者在引言强调的重要风险。

---

# 8. 直接证据与待验证项

## 直接证据

1. 作者声明发现：
   > “39 previously unreported iOS OS-level measurement primitives spanning 24 system interfaces.”
   - 见 Introduction。

---

2. Foreground app identification：
   > “98.1% accuracy over 100 apps.”
   - 见 Introduction。

---

3. Website fingerprinting：
   > “96.7% accuracy over 100 websites.”
   - 见 Introduction。

---

4. 即使移除 prior work 已知 channels：
   > “95.0% accuracy on foreground app identification and 90.1% accuracy on website fingerprinting.”
   - 见 Introduction。

---

5. Figure 1 展示：
   - TikTok 模板选择
   - Flo pregnancy tracking
   - Trip travel planning
   - 属于自动发现的 sensitive in-app activities
   - 见 Figure 1。

---

## 待验证项

1. [需验证]
   ROCKET 与 TabPFN 的具体超参数与 feature dimension。

---

2. [需验证]
   Verifier 是否包含自动 runtime probing，还是主要依赖 documentation reasoning。

---

3. [需验证]
   39 个 measurement primitives 的完整列表与 subsystem 分类。

---

4. [需验证]
   iOS 26.4.2 实验的数据规模与统计显著性。

---

5. [需验证]
   Few-shot setting 下每个任务实际所需 trace 数量。

---

（完）
