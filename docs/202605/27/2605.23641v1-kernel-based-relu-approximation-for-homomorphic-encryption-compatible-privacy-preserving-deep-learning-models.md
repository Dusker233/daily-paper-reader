---
title: Kernel-Based ReLU Approximation for Homomorphic Encryption-Compatible Privacy-preserving Deep Learning Models
title_zh: 基于核的ReLU近似用于同态加密兼容的隐私保护深度学习模型
authors: "Dimitrios Sygletos, Dimitra Papatsaroucha, Marios Choudetsanakis, Ilias Politis, Evangelos K. Markakis"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23641v1"
tags: ["query:q5"]
score: 6.8
relevance_score: 7.0
quality_score: 7.0
reliability_score: 7.0
practicality_score: 6.0
evidence: 兼容同态加密的ReLU用于隐私深度学习
tldr: 本文针对同态加密（HE）环境下深度学习模型无法直接使用ReLU激活函数的问题，提出一种基于核方法的ReLU近似方案。通过将ReLU平滑化并用二次多项式逼近，该方法兼容HE约束，支持在加密数据上进行推理。实验显示，在预训练大型语言模型的token嵌入上，近似函数在精度与计算效率间取得平衡，表现出在隐私保护推理任务中可行性。值得继续精读其方法实现和实验评估部分以评估实际可用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23641v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 700, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2605.23641v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 939, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.23641v1/fig-003.webp\", \"caption\": \"\", \"page\": 6, \"index\": 3, \"width\": 939, \"height\": 394}]"
motivation: 如何在同态加密约束下，实现对深度学习模型ReLU激活函数的高效近似，以支持隐私保护的推理。
method: 通过核方法将ReLU函数平滑化，再使用低阶多项式进行逼近，使其可在仅支持加法和乘法的同态加密环境中使用。
result: 在对预训练LLM的token嵌入进行测试中，所提出的二次多项式近似实现了对ReLU行为的高保真模拟，同时保持了HE兼容性和合理的计算复杂度。
conclusion: 优先阅读第4节方法实现及第5节实验评估，这两部分详细描述了ReLU平滑化、核方法应用及多项式逼近的技术细节和性能验证结果。
key_findings: [核方法平滑化后的ReLU可被二次多项式高效逼近，减少了近似误差。, 在加密和明文环境下，近似ReLU在LLM token嵌入任务上保持竞争力表现。, 方法在HE约束下实现了隐私保护推理，同时计算复杂度可控。]
limitations: 仅使用二次多项式近似，可能限制在更复杂非线性任务中的表现；从现有文本无法确认跨模型和大规模数据的通用性。
---

## 1. 问题与背景

论文关注同态加密（Homomorphic Encryption, HE）环境下深度学习模型的激活函数兼容问题，尤其是大语言模型（LLM）中广泛使用的 ReLU 函数无法直接在加密域中执行的问题。由于 HE 仅支持加法与乘法，而 ReLU 本身包含非线性与不可微特性，因此现有深度学习模型难以直接用于隐私保护推理场景。随着 NLP 与 LLM 在医疗、聊天机器人等敏感场景中的普及，用户输入在服务端明文处理带来的隐私风险日益突出，因此构造一种既保留 ReLU 行为、又满足 HE 计算限制的替代函数具有重要意义。

## 2. 核心思路 / 方法

论文提出了一种基于 Kernel smoothing 的 ReLU 近似方法，核心思想是先将非平滑的 ReLU 转换为一个平滑函数，再使用低阶多项式进行逼近。作者受到 Jackson 定理启发，认为函数越平滑，越容易被低阶多项式准确逼近，因此首先利用 Kernel-based 方法构造一个模仿 ReLU 的平滑函数，再通过二次多项式进行拟合，以降低同态加密中的 multiplicative depth。论文特别强调在不同多项式阶数之间权衡近似精度与 HE 推理成本，最终选择二阶多项式作为兼顾精度与计算效率的方案。与传统直接使用平方函数等简单替代方案相比，该方法能够更好保留 ReLU 对负值的抑制行为，并减少误差传播问题。

## 3. 结果与结论

实验部分将该方法应用于多种场景，包括模拟数据、tokenized 数据、深度学习模型以及 transformer 模型，并直接在预训练 LLM 的 token embedding 上进行训练与评估。结果表明，该 Kernel-based ReLU approximation 在近似 fidelity 上优于文献中的其他 HE-compatible ReLU 替代方案，同时在明文与加密环境下都保持了较有竞争力的表现。论文最终认为，该方法能够在满足 HE 运算限制的前提下，更有效地保留深度模型中的非线性表达能力，因此适合作为隐私保护推理与未来隐私保护 LLM 部署中的基础组件。

## 4. 局限与适用边界

论文主要聚焦于 ReLU 的低阶多项式近似问题，因此方法的适用范围主要限于需要 HE-compatible activation 的推理场景，而非完整的大规模加密训练。从现有文本无法确认该方法在超大规模 LLM、长上下文推理或真实工业级 HE 部署中的性能开销与扩展性。此外，由于最终仍采用二次多项式近似，其对原始 ReLU 行为的逼近能力在极端输入区间可能仍存在误差。

## Abstract
As privacy concerns in AI technologies continue to grow, Homomorphic Encryption (HE) offers a way to perform computations on encrypted data without the need of decryption during operations. However, HE is limited to addition and multiplication, making non-linear functions incompatible in their original form. This limitation has become more critical with the widespread use of Large Language Models (LLMs), where the non-linearity of activation functions such as the Rectified Linear Unit (ReLU) poses challenges for deployment in privacy-preserving Natural Language Processing (NLP) settings. This paper proposes a kernel-based approximation of ReLU, enabling its use within HE-constrained settings and thus contributing a critical step toward supporting privacy-preserving LLMs. A smooth kernel-based function, mimicking ReLU, is approximated using a second-degree polynomial, inspired by Jackson's theorem, to achieve low multiplicative depth. The proposed method is trained and assessed directly on token embeddings from pre-trained LLMs and evaluated in various scenarios, from simulated and tokenized data to deep learning and transformer models. Results show improved approximation fidelity, supporting the method's suitability for secure and privacy-preserving inference in various tasks.