---
title: "\"What is the Problem Space?\" Defining Host-space Adversarial Perturbations against Network Intrusion Detection Systems"
title_zh: “问题空间是什么？”——定义针对网络入侵检测系统的主机空间对抗扰动
authors: "Miel Verkerken, Laurens D'hooge, Bruno Volckaert, Filip De Turck, Giovanni Apruzzese"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25822v1"
tags: ["query:q4"]
score: 6.8
relevance_score: 7.0
quality_score: 7.0
reliability_score: 7.0
practicality_score: 6.0
evidence: 基于机器学习的网络入侵检测及对抗扰动
tldr: 论文质疑现有 ML-NIDS 对抗样本研究的实验设定是否真实，提出“Host-space perturbation（HsP）”概念：攻击者真正能控制的是主机上的攻击命令，而非路由器或 NIDS 已采集的数据包/特征。作者通过系统综述与真实网络实验发现，仅修改攻击命令中的极小参数（甚至单字符）即可让高准确率 ML-NIDS 完全失效，说明现有鲁棒性评估严重脱离真实攻击面。若关注对抗机器学习、网络安全评测可信性，这篇值得细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25822v1/fig-001.webp\", \"caption\": \"Fig. 1: Typical NIDS scenario. N.b.: the attacker cannot control the router or the NIDS (otherwise, it wouldn’t be surprising if the NIDS is bypassed.\", \"page\": 2, \"index\": 1, \"width\": 475, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2605.25822v1/fig-002.webp\", \"caption\": \"Fig. 2: From attackers’ actions to ML inputs. [Left] The attacker launches nmap on their controlled host. [Middle] This leads to the creation of multiple network packets that are captured by some dedicated network appliance (e.g., a router). [Right] Then, NetFlows are extracted from the PCAP trace, which are sent to (and analysed by) the ML-NIDS. A realistic attacker has no access to the “traffic space” (i.e., middle panel) and “NetFlow space” (i.e., right panels): the attacker can only operate at the host level (i.e., left panel). Therefore, directly manipulating the PCAP trace (captured by the router), the NetFlows (extracted from the PCAP trace), or their feature representation (analysed by the ML model) would violate the assumptions of the threat model.\", \"page\": 4, \"index\": 2, \"width\": 877, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.25822v1/fig-003.webp\", \"caption\": \"Fig. 3: Low-level effects of a host-space perturbation. We show what happens when switching from patator --persistent=0 (-P=0) to patator --persistent=1 (-P=1). The former attempts a single login and closes the connection; the latter continues issuing login attempts until the SSH server closes the connection.\", \"page\": 9, \"index\": 3, \"width\": 1052, \"height\": 444}]"
motivation: 现实攻击者只能控制主机行为时，ML-NIDS 的对抗鲁棒性应如何被正确定义与评估。
method: 将对抗扰动从传统“特征空间/数据包空间”重新约束到攻击者真实可操作的“主机空间”，并通过修改攻击命令参数来研究其对 ML-NIDS 的规避效果。
result: "作者对 316 篇文献进行系统综述，指出多数工作直接修改预采集特征或网络包，而非真实攻击行为；实验中仅把 SSH 暴力破解命令中的 persistent=1 改为 persistent=0，就让原本检测率接近 100% 的 ML-NIDS 完全漏检。附录中 hydra、slowloris 等攻击也在多个模型与数据集上复现了类似现象。"
conclusion: 优先阅读 §3 与 §4，理解作者为何认为现有 adversarial ML-NIDS 研究偏离真实攻击面；随后重点看 §5 与 §6 的实验与分析，它们具体展示了微小命令改动如何在特征空间中引发巨大分布偏移。
key_findings: [多数 ML-NIDS 对抗研究默认攻击者可直接操纵特征或数据包，现实中往往不成立。, 极小的主机侧命令修改即可让高精度 ML-NIDS 完全失效，暴露模型对攻击实现细节过拟合。, Host-space perturbation 在不同模型、数据集与真实网络环境下均能复现规避效果。]
limitations: 主要围绕少数攻击工具与 NetFlow 场景展开，尚未系统覆盖更复杂协议、在线防御机制及大型生产网络。
---

## 1. 问题与背景

论文关注机器学习驱动的网络入侵检测系统（ML-NIDS）在对抗样本攻击下的真实性与可实现性问题。作者指出，已有大量研究通过修改预采集的数据包、NetFlow 或特征向量来生成“对抗扰动”，但这些操作往往默认攻击者能够控制路由器、NIDS 或数据处理链路，而现实中的攻击者通常只能控制某些主机本身。基于这一观察，论文提出需要重新思考“攻击发生在哪个空间（problem space）”，并强调真实攻击者更可能通过改变自己主机上的命令、参数或行为模式来规避检测，而不是直接篡改网络侧采集的数据。由于 ML-NIDS 已逐渐被部署到真实安全运营中心中，这一问题直接关系到当前安全评估结果是否具有现实可信度。

## 2. 核心思路 / 方法

论文提出“Host-space Perturbations（HsP）”概念，即攻击者仅通过自己可控制主机上的微小行为变化来实现对 ML-NIDS 的规避，而不依赖对网络设备或检测器本身的控制。作者首先对 316 篇相关论文进行了系统性文献回顾，发现已有研究大多集中在特征空间或预采集网络数据上的扰动，而忽略了真实攻击者的操作边界。随后，论文在多个基准数据集与真实网络环境中复现攻击场景，通过仅修改攻击命令中的细微参数来测试检测器鲁棒性，例如将 SSH 暴力破解命令中的 `persistent=1` 改为 `persistent=0`。实验过程中，作者训练并测试了决策树、随机森林、梯度提升树、SVM 与 DNN 等模型，并进一步分析为何主机侧极小改动会在特征空间中产生巨大分布偏移。论文的创新点在于，它不是继续研究“如何生成数学意义上的对抗样本”，而是重新定义了 ML-NIDS 安全评估中的攻击现实性与攻击空间边界。

## 3. 结果与结论

实验结果表明，仅通过修改攻击命令中的单个参数甚至单个字符，就可能使原本具有近乎完美检测率的 ML-NIDS 完全失效。例如，对于基于 SSH bruteforce 的攻击，模型能够检测默认命令生成的恶意流量，但面对 Host-space Perturbation 变体时，多个模型的检测率直接降为 0。在附录实验中，作者还测试了 hydra、slowloris、nmap、wfuzz 等不同攻击工具，发现这种现象不仅存在于单一数据集，而是在不同网络环境与不同攻击类型中普遍出现。论文最终认为，当前大量关于 ML-NIDS 对抗鲁棒性的研究缺乏现实攻击视角，因此 ML-NIDS 的安全性需要被重新评估。

## 4. 局限与适用边界

论文主要聚焦于网络入侵检测场景中的 Host-space Perturbation，对其他类型的安全模型或非网络环境是否同样适用，从现有文本无法确认。作者虽然在多个数据集和真实网络中进行了实验，但实验覆盖的攻击类型与网络规模仍有限，因此尚不能证明所有 ML-NIDS 都会受到同等程度影响。此外，论文强调的是“现实攻击可实现性”的评估框架，而非提出一种通用防御机制。

## Abstract
Network Intrusion Detection Systems (NIDS) are now increasingly leveraging Machine Learning (ML) techniques to detect malicious network activities. Numerous papers have scrutinized the security of ML-based NIDS (ML-NIDS) by testing them against various attacks involving adversarial perturbations. The findings were oftentimes worrying: by making imperceptible changes to a given input, powerful ML models would be bypassed. In this context, we took a step back and wondered: where (i.e., in what "space") have these perturbations been applied?   We argue that real-world adversaries can apply adversarial perturbations only by operating on the hosts they can control -- a concept which we define as _host-space perturbations_. To some, such an observation may seem trivial. And yet, through a systematic literature review (n=316), we found that prior work applied perturbations by manipulating pre-collected datapoints (e.g., a packet _captured by the router_, or a network flow _analysed by the ML-NIDS_). Such operations, while not impossible, may be outside the reach of an attacker who can only control some (unprivileged) hosts in a network. Hence, to demonstrate how to craft host-space perturbations and study some of their effects, we experimented on well-known benchmarks and a real-world network. We show that ML-NIDS that can detect the SSH-bruteforcing attempts launched via a given command string cannot detect any attempt launched by changing _a single character_ of such a string. We then examined how such a minuscule change in the "problem space" (i.e., the attacker's host) can lead to devastating effects on the "feature space". We derive lessons learned on how to practically assess host-space perturbations. Our stance is that the security of ML-NIDS should be re-assessed.