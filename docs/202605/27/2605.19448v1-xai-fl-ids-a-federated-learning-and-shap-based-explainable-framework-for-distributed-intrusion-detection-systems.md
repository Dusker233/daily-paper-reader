---
title: "XAI FL-IDS: A Federated Learning and SHAP-Based Explainable Framework for Distributed Intrusion Detection Systems"
title_zh: XAI FL-IDS：基于联邦学习与SHAP的可解释分布式入侵检测系统框架
authors: "Mohammad Hossein Gholamrezazadeh, AhmadReza Montazerolghaem"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19448v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 8.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.0
evidence: 用于网络入侵检测的联邦学习
tldr: "本文提出XAI FL-IDS框架，结合联邦学习(FL)与SHAP解释性AI，旨在解决传统IDS在隐私保护和决策透明性上的局限。通过在每个节点本地训练XGBoost模型并仅传递参数更新，同时使用SHAP解释每次检测结果，系统在Edge-IIoTset数据集上实现了超过99%的检测准确率。该方法兼顾隐私与性能，值得网络安全与IoT领域读者深入研究。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19448v1/fig-001.webp\", \"caption\": \"Fig. 3. Server Side of XAI FL-IDS\", \"page\": 6, \"index\": 1, \"width\": 518, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-002.webp\", \"caption\": \"Fig. 6. Average Training Time Per Client of XAI FL-IDS\", \"page\": 7, \"index\": 2, \"width\": 511, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-003.webp\", \"caption\": \"Fig. 7. Average Training Time Per Client of Base Method\", \"page\": 7, \"index\": 3, \"width\": 549, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-004.webp\", \"caption\": \"Table 1: Comparative Summary of Methods, Data, and Performance in Related FL-IDS Work\", \"page\": 3, \"index\": 4, \"width\": 1086, \"height\": 1057}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-005.webp\", \"caption\": \"Fig. 1. Data Processing Framework for XAI FL-IDS\", \"page\": 4, \"index\": 5, \"width\": 541, \"height\": 498}]"
motivation: 如何在分布式IoT环境中实现既高效又可解释的入侵检测，同时保护节点数据隐私。
method: 通过联邦学习在各节点本地训练模型并仅汇总参数，同时利用SHAP对检测结果提供解释，实现隐私保护与决策可解释性。
result: "在10客户端分布式实验中，XAI FL-IDS在Edge-IIoTset数据集上实现超过99%的准确率，有时达到100%，显示了系统的高性能与隐私安全性。"
conclusion: 重点阅读方法部分，理解FL与SHAP的整合机制，以及实验与评估部分，关注模型在各客户端的性能和解释性分析。
key_findings: ["XAI FL-IDS在分布式IoT环境中实现超过99%的入侵检测准确率，兼顾性能与隐私。", 结合SHAP可对每个检测决策进行可解释分析，提升系统透明性和可信度。, 利用联邦学习实现本地训练与参数汇总，避免原始数据传输，保障节点数据隐私。]
limitations: 实验规模有限，仅使用10客户端和单一数据集，尚未验证大规模或异构网络环境的适用性。
---

## 1. 问题与背景

论文关注工业物联网（IIoT）和 IoT 场景下入侵检测系统（IDS）中的两个核心问题：传统集中式训练需要各节点上传原始网络数据，导致隐私泄露与通信开销增加；同时，大多数基于机器学习或深度学习的 IDS 属于“黑盒”模型，无法解释攻击检测的依据。随着 IoT 设备数量快速增长以及网络攻击复杂度提升，仅依赖集中式检测已难以满足安全性、透明性与可扩展性的需求。因此，作者希望构建一种兼顾隐私保护与可解释性的分布式 IDS 框架。

## 2. 核心思路 / 方法

论文提出了名为 XAI FL-IDS 的框架，将联邦学习（Federated Learning, FL）与 SHAP 可解释机制结合，用于分布式入侵检测。系统由一个中心服务器和 10 个客户端组成，每个节点仅在本地训练数据，并向服务器上传模型更新参数，从而避免原始数据传输带来的隐私风险。局部模型采用 XGBoost，并使用 Edge-IIoTset 数据集进行训练，同时在客户端划分时考虑类别平衡问题。该方法的一个关键创新在于聚合阶段并非简单平均所有模型，而是选择上一轮中表现最佳的模型并广播给所有客户端作为下一轮初始化模型。与此同时，作者在客户端与服务器两侧均使用 SHAP 分析模型决策过程，从而解释不同网络特征对攻击判定的贡献，提高 IDS 的透明性与可解释性。

## 3. 结果与结论

实验结果表明，XAI FL-IDS 在入侵检测任务中表现出较高性能，准确率超过 99%，部分情况下可达到 100%。论文认为，联邦学习机制能够有效保障各节点网络数据的机密性，而 SHAP 的引入则能够持续分析特征重要性并解释模型预测依据，从而缓解传统 IDS 的黑盒问题。作者最终认为，该框架能够同时解决现代 IDS 中的隐私保护与模型可解释性问题，实现安全、分布式且可解释的入侵检测。

## 4. 局限与适用边界

论文指出，目前系统仍存在资源消耗问题，尤其 IoT 节点计算能力有限，因此未来需要进一步采用轻量化模型以降低能耗与内存占用。此外，作者计划尝试更多特征选择方法并在更多数据集上验证模型泛化能力。从现有文本无法确认该方法在极端异构数据、超大规模节点环境或真实在线攻击场景中的稳定性与部署成本。

## Abstract
An Intrusion Detection System (IDS) is vital in cybersecurity, detecting unauthorized activity across networks. With attacks on network layers increasing, stronger IDSs are needed. Yet most IDSs rely on centralized detection, forcing IoT nodes to ship data to a server, adding overhead and offering no privacy guarantees. Moreover, conventional models focus solely on flagging attacks, without explaining how individual features influence those decisions. This research aims to address these dual limitations by first proposing a solution for privacy preservation and then adding explainability to the new system. We introduce an innovative framework called XAI FL-IDS, which integrates Federated Learning (FL) with Explainable AI (XAI). The XAI FL-IDS system eliminates concerns over data transfer because each node trains its data locally and only sends the necessary update parameters to the server. Additionally, all detections, both at the local node and central server levels, are scrutinized using SHapley Additive exPlanations (SHAP), providing detailed insight into the decision-making process. This system consists of a central server and 10 clients and utilizes the Edge-IIoTset dataset, which is distributed among all clients with careful attention paid to class balancing. On each client, the XGBoost model is executed on local data. The proposed method demonstrates robust efficiency and strong performance in intrusion detection, achieving an accuracy of over 99% and, at times, reaching 100%. By incorporating FL, the confidentiality of the network information on every local node is guaranteed.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文提出 XAI FL-IDS，一个融合联邦学习（FL）与 SHAP 可解释机制的分布式入侵检测框架，旨在同时解决 IoT/IIoT 场景中的数据隐私与模型黑盒问题。系统采用 XGBoost 作为本地模型，在 10 个客户端上基于 Edge-IIoTset 数据集进行训练，并通过“最佳模型广播”替代传统 FedAvg 聚合。实验表明，该框架在保证数据不出本地节点的前提下，实现了超过 99% 的检测准确率，并能通过 SHAP 对检测依据进行可解释分析。

**关键词：** Federated Learning、XAI、SHAP、IDS、XGBoost、Edge-IIoTset、IoT Security

---

# 2. 核心故事与贡献

## 2.1 问题与动机

传统 IDS 在 IoT/IIoT 场景中存在两个核心缺陷：

- **隐私问题：**
  集中式 IDS 要求所有 IoT 节点上传原始流量数据到中心服务器，导致：
  - 数据泄露风险；
  - 网络传输开销；
  - 边缘节点隐私无法保障。
  
- **可解释性问题：**
  现有 ML/DL IDS 多属于“黑盒模型”，只能输出攻击类别，无法解释：
  - 为什么判定为攻击；
  - 哪些特征影响了决策；
  - 模型是否可信。

论文指出，随着 IoT 设备数量预计在 2025 年达到 **700 亿台**（见 Introduction），传统集中式 IDS 已难以满足现代工业网络需求。

---

## 2.2 核心洞察

作者的核心洞察有两个：

### （1）FL 可以解决 IDS 的隐私与通信问题

通过联邦学习：

- 每个客户端仅在本地训练；
- 服务器只接收模型参数更新；
- 原始网络流量永不离开本地节点。

这使 IDS 能在分布式环境中实现隐私保护。

---

### （2）SHAP 可以解决 IDS 的“黑盒”问题

作者将 SHAP 同时用于：

- 客户端本地检测解释；
- 中央服务器聚合后模型解释。

从而实现：

- 特征贡献可视化；
- 攻击原因解释；
- 模型透明性增强。

---

## 2.3 主要贡献

### Contribution 1：提出 XAI FL-IDS 框架

系统由：

- 1 个中心服务器；
- 10 个客户端；
- 本地 XGBoost 模型；
- SHAP 解释模块

组成（见 Section III）。

---

### Contribution 2：提出“最佳模型广播”聚合策略

不同于标准 FedAvg：

- 作者并不平均所有客户端参数；
- 而是选择“上一轮性能最佳模型”；
- 广播给所有客户端作为下一轮初始化。

这是全文最核心的方法创新之一（见 Introduction / Section III-C）。

---

### Contribution 3：实现高精度 + 可解释 IDS

在 Edge-IIoTset 数据集上：

- Accuracy > 99%
- 某些轮次达到 100%

同时还能解释每次检测的特征贡献（见 Figure 4、Table 2、Table 3）。

---

# 3. 相关工作与定位

## 3.1 与已有 FL-IDS 工作对比

| 工作 | FL | 可解释性 | 本地模型 | 聚合机制 | 数据集 | 准确率 |
|---|---|---|---|---|---|---|
| Rashid et al. 2023 | ✓ | ✗ | CNN/RNN | FedAvg | Edge-IIoTset | 92.49% |
| Bhavsar et al. 2024 | ✓ | ✗ | PCC-CNN | 标准聚合 | Car-Hacking | 99.93% |
| Thein et al. 2024 | ✓ | ✗ | CNN | Personalized FL | 多数据集 | 未突出 XAI |
| Gutti et al. 2025 | ✓ | 部分 | CNN | HADA | IoT Security | 强调鲁棒性 |
| FEDXAIIDS 2025 | ✓ | ✓（SHAP） | ANN | 标准聚合 | IoT | 88.2% |
| **本文 XAI FL-IDS** | ✓ | ✓（SHAP） | XGBoost | 最佳模型广播 | Edge-IIoTset | >99% |

---

## 3.2 本文定位

本文位于以下研究交叉点：

- FL-based IDS
- Explainable IDS
- Privacy-preserving IIoT security

其差异化特点：

1. 不仅做 FL；
2. 不仅做 SHAP；
3. 还修改了聚合机制；
4. 同时强调：
   - 高精度；
   - 隐私保护；
   - 决策透明性。

---

# 4. 方法详解

# 4.1 总体架构

系统包含：

- 中央服务器（Server）
- 10 个客户端（Clients）
- SHAP 解释模块

整体流程见 Figure 3、Figure 1。

核心流程：

```text
数据预处理
    ↓
客户端本地训练 XGBoost
    ↓
上传模型参数
    ↓
服务器选择最佳模型
    ↓
广播回客户端
    ↓
SHAP 解释预测结果
```

---

# 4.2 数据处理模块

数据集：

- Edge-IIoTset
- 157,800 条记录
- 63 个特征
- 14 类攻击

（见 Section III-A）

---

## 数据预处理步骤

### Step 1：异常值处理

- 将 Inf 替换为 NaN；
- 删除无效特征。

---

### Step 2：类别特征编码

非数值字段转化为数值型。

---

### Step 3：缺失值填充

采用：

```text
Mode Imputation
```

即：

- 使用类别众数填补 NaN。

---

### Step 4：归一化

对所有特征进行 normalization。

---

### Step 5：联邦划分

- 80/20 train-test split；
- 训练集分发至 10 个客户端；
- 强调 class balancing。

（见 Section III-B）

---

# 4.3 联邦学习模块

## 本地训练

每个客户端：

- 使用本地数据；
- 训练 XGBoost；
- 不上传原始数据。

---

## 聚合机制（核心创新）

传统 FedAvg：

$$
w_{global}=\sum_{k=1}^{K}\frac{n_k}{n}w_k
$$

而本文：

- 不做参数平均；
- 而是：
  - 比较上一轮客户端性能；
  - 选择最佳模型；
  - 广播给所有节点。

其本质更接近：

```text
Best Model Selection FL
```

而非标准参数融合。

---

# 4.4 SHAP 可解释模块

SHAP 基于 Shapley Value：

$$
\phi_i = \sum_{S \subseteq F \setminus \{i\}}
\frac{|S|!(|F|-|S|-1)!}{|F|!}
[f(S \cup \{i\}) - f(S)]
$$

其中：

- $\phi_i$ 表示特征贡献；
- $f(\cdot)$ 为模型输出。

---

## SHAP 的使用位置

作者同时在：

- 本地客户端；
- 中央模型；

进行解释分析。

---

## SHAP 输出内容

解释：

- 哪些特征推动攻击判定；
- 特征影响方向；
- 不同轮次的重要性变化。

见：

- Table 2
- Table 3

---

# 5. 实验分析

# 5.1 实验设置

| 项目 | 内容 |
|---|---|
| 数据集 | Edge-IIoTset |
| 客户端数量 | 10 |
| 本地模型 | XGBoost |
| 解释方法 | SHAP |
| 任务 | Intrusion Detection |

---

# 5.2 主实验结果

## Accuracy

论文报告：

- Accuracy > 99%
- 部分轮次达到 100%

（见 Abstract / Conclusion）

---

## 评估指标

Figure 4 展示：

- Accuracy
- Precision
- Recall
- F1-score

整体均接近理想水平。

---

## Log Loss

Figure 5 表明：

- 随训练轮次增加；
- Log Loss 持续下降；
- 模型逐渐收敛。

---

# 5.3 SHAP 分析结果

## Round 1 vs Round 9

Table 2 与 Table 3 对比显示：

- 后期训练轮次中；
- SHAP 特征贡献更稳定；
- 模型解释更加清晰。

作者认为：

> 模型对网络特征的理解随 FL 训练逐渐增强。

---

# 5.4 训练效率

Figure 6 与 Figure 7：

- 对比 XAI FL-IDS 与 baseline；
- 展示平均训练时间。

论文指出：

- FL 引入一定训练成本；
- 但在隐私收益下是可接受的。

具体时间数值在当前文本中未完全展示。[需验证]

---

# 5.5 消融实验情况

未看到完整消融实验。

目前缺失：

- 无 SHAP 对比；
- 无 FedAvg 对比；
- 无客户端数量变化实验；
- 无 Non-IID 强度分析。

因此：

> 本文更偏“系统框架验证”，而非严格算法消融论文。

---

# 6. 局限性与风险

## 6.1 假设约束

### 单数据集验证

仅使用：

- Edge-IIoTset

缺少跨数据集泛化实验。

---

### 客户端规模有限

仅：

- 10 个客户端。

无法验证超大规模 FL 场景性能。

---

### 聚合机制理论不足

“最佳模型广播”：

- 缺少理论收敛分析；
- 未讨论极端 Non-IID 情况。

---

# 6.2 应用边界

### 资源受限 IoT 节点

XGBoost + SHAP：

- 仍可能较重；
- 对低功耗边缘设备不友好。

作者也明确提出未来需轻量化（见 Conclusion）。

---

### 实时性未充分验证

未展示：

- 在线检测 latency；
- 流式攻击处理能力；
- 大规模实时部署结果。

---

# 6.3 潜在风险

### 最佳模型选择可能导致过拟合

若某客户端数据分布偏置：

- 最佳模型可能偏向局部模式；
- 降低全局泛化。

---

### SHAP 解释稳定性问题

SHAP 在高维特征下：

- 计算成本高；
- 解释可能不稳定。

论文未讨论解释鲁棒性。

---

# 7. 复现与后续问题

# 7.1 复现重点

## 重点 1：客户端数据均衡划分

论文特别强调：

```text
careful attention paid to class balancing
```

因此：

- 数据划分方式；
- 是否 IID；
- 类别比例控制；

会显著影响结果。

---

## 重点 2：最佳模型选择逻辑

需明确：

- 依据哪个指标选“best model”；
- Validation set 如何构建；
- Server 是否拥有测试数据。

正文未完全展开。[需验证]

---

## 重点 3：SHAP 配置

需确认：

- TreeSHAP 还是 KernelSHAP；
- 解释对象是：
  - 单样本；
  - 全局特征；
  - 聚合模型。

当前文本未完全说明。[需验证]

---

# 7.2 后续研究方向

## 方向 1：轻量化 FL-IDS

包括：

- TinyML；
- LightGBM；
- Distillation；
- Edge pruning。

---

## 方向 2：鲁棒联邦安全

可进一步研究：

- Poisoning defense；
- Byzantine aggregation；
- Secure aggregation；
- Differential Privacy。

---

# 8. 直接证据与待验证项

## 直接证据

### Evidence 1

论文明确指出：

> “each node trains its data locally and only sends the necessary update parameters to the server”

说明其采用标准 FL 隐私保护机制（见 Abstract）。

---

### Evidence 2

论文指出：

> “the central model selects the single best model to broadcast to all clients”

说明其聚合机制不是传统 FedAvg（见 Conclusion / Section III）。

---

### Evidence 3

Edge-IIoTset：

- 63 个特征；
- 157,800 条数据；
- 14 类攻击。

（见 Section III-A）

---

### Evidence 4

Figure 4 展示：

- Accuracy；
- Precision；
- Recall；
- F1-score；

均接近理想水平（见 Figure 4）。

---

### Evidence 5

Table 2 与 Table 3：

展示了：

- Client 5
- Round 1 vs Round 9

的 SHAP 解释结果，用于说明模型解释能力逐渐增强。

---

## 待验证项

### [需验证] 训练轮数

正文未明确给出：

- communication rounds 数量；
- convergence epoch。

---

### [需验证] 聚合算法细节

未明确：

- 如何定义“best model”；
- 是否采用 validation accuracy；
- 是否包含 server-side evaluation。

---

### [需验证] Non-IID 鲁棒性

论文强调 class balancing，但未系统研究：

- heterogeneous client distributions；
- severe Non-IID；
- client drift。

---

### [需验证] 与 FedAvg 的直接对比

未看到：

- 同数据集；
- 同模型；
- 同客户端规模；

下与 FedAvg 的完整 benchmark。

---

（完）
