---
title: Retrieval with Multiple Query Vectors through Anomalous Pattern Detection
title_zh: 通过异常模式检测的多查询向量检索
authors: "Allassan Tchangmena A Nken, Baimam Boukar Jean Jacques, Miriam Rateike, Celia Cintas, Skyler Speakman"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01965v1"
tags: ["query:profile-1"]
score: 6.8
relevance_score: 7.0
quality_score: 7.0
reliability_score: 7.0
practicality_score: 6.0
evidence: 多向量检索方法与复杂LLM网络查询相关
tldr: 本文提出了一种基于异常模式检测的多查询向量检索方法，针对传统单查询向量在复杂信息检索场景中的局限，通过识别查询向量中的异常维度并检索数据库中具有相似异常模式的向量，实现更精准的检索。实验覆盖文本、图像及表格数据集，结果显示查询向量数量增加显著提升检索性能，尤其从1到8个查询向量时效果提升明显。该方法值得在多向量检索及LLM增强生成场景中进一步精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01965v1/fig-001.webp\", \"caption\": \"Figure 1: Precision (↑) and recall (↑) for varying number of query vectors M = |Q| in the query set. Mean (line) and std (shaded) over 50 random draws of query vectors in Q.\", \"page\": 3, \"index\": 1, \"width\": 1052, \"height\": 554}]"
motivation: 如何在多查询向量情况下，通过异常模式检测实现更精准的信息检索。
method: 通过识别查询向量中共享的异常维度，并在数据库中检索在相同维度上表现异常的向量，实现多向量联合检索。
result: 在LLM文本嵌入实验中，该方法在精确率和召回率上均持续优于多查询向量适配的KDTree基线。
conclusion: 优先阅读方法章节中“扫描算法”和“异常模式检测”的两步过程，以及实验部分中多数据集的性能比较，以理解技术核心和实证效果。
key_findings: [多查询向量检索优于单查询向量，尤其在查询数量从1增加到8时提升显著。, 异常模式检测方法可捕捉查询向量之间的联合特征，带来比简单聚合或独立检索更高的相关性。, 在文本嵌入上，本方法精确率和召回率均超越多查询向量适配的KDTree基线。]
limitations: 依赖异常模式假设，数据库维度高时计算成本可能增加；对非异常模式分布数据效果不明。
---

## 1. 问题与背景
本论文研究的问题是如何在信息检索中利用多个查询向量进行高效检索，而不是传统的单查询向量方式。背景在于复杂检索任务和大型语言模型（LLM）增强生成场景中，多查询向量往往更能捕捉上下文和多维信息。该问题重要性体现在现有方法往往忽略查询向量间的关系，或依赖固定距离度量，难以充分利用高维向量表示的非线性和上下文信息。  

## 2. 核心思路 / 方法
论文提出了一种基于异常模式检测的多查询向量检索方法。核心机制是首先从查询向量中识别形成异常模式的维度子集，然后在数据库中检索在相同维度上也呈现异常模式的向量，从而确定最相关的结果。与传统单查询或向量聚合方法不同，这种方法不依赖固定距离度量，能够捕捉查询向量间的复杂关联，且无需预先指定检索数量。方法在文本、图像和表格数据上均进行了验证，并与多查询KDTree基线对比。  

## 3. 结果与结论
实验结果显示，随着查询集合规模的增加（尤其从1到8），检索性能显著提升，超过传统基线方法。论文的核心结论是，通过异常模式识别，多查询向量检索能够更准确地捕捉相关性，特别是在高维LLM文本嵌入上表现优越。  

## 4. 局限与适用边界
论文主要局限在于对异常模式依赖较高，可能受查询向量分布和数据类型影响。该方法适用于同一向量空间的文本、图像或表格嵌入，但对于分布差异过大或向量维度极高的数据，效果可能受限。

## Abstract
A classical vector retrieval problem typically considers a \emph{single} query embedding vector as input and retrieves the most similar embedding vectors from a vector database. However, complex reasoning and retrieval tasks frequently require \emph{multiple query vectors}, rather than a single one. In this work, we propose a retrieval method that considers multiple query vectors simultaneously and retrieves the most relevant vectors from the database using concepts from anomalous pattern detection. Specifically, our approach leverages a set of query vectors $Q$ (with $|Q|\geq 1$), and identifies the subset of vector dimensions within $Q$ that standout (anomalous) from the rest of dimensions. Next, we scan the vector database to retrieve the set of vectors that are also anomalous across the previously identified vector dimensions and return them as our retrieved set of vectors. We validate our approach on two image datasets, a text dataset, and a tabular dataset. Overall, we observe that, across most datasets, larger query sets lead to improved retrieval performance. The improvement is most pronounced when increasing the query sets from 1 to 8, while the gains become smaller beyond that.