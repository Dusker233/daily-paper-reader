---
title: Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems
title_zh: 大型语言模型作为能源工业控制系统可解释的网络攻击检测器
authors: "Weiyi Kong, Ahmad Mohammad Saber, Amr Youssef, Deepa Kundur"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26079v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 8.0
quality_score: 8.5
reliability_score: 8.0
practicality_score: 7.5
evidence: 用于ICS中可解释的网络攻击检测的大型语言模型，与网络流量分析相关
tldr: 本研究探讨了大语言模型（LLM）是否可以作为能源工业控制系统（ICS）中Modbus流量的辅助检测层。研究发现，LLM在不需特定任务训练的情况下，能够与传统监督模型相媲美，进行正常/危急流量的分类，并生成可审计的事件记录。该方法具有较高的检测精度（接近0.98）且无需特定训练，是传统方法的有效补充。值得精读，尤其是对Modbus协议的应用及实验结果的分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26079v1/fig-001.webp\", \"caption\": \"Figure 3: Pipeline and leakage guards. Preprocessing includes UTCnormalization, aggregation of attacker logs into contiguous time windows, and a canonical train/validation/test split that prevents temporal leakage across windows. LEN/IAT bins are fit once on the training portion and then frozen for validation and test, and each flow is branched into a compact LLM token string and a numeric feature vector.\", \"page\": 5, \"index\": 1, \"width\": 357, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-002.webp\", \"caption\": \"Figure 4: Pass-rate vs 𝜀 for sufficiency and comprehensiveness (95% CIs); dashed line shows the label flipped baseline.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed framework. Offline preparation convertsModbus/TCP traces from LeMay and CIC into a unified token-string representation (no model training). Online inference uses a prompt-guided LLM to output a binary label (normal/critical), followed by an auditing step that generates supporting evidence tokens, risk tags, and optional what-if edits.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 286}]"
motivation: 本研究的核心问题是，是否可以通过现成的大语言模型（LLM）作为能源工业控制系统中Modbus流量的检测层，提供可审计的检测结果。
method: 本研究通过将大语言模型应用于Modbus流量分类任务，提出一种无需任务特定训练即可实现的检测和审计方法。
result: 在两个公开ICS Modbus数据集上，LLM模型表现出了接近0.98的高准确率，且生成的事件记录在审计中具有较高的决策相关性。
conclusion: 若继续精读，建议重点关注方法部分和实验结果部分，尤其是如何配置LLM的提示（prompt）以及其与监督模型的比较。
key_findings: [大语言模型（LLM）能够在不需要任务特定训练的情况下，实现对Modbus流量的正常/危急分类，并与监督学习模型达到类似的检测精度。, 通过将预测与解释分离，LLM模型生成了具有审计价值的事件记录，为后续人工审核提供了支持。, LLM的实验结果表明，其对危急类流量的召回率很高，达到了与传统方法相当的检测性能。]
limitations: 该方法虽然在实验中表现良好，但尚未对LLM在实际部署中的稳定性和可扩展性进行深入验证，且模型的解释能力仍需进一步提高。
---

## 1. 问题与背景
现代能源系统中的工业控制系统（ICS）和电力SCADA协议如Modbus容易受到网络攻击，包括伪数据注入。现有入侵检测多依赖监督学习，虽然预测准确，但在实际部署中需要额外的标注数据、特征工程或重新训练，同时缺乏可直接归档为操作员可审计记录的解释性方法。因此，研究如何在保持高检测性能的同时提供可审计、易理解的警报，对于保障电网安全和快速响应异常事件具有重要意义。

## 2. 核心思路 / 方法
论文提出使用通用大型语言模型（LLM）作为Modbus流量的辅助手工审核层，通过将每条Modbus通信转换为离散化协议字段的紧凑token字符串，并利用prompt引导LLM输出“正常/关键”标签以及简明、可归档的事件记录。方法设计为二阶段：第一阶段生成预测标签，第二阶段生成基于关键tokens的简短解释，并可提供最小“what-if”修改。该方法无需任务特定的权重更新，通过少量示例的prompt配置即可实现检测，同时提供token级决策相关性审计，创新在于将LLM用于工业协议token分类并生成可操作的审计记录，而非仅处理自然语言报告。

## 3. 结果与结论
在LeMay CSET’16和CIC Modbus 2023两个公开数据集上，LLM方法在关键类上实现了约0.98的准确率和高召回率，性能与强监督基线相当。核心结论是，off-the-shelf LLM在prompt配置下可作为高效、可审计的辅助入侵检测工具，无需任务特定训练即可为操作员提供有价值的警报和可追溯的解释。

## 4. 局限与适用边界
该方法不用于攻击归因或自动响应，仅作为被动决策支持层，错误漏报可能延迟恶意流量的检测。适用范围限于Modbus网络流量的二分类正常/关键警报场景，对于复杂多类别攻击识别仍依赖下游分析或专用分类器。

## Abstract
In modern energy systems, industrial control systems (ICS) and power-system SCADA require intrusion detection that is not only accurate but also auditable by operators. The ICS intrusion-detection landscape is currently dominated by established supervised detectors. In this paper, we study whether an off-the-shelf large language model (LLM) can serve as a complementary, human-in-the-loop layer for Modbus traffic. We cast this as a binary network-side normal/critical decision task on two public ICS Modbus datasets, collapsing attack periods and other safety-critical behaviors into a single critical class. Each Modbus communication instance is converted into a compact token string derived from discretized protocol fields, and a prompt-configured LLM produces a normal/critical alert together with a concise, token-grounded incident record for analyst review. Under matched event information and shared evaluation splits, the resulting LLM-based triage pipeline achieves high predictive performance on both benchmarks and is broadly comparable to strong supervised baselines, while requiring no task-specific weight updates. To assess the audit record, we apply intervention-based diagnostics, including sufficiency- and necessity-style tests, which provide evidence that the cited tokens are often decision-relevant to the model's own prediction. These records are intended as audit signals rather than full human-grounded explanations.

---

## 论文详细总结（自动生成）

## 1. TLDR
本论文探讨了大型语言模型（LLM）作为能源工业控制系统（ICS）中Modbus流量检测的可解释补充层，展示了LLM在无需任务特定训练的情况下，能够有效分类流量为正常或危急，并生成可审计的事件记录。方法显示出与传统监督模型相似的检测性能。

**关键词**：LLM，Modbus，ICS，入侵检测，可解释人工智能

## 2. 核心故事与贡献
- **问题与动机**：现有ICS入侵检测方法多依赖监督学习，虽然准确，但需要大量标注数据、特征工程以及重新训练；同时缺乏能直接归档为操作员审计记录的可解释方法。缺乏有效的、高效且可审计的网络攻击检测层是当前ICS安全领域的痛点。
- **核心洞察**：大型语言模型（LLM）可以作为Modbus流量的辅助检测层，无需任务特定训练，通过少量示例的prompt配置即可在保持高精度的同时，生成可审计的事件记录。
- **主要贡献**：本文提出了一种基于LLM的检测与审计方法，不仅能实现高精度的流量分类（接近0.98准确率），还能生成具有审计价值的事件记录。这种方法通过prompt配置与LLM协同工作，提供了一个无需额外训练即可部署的解决方案。

## 3. 相关工作与定位

| 维度                   | 传统监督学习方法          | 本文方法                   |
|------------------------|--------------------------|----------------------------|
| **特征工程**            | 需要大量特征工程         | 仅需通过prompt配置，简化特征表达 |
| **模型训练**            | 需要任务特定训练         | 无需任务特定训练            |
| **可解释性**            | 依赖于SHAP/LIME等后处理方法 | 生成可审计的事件记录         |
| **部署验证**            | 基于真实应用环境的验证   | 基于公开数据集验证          |
| **误报处理**            | 可能需要调整模型参数    | 高召回率，误报增加分析工作负担 |

## 4. 方法详解

### 核心方法
- 本文提出的模型使用LLM作为辅助入侵检测层，分类任务为“正常”与“危急”。
- 输入为离散化的Modbus协议字段，转换为紧凑的token字符串。
- 通过prompt配置LLM，生成二分类标签，并为每个警报生成简洁的事件记录。

### 关键公式
1. **预测模型**：  
   $$ f(x; \pi) = \hat{y}(x), s(x) $$  
   其中，$x$ 为Modbus帧，$\hat{y}(x) \in \{normal, critical\}$ 为预测标签，$s(x)$ 为置信度分数。

### 方法模块
- **第一阶段**：基于输入的Modbus帧，LLM输出“正常”或“危急”分类。
- **第二阶段**：生成简短的审计记录，包含与决策相关的token，并根据需要提供“what-if”修改。

## 5. 实验分析

### 主实验结果
- 在LeMay CSET’16和CIC Modbus 2023两个数据集上，LLM方法的准确率达到0.98，且在危急类流量上的召回率较高，表现与监督学习模型相当。

| 数据集                | 准确率   | 召回率   | F1分数  |
|----------------------|---------|---------|--------|
| LeMay CSET’16         | 0.98    | 高       | 高      |
| CIC Modbus 2023       | 0.98    | 高       | 高      |

### 消融实验
- 本研究展示了不同prompt配置对模型性能的影响，验证了少量示例的配置即可达到与完整监督模型相当的性能（见正文 Section 4）。

### 核心发现
- 通过简单的prompt配置，LLM能够提供与传统模型相当的检测精度，并生成可操作的审计记录，为后续分析提供支持。

## 6. 局限性与风险

- **假设约束**：该方法依赖于Modbus协议和简单的二分类任务，未涉及复杂的多类别攻击检测。
- **应用边界**：该方法适用于Modbus网络流量的二分类检测，不适合复杂或动态变化的网络环境。
- **潜在风险**：LLM的解释性能力仍需提高，当前提供的审计记录更多是决策相关性信号而非全面的人工可理解解释。

## 7. 复现与后续问题

### 复现重点
1. **Prompt配置**：对LLM进行少量示例配置，生成基于Modbus协议的简洁token字符串。
2. **无任务特定训练**：通过prompt引导LLM完成分类任务，而无需额外的模型训练。

### 后续研究方向
1. **LLM解释性增强**：探索如何通过域专家反馈进一步提升LLM生成的审计记录的可解释性。
2. **模型稳定性与可扩展性**：评估LLM在不同ICS环境中的稳定性及其应对大规模数据集的能力。

## 8. 直接证据与待验证项

- **直接证据**：
  1. 高准确率与召回率（见正文 Section 5）。
  2. 通过prompt配置实现高性能检测（见正文 Section 4）。
  3. 消融实验验证prompt配置对性能的影响（见正文 Section 5）。

- **[需验证]**：LLM在不同工业协议中的适用性，及其对实际环境的扩展能力。  

（完）
