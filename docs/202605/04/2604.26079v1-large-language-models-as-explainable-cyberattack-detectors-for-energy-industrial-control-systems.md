---
title: Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems
title_zh: 将大型语言模型作为能源工业控制系统的可解释网络攻击检测器
authors: "Weiyi Kong, Ahmad Mohammad Saber, Amr Youssef, Deepa Kundur"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26079v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 7.0
practicality_score: 8.0
evidence: LLM用于工业控制系统网络流量异常检测
tldr: 本文研究了将现成大型语言模型（LLM）作为能源工业控制系统（ICS）Modbus流量的可解释入侵检测辅助工具，采用将Modbus通信离散化为令牌并通过提示配置LLM进行二分类（正常/关键）的方法。实验在两个公开数据集上显示，该方法在无需任务特定训练的情况下，与监督学习基线性能相当，同时提供可审计的事件记录，适合人机协作的安全监控场景，值得对方法设计和实验评估部分深入精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26079v1/fig-001.webp\", \"caption\": \"Figure 3: Pipeline and leakage guards. Preprocessing includes UTCnormalization, aggregation of attacker logs into contiguous time windows, and a canonical train/validation/test split that prevents temporal leakage across windows. LEN/IAT bins are fit once on the training portion and then frozen for validation and test, and each flow is branched into a compact LLM token string and a numeric feature vector.\", \"page\": 5, \"index\": 1, \"width\": 357, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-002.webp\", \"caption\": \"Figure 4: Pass-rate vs 𝜀 for sufficiency and comprehensiveness (95% CIs); dashed line shows the label flipped baseline.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed framework. Offline preparation convertsModbus/TCP traces from LeMay and CIC into a unified token-string representation (no model training). Online inference uses a prompt-guided LLM to output a binary label (normal/critical), followed by an auditing step that generates supporting evidence tokens, risk tags, and optional what-if edits.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 286}]"
motivation: 能否使用通用大型语言模型作为ICS Modbus流量的可解释入侵检测辅助工具，在无需任务特定训练的情况下实现有效警报。
method: 将Modbus通信离散化为令牌字符串，通过少量示例提示配置LLM输出正常/关键二分类结果，并生成可审计的简洁事件记录。
result: 在两个公开Modbus数据集上，LLM实现约0.98的整体准确率，并在关键类上保持高召回，与强监督基线表现接近。
conclusion: 精读方法学部分了解令牌化及提示设计，评估部分重点关注性能指标和审计记录验证方法，以判断实际部署可行性。
key_findings: [提示配置的少样本LLM可在无需任务特定训练的情况下实现与监督基线相当的入侵检测性能。, LLM可生成基于原始协议令牌的简洁事件记录，为操作员提供可审计警报。, 介入式诊断（充分性、必要性）验证了所引用令牌通常与模型决策相关。]
limitations: LLM仅作为辅助分流，不替代多类攻击归因，解释性尚非完全人类可理解。
---

## 1. 问题与背景
现代能源系统中的工业控制系统（ICS）依赖 Modbus 和 SCADA 协议进行测量和控制，但易受到网络侧攻击，如伪数据注入。现有入侵检测方法主要依赖监督学习，虽然准确性高，但在实际部署中仍需要大量特定场景的数据收集、特征工程和模型再训练，同时缺乏可直接归档给操作员的可审计解释。因此，研究问题是探索一种可解释、可审计且无需任务特定训练的入侵检测方法，以提高 ICS 安全监控的可用性和可信度。

## 2. 核心思路 / 方法
论文提出使用现成大语言模型（LLM）作为 ICS Modbus 流量的辅助手段，通过将每条 Modbus 通信转化为离散协议字段构成的紧凑 token 字符串，LLM 在提示引导下输出“正常/关键”二分类决策，同时生成简明、基于 token 的操作员可归档事件记录。方法采用两步流程，第一步生成预测标签，第二步生成解释，包括关键 token 和可选的“what-if”编辑。相比传统监督学习方法，该方法无需任务特定权重更新，且结合了 prompt-only 配置和决策可审计性，实现了高准确率与解释能力的平衡。

## 3. 结果与结论
在 LeMay CSET’16 和 CIC Modbus 2023 两个公开数据集上，LLM 辅助的检测管线在关键类上实现约 0.98 的准确率和高召回率，其性能在整体上与强监督基线相当。核心结论是，prompt 配置的 LLM 可以作为人机协作的 triage 层，为操作员提供可审计的报警，同时保持竞争力的检测性能。

## 4. 局限与适用边界
论文方法不替代完整攻击归因或自动响应，仅作为被动决策支持层部署，依赖公共数据集进行离线评估，其解释性仍基于 token 的决策相关性，而非完全的人类可理解理由。

## Abstract
In modern energy systems, industrial control systems (ICS) and power-system SCADA require intrusion detection that is not only accurate but also auditable by operators. The ICS intrusion-detection landscape is currently dominated by established supervised detectors. In this paper, we study whether an off-the-shelf large language model (LLM) can serve as a complementary, human-in-the-loop layer for Modbus traffic. We cast this as a binary network-side normal/critical decision task on two public ICS Modbus datasets, collapsing attack periods and other safety-critical behaviors into a single critical class. Each Modbus communication instance is converted into a compact token string derived from discretized protocol fields, and a prompt-configured LLM produces a normal/critical alert together with a concise, token-grounded incident record for analyst review. Under matched event information and shared evaluation splits, the resulting LLM-based triage pipeline achieves high predictive performance on both benchmarks and is broadly comparable to strong supervised baselines, while requiring no task-specific weight updates. To assess the audit record, we apply intervention-based diagnostics, including sufficiency- and necessity-style tests, which provide evidence that the cited tokens are often decision-relevant to the model's own prediction. These records are intended as audit signals rather than full human-grounded explanations.

---

## 论文详细总结（自动生成）

## 1. TLDR
本文提出将现成大型语言模型（LLM）作为能源 ICS Modbus 流量的辅助手段，通过离散 token 与提示配置实现二分类（正常/关键）检测，并生成可审计事件记录。实验在 LeMay CSET’16 与 CIC Modbus 2023 数据集上显示，与强监督基线性能接近，关键类召回高。  
**关键词**：LLM、工业控制系统、Modbus、入侵检测、可解释性、提示学习

## 2. 核心故事与贡献
- **问题与动机**  
  - ICS 网络易受伪数据注入及异常控制流量攻击，现有监督检测方法虽准确，但依赖场景特定数据和特征工程，缺乏可直接归档的审计解释（Section 1 引言）。
- **核心洞察**  
  - 将每条 Modbus 消息离散化为 token，并通过少量提示引导 LLM 输出二分类与简洁事件记录，可在无需任务特定训练下实现高性能、人机可审计的报警。
- **主要贡献**  
  1. 提出 prompt-configured LLM 作为 ICS Modbus 辅助 triage 层，兼顾检测性能和可审计性（Section 1）。  
  2. 设计两步预测与解释流程，生成基于原始 token 的事件记录（Section 3.1）。  
  3. 采用介入式诊断（充分性、必要性）验证事件记录的决策相关性（Section 3.1、Figure 4）。  
  4. 实验显示 LLM 达到约 0.98 总体准确率，并在关键类保持高召回，与强监督基线相当（Section 5）。

## 3. 相关工作与定位

| 维度 | 监督检测方法 | LLM-for-Security 现有工作 | 本文方法 |
|------|--------------|---------------------------|----------|
| 输入特征 | 原始/手工特征 | 自然语言日志 | 离散 Modbus token |
| 模型依赖 | 训练权重 | 主要辅助自然语言分析 | prompt-only LLM，无任务特定训练 |
| 输出类型 | 分类标签 | 建议/辅助分析 | 二分类 + 可审计事件记录 |
| 误报处理 | 阈值调优 | 需人工解读 | 保留人工复核，事件记录可审计 |
| 部署验证 | 公共数据集/特定场景 | 主要模拟或日志分析 | 公共 Modbus 数据集离线评估（LeMay、CIC） |

## 4. 方法详解
### 4.1 数据表示与 token 化
- 每条 Modbus 帧映射为紧凑 token 字符串，包括：
  - **DIR**（方向 C2S/S2C）  
  - **FC**（功能码）  
  - **UNIT**（单元 ID）  
  - **LEN**（负载长度桶）  
  - **IAT**（帧间隔桶）  
  - **EX**（异常码）  
- 连续特征离散化，训练集拟合桶边界，验证/测试集冻结（Section 3.3, Figure 2）。

### 4.2 LLM 分类与提示配置
- 使用 GPT-4o 作为 off-the-shelf LLM，无 fine-tuning（Section 3.2）。  
- 采用 prompt-only 配置 𝜋，少量示例（few-shot），输出：
  - 预测标签 ˆ𝑦 ∈ {normal, critical}  
  - 审计记录 s(x) 与关键 token
- 预测与解释分两步：
  1. 二分类输出  
  2. 生成事件记录 + 可选 minimal “what-if” token 编辑（Section 3.1）

### 4.3 监督基线对比公式
\[
\hat{y}_{\text{base}}(x) = 
\begin{cases} 
\text{critical}, & s_\text{base}(x) \ge \tau_\text{base} \\ 
\text{normal}, & s_\text{base}(x) < \tau_\text{base} 
\end{cases}
\]
- 阈值 τ_base 在验证集上最大化 macro-F1（Section 3.2）。

## 5. 实验分析
### 5.1 主实验
| 数据集 | 方法 | Accuracy | Critical Recall | 说明 |
|--------|------|---------|----------------|------|
| LeMay CSET’16 | LLM | 0.98 | 高 | Prompt-only，二分类 |
| CIC Modbus 2023 | LLM | 0.98 | 高 | 同上，与强监督基线接近 |

- LLM 性能与完整监督训练的基线相当，无任务特定权重更新（Section 5）。

### 5.2 审计记录验证
- 介入式诊断：充分性、必要性、可选 counterfactual probes（Figure 4）  
- 核心发现：引用 token 多数与模型决策相关，适合作为可审计报警（Section 5, Figure 4）

### 5.3 消融/对比
- 与仅使用预测标签或非 token-grounded 方法相比，生成事件记录提供额外审计价值 [需验证]  
- prompt-only 配置即可达到近似监督基线性能，无需完整训练数据（Section 5）

## 6. 局限性与风险
- 假设与约束：仅覆盖二分类（normal/critical），不替代多类攻击归因（Section 2）。  
- 应用边界：被动决策支持层，无法主动阻断 Modbus 流量或替代下游分析（Section 2）。  
- 潜在风险：高误报可能增加分析员负担；解释性依赖 token 决策相关性，而非完全人类可理解（Section 4, 6）。

## 7. 复现与后续问题
- **复现重点**
  1. Token 离散化边界需固定训练集拟合值（Section 3.3）。  
  2. Prompt 配置和 few-shot 示例需完整复制，包括风险原则和内部推理步骤（Appendix）。  
  3. 使用 temperature=0 解码以确保输出稳定（Section 3.2）。  
- **后续研究**
  - 结合多类攻击归因或更复杂分类策略。  
  - 融合领域专家评价，提升解释的完全人类可理解性。

## 8. 直接证据与待验证项
1. LLM 在 LeMay 和 CIC 数据集上实现 0.98 准确率，高关键类召回（Section 5, Table/正文）。  
2. 事件记录基于 token，介入式诊断验证 token 决策相关性（Section 5, Figure 4）。  
3. Prompt-only 配置无需任务特定权重更新即可与监督基线相当（Section 3.2, 5）。  
4. LLM 仅作为 triage 层，不替代多类攻击归因或自动响应（Section 2）。  
5. 可审计解释为简明事件记录而非完整人类可理解解释 [需验证]（Section 3.1, Appendix）。

（完）
