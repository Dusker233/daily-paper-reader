---
title: "MambaNetBurst: Direct Byte-level Network Traffic Classification without Tokenization or Pretraining"
title_zh: MambaNetBurst：无需分词或预训练的直接字节级网络流量分类
authors: "Gayan K. Kulatilleke, Siamak Layeghy, Mahsa Baktashmotlagh, Marius Portmann"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11034v1"
tags: ["query:q4"]
score: 8.2
relevance_score: 8.5
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.0
evidence: 字节级加密网络流量分类
tldr: 本论文提出MambaNetBurst，一种无需分词和预训练的字节级网络流量分类方法，直接利用原始数据进行端到端监督学习。基于Mamba-2状态空间模型，通过固定长度的数据burst和可学习CLS token实现分类。在六个公开数据集上，包括加密应用识别、VPN/Tor流量分类、恶意软件流量检测和IoT攻击流量，MambaNetBurst表现优异且高效。结果表明，保持字节级时间分辨率和适中状态规模对泛化至关重要，值得进一步精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11034v1/fig-001.webp\", \"caption\": \"TABLE III: Comparison Results on ISCXTor2016, ISCXVPN2016 and USTC-TFC2016. Source baselines: [7].\", \"page\": 6, \"index\": 1, \"width\": 1064, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-002.webp\", \"caption\": \"TABLE II: Comparison Results on CrossPlatform(Android), CrossPlatform(iOS) and CICIoT2022. Source baselines: [7].\", \"page\": 6, \"index\": 2, \"width\": 1064, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-003.webp\", \"caption\": \"TABLE VI: Scaling Comparison: Mamba-1 vs Mamba-2 at Larger Batch Sizes. d model = 256,mlp = 512, layers = 4(default). Times in ms. Peak memory in MiB. Averaged over 10 runs, each with a warm-up of 10. Implementation: mamba ssm. OOM indicates out of memory on RTX 3090, 23.54GiB. Note As Flash attention in linear transformer uses torch.autocast resulting in lower precision (fp16 and bf16), all models evaluated under torch AMP (automatic mixed precision).\", \"page\": 15, \"index\": 3, \"width\": 1060, \"height\": 1390}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-004.webp\", \"caption\": \"TABLE I: Comparison of Existing Representation Schemes (updated with pre-training and Mamba details). ¶ PT: Padded/truncated Fixed sizes for headers and payloads. ∗ MM: Multimodal input features.\", \"page\": 2, \"index\": 4, \"width\": 1062, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-005.webp\", \"caption\": \"TABLE IV: Ablations. Unless specified: backbone=Mamba-2, d state=16, d model=256, layers=4, d mlp=512, d conv=4. A: indicates Accuracy and F1: is macro F1-score. (64/64/2) and (32/32/2) are compact variants where the values indicate d model,mlp,layers. * Indicates linear transformer using flash attention 2, which only supports fp16/bf16\", \"page\": 7, \"index\": 5, \"width\": 1064, \"height\": 615}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-006.webp\", \"caption\": \"Fig. 2: Comparison of average macro-F1 accuracy against evaluation time (ms) for different models across batch sizes 8, 16, 32, 64, 128, 256. MambaNetBurst using Mamba-2 provides the best Pareto-optimal frontier, combining near-maximum accuracy with dramatically reduced inference latency compared to even Linear Transformers (with flash attention 2). Stride based model F1 scores are the lowest. All models use d model = 256,mlp = 512, layers = 4.\", \"page\": 10, \"index\": 6, \"width\": 1052, \"height\": 733}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-007.webp\", \"caption\": \"Fig. 1: Overview of MambaNetBurst. Left: PCAP files are split into 5-tuple flows. The first 5 packets are selected, and each packet is padded or truncated to 320 bytes, yielding a 1600-byte sequence. Center: the sequence is embedded, augmented with positional information and a CLS token, and processed by stacked Mamba-2 layers. The final CLS representation is used for classification. Right: Mamba-2 block architecture [16].\", \"page\": 4, \"index\": 7, \"width\": 1076, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-008.webp\", \"caption\": \"TABLE V: Scaling Comparison: Mamba-1 vs Mamba-2 at Larger Batch Sizes. (times in ms, peak memory in MiB.) Averaged over 10 runs, each with a warm-up of 10. Implementation: mamba ssm. OOM indicates out of memory on RTX 3090, 23.54GiB.\", \"page\": 9, \"index\": 8, \"width\": 810, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.11034v1/fig-009.webp\", \"caption\": \"TABLE VII: Scaling Comparison: Mamba-1 vs Mamba-2 at Larger Batch Sizes. d model = 64,mlp = 64, layers = 2(default). Times in ms. Peak memory in MiB. Averaged over 10 runs, each with a warm-up of 10. Implementation: mamba ssm. OOM indicates out of memory on RTX 3090, 23.54GiB. Note As Flash attention in linear transformer uses torch.autocast resulting in lower precision (fp16 and bf16), all models evaluated under torch AMP (automatic mixed precision).\", \"page\": 16, \"index\": 9, \"width\": 1060, \"height\": 1390}]"
motivation: 如何在不依赖分词、patching或预训练的情况下，实现高效准确的字节级网络流量分类。
method: 利用Mamba-2状态空间模型直接对原始数据包字节序列进行端到端监督分类，无需分词或预训练。
result: 在六个公开基准上，MambaNetBurst在加密应用识别、VPN/Tor流量分类及恶意流量检测中表现优于或竞争于更重且常需预训练的模型。
conclusion: 优先阅读方法部分的Mamba-2模型结构与burst构建策略，以及实验部分的性能对比和消融研究，这能快速理解技术实现和实际效果。
key_findings: [保持原始字节级时间分辨率对分类性能至关重要，过早下采样会降低效果。, 适中状态规模即可实现稳健泛化，大规模状态未必提升性能。, Mamba-2在保持高效的同时仍能有效建模字节序列，训练速度显著优于传统Transformer。]
limitations: 研究集中在固定长度burst和公开基准数据，实际部署中对长序列和复杂网络环境的适应性尚不明确。
---

## 1. 问题与背景

论文关注网络流量分类与网络入侵检测中的一个核心问题：如何在不依赖复杂预训练、分词(tokenization)或人工特征工程的情况下，直接从原始网络字节流中学习有效表示。现有主流方法大多采用 Transformer 架构，并依赖 token、patch、stride 等早期压缩策略来降低长序列计算成本，同时通常需要大规模自监督预训练。作者认为，这种早期聚合会损失协议字段、载荷签名和局部模式等细粒度判别信息，而网络流量本身具有天然的顺序结构，因此适合采用线性复杂度的状态空间模型进行直接建模。

## 2. 核心思路 / 方法

论文提出了 MambaNetBurst，这是一个基于 Mamba-2 的纯字节级(tokenizer-free)、无需预训练(pretraining-free)的网络流量分类模型。方法中，输入流量被截取为由前若干数据包组成的固定长度 burst，然后直接以原始 byte 序列形式输入模型，通过字节嵌入和可学习 CLS token 后，送入多层残差式、预归一化的 Mamba-2 block 进行端到端监督分类。与现有方法相比，该方法不使用 tokenization、patching、多模态人工特征，也不依赖 MAE、MLM 或 next-token 等自监督预训练阶段，而是完全通过监督学习完成字节到分类的映射。论文还系统比较了 Mamba-1 与 Mamba-2，并指出 Mamba-2 虽然使用了更受约束的状态转移矩阵（scalar × identity），但在网络字节序列建模中仍具有足够表达能力，同时带来更好的 GPU 训练效率。

## 3. 结果与结论

实验在六个公开数据集上进行，覆盖加密移动应用识别、VPN/Tor 流量分类、恶意软件流量检测以及 IoT 攻击检测等任务。结果显示，MambaNetBurst 在多个任务上能够达到或超过大量更重、更依赖预训练的基线模型，同时保持较小参数规模。消融实验表明，保持完整字节级时间分辨率非常关键，而早期 stride/downsampling 会持续损害性能；适中的 state size 已足够实现稳健泛化。效率测试进一步显示，相比标准 Transformer，Mamba 系列在长序列场景下显著降低显存占用与训练时间，例如 batch size 为 128 时 Transformer 已 OOM，而 Mamba 结构仍可正常运行。

## 4. 局限与适用边界

论文主要聚焦 burst-level 的固定长度流量分类任务，因此对于超长时序依赖、动态流量演化或在线流式检测场景的适用性，从现有文本无法确认。虽然作者强调无需预训练即可取得较强结果，但在更大规模、更复杂或跨域网络环境中的泛化能力仍缺乏进一步验证。此外，论文主要比较的是字节级表示与结构设计，对真实部署中的吞吐量、延迟和工业级鲁棒性讨论相对有限。

## Abstract
We present MambaNetBurst, a compact tokenizer-free byte-level sequence classifier for network burst classification based on a Mamba-2 backbone. In contrast to most recent strong traffic-classification and intrusion-detection approaches, our method operates directly on raw packet bytes, avoids tokenization, patching, and heavy engineered multimodal representations, and does not require any self-supervised pre-training stage. Given a packet flow, we form a fixed-length burst from the first few packets, embed the resulting byte sequence appending a learnable CLS token, and process it with a stack of residual pre-normalized Mamba-2 blocks for end-to-end supervised classification. Across six public benchmarks spanning encrypted mobile app identification, VPN/Tor traffic classification, malware traffic classification, and IoT attack traffic, MambaNetBurst achieves consistently strong results and is competitive with, or outperforms, substantially heavier and often pre-trained baselines. Our ablation study shows that preserving byte-level temporal resolution is critical, that early downsampling through striding is consistently harmful, and that moderate state sizes are sufficient for robust generalization.   We further show that Mamba-2, despite its more constrained transition structure relative to Mamba-1, remains highly effective for packet-byte modeling while providing clear efficiency advantages, particularly in training speed. Overall, our results demonstrate that direct **undiluted** byte-to-classification learning with compact selective state space models is a practical, effective and novel direction for efficient, deployable traffic analysis that bypasses the complexity of pre-training pipelines even over highly optimized linear attention architectures.

---

## 论文详细总结（自动生成）

## 1. TLDR

本文提出 **MambaNetBurst**，一种无需 tokenization 与预训练的纯字节级网络流量分类方法，直接基于原始 packet bytes 进行端到端监督学习。模型采用 **Mamba-2 状态空间模型**处理固定长度 burst 序列，在多种加密流量与安全检测任务上取得与强基线相当或更优性能，同时显著提升训练与推理效率。

**Keywords**：byte-level modeling, Mamba-2, network traffic classification, pretraining-free, intrusion detection, SSM

---

## 2. 核心故事与贡献

### 2.1 问题与动机
- 传统网络流量分类依赖：
  - tokenization / patching / stride 等早期压缩策略
  - 自监督预训练（如 ET-BERT、TrafficFormer 等）
- 论文指出这些方法存在两个核心问题：
  - **信息损失**：早期聚合会破坏 packet byte-level 的细粒度结构（协议字段、payload signature）
  - **成本高**：预训练 + 长序列 Transformer 带来高计算与数据成本（见 Introduction）

---

### 2.2 核心洞察
- 网络流量天然是 **byte-level sequential signal**
- 关键 discriminative signal 往往出现在：
  - packet header 字段
  - payload 局部模式
- 因此：
  > “direct undiluted byte-to-classification learning is sufficient”

并且：
- **Mamba-2 SSM** 可在 O(n) 复杂度下建模长序列
- 不需要 attention 或 tokenizer 即可捕获有效结构

---

### 2.3 主要贡献
- 提出 **MambaNetBurst（首个纯 byte-level + pretraining-free + Mamba-2 traffic classifier）**
- 证明：
  - Mamba-2 的 **scalar × identity transition matrix** 仍具足够表达能力
  - 且作为正则化有利于泛化
- 在 6 个 benchmark（VPN/Tor / IoT / malware / encrypted apps）上：
  - 达到或超过重型预训练模型
- 系统分析：
  - stride/downsampling 显著损害性能
  - state size 适中最优（见 ablation）

---

## 3. 相关工作与定位

### 方法对比表

| 方法 | 输入粒度 | 是否预训练 | 结构 | 是否降采样 | 应用场景 | 计算成本 |
|------|----------|------------|------|------------|----------|----------|
| ET-BERT | token | ✔ | Transformer | ✔ | 加密流量 | 高 |
| TrafficFormer | patch/token | ✔ | Transformer | ✔ | NIDS | 高 |
| NetMamba | stride/token | ✔ | Mamba-1 | ✔ | 流量分类 | 中 |
| ByT5 | byte | ✔ | Transformer | ✖ | NLP | 高 |
| 本文 MambaNetBurst | **byte** | ✖ | **Mamba-2** | ✖ | traffic/NIDS | **低** |

---

### 定位总结
- 属于：**tokenizer-free + pretraining-free SSM traffic modeling**
- 与 prior work 最大差异：
  - 去掉 preprocessing pipeline
  - 去掉 self-supervised pretraining
  - 直接 byte → classification

---

## 4. 方法详解

## 4.1 输入构造：Burst-level packet modeling
- PCAP → 5-tuple flow
- 取前 N 个 packets（默认 5）
- 每个 packet：
  - padding / truncation 到固定长度（约 320 bytes）
- 拼接形成序列：
  $$X \in \mathbb{R}^{1600 \text{ bytes}}$$

---

## 4.2 Byte embedding + CLS token
- 每个 byte ∈ [0, 255] → embedding
- 加入 learnable CLS token
- 加 positional encoding（见 Fig.1）

---

## 4.3 Mamba-2 backbone

核心结构：

- stacked residual blocks
- pre-normalized Mamba-2 layers

Mamba-2 SSM核心形式（概念级）：
- 输入驱动状态更新（selective state space）
- structured state-space duality (SSD)
- kernel reformulation → GEMM-friendly

特点：
- 线性复杂度 O(n)
- 无 attention
- GPU-friendly matmul execution

---

## 4.4 分类头
- CLS token output → MLP classifier
- end-to-end supervised loss（cross-entropy）

---

## 5. 实验分析

## 5.1 主结果

- 6 datasets：
  - VPN/Tor (ISCX2016)
  - USTC-TFC2016
  - CICIoT2022
  - mobile apps (Android/iOS)
  - malware traffic

结论：
- MambaNetBurst：
  - **优于或匹配 pre-trained Transformer baselines**
  - 在低延迟场景明显更优（见 Fig.6）

---

## 5.2 效率结果

### Fig.6（Pareto frontier）
- 横轴：evaluation time
- 纵轴：macro-F1
- 结论：
  - MambaNetBurst 在 Pareto frontier 最优
  - Linear Transformer 次之
  - stride model 最差

---

## 5.3 消融实验（Table IV）

关键发现：

| 组件变化 | 影响 |
|----------|------|
| stride/downsampling | 性能下降明显 |
| embedding projection removal | 性能下降 |
| deeper layers | 收益有限 |
| state size 增大 | 不一定提升 |

---

## 5.4 结构对比

- Transformer：
  - OOM at batch ≥128（见 Table VII）
- Linear Transformer：
  - 可运行但性能/效率不如 Mamba-2
- Mamba-2：
  - 最优 memory-performance tradeoff

---

## 6. 局限性与风险

- **固定 burst 假设**
  - 只取前几个 packet → 忽略后续流动态（[需验证]真实部署影响）
- **数据集偏静态**
  - 多为公开 benchmark，未验证真实互联网噪声环境
- **攻击泛化风险**
  - 对 adversarial traffic / obfuscation 未深入分析
- **序列截断风险**
  - 固定长度 padding 可能引入偏差

---

## 7. 复现与后续问题

### 复现重点
- burst 构造（前 5 packets + 320 bytes/pkt）
- Mamba-2 block 实现（SSD kernel）
- CLS pooling 方式
- AMP training（影响速度对比）

---

### 后续研究方向
- 支持 **online streaming traffic classification**
- 扩展到 **variable-length adaptive burst modeling**
- 探索 **robustness under adversarial traffic obfuscation**

---

## 8. 直接证据与待验证项

### 直接证据

- “operates directly on raw packet bytes, avoids tokenization, patching, and heavy engineered multimodal representations”  
  → 见 Abstract / Section I

- “does not require any self-supervised pre-training stage”  
  → 见 Abstract / Introduction

- “MambaNetBurst achieves consistently strong results… competitive with heavier pre-trained baselines”  
  → 见 Abstract / Section V 实验

- “early downsampling through striding is consistently harmful”  
  → 见 Ablation / Table IV

- “Transformer reaches OOM at larger batch sizes”  
  → 见 Table VII

---

### 待验证项

- [需验证] “Mamba-2 scalar × identity transition acts as beneficial regularizer”
- [需验证] 在真实工业网络环境（非 benchmark）中的泛化性能
- [需验证] burst-only（前5包）策略对长会话攻击检测的影响
- [需验证] 与更强新型 SSM（非 Mamba）模型对比结果

---

（完）
