---
title: "PersonaFingerprint: Measuring Persona Inference on Modern Websites with LLM-Driven Browsing"
title_zh: PersonaFingerprint：利用大语言模型驱动的浏览测量现代网站上的用户画像推断
authors: "Chuxu Song, Hao Wang, Richard Martin"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15962v1"
tags: ["query:q7"]
score: 8.3
relevance_score: 9.0
quality_score: 8.5
reliability_score: 8.0
practicality_score: 7.5
evidence: 基于LLM的加密流量人物识别
tldr: "本文提出PersonaFingerprint方法，通过LLM驱动的多代理浏览框架，在现代网站上仅利用加密流量元数据（数据包长度与到达时间）推断用户行为人格，实现行为指纹分析。实验覆盖10个主流网站与15种人格类型，混合站点条件下准确率约84%，同时保留网站识别性能。结果显示，即便在加密环境下，用户身份特征仍可被显著泄露，提示强烈隐私风险，值得进一步精读方法与实验细节。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15962v1/fig-001.webp\", \"caption\": \"Figure 5: Joint multi-task trade-off between website and persona accuracy as a function of the persona loss weight λ. Increasing λ dramatically improves persona accuracy (from ≈ 45% to ≈ 80%) while only gradually reducing website accuracy (from ≈ 90% to ≈ 75%).\", \"page\": 11, \"index\": 1, \"width\": 504, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.15962v1/fig-002.webp\", \"caption\": \"Table 1: Canonical personas used in closed-set experiments (P1–P15). We denote the open-world class by OW, which aggregates additional unseen personas (Section 2).\", \"page\": 7, \"index\": 2, \"width\": 372, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.15962v1/fig-003.webp\", \"caption\": \"Figure 1: Website fingerprinting baseline: per-site accuracy and macro-F1 of the site-only classifier across all ten target websites using 1,000-packet windows. All sites achieve high accuracy (typically 90%−95%), indicating that the encoder hθ learns strong discriminative features from packet metadata alone.\", \"page\": 8, \"index\": 3, \"width\": 504, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.15962v1/fig-004.webp\", \"caption\": \"Figure 2: Per-site persona fingerprinting: macro-F1 for each of the fifteen canonical personas and the OW label on all ten websites. Each subfigure corresponds to one site, with the red horizontal line indicating overall persona accuracy on that site. Despite observing only 1,000-packet windows and no application payload, the attacker can reliably distinguish personas within each website.\", \"page\": 9, \"index\": 4, \"width\": 1052, \"height\": 247}]"
motivation: 加密流量元数据能在多站点条件下推断用户的行为人格吗。
method: 利用LLM生成可控多人格浏览行为，并采集固定长度数据包窗口进行人格指纹分类。
result: "在混合站点实验中，基于固定长度数据包窗口的人格识别准确率约84%，网站分类保持约93%的基线水平。"
conclusion: 精读方法部分优先关注LLM多代理框架与人格条件决策代理的设计；实验部分重点关注混合站点和多任务学习对准确率的提升效果。
key_findings: [固定长度数据包窗口不仅可用于网站指纹识别，也能有效区分不同用户行为人格。, LLM生成的可控多人格浏览行为可大规模生成训练数据，降低获取真实用户标注的难度。, 现有网站指纹模型表示中已包含部分人格信息，通过轻量探针可进一步提取提升识别准确率。]
limitations: LLM生成行为虽可控，但未必完全模拟真实用户多样化行为，可能影响结果泛化。
---

## 1. 问题与背景
本文研究的问题是：在现代网站上，仅通过加密流量的包长度和到达时间序列，攻击者能否推断用户的行为人格（persona）。背景是现代网站高度交互且动态更新，使得传统的网页指纹方法难以分割页面边界，但加密流量元数据仍可能泄露隐私信息。研究动机在于揭示传统网站指纹攻击之外的潜在隐私风险，强调即使在匿名和加密环境下，用户身份特征仍可能被推测。

## 2. 核心思路 / 方法
论文提出了一种基于大语言模型（LLM）的多代理浏览框架，通过一个 persona 条件决策代理生成高层指令，并由计算机使用代理在真实浏览器中执行这些操作以收集加密流量数据。方法的创新在于无需真实用户数据即可生成可控、多样化的行为数据，用固定长度的包窗口（1000个连续包）表示，仅利用包长度、方向和到达时间进行 persona 指纹分析。同时，研究还探讨了现有网站指纹模型中潜在的 persona 信息，并通过轻量级多任务学习提升 persona 分类性能。

## 3. 结果与结论
实验表明，在10个现代网站和15种 persona（加开放世界类别）的混合站点设置下，persona 推断准确率约为84%，而网站指纹准确率约为93%。进一步使用固定站点编码器加轻量 MLP 探针，可在不影响站点分类的情况下，将 persona 分类性能显著提高约20–30个百分点。结论是加密流量元数据不仅泄露用户访问的网站，也能揭示用户的浏览行为特征和身份信息。

## 4. 局限与适用边界
论文主要局限在于使用 LLM 代理模拟用户行为，可能无法完美反映真实用户行为多样性。适用边界为需要分析现代高度交互网站上的加密流量元数据，但不包括对应用层内容或真实用户隐私数据的直接分析。

## Abstract
Website Fingerprinting (WFP) has traditionally focused on inferring which website a user visits from encrypted traffic metadata such as packet sizes and timing. In this paper, we identify and quantify a new privacy risk in modern web settings: an adversary can infer a user's persona using only packet-length and inter-arrival-time sequences. To study this risk at scale, we build an LLM-driven multi-agent browsing framework that enforces controllable persona constraints while a computer-use agent interacts with real websites and collects corresponding encrypted traffic traces. We formalize persona fingerprinting under both closed-set and open-world settings and further evaluate whether persona information is already embedded in representations learned by existing WFP models and can be amplified at low cost. Across 10 modern websites and 15 personas (plus an open-world class), persona inference achieves about 84% accuracy on mixed-site traffic; moreover, a lightweight multi-task objective can boost persona accuracy to around 80% while retaining strong site classification performance (about 93% baseline). Our results show that, on modern websites, encrypted traffic metadata can leak not only which site a user visits, but also how they browse and who is browsing.

---

## 论文详细总结（自动生成）

## 1. TLDR
本文提出 **PersonaFingerprint** 方法，通过 LLM 驱动多代理浏览框架，仅利用加密流量元数据（包长度与到达时间）推断用户行为人格。混合站点实验下 persona 分类准确率约 **84%**，网站指纹仍保持约 **93%**。  
**关键词**：Persona fingerprinting, LLM-driven browsing, encrypted traffic, packet metadata, multi-agent framework

## 2. 核心故事与贡献
- **问题与动机**：传统网站指纹研究关注网站识别，但现代网站交互复杂、动态加载，攻击者能否从加密流量推断用户行为人格尚未研究（见 Section 1）。  
- **核心洞察**：固定长度包窗口（1000个包）包含足够结构化信息用于识别用户 persona，即便无 payload、cookies 或浏览器指纹。LLM 多代理框架可生成大规模可控行为数据，降低真实用户采集难度。  
- **主要贡献**：
  - 正式定义 persona fingerprinting（Section 2.2）；提出闭集与开放世界分类方法。
  - 构建 LLM 驱动多代理浏览框架，结合 persona 条件决策代理和计算机执行代理收集包级流量（Section 3）。
  - 实证分析 10 个网站 × 15 personas + OW 类别，混合站点 persona 推断准确率 **84%**，网站指纹约 **93%**（Section 5 / Figure 1 & Figure 2）。
  - 验证现有网站指纹模型表示中可提取 persona 信息，通过轻量 MLP 探针提升识别准确率 20–30 点（Section 5 / Figure 5）。

## 3. 相关工作与定位

| 方法 / 维度 | 特征依赖 | 场景 | 多人格识别 | 开放世界处理 | 部署验证 |
|-------------|-----------|------|-----------|--------------|---------|
| 传统 WFP [2,4,10...] | 包长度、到达时间 | 单站或多站 | × | × | 模拟实验 |
| 多标签 WFP / DeepFingerprint [29,30] | 深度特征 | 多站 | × | × | 部署有限 |
| **PersonaFingerprint (本工作)** | 包长度、方向、到达时间 | 现代网站混合站点 | ✓ | ✓ (OW 类别) | LLM 代理生成真实网站行为数据，可大规模实验（Section 3） |

## 4. 方法详解
### 核心架构
- **Persona 条件决策代理（LLM）**：
  - 输入：persona profile、当前页面截图、历史操作
  - 输出：高层浏览指令（如滚动、点击推荐内容）
- **计算机使用代理**：
  - 执行指令 → 真实浏览器操作 → 捕获加密流量（1000 包窗口，lt, dt）
- **数据表示**：
  $$
  X = \{(l_t, d_t)\}_{t=1}^{L}, \quad L=1000
  $$
  其中 $l_t$ 为带方向的包长度，$d_t$ 为包间到达时间（Section 2.1）。

### 多任务学习
- 目标：同时优化网站分类与 persona 分类
- 损失函数：
  $$
  \mathcal{L} = \mathcal{L}_{\text{site}} + \lambda \mathcal{L}_{\text{persona}}
  $$
  λ 权衡 persona 精度与网站指纹精度（Figure 5）。

### 闭集与开放世界
- **闭集**：仅预测 P1–P15 15 种 persona  
- **开放世界**：增加 OW 类别以捕获未知 persona（Section 2.2）

## 5. 实验分析
- **主实验**：
  | 设置 | Persona 准确率 | 网站准确率 | 数据 |
  |------|----------------|------------|------|
  | 混合站点 10 网站 | ~84% | ~93% | 1000 包窗口，15 personas + OW |
  | 单站点 (per-site) | 0.78–0.91 F1 | 见 Figure 2 | 各站点 macro-F1 |
- **消融实验**：
  - 冻结 site-only 编码器 + MLP 探针 → persona 精度提升 20–30 点（Section 5 / Figure 5）
  - 多任务权重 λ 增大 → persona 精度明显上升，网站精度轻微下降
- **核心发现**：
  - 加密流量元数据可泄露用户身份特征
  - LLM 生成的可控多样行为足够训练高性能模型
  - 现有 WFP 表示可被 repurpose 为 persona fingerprinting 工具

## 6. 局限性与风险
- 假设约束：攻击者只能观察加密流量元数据，无法获取 payload、cookies 或浏览器指纹。  
- 应用边界：现代高度交互网站；不适用于真实用户敏感数据直接分析。  
- 潜在风险：LLM 模拟行为可能不覆盖所有真实用户行为模式 → 泛化性受限；存在隐私泄露风险。

## 7. 复现与后续问题
- **复现重点**：
  - LLM persona 决策代理与计算机代理接口实现及页面截图输入
  - 固定长度包窗口提取及表示 $(l_t,d_t)$
  - 多任务损失权重 λ 调参及训练策略
- **后续方向**：
  - 验证在真实用户行为下 persona fingerprinting 的泛化能力  
  - 探索更多开放世界 persona 类别及自适应攻击策略

## 8. 直接证据与待验证项
- Persona 推断混合站点准确率约 84%，网站准确率约 93%（Section 1, 5 / Figure 1, Figure 2）  
- 固定长度 1000 包窗口可支持 persona 分类（Section 2.1, 2.2）  
- LLM 多代理框架生成大规模可控浏览行为（Section 3）  
- 轻量 MLP 探针可从冻结编码器中提升 persona 精度 20–30 点（Section 5 / Figure 5）  
- [需验证] 真实用户行为下 persona fingerprinting 泛化性能

（完）
