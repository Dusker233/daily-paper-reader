---
title: "MCPShield: Content-Aware Attack Detection for LLM Agent Tool-Call Traffic"
title_zh: MCPShield：面向 LLM 代理工具调用流量的内容感知攻击检测
authors: Sultan Zavrak
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11053v2"
tags: ["query:q7"]
score: 8.3
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 8.0
evidence: 使用GNN检测LLM代理工具调用流量中的攻击
tldr: 本文提出MCPShield，一种面向LLM代理工具调用流量的内容感知攻击检测框架，通过将每个代理会话编码为图结构（工具调用为节点，顺序与数据流为边），并利用SBERT内容嵌入增强节点特征，分类会话是否受到攻击。实验显示，内容特征显著提升检测性能，树集成在主要任务评测中超越神经网络架构。对于关注代理安全和工具调用监控的研究者，本文方法与评测结果值得精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11053v2/fig-001.webp\", \"caption\": \"Figure 1: Overview of the MCPShield framework. MCP tool-call sessions are encoded as graphs with sequential and data-flow edges, node features are extracted via sentence embeddings, and a classifier (GNN, MLP, or classical ML on pooled features) produces per-session benign/attack predictions.\", \"page\": 6, \"index\": 1, \"width\": 806, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-002.webp\", \"caption\": \"Table 7: Classical baselines on pooled content features (3 seeds). RAS-Eval uses taskstratified splits; ATBench and Combined use label-stratified splits. All classifiers use class weighting for imbalance.\", \"page\": 15, \"index\": 2, \"width\": 758, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-003.webp\", \"caption\": \"Figure 2: Example MCP tool-call session showing a data exfiltration attack: the agent reads sensitive data via read_file and exfiltrates it via send_email.\", \"page\": 7, \"index\": 3, \"width\": 802, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-004.webp\", \"caption\": \"Figure 3: Session-to-graph encoding. Left: sequential tool calls. Right: the resulting session graph with sequential edges (solid) and data-flow edges (dashed). Node features are SBERT embeddings of arguments and responses.\", \"page\": 7, \"index\": 4, \"width\": 804, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-005.webp\", \"caption\": \"Figure 5: Label efficiency across datasets. Supervised training (circles) matches or performs better than SSL fine-tuning (triangles) at all label fractions beyond 1%, with the two methods converging at the full label budget on every dataset.\", \"page\": 18, \"index\": 5, \"width\": 797, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-006.webp\", \"caption\": \"Table 10: Label efficiency: AUROC at varying label fractions (5-fold cross-validation). RAS-Eval uses task-disjoint folds; ATBench and Combined use label-stratified folds.\", \"page\": 18, \"index\": 6, \"width\": 770, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-007.webp\", \"caption\": \"Table 5: Paradigm and feature-mode comparison on RAS-Eval (task-disjoint splits). Supervised uses GraphSAGE under the 70/10/20 protocol with 3 seeds; SSL+FT uses a GAT encoder for contrastive pre-training followed by fine-tuning, with the AUROC drawn from the full-label point of the label-efficiency study (5-fold cross-validation, Table 10). Content features use 768-dim sentence embeddings; metadata uses one-hot tool + params hash + response length. The dashes in the SSL+FT (Content) row reflect that F1, recall, and FPR were not collected at the label-efficiency full-label point under matched threshold settings; the architecture-matched supervised counterpart at full labels is reported in Table 10. Architecture-matched SSL vs. supervised comparisons should be read against Table 10 rather than against this table.\", \"page\": 13, \"index\": 7, \"width\": 779, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-008.webp\", \"caption\": \"Figure 4: Per-attack-category recall on the Combined dataset (GraphSAGE, content features). Categories with both tool input and output manipulation are the most detectable. Semantic-level attacks (jailbreak, misinformation) are the hardest.\", \"page\": 17, \"index\": 8, \"width\": 792, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2605.11053v2/fig-009.webp\", \"caption\": \"Table 4: Hyperparameter settings for all experiments.\", \"page\": 12, \"index\": 9, \"width\": 504, \"height\": 886}]"
motivation: 如何有效检测LLM代理在MCP协议下的工具调用流量中的恶意攻击。
method: 将代理会话编码为图结构，结合工具调用的内容嵌入，通过GNN或传统分类器实现内容感知的攻击检测。
result: 在RAS-Eval任务分离评测中，使用SBERT嵌入的树集成达到AUROC 0.975，显著高于GNN (0.917) 和 MLP (0.896)，表明内容特征是性能关键。
conclusion: 优先阅读方法部分（会话图构建与特征编码）和实验部分（RAS-Eval与AT-Bench比较），理解内容嵌入与图结构如何驱动检测性能。
key_findings: [仅使用元数据进行检测效果有限，AUROC约0.64，而加入内容嵌入后AUROC超过0.89。, 随机拆分评测会高估性能，任务分离拆分更能真实反映检测能力。, 树集成在SBERT嵌入上的表现优于GNN和MLP，自监督预训练未显著提升标签效率。]
limitations: 方法依赖SBERT内容嵌入，对未见攻击类型的泛化能力及在线部署延迟从文本中无法确认。
---

## 1. 问题与背景
本文关注大型语言模型（LLM）代理通过 Model Context Protocol (MCP) 调用外部工具时的攻击检测问题。背景是随着 MCP 被广泛采用，代理会生成工具调用序列，但现有检测系统多只关注元数据而忽略内容，导致攻击难以发现。研究动机在于填补对 MCP 工具调用流量进行内容感知的监控与检测的空白，这对于保障代理系统安全具有重要意义。

## 2. 核心思路 / 方法
论文提出 MCPShield 框架，将每个代理会话编码为图结构，其中节点表示工具调用，边表示调用顺序和数据流依赖，并使用句子嵌入（SBERT）对参数和响应进行特征增强。通过图神经网络（GAT、GCN、GraphSAGE）、无图 MLP 以及经典机器学习方法进行分类，同时引入任务划分的评估协议以避免任务记忆干扰。与现有方法相比，MCPShield 的创新在于无需访问模型内部即可进行内容感知的攻击检测，并支持监督与自监督训练。

## 3. 结果与结论
实验结果显示，内容嵌入特征对检测性能至关重要，元数据仅能达到 AUROC 约 0.64，而加入 SBERT 嵌入可将 AUROC 提升至 0.89 以上。树模型在汇总 SBERT 特征上表现优于神经网络架构，在 RAS-Eval 主设置下 AUROC 达到 0.975。结论表明，基于内容的特征显著提升 MCP 工具调用攻击检测效果，而自监督预训练未能提供额外的标签效率优势。

## 4. 局限与适用边界
论文主要局限在于依赖外部可观察的工具调用内容，尚未探讨对内部模型行为或未知攻击模式的适应性。其适用范围主要针对通过 MCP 协议调用外部工具的 LLM 代理流量检测。

## Abstract
The Model Context Protocol (MCP) has become a widely adopted interface for LLM agents to invoke external tools, yet learned monitoring of MCP tool-call traffic remains underexplored. In this article, MCPShield is presented as an attack detection framework for MCP tool-call traffic that encodes each agent session as a graph (tool calls as nodes, sequential and data-flow links as edges), enriches nodes with sentence-embedding features over arguments and responses, and classifies sessions as benign or attacked. Three GNN architectures (GAT, GCN, GraphSAGE), a no-graph MLP, and classical baselines (XGBoost, random forest, logistic regression, linear SVM) are evaluated, with the full architecture comparison conducted on RAS-Eval (task-stratified splits) and GraphSAGE retained as the GNN baseline on ATBench and a combined-source variant (both label-stratified). Three findings emerge. First, content-level features are essential: metadata-only detection plateaus around an AUROC of 0.64 regardless of architecture, while content embeddings push the AUROC above 0.89. Second, naive random-split evaluation inflates AUROC by up to 26 percentage points relative to task-disjoint splits, a memorization confound that prior agent-detection work has not addressed. Third, the detection signal resides primarily in the SBERT content embeddings: an AUROC of 0.975 was reached by tree ensembles on pooled embeddings, performing, for the most part, better than the neural architectures in the primary RAS-Eval setting including GNNs (0.917) and the MLP (0.896), and self-supervised pre-training does not deliver a label-efficiency advantage on this task.

---

## 论文详细总结（自动生成）

## 1. TLDR
MCPShield 提出了一种针对 LLM 代理 MCP 工具调用流量的内容感知攻击检测框架，将每个代理会话编码为图结构并利用 SBERT 嵌入增强节点特征，通过 GNN 或经典分类器实现高性能检测。树集成在主要任务评测中 AUROC 高达 0.975，显著优于 GNN 和 MLP。  

**关键词**：MCPShield, LLM 代理, 工具调用, 图神经网络, SBERT, 攻击检测  

---

## 2. 核心故事与贡献

- **问题与动机**  
  - MCP 协议下 LLM 代理广泛调用外部工具（>14,000 GitHub mcp-server 仓库），产生可观察攻击面，但现有检测方法多仅依赖元数据，忽略工具调用语义内容（见正文 Section 1）。  
  - 动机在于开发无需访问模型内部即可对代理工具调用进行内容感知监控与攻击检测。  

- **核心洞察**  
  - 攻击信号主要存在于工具调用内容层面（SBERT 嵌入），元数据特征 AUROC 仅 ~0.64，而内容嵌入可提升至 >0.89。  
  - 随机拆分评测会高估性能，任务分离拆分更能真实反映检测能力。  

- **主要贡献**  
  1. 构建会话图编码与内容感知检测器（节点：工具调用，边：顺序+数据流，特征：SBERT 嵌入）。  
  2. 提出任务分离评估协议（70/10/20 拆分），避免任务记忆干扰 AUROC 高达 26 个百分点。  
  3. 实证显示树集成在 SBERT 嵌入上优于 GNN/MLP，自监督预训练未显著提升标签效率。  

---

## 3. 相关工作与定位

| 方法 | 特征类型 | 场景 | 误报/评估 | 模型内部依赖 | 部署可行性 |
|------|----------|------|------------|--------------|------------|
| TraceAegis | 行为溯源图 | 单任务代理 | 模板 F1>0.94 | 需场景标签 | 中等 |
| TrajAD | 行为轨迹 | 单任务代理 | 模型验证 | LLM 推理 | 中等 |
| MindGuard | 决策依赖图 | MCP 特定 | 94–99% 精度 | LLM 注意力权重 | 有限制 |
| SentinelAgent | 执行图 + LLM | 多代理系统 | 需 LLM 推理 | LLM 内部 | 低 |
| Trajectory Guard | 序列规则 | 多代理 | 轻量规则 | 无 | 高 |
| Temporal Attacks | OpenTelemetry traces | 多代理 | 高 FPR（66.7%） | 无 | 中等 |
| Silent Failures | Trace 特征 | 多代理 | 高 AUROC | 无 | 中等 |
| **MCPShield (本工作)** | 会话图 + SBERT 内容嵌入 | MCP 代理 | 高 AUROC (0.975) | 无 | 高，可监督/自监督 |

---

## 4. 方法详解

### 4.1 会话图构建
- **节点**：单次工具调用  
- **边**：顺序边 + 数据流边  
- **节点特征**：SBERT 嵌入  
  $$ x_i = \text{SBERT}(\text{args}_i) \parallel \text{SBERT}(\text{resp}_i) \in \mathbb{R}^{768} $$

### 4.2 编码器
- **图神经网络**：GAT / GCN / GraphSAGE  
- **无图方法**：多层感知机 (MLP)  
- **传统方法**：XGBoost、随机森林、Logistic Regression、线性 SVM  

### 4.3 图读出
- **全图向量**：
  $$ z_G = \text{MEAN}(H) \parallel \text{MAX}(H) \in \mathbb{R}^{256} $$

### 4.4 分类器
- MLP 分类器：Linear → ELU → Dropout → Linear → Softmax  
- 输出：每会话的攻击/良性预测

---

## 5. 实验分析

### 主实验结果
- **RAS-Eval (任务分离)**：树集成 on pooled SBERT AUROC = 0.975；GraphSAGE AUROC = 0.917；MLP AUROC = 0.896（见正文 Figure 1, Table 7）  
- **ATBench / Combined 数据集**：任务分离 vs. 标签分层，树模型表现依然优越  

### 消融与发现
- **元数据 vs 内容嵌入**：AUROC 0.64 vs >0.89，内容嵌入显著提升性能（见正文 Section 5）  
- **随机拆分评估偏差**：高估 AUROC 可达 26 个百分点  
- **自监督对比**：SSL 预训练未明显提升标签效率，监督训练表现相当（见正文 Figure 5, Table 10）  

---

## 6. 局限性与风险
- 依赖 SBERT 嵌入，可能受限于未见攻击类型的泛化能力  
- 当前为 **会话后审计**，无法实时阻断，延迟风险存在  
- 假设有足够的良性会话数据用于训练  
- 对模型内部异常或对抗性 LLM 可能无效 [需验证]  

---

## 7. 复现与后续问题
- **复现重点**  
  1. 会话图构建：节点/边定义与 SBERT 嵌入  
  2. 图读出与全图向量拼接  
  3. 训练 protocol：任务分离 70/10/20 拆分，class weighting 调整不平衡  

- **后续研究方向**  
  1. 实时在线检测与增量图构建  
  2. 联邦部署与跨组织监控，保护敏感工具调用数据  

---

## 8. 直接证据与待验证项
- 核心性能数据：树集成 AUROC 0.975 > GraphSAGE 0.917 > MLP 0.896（见正文 Section 5 / Table 7）  
- 元数据 AUROC ~0.64 vs 内容嵌入 >0.89（见正文 Section 5）  
- 随机拆分 AUROC 高估可达 26 个百分点（见正文 Section 2 / 3）  
- 自监督预训练未提升标签效率（见正文 Figure 5, Table 10）  
- 对未知攻击类型泛化能力及在线部署延迟 [需验证]  

（完）
