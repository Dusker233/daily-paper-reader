---
title: "Rethinking Agentic RAG: Toward LLM-Driven Logical Retrieval Beyond Embeddings"
title_zh: 重新思考 Agentic RAG：迈向超越嵌入的、由大语言模型驱动的逻辑检索
authors: "Yuqi Zeng, Qixiang Deng, Yulei Wan, Ruiquan Jiang, Xiaoqing Zheng, Xuanjing Huang"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27123v1"
tags: ["query:profile-1"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 基于LLM的代理检索实现结构化查询
tldr: 本文提出了一种以大语言模型（LLM）为主导的逻辑检索框架（Logical RAG），旨在替代传统依赖嵌入或图结构的复杂检索系统，通过让LLM生成精确的逻辑查询并在倒排索引后端执行，实现多轮迭代查询和精确检索。实验显示该方法在匹配强基线的同时，显著降低了系统构建和服务成本，并减少了生成内容中的幻觉。论文值得关注，尤其是逻辑检索对多轮问答性能和成本优化的影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27123v1/fig-001.webp\", \"caption\": \"Table 1: Main experimental results on medium-scale and KILT-scale corpora. “Judge” denotes LLM-as-a-Judge accuracy. Avg. Judge is the average Judge score across the three datasets within each corpus setting.\", \"page\": 6, \"index\": 1, \"width\": 908, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-002.webp\", \"caption\": \"Table 5: Experimental settings.\", \"page\": 11, \"index\": 2, \"width\": 446, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-003.webp\", \"caption\": \"Table 6: Full ablation results on the first 400 examples of each KILT-scale dataset.\", \"page\": 11, \"index\": 3, \"width\": 420, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-004.webp\", \"caption\": \"Figure 1: Motivating example and trajectory statistics for LLM-driven logical retrieval. Agentic Hybrid repeatedly retrieves similar-name distractors, while LOGICALRAG repairs the search with explicit lexical constraints. The right panel reports same-intent result overlap and intent recovery across trajectories.\", \"page\": 2, \"index\": 4, \"width\": 950, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-005.webp\", \"caption\": \"Figure 2: Model scaling on the KILT-scale corpus. As model capability increases, LOGICALRAG progressively closes the gap with Agentic Hybrid, reaching near parity on Qwen3.5-Plus.\", \"page\": 7, \"index\": 5, \"width\": 445, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-006.webp\", \"caption\": \"Table 4: Answer-unavailable evaluation after removing gold supporting passages. Hallucination denotes an incorrect non-refusal answer unsupported by the remaining corpus.\", \"page\": 8, \"index\": 6, \"width\": 715, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.27123v1/fig-007.webp\", \"caption\": \"Table 8: EM and F1 model scaling results on the KILTscale datasets. Gap is Logical minus Hybrid on Avg.\", \"page\": 12, \"index\": 7, \"width\": 447, \"height\": 356}]"
motivation: 如何在多轮检索增强生成（RAG）中让LLM主导查询过程，同时降低后端复杂性并提升精确性。
method: 通过让LLM生成逻辑表达式驱动倒排索引后端执行，实现高精度、多轮迭代的检索控制。
result: 实验表明，Logical RAG在与强Agentic Hybrid基线比较时，性能相当，同时降低构建和运行成本，并显著减少检索失败和幻觉生成。
conclusion: 重点阅读第3节方法部分，理解逻辑查询生成和倒排索引结合机制；结果分析部分关注实验对比表和同意图重叠及意图恢复指标，以评估性能和检索质量。
key_findings: [LLM驱动的逻辑查询能有效修复检索失败，实现精确候选集控制。, 倒排索引后端结合逻辑查询性能可匹配复杂混合检索，同时降低成本。, 逻辑查询在多轮迭代中减少了重复检索和生成幻觉，提高信息可靠性。]
limitations: 从现有文本无法确认其在极大规模知识库或开放域检索的性能和泛化能力。
---

## 1. 问题与背景

论文关注 Agentic RAG 中一个被忽视的问题：当大语言模型通过多轮检索不断修正查询时，现有以向量检索和混合检索为核心的后端往往对查询改写不敏感，导致不同轮次返回高度重叠的结果。作者认为，随着 LLM 构造结构化查询能力的提升，Agentic RAG 的瓶颈正在从“如何设计更复杂的检索后端”转向“如何让模型更直接地控制检索过程”。这一问题之所以重要，是因为检索失败后的修复能力、多轮推理效率以及幻觉控制都依赖于检索系统能否准确响应模型的真实检索意图。

## 2. 核心思路 / 方法

论文提出 L​OGICAL RAG，将检索控制权更多地交给 LLM，由模型直接生成包含逻辑运算符和组合约束的结构化检索表达式，而不是依赖语义嵌入相似度进行模糊匹配。系统后端采用轻量级倒排索引架构，能够忠实执行诸如 AND、NOT 等显式词法约束，并向模型提供透明的检索反馈。作者将检索问题重新定义为“接口控制问题”，强调模型应能够主动扩大召回范围、收紧检索条件、排除干扰项以及修复失败查询。与混合检索相比，该方法不依赖复杂的向量库、图结构或融合机制，而是利用 LLM 自身越来越强的查询构造能力来完成精细化检索控制。

## 3. 结果与结论

实验结果表明，L​OGICAL RAG 在多个多跳问答数据集上整体达到与强 Agentic Hybrid 基线接近的性能。例如在模型规模最大的 Plus 模型上，Logical 与 Hybrid 的平均 LLM-as-a-Judge 准确率分别为 0.717 和 0.716，基本持平；EM/F1 指标差距也非常小。论文还发现逻辑检索能够更有效地修复失败检索路径，降低同一检索意图下重复结果的重叠度，并减少证据缺失时的无依据回答和幻觉。模型扩展实验进一步显示，随着 LLM 能力增强，逻辑检索与混合检索之间的性能差距逐渐缩小，最终达到近似水平。

## 4. 局限与适用边界

论文指出逻辑检索的有效性高度依赖 LLM 构造高质量逻辑查询的能力，在较弱模型（如 4B、9B）上性能通常略低于混合检索。作者同时承认，图检索在自主研究（autonomous research）等极其复杂的信息整合任务中仍具有价值，因此该方法更适用于能够通过多轮查询逐步定位证据的 Agentic RAG 场景。关于开放域超大规模知识库、跨模态检索等场景的表现，从现有文本无法确认。

## Abstract
Recent advances in RAG have shifted toward an agentic paradigm, where LLMs interact with retrieval systems over multiple turns and iteratively refine queries based on intermediate results. At the same time, LLMs have demonstrated a strong ability to construct structured queries that precisely express their information needs. However, contemporary RAG systems remain heavily focused on engineering complex retrieval backends, including dense, hybrid, and graph-based retrieval architectures. In this study, we argue that agentic RAG should delegate greater control to the LLM to steer the retrieval process, while relying on a lightweight retrieval interface that provides fine-grained control and faithfully executes the LLM's structured intent. Guided by this principle, we propose an agentic RAG framework that enables LLMs to formulate retrieval intents using logical expressions while simplifying the retrieval backend to an inverted-index-based system. Extensive experiments show that our framework matches a strong agentic hybrid baseline, while substantially reducing construction and serving cost. Moreover, we show that anchoring the retrieval process in logical queries substantially reduces hallucinations in generated responses.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文提出 **Logical RAG**：一种将 Agentic RAG 的控制权从复杂检索后端转移到 LLM 的新范式。核心思想是让 LLM 直接生成带有逻辑约束（AND/OR/NOT）的结构化查询，并由轻量级倒排索引执行，而非依赖向量检索或图检索进行隐式匹配。

实验表明，在多跳问答任务中，Logical RAG 达到了与强 Agentic Hybrid 检索系统接近甚至持平的性能，同时显著降低系统复杂度，并减少证据缺失场景下的幻觉回答。

**关键词：** Agentic RAG、Logical Retrieval、Boolean Query、Inverted Index、BM25、LLM-driven Retrieval、Hallucination Reduction

---

# 2. 核心故事与贡献

## 2.1 问题与动机：Agentic RAG 的瓶颈可能不在 Retriever

传统观点认为：

- Dense Retrieval → 提升语义匹配能力
- Hybrid Retrieval → 结合词法与语义
- Graph Retrieval → 支持复杂多跳推理

但作者发现：

在 Agentic RAG 中，LLM 已具备多轮迭代检索能力，因此真正的问题变成：

> 检索系统是否允许 LLM 有效修复失败搜索？

Figure 1 给出了典型失败案例：

问题：

> Was Manuel Díaz Rodríguez or Mona Tyndall born first?

Hybrid Retrieval：

- 多轮改写查询
- 始终返回类似名字的人
- 候选集几乎不变化
- 最终检索失败

Logical Retrieval：

```text
(Manuel AND Díaz AND Rodríguez) AND NOT Juan
```

成功定位目标实体。

作者认为：

> Agentic RAG 的核心矛盾已经从 Retrieval Quality 转向 Retrieval Controllability。（Section 1）

---

## 2.2 核心洞察：检索应该是“接口问题”而非“后端问题”

论文提出一个重要观点（Section 3.1）：

### Intent-faithful Retrieval

当 LLM 指定：

- 某个实体
- 某个短语
- 某些必须出现词
- 某些必须排除词

Retriever 应该忠实执行，而非语义近似。

例如：

```text
Manuel
AND Díaz
AND Rodríguez
NOT Juan
```

应直接改变候选集。

而不是：

```text
embedding(Manuel Díaz Rodríguez)
≈ embedding(Juan Manuel Rodríguez)
```

导致候选集变化极小。

---

## 2.3 主要贡献

### Contribution 1

提出：

> LLM-Driven Retrieval Paradigm

将 Agentic RAG 重新定义为：

> Retrieval Interface Design Problem

而不是 Retrieval Backend Design Problem。

---

### Contribution 2

提出：

> LOGICAL RAG

使用：

- Logical Query Language
- BM25
- Inverted Index

构建轻量级检索系统。

见 Section 3。

---

### Contribution 3

发现新的 Scaling Law：

模型能力越强：

```text
Logical Retrieval
        ↓
逐渐追平
        ↓
Hybrid Retrieval
```

最终在 Qwen3.5-Plus 上实现近乎完全持平。

见 Figure 2、Table 7、Table 8。

---

# 3. 相关工作与定位

| 方向 | 代表工作 | 核心思想 | 优势 | 局限 | 本文区别 |
|--------|--------|--------|--------|--------|--------|
| Dense Retrieval | DPR | 向量相似度检索 | 语义泛化强 | 实体区分弱 | 放弃 embedding 依赖 |
| Hybrid Retrieval | Hybrid Search | Sparse + Dense | 效果稳定 | 多轮检索结果高度重叠 | 强调检索可控性 |
| Graph Retrieval | GraphRAG、HippoRAG | 图结构组织知识 | 多跳推理强 | 构建成本高 | 不依赖图结构 |
| Tree Retrieval | PageRAG等 | 层级导航 | 全局聚合强 | 工程复杂 | 用逻辑查询代替结构导航 |
| Agentic Retrieval | ReAct、IRCoT | 推理-检索交替 | 支持搜索修复 | Retriever仍是黑盒 | Retriever变成可控工具 |
| 本文 | Logical RAG | Logical Query + BM25 | 高可控、低成本 | 依赖LLM能力 | 强调LLM主导 |

---

# 4. 方法详解

## 4.1 总体架构

论文核心流程：

```text
Question
    ↓
LLM Agent
    ↓
Logical Query
    ↓
Inverted Index
    ↓
BM25 Ranking
    ↓
Retrieved Evidence
    ↓
LLM Reflection
    ↓
Query Repair
```

见 Section 3。

---

## 4.2 LLM-Driven Retrieval Principle

作者定义两个关键属性。

### （1）Intent-faithful

检索器必须忠实执行：

- Entity
- Phrase
- Inclusion
- Exclusion

例如：

```text
"Antonio Vivaldi"
AND opera
AND libretto
```

而非语义近似匹配。

---

### （2）Query Repairability

LLM 可以主动：

- 扩大召回
- 收紧约束
- 排除干扰项
- 调整匹配粒度

例如：

```text
Q1:
Manuel Díaz Rodríguez

Q2:
Manuel AND Díaz AND Rodríguez

Q3:
(Manuel AND Díaz AND Rodríguez)
NOT Juan
```

逐步修复搜索。

见 Figure 1。

---

## 4.3 Logical Query Language

支持：

### AND

```text
A AND B
```

交集。

---

### OR

```text
A OR B
```

扩召回。

---

### NOT

```text
A NOT B
```

排除干扰项。

---

### Phrase Match

```text
"Antonio Vivaldi"
```

短语匹配。

---

### Adjustable Granularity

支持：

- Keyword
- Phrase
- Boolean Expression

多粒度搜索。

见 Section 3。

---

## 4.4 Retrieval Backend

后端非常简单：

### Index

倒排索引（Inverted Index）

### Ranking

BM25

论文实验参数（Table 5）：

| 参数 | 值 |
|--------|--------|
| BM25 k1 | 1.2 |
| BM25 b | 0.75 |

相比 Hybrid：

```text
Dense Encoder
+
Vector DB
+
RRF Fusion
```

系统复杂度显著下降。

---

# 5. 实验分析

## 5.1 主实验结果

数据集：

- 2Wiki
- HotpotQA
- MuSiQue

评价：

- EM
- F1
- LLM-as-a-Judge

见 Table 1。

---

## 5.2 Model Scaling

### Judge Accuracy（Table 7）

| Model | Hybrid | Logical | Gap |
|---------|---------|---------|---------|
| 4B | 0.621 | 0.591 | -0.030 |
| 9B | 0.639 | 0.635 | -0.004 |
| 35B-A3B | 0.694 | 0.671 | -0.023 |
| Plus | 0.716 | 0.717 | +0.001 |

核心结论：

> 在最强模型上 Logical RAG 已达到 Hybrid Retrieval 的性能水平。

见 Figure 2、Table 7。

---

## 5.3 EM/F1结果

Table 8：

| Model | Hybrid F1 | Logical F1 | Gap |
|---------|---------|---------|---------|
| 4B | 0.524 | 0.504 | -0.020 |
| 9B | 0.534 | 0.526 | -0.008 |
| Plus | 0.628 | 0.624 | -0.004 |

说明：

随着 Agent 能力增强：

```text
Retriever Importance ↓
Agent Capability Importance ↑
```

---

## 5.4 消融实验

见 Table 6。

### 去掉逻辑操作符

```text
Avg Judge
0.736 → 0.712
```

下降明显。

说明：

AND / OR / NOT 是核心能力来源。

---

### 仅保留语法描述

```text
0.736 → 0.732
```

下降较小。

说明：

真正重要的是：

- 逻辑控制能力

而非提示词中的 BM25 教学。

---

## 5.5 幻觉控制

作者构造：

Answer-Unavailable Setting

移除 Gold Evidence。

结果（Table 4）：

Logical RAG：

- 更容易拒答（abstain）
- 更少 unsupported answers
- 更少 hallucination

原因：

Logical Query 可以显式返回：

```text
No Match
```

而非 embedding 强行找到近邻文档。

---

## 5.6 Trajectory Analysis

见 Figure 1、Appendix F。

### Same-Intent Overlap

衡量：

```text
同一检索意图
不同轮结果重叠率
```

越低越好。

Logical RAG 更低。

---

### Intent Recovery

衡量：

```text
首次失败
后续是否成功修复
```

Logical RAG 更高。

说明：

LLM 能有效修复检索错误。

---

# 6. 局限性与风险

## 6.1 假设约束

Logical RAG 假设：

> LLM 已具备较强结构化查询生成能力。

在 4B、9B 等较弱模型上仍落后于 Hybrid（Table 7、Table 8）。

---

## 6.2 应用边界

对于：

- Autonomous Research
- Deep Research
- 超长链推理

GraphRAG 等结构化系统可能仍有优势。

作者在 Introduction 中明确承认这一点。

---

## 6.3 潜在风险

### Query Construction Error

若 LLM 构造错误逻辑表达式：

```text
NOT
AND
Phrase
```

使用不当，

可能直接过滤掉正确答案。

---

### Recall Risk

逻辑检索本质是词法检索。

对于：

- 同义表达
- 拼写变体
- 隐式语义

召回能力可能弱于 Dense Retrieval。

---

### Corpus Dependency

[需验证]

在超大规模开放域知识库（10B+文档级别）上的效果与成本优势，正文未给出系统验证。

---

# 7. 复现与后续问题

## 7.1 复现重点

### ① Query Interface

必须完整支持：

```text
AND
OR
NOT
Phrase Match
```

这是论文核心贡献。

---

### ② BM25配置

Table 5：

| 参数 | 值 |
|--------|--------|
| k1 | 1.2 |
| b | 0.75 |

需严格对齐。

---

### ③ Agent Loop

最大检索轮数：

```text
8 turns
```

见 Table 5。

Agent：

```text
Qwen3.5-Plus
temperature=0.6
top-p=0.95
```

---

## 7.2 后续研究方向

### Direction 1

Logical + Dense Hybrid

让 LLM 同时控制：

```text
Logical Constraint
+
Semantic Expansion
```

兼顾精度与召回。

---

### Direction 2

Logical Graph Retrieval

将：

```text
Boolean Query
+
Knowledge Graph Navigation
```

结合到统一 Agent 框架中。

---

# 8. 直接证据与待验证项

### Evidence 1

Figure 1：

Agentic Hybrid 在同名实体场景下持续返回相似名字干扰项，而 Logical RAG 通过：

```text
(Manuel AND Díaz AND Rodríguez)
NOT Juan
```

成功修复检索路径。

见 Figure 1。

---

### Evidence 2

Table 7：

Qwen3.5-Plus：

```text
Hybrid Judge = 0.716
Logical Judge = 0.717
```

Logical RAG 首次达到并略超 Hybrid。

见 Table 7。

---

### Evidence 3

Table 8：

Plus 模型：

```text
Hybrid F1 = 0.628
Logical F1 = 0.624
Gap = -0.004
```

说明二者已近乎持平。

见 Table 8。

---

### Evidence 4

Table 6：

移除逻辑操作符：

```text
Judge
0.736 → 0.712
```

证明 Boolean Logic 是关键贡献来源。

见 Table 6。

---

### Evidence 5

Table 4：

Answer-Unavailable Evaluation 中，

Logical RAG 产生更少 unsupported answers 和 hallucinations。

见 Table 4。

---

### [需验证]

1. 是否能在 Web-scale Corpus 上保持与 Hybrid 相同效果。
2. 是否适用于跨模态检索（图像、视频、表格）。
3. 倒排索引在超长文档场景中的召回表现。
4. 不同语言（中文、多语言）场景的泛化能力。
5. 与最新 GraphRAG 系统的大规模对比是否仍成立。

（完）
