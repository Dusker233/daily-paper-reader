---
title: Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems
title_zh: 大型语言模型作为能源工业控制系统的可解释网络攻击检测器
authors: "Weiyi Kong, Ahmad Mohammad Saber, Amr Youssef, Deepa Kundur"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26079v1"
tags: ["query:q3"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: LLM用于ICS网络流量检测
tldr: 本文探索将通用大语言模型（LLM）作为能源工业控制系统（ICS）Modbus流量的可审计入侵检测辅助层，采用二分类normal/critical任务，将Modbus通信离散化为token序列，通过prompt驱动模型生成警报及简明事件记录。实验显示，LLM无需任务特定训练即可在两套公开数据集上实现约0.98的准确率，检测性能与强监督模型相当，同时提供可供人工审查的可解释记录，值得关注其人机协作和可审计机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26079v1/fig-001.webp\", \"caption\": \"Figure 3: Pipeline and leakage guards. Preprocessing includes UTCnormalization, aggregation of attacker logs into contiguous time windows, and a canonical train/validation/test split that prevents temporal leakage across windows. LEN/IAT bins are fit once on the training portion and then frozen for validation and test, and each flow is branched into a compact LLM token string and a numeric feature vector.\", \"page\": 5, \"index\": 1, \"width\": 357, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-002.webp\", \"caption\": \"Figure 4: Pass-rate vs 𝜀 for sufficiency and comprehensiveness (95% CIs); dashed line shows the label flipped baseline.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed framework. Offline preparation convertsModbus/TCP traces from LeMay and CIC into a unified token-string representation (no model training). Online inference uses a prompt-guided LLM to output a binary label (normal/critical), followed by an auditing step that generates supporting evidence tokens, risk tags, and optional what-if edits.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 286}]"
motivation: 通用大语言模型能否作为能源ICS Modbus流量的可审计入侵检测辅助层，实现高性能且可解释的二分类告警。
method: 通过prompt驱动的离散Modbus token序列输入，让off-the-shelf LLM进行normal/critical二分类，同时生成基于token的可审计事件记录。
result: 在LeMay CSET’16和CIC Modbus 2023数据集上，LLM在匹配事件信息和共享评估分割下达到约0.98准确率，并对关键类召回率高，且无需任务特定权重更新。
conclusion: 优先阅读方法（Section 3）和实验结果（Section 5），了解token化处理、prompt设计及二阶段可审计管道；随后关注审计分析（Section 5及附录）以理解可解释性实现。
key_findings: [LLM无需任务特定训练即可实现与强监督基线相当的入侵检测性能。, 通过token-grounded事件记录，LLM提供了可供人工审查的可解释警报。, Prompt-only配置在数据效率和部署灵活性上具有优势。]
limitations: 方法依赖prompt设计，未替代多类攻击分类，解释性仅为token决策相关，非完全人类可理解。
---

## 1. 问题与背景
现代能源系统中的工业控制系统（ICS）和电力SCADA协议如Modbus易受网络攻击，例如虚假数据注入。现有入侵检测方法虽然准确，但在实际部署中通常依赖特定站点的数据标注、特征工程或模型重训练，同时缺乏可供操作员审计的解释性记录。确保检测系统既准确又可审计对于及时响应和保障电网安全至关重要，因此研究如何在不依赖任务特定训练的情况下实现可解释的入侵检测具有重要意义。

## 2. 核心思路 / 方法
论文提出使用现成的大语言模型（LLM）作为Modbus流量的辅助分类和审计层，将每条Modbus通信实例转换为离散化协议字段的紧凑token字符串，再通过prompt引导LLM输出正常或关键警报，同时生成可供操作员归档的简明事件记录。方法采用两阶段流程，第一阶段生成二分类预测，第二阶段生成token为基础的解释和可选反事实编辑，以验证引用token与模型决策的关联性。该方法无需任务特定的权重更新，仅通过少量示例配置prompt即可实现检测，相较于传统监督学习方法，实现了可审计、prompt驱动的快速部署。

## 3. 结果与结论
在LeMay CSET’16和CIC Modbus 2023两个公共数据集上，LLM驱动的检测流程在关键类上取得了约0.98的准确率和高召回率，性能与强监督基线相当。研究表明，通过prompt配置的LLM可以作为人机协作的初步筛查层，为操作员提供既可靠又可审计的入侵警报，同时无需专门训练模型。

## 4. 局限与适用边界
该方法不执行攻击归因或自动响应，仅作为被动决策支持层。其有效性依赖于输入为离散化的Modbus通信记录，无法保证对未见协议或复杂多类攻击的直接适用。

## Abstract
In modern energy systems, industrial control systems (ICS) and power-system SCADA require intrusion detection that is not only accurate but also auditable by operators. The ICS intrusion-detection landscape is currently dominated by established supervised detectors. In this paper, we study whether an off-the-shelf large language model (LLM) can serve as a complementary, human-in-the-loop layer for Modbus traffic. We cast this as a binary network-side normal/critical decision task on two public ICS Modbus datasets, collapsing attack periods and other safety-critical behaviors into a single critical class. Each Modbus communication instance is converted into a compact token string derived from discretized protocol fields, and a prompt-configured LLM produces a normal/critical alert together with a concise, token-grounded incident record for analyst review. Under matched event information and shared evaluation splits, the resulting LLM-based triage pipeline achieves high predictive performance on both benchmarks and is broadly comparable to strong supervised baselines, while requiring no task-specific weight updates. To assess the audit record, we apply intervention-based diagnostics, including sufficiency- and necessity-style tests, which provide evidence that the cited tokens are often decision-relevant to the model's own prediction. These records are intended as audit signals rather than full human-grounded explanations.

---

## 论文详细总结（自动生成）

## 1. TLDR
本文提出使用通用大语言模型（LLM）作为能源工业控制系统（ICS）Modbus流量的可审计入侵检测辅助层，通过prompt驱动的token序列输入实现normal/critical二分类，并生成token-grounded事件记录。实验显示，LLM无需任务特定训练即可在两套公开数据集上达到约0.98准确率，性能可与强监督基线媲美。  

**关键词：** LLM，ICS入侵检测，Modbus，prompt驱动，可解释性，token-grounded  

---

## 2. 核心故事与贡献

### 问题与动机
- 现代能源ICS和SCADA协议（如Modbus）易受虚假数据注入等攻击。  
- 现有检测方法虽准确，但依赖站点特定标注、特征工程或重训练，缺乏可供操作员审计的解释记录（Section 1）。  

### 核心洞察
- 通用LLM无需任务特定权重更新，仅通过prompt引导即可理解离散化Modbus通信，生成分类和简明审计记录（Section 3.2）。  
- 可通过token-grounded事件记录提供决策相关的审计信号，而非完全人类可解释理由。  

### 主要贡献
1. 提出prompt配置、少样本驱动的LLM作为ICS入侵检测辅助层，实现normal/critical二分类（Section 2）。  
2. 设计两阶段管道：第一阶段预测，第二阶段生成可审计事件记录并支持干预式诊断（Section 3.1）。  
3. 在LeMay CSET’16和CIC Modbus 2023数据集上，LLM达约0.98准确率，关键类召回高，检测性能可与强监督基线媲美（Section 5）。  

---

## 3. 相关工作与定位

| 维度                     | 传统监督方法                     | 可解释AI方法                       | 本文LLM方法                        |
|--------------------------|---------------------------------|-----------------------------------|-----------------------------------|
| 特征类型                 | 原始Modbus字段 + 手工工程        | 手工特征 + SHAP/LIME              | 离散化Modbus token序列           |
| 分类能力                 | 高，需训练                       | 高，需训练/后处理                   | 高，无需任务特定训练             |
| 可解释性                 | 模型特定/后验特征贡献             | 人类可理解特征贡献                  | token-grounded审计记录           |
| 部署与数据效率           | 需站点特定标注/训练               | 同上                               | Prompt-only配置，快速部署         |
| 人机协作                 | 限操作员解释                       | 支持分析师解读                      | 明确为人机协作的初步筛查层       |
| 实验验证                 | 公共数据集+交叉验证               | 公共数据集+干预式分析               | 两公共数据集，匹配事件信息+共享评估分割（Section 4） |

---

## 4. 方法详解

### 输入表示
- 每条Modbus通信编码为紧凑token序列：
  - DIR: 通信方向（C2S/S2C）  
  - FC: Modbus功能码  
  - UNIT: 单元ID  
  - LEN: 负载长度桶  
  - IAT: 到达间隔桶  
  - EX: 异常码  
- 连续量离散化为有限桶，避免验证/测试泄漏（Section 3.3）。  

### LLM分类器
- 使用GPT-4o off-the-shelf模型，无任务特定训练/微调（Section 3.2）。  
- Prompt配置π包含：
  - Modbus通用语义  
  - ICS安全高层提示  
  - 少量示例（few-shot）  

分类公式：  
$$
f(x;\pi) = (\hat{y}(x), s(x)), \quad \hat{y} \in \{\text{normal, critical}\},\ s \in [0,1]
$$  

### 两阶段审计
1. **预测阶段**：输出normal/critical标签  
2. **解释阶段**：生成简明事件记录，包含：
   - 引证token  
   - 风险类别标签  
   - 可选反事实编辑  
- 通过干预式诊断（sufficiency、necessity、counterfactual probes）验证token决策相关性（Section 3.1、附录）。  

### 流程图
- Figure 1: 框架概览（token化、LLM分类、审计记录生成）  

---

## 5. 实验分析

### 主实验结果
| 数据集                 | 准确率 | 关键类召回 | 备注 |
|------------------------|--------|------------|------|
| LeMay CSET’16          | 0.98   | 高         | 与强监督基线相当 |
| CIC Modbus 2023        | 0.98   | 高         | Prompt-only配置，无需任务特定训练 |

- Figure 3: 数据预处理与token生成管道  
- Figure 4: 干预式诊断结果（sufficiency/comprehensiveness）  

### 消融实验
- Prompt-only vs 任务特定微调：Prompt-only即可达到可比性能  
- 两阶段审计验证：提供token决策相关性证据，但非完整人类可理解解释（Section 5）  

### 核心发现
- LLM无需特定训练即可实现高性能入侵检测  
- token-grounded事件记录为操作员提供可靠审计信号  
- Prompt配置支持高数据效率与部署灵活性  

---

## 6. 局限性与风险
- 假设约束：输入需为离散化Modbus通信记录（Section 4）  
- 应用边界：仅作为被动决策支持层，不执行攻击归因或自动响应  
- 潜在风险：
  - 未见协议或复杂多类攻击可能降低有效性  
  - token-grounded解释不等同于完整人类可理解解释  

---

## 7. 复现与后续问题

### 复现重点
1. 确保Modbus通信token化严格遵循训练split边界（Section 3.3）  
2. Prompt配置π的少样本示例与高层安全提示保持一致  
3. 干预式诊断依赖JSON输出中的confidence值与token引用  

### 后续研究
- 融合多类攻击归因与分类能力  
- 提升token-grounded解释向人类可理解解释的映射  

---

## 8. 直接证据与待验证项
- LLM在LeMay和CIC Modbus 2023数据集上达到约0.98准确率，高关键类召回（Section 5 / Table 1）  
- Prompt-only配置无需任务特定权重更新即可实现竞争性能（Section 3.2 / Section 5）  
- 两阶段审计生成的token-grounded事件记录提供决策相关证据（Section 3.1 / Figure 1）  
- 干预式诊断验证引用token的决策相关性（Section 5 / Figure 4）  
- [需验证] LLM可直接推广到未见协议或复杂多类攻击场景  

（完）
