---
title: Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems
title_zh: 大语言模型作为能源工业控制系统可解释网络攻击检测器
authors: "Weiyi Kong, Ahmad Mohammad Saber, Amr Youssef, Deepa Kundur"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26079v1"
tags: ["query:q3"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 将LLM应用于ICS Modbus流量的可解释入侵检测
tldr: 本文研究了将现成大语言模型(LLM)用于能源工业控制系统(ICS)Modbus流量的入侵检测，并生成可审计的事件记录。通过将Modbus通信离散化为令牌字符串，并通过提示驱动的LLM进行二分类(normal/critical)，实现了与监督学习基线相当的高预测性能，同时无需任务特定权重更新。实验显示在两个公开数据集上关键类召回率高，提供了可供人工审查的结构化审计记录，值得对LLM在安全辅助决策的可行性进一步研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26079v1/fig-001.webp\", \"caption\": \"Figure 3: Pipeline and leakage guards. Preprocessing includes UTCnormalization, aggregation of attacker logs into contiguous time windows, and a canonical train/validation/test split that prevents temporal leakage across windows. LEN/IAT bins are fit once on the training portion and then frozen for validation and test, and each flow is branched into a compact LLM token string and a numeric feature vector.\", \"page\": 5, \"index\": 1, \"width\": 357, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-002.webp\", \"caption\": \"Figure 4: Pass-rate vs 𝜀 for sufficiency and comprehensiveness (95% CIs); dashed line shows the label flipped baseline.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.26079v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed framework. Offline preparation convertsModbus/TCP traces from LeMay and CIC into a unified token-string representation (no model training). Online inference uses a prompt-guided LLM to output a binary label (normal/critical), followed by an auditing step that generates supporting evidence tokens, risk tags, and optional what-if edits.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 286}]"
motivation: 是否可以用现成大语言模型作为能源ICS Modbus流量的可解释入侵检测辅助工具。
method: 将Modbus协议通信离散化为令牌字符串，使用提示驱动的LLM进行二分类，并生成基于令牌的可审计事件记录。
result: 在LeMay CSET’16和CIC Modbus 2023两个数据集上，LLM提示驱动方法在关键类上实现约0.98准确率和高召回，与监督学习基线相当。
conclusion: 精读方法部分(Section 3)可了解令牌化与提示配置；结果部分(Section 5)重点关注性能对比和审计记录质量。
key_findings: [LLM提示驱动方法在无需任务特定训练下实现高精度和关键类高召回。, 两阶段管道可同时提供预测结果和基于令牌的审计记录，支持人工复核。, 干预式诊断显示引用的令牌与模型决策高度相关，为操作员提供可审计依据。]
limitations: 不提供完全人类解释，仍依赖人工复核和多类攻击归因；实验仅限两个公开Modbus数据集。
---

## 1. 问题与背景

论文关注能源工业控制系统（ICS）中的 Modbus 网络流量入侵检测问题，尤其强调检测系统不仅需要高准确率，还需要具备可审计性与可解释性，以便运维人员能够理解、信任并归档告警结果。现有 ICS 入侵检测大多依赖监督学习模型，虽然精度较高，但通常需要针对具体场景进行数据标注、特征工程与模型重训练。与此同时，现有解释方法往往依赖模型特定诊断工具，难以直接形成面向操作员的事件记录。论文因此探索：是否可以利用无需额外训练的大语言模型（LLM），直接基于离散化后的 Modbus 协议 token，实现可解释的网络侧异常分诊。

## 2. 核心思路 / 方法

论文提出了一种基于提示词配置（prompt-configured）的 LLM 入侵检测框架，将 Modbus/TCP 通信实例编码为紧凑的 token 字符串，再输入通用大语言模型 GPT-4o 进行 normal/critical 二分类判断。方法采用“两阶段”机制：第一阶段输出告警标签，第二阶段生成结构化审计记录，包括关键证据 token、风险标签以及可选的反事实修改建议。系统提示词中显式注入了工业控制场景知识，例如“读操作通常正常、写控制操作更危险”“机器流量具有稳定节律”“异常函数码应被视为高风险”等原则，引导模型基于协议语义和时间模式推理。与传统监督学习方法相比，该方法无需任务特定参数更新，仅通过 few-shot prompt 即可部署，同时还能够生成可归档、面向操作员的解释记录。

## 3. 结果与结论

论文在 LeMay CSET’16 Modbus 数据集与 CIC Modbus 2023 数据集上进行了评估，并将所有攻击行为与安全关键行为统一归入 critical 类。实验结果表明，该 LLM 分诊框架在共享测试划分下取得约 0.98 的准确率，并在 critical 类上具有较高召回率，其性能与强监督学习基线总体相当。作者进一步通过 sufficiency、necessity 与 counterfactual 等干预式诊断方法评估解释质量，结果显示模型给出的证据 token 通常与自身预测具有较强决策相关性。论文最终认为，LLM 可作为工业控制系统中的“人机协同”告警分诊层，在无需重新训练的前提下提供兼具检测能力与审计能力的辅助分析。

## 4. 局限与适用边界

论文明确指出，该系统并不用于自主决策或自动响应，而是作为人工审查前的保守分诊层使用，因此并不替代后续取证分析或多类别攻击归因。其解释能力本质上属于“token 级决策相关性审计”，而非经过领域专家验证的人类可理解解释。此外，论文主要基于公开离线数据集验证，关于真实部署中的隐私治理、API 安全以及在线性能等问题，从现有文本无法确认。

## Abstract
In modern energy systems, industrial control systems (ICS) and power-system SCADA require intrusion detection that is not only accurate but also auditable by operators. The ICS intrusion-detection landscape is currently dominated by established supervised detectors. In this paper, we study whether an off-the-shelf large language model (LLM) can serve as a complementary, human-in-the-loop layer for Modbus traffic. We cast this as a binary network-side normal/critical decision task on two public ICS Modbus datasets, collapsing attack periods and other safety-critical behaviors into a single critical class. Each Modbus communication instance is converted into a compact token string derived from discretized protocol fields, and a prompt-configured LLM produces a normal/critical alert together with a concise, token-grounded incident record for analyst review. Under matched event information and shared evaluation splits, the resulting LLM-based triage pipeline achieves high predictive performance on both benchmarks and is broadly comparable to strong supervised baselines, while requiring no task-specific weight updates. To assess the audit record, we apply intervention-based diagnostics, including sufficiency- and necessity-style tests, which provide evidence that the cited tokens are often decision-relevant to the model's own prediction. These records are intended as audit signals rather than full human-grounded explanations.

---

## 论文详细总结（自动生成）

## 1. TLDR

本文研究了将现成大语言模型（LLM）用于能源工业控制系统（ICS）Modbus 流量入侵检测，通过 token 化与提示驱动，实现高精度二分类与可审计事件记录。实验显示在两个公开数据集上关键类召回高，性能可比强监督学习基线。  

**关键词**：ICS、Modbus、LLM、入侵检测、可解释性、审计记录  

---

## 2. 核心故事与贡献

### 问题与动机
- ICS Modbus 流量容易遭受网络攻击，如伪造数据注入（FDIA），需检测系统既准确又可审计（见正文 Section 1）。  
- 现有监督学习方法精度高，但依赖任务特定数据标注、特征工程和模型重训练，解释方法难以生成操作员可用的事件记录。

### 核心洞察
- 利用无需训练的通用 LLM，通过离散化 Modbus token 与提示词，可实现二分类 (normal/critical) 并生成可审计记录。  
- 提示词中嵌入工业控制知识（读写操作、流量节律、异常函数码）可指导 LLM 推理。

### 主要贡献
1. 提出基于提示词配置的 LLM ICS 分诊框架，无需任务特定训练即可进行 Modbus 入侵检测（见正文 Section 3）。  
2. 设计两阶段管道：预测 + 结构化审计记录生成，支持人工复核（见正文 Section 3.1）。  
3. 通过 sufficiency、necessity 和 counterfactual 干预式诊断验证解释质量，证据 token 与模型决策高度相关（见正文 Section 5）。  

---

## 3. 相关工作与定位

| 方法类别 | 数据特征 | 可解释性 | 部署方式 | 本文特点 |
|----------|----------|---------|---------|---------|
| 监督学习 ICS 检测（树/神经网络） | Modbus 特定特征 | SHAP/LIME 等后验解释 | 需特定训练数据 | LLM 无需训练即可部署，生成操作员可审计事件记录 |
| 可解释 AI 入侵检测（Industry 5.0） | 多类攻击 | 人类可理解解释 | 依赖模型特定诊断 | 本文提供 token 级决策相关性解释，偏向操作员审计 |
| LLM 安全辅助（日志分析、漏洞检测） | 自然语言 | 辅助建议 | 自然语言交互 | 本文直接对 Modbus token 分类，输出 JSON 告警 + 审计记录 |

---

## 4. 方法详解

### 数据处理与 token 化
- Modbus/TCP 每条通信编码为紧凑 token 字符串：方向(DIR)、功能码(FC)、单元 ID(UNIT)、载荷长度(LEN)、间隔时间(IAT)、异常(EX)（见正文 Section 3.3）。  
- 连续特征离散化，训练集拟合边界，验证/测试集复用，防止信息泄漏。

### LLM 提示配置
- 使用 GPT-4o，temperature=0，无任务特定训练。  
- 提示词包含三大原则：
  1. **Impact**：读操作通常正常，写控制操作风险高。  
  2. **Rhythm**：机器节律 vs 攻击突发流量。  
  3. **Protocol Compliance**：异常函数码立即判为 critical。  

### 二阶段管道
1. **预测阶段**：输出 normal/critical 标签及置信度 $s(x) \in [0,1]$。
   $$ f(x;\pi) = (\hat{y}(x), s(x)) $$
2. **审计阶段**：生成结构化 JSON，包含：
   - 关键证据 token  
   - 风险标签  
   - 可选反事实修改建议（what-if edits）  
- 预测固定，审计仅解释决策，不改变标签。

---

## 5. 实验分析

### 数据集与设置
- LeMay CSET’16 Modbus 数据集 & CIC Modbus 2023  
- 攻击行为和安全关键行为统一归入 critical 类  
- 与强监督基线共享训练/验证/测试划分（见正文 Section 4）

### 主实验结果
| 方法 | Accuracy | Critical Recall | Macro-F1 | 说明 |
|------|---------|----------------|----------|------|
| LLM 提示驱动 | ~0.98 | 高 | [见正文] | 无任务特定训练，few-shot prompt 配置 |
| 强监督基线 | ~0.98 | 高 | [见正文] | 需完整训练集、特征工程 |

- 核心发现：
  - LLM 提示驱动方法在关键类上召回高，与监督基线接近（见正文 Section 5）。  
  - 审计记录提供操作员可用证据 token，干预式诊断显示其与预测高度相关（见正文 Figure 4）。  

### 消融实验
- Prompt 中移除 ICS 原则或减少 few-shot 示例，性能下降 [需验证]。  
- 不使用审计阶段不会影响预测，但丧失可审计性。

---

## 6. 局限性与风险

- 系统仅为保守分诊层，不可自主决策或自动响应。  
- 解释属于 token 级决策相关性审计，非完全人类可理解解释。  
- 仅基于两个公开数据集离线验证，真实部署的隐私、API 安全与在线性能未评估。  
- 高误报可能增加操作员工作量和告警疲劳。

---

## 7. 复现与后续问题

### 复现重点
1. 离散化 token 的边界需从训练集拟合，验证/测试集复用（见正文 Section 3.3）。  
2. 提示词配置中嵌入 ICS 原则，few-shot 示例对预测效果关键。  
3. 审计阶段 JSON 输出需严格按标签固定决策生成，不影响预测。

### 后续研究方向
- 引入领域专家评估提升人类可理解解释。  
- 扩展到多类别攻击识别与实时部署，评估在线性能和隐私安全。

---

## 8. 直接证据与待验证项

1. LLM 在 LeMay/CIC 数据集上准确率约 0.98，critical 类召回高（见正文 Section 5）。  
2. 两阶段管道输出结构化审计记录，包括证据 token、风险标签与 what-if 建议（见正文 Section 3.1 / Figure 1）。  
3. 干预式诊断显示引用 token 与模型预测高度相关（见正文 Figure 4）。  
4. 系统无需任务特定权重更新，仅依靠 prompt 配置即可部署（见正文 Section 3.2）。  
5. [需验证] Prompt 中 ICS 原则对性能提升的具体量化影响。  

（完）
