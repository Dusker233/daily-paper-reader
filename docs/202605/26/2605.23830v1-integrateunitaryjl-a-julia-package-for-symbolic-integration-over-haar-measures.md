---
title: "IntegrateUnitary.jl: A Julia package for symbolic integration over Haar measures"
title_zh: IntegrateUnitary.jl：一个用于 Haar 测度符号积分的 Julia 软件包
authors: "Łukasz Pawela, Zbigniew Puchała"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23830v1"
score: 8.0
relevance_score: 0.0
quality_score: 8.5
reliability_score: 8.5
practicality_score: 7.0
evidence: Haar测度符号积分工具包
tldr: 论文提出 Julia 开源包 IntegrateUnitary.jl，用于对 Haar 测度下的紧群积分进行符号化计算，覆盖 U(d)、O(d)、Sp(d)、SU(d) 等群以及多类随机矩阵与张量网络场景。核心抓手是 Weingarten calculus、Wick contraction 与 Symbolics.jl 的深度融合，实现符号维度 d、渐近展开与高层 trace 表达自动化。作者强调其在高阶矩、量子信息指标与张量网络平均中的性能和可扩展性。若你关注量子信息、随机矩阵或符号计算基础设施，这篇值得细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23830v1/fig-001.webp\", \"caption\": \"Table 3. Benchmark results for ITensor network integration (Haar measure) showing scaling with degree 𝑘 and dimension 𝑑 . Timings exclude construction of the random ITensor constants and index objects.\", \"page\": 21, \"index\": 1, \"width\": 483, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2605.23830v1/fig-002.webp\", \"caption\": \"Table 1. Feature comparison of symbolic Haar integration packages. ✓ = fully supported, ∼a/∼b = partial support with caveat a/b, – = not supported.\", \"page\": 10, \"index\": 2, \"width\": 889, \"height\": 927}, {\"url\": \"assets/figures/arxiv/2605.23830v1/fig-003.webp\", \"caption\": \"Table 5. Performance comparison: IntegrateUnitary.jl vs. Haarpy (IntegrateUnitary.jl: fixed 𝑁 = 30 cold-cache samples; Haarpy: default 𝑁 = 30 with probe-based reduction to 𝑁 = 5 enabled, not triggered in this run; speedup =Haarpy / IntegrateUnitary.jl from row medians). Both packages were run on the same hardware; timings exclude JIT/import overhead.\", \"page\": 23, \"index\": 3, \"width\": 675, \"height\": 510}, {\"url\": \"assets/figures/arxiv/2605.23830v1/fig-004.webp\", \"caption\": \"Table 6. Performance comparison: IntegrateUnitary.jl vs. RTNI (Mathematica). Median runtime over repeated runs. All rows produce fully evaluated scalar results in both tools.\", \"page\": 23, \"index\": 4, \"width\": 835, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2605.23830v1/fig-005.webp\", \"caption\": \"Table 2. Benchmark results for symbolic vs. numeric integration. Times are median values over 30 samples.\", \"page\": 20, \"index\": 5, \"width\": 465, \"height\": 629}, {\"url\": \"assets/figures/arxiv/2605.23830v1/fig-006.webp\", \"caption\": \"Fig. 1. Architecture of IntegrateUnitary.jl. Scalar expressions follow the left path through normalization, substitution, expansion, and dispatch into the library lookup. Trace expressions are first analyzed by the symbolic trace logic, which reconstructs the Weingarten graph before entering the same dispatch stage. Common patterns are returned directly by the library lookup in O(1) time, while misses fall through to the specialized engines. Performance-critical internals (representation-theoretic quantities and Weingarten-related data) are memoized/cached for reuse.\", \"page\": 16, \"index\": 6, \"width\": 745, \"height\": 677}]"
motivation: 如何构建一个高性能、可扩展、支持符号维度的统一框架，用于 Haar 测度下紧群与相关随机矩阵模型的精确符号积分。
method: 通过在 Julia 中实现基于 Weingarten calculus、Wick contraction 与符号计算系统的统一积分框架，将高层 trace 多项式自动转换为组合图与指标收缩计算。
result: 论文展示了该包可处理高阶矩、trace-polynomial 积分、随机张量网络平均及渐近展开，并与 Haarpy、RTNI 做了功能与性能比较。作者还强调利用 Julia JIT 与缓存机制提升高次数积分效率，但当前节选文本未给出具体 benchmark 数值。
conclusion: 优先阅读 Section 3 与 Section 5，可快速理解软件架构、符号 trace 接口及 Weingarten 实现细节；随后看 Section 6 的 benchmark 与 Haarpy/RTNI 对比，判断其工程价值与性能优势是否成立。
key_findings: [支持统一处理多类紧群、Gaussian/Ginibre ensemble、随机纯态与 unitary t-design 等对象。, 通过 Symbolics.jl 实现符号维度 d 的原生支持，可自动得到渐近 Laurent 展开。, 提供 trace-level 高层接口与 ITensors.jl 集成，降低张量网络平均与量子信息推导门槛。]
limitations: 部分工作流仍要求具体整数维度；文中节选缺少详细 benchmark 数据与复杂度分析。
---

## 1. 问题与背景

论文关注的是如何对紧致群（尤其是酉群、正交群与辛群）上的 Haar 测度进行符号积分，并将其高效应用于量子信息与随机矩阵理论中的高阶矩计算。该问题长期以来是量子纠缠、随机量子线路、unitary t-design、随机张量网络等领域的基础工具，但实际计算往往受到 Weingarten calculus 组合复杂度和表示论计算成本的限制。现有工具虽然已经能自动化部分积分过程，但通常存在依赖专有软件、符号维度支持不足、难以处理高层 trace 表达式等问题，因此作者希望构建一个兼具开放性、性能和符号灵活性的统一 Julia 工具链。

## 2. 核心思路 / 方法

论文提出了 IntegrateUnitary.jl，这是一个基于 Julia 的 Haar 测度符号积分软件包，其核心建立在 Weingarten calculus 与 Wick contraction 的统一实现之上。系统支持对 \(U(d)\)、\(O(d)\)、\(Sp(d)\)、\(SU(d)\) 等紧致群以及 Gaussian ensemble、Ginibre ensemble、随机纯态、unitary t-design 等对象进行统一积分，并且将维度 \(d\) 原生视为符号变量，从而能够直接推导渐近展开与有理函数形式结果。

其关键创新之一是“symbolic trace logic”，用户可以直接输入 trace polynomial 等无指标表达式，系统自动重建对应的 Weingarten 图结构，而不再需要手工展开指标张量。论文还将 Symbolics.jl 与 ITensors.jl 深度结合，使随机张量网络的符号平均成为可能。此外，作者实现了 Murnaghan–Nakayama rule、symplectic-orthogonal duality 等底层算法，并通过缓存与 Julia 的 JIT 编译优化高阶矩计算性能。

## 3. 结果与结论

论文表明，该软件能够有效处理高阶 Haar 矩、trace polynomial 以及多类随机矩阵积分问题，并支持符号维度下的精确结果与 Laurent 渐近展开。作者强调，与已有 Mathematica 工具 RTNI 和 Python 工具 Haarpy 相比，IntegrateUnitary.jl 在符号灵活性、可扩展性以及与 Julia 数值生态的集成方面具有明显优势。

文中还指出，该系统能够自动化处理随机量子态、部分迹、随机张量网络平均等量子信息中的典型任务，并且对高阶矩计算具有较好的性能表现。不过从现有文本无法确认具体 benchmark 数值、运行时间提升比例以及实验规模细节。

## 4. 局限与适用边界

论文明确指出，部分工作流仍然依赖具体整数维度，而无法完全保持符号化，例如高阶纯 trace 矩 \(|\mathrm{tr}(U)|^{2k}\)（当 \(k>1\) 时）以及某些 SymbolicMatrix 输入下的 HCIZ 积分。作者还提到 Weingarten 函数在小维度下会出现极点或可去奇异性，因此符号计算需要额外化简机制。对于非常特殊或非平衡的积分结构，当前系统的适用范围从现有文本无法完全确认。

## Abstract
Symbolic integration over the Haar measure of compact groups is a computational cornerstone in quantum information science and random matrix theory. We present \texttt{IntegrateUnitary.jl}, a comprehensive Julia package for computing exact expectations of polynomial functions over a wide range of compact groups ($U(d)$, $O(d)$, $Sp(d)$, and $SU(d)$ for balanced polynomials), circular and Gaussian ensembles, Ginibre ensembles, permutation groups, random pure states, and unitary $t$-designs. The package provides a fully open-source implementation of the Weingarten calculus and Wick contractions with broad symbolic-$d$ support for entry-wise and trace-polynomial integrals, while selected workflows currently require concrete integer dimensions (including higher pure trace moments $|\mathrm{tr}(U)|^{2k}$ for $k > 1$ and HCIZ with \texttt{SymbolicMatrix} inputs, and direct matrix-valued integration of \texttt{SymbolicMatrix}/\texttt{SymbolicMatrixProduct} expressions), automatic asymptotic expansions, a high-level symbolic trace interface that reconstructs Weingarten graphs from index-free expressions, and a bridge to \texttt{ITensors.jl} for tensor network averaging. We discuss the underlying algorithms, including the Murnaghan-Nakayama rule and symplectic-orthogonal duality, and demonstrate that the package efficiently handles high-degree moments and quantum information metrics.

---

## 论文详细总结（自动生成）

# 1. TLDR

论文提出了一个面向 Haar 测度符号积分的 Julia 开源工具包 ，统一支持 \(U(d)\)、\(O(d)\)、\(Sp(d)\)、\(SU(d)\) 等紧群，以及 Gaussian/Ginibre ensemble、随机纯态、unitary \(t\)-design 等对象的精确积分计算。其核心价值在于：把 Weingarten calculus、Wick contraction 与 Julia 的 Symbolics.jl 深度融合，实现“符号维度 \(d\)”下的高阶矩、trace polynomial、张量网络平均与渐近展开自动化。

相比 RTNI（Mathematica）与 Haarpy（Python），该工作强调三点：原生 symbolic-\(d\) 支持、高层 trace interface 自动重建 Weingarten graph、以及与 ITensors.jl 的张量网络集成，从而把原本高度手工化的 Haar 积分流程工程化、统一化。

**关键词：** Haar measure、Weingarten calculus、Julia、Symbolic computation、Random matrix theory、Quantum information、Tensor network、Unitary \(t\)-design

---

# 2. 核心故事与贡献

## 2.1 问题与动机

Haar 测度积分是量子信息与随机矩阵理论中的基础算子，广泛用于：

- 随机量子线路；
- Page entropy；
- unitary \(t\)-design；
- 随机张量网络；
- 高阶矩与 scrambling 分析。

但现实中存在三个核心痛点：

1. **Weingarten calculus 组合复杂度极高**  
   积分需要对 permutation / pair partition 求和（Section 2）。

2. **现有工具生态割裂**
   - RTNI 依赖 Mathematica；
   - Haarpy 偏 Python symbolic workflow；
   - 很少有工具支持“原生 symbolic dimension”。

3. **trace-level 表达不友好**
   研究者往往需要手工把
   \[
   \mathrm{tr}(UAU^\dagger B)
   \]
   展开成 index contraction，再送入 Weingarten machinery，过程非常易错（Introduction）。

---

## 2.2 核心洞察

作者的核心洞察是：

> Haar 积分真正难的不是公式本身，而是“从高层数学表达自动降解为组合 contraction graph”的工程链条。

因此论文不是只实现 Weingarten function，而是构建了完整 symbolic integration pipeline：

- symbolic trace parsing；
- graph reconstruction；
- permutation / pair-partition contraction；
- asymptotic Laurent expansion；
- tensor-network averaging。

这使 Haar integration 从“表示论技巧”变成“可编程基础设施”。

---

## 2.3 主要贡献

### （1）统一支持多类 Haar/随机矩阵积分对象

支持对象包括（Section 3.1）：

- \(U(d)\)
- \(SU(d)\)
- \(O(d)\)
- \(Sp(d)\)
- Circular ensemble
- GUE/GOE/GSE
- Ginibre ensemble
- permutation group
- random pure states
- unitary \(t\)-design

这一覆盖面明显超出现有多数工具。

---

### （2）原生 symbolic-\(d\) 支持

论文强调：

\[
\mathrm{Wg}_U(\sigma,d)
\]
是 \(d\) 的有理函数，因此可以：

- 保持 exact symbolic result；
- 自动做 Laurent asymptotic expansion；
- 推导 scaling law。

这是 IntegrateUnitary.jl 与 prior work 的重要差异（Introduction, Section 2.1）。

---

### （3）高层 trace interface

用户可直接输入：

\[
\mathrm{tr}(UAU^\dagger B)
\]

系统自动：

- 重建 Weingarten graph；
- 转换为 contraction；
- 执行积分。

作者将其称为：

> symbolic trace logic（Introduction）

这是论文最重要的软件工程创新之一。

---

### （4）ITensors.jl 集成

IntegrateUnitary.jl 与  集成，可直接做：

- 随机 tensor network averaging；
- quantum circuit averaging；
- symbolic tensor contraction。

见 Figure 1 与 Table 3。

---

# 3. 相关工作与定位

## 3.1 与已有工具对比

根据 Table 1（Feature comparison）：

| 维度 | IntegrateUnitary.jl | RTNI | Haarpy |
|---|---|---|---|
| 开源 | 是 | 部分依赖 Mathematica | 是 |
| Julia 原生 | 是 | 否 | 否 |
| symbolic-\(d\) | 强支持 | 部分 | 部分 |
| trace-level 输入 | 支持 | 有限 | 有限 |
| ITensor integration | 支持 | 不支持 | 不支持 |
| asymptotic expansion | 自动支持 | [需验证] | 有限 |
| 多 ensemble 支持 | 广 | 中 | 中 |
| tensor network averaging | 支持 | 支持部分 workflow | 有限 |
| Weingarten engine | Julia 原生 | Mathematica backend | Python symbolic |

---

## 3.2 与 RTNI 的差异

RTNI 更偏：

- tensor network contraction；
- Mathematica symbolic ecosystem。

而 IntegrateUnitary.jl：

- 更强调 symbolic dimension；
- 更强调 trace-polynomial workflow；
- 更适配 Julia HPC/JIT 生态。

见 Introduction 与 Table 1。

---

## 3.3 与 Haarpy 的差异

Haarpy 更偏：

- Python 可访问性；
- compact group integration。

IntegrateUnitary.jl 则进一步扩展到：

- asymptotic symbolic expansion；
- ITensor integration；
- symbolic trace reconstruction；
- partial trace subsystem support。

---

# 4. 方法详解

## 4.1 整体架构

Figure 1 给出了整体 pipeline。



核心流程：

```text
Symbolic expression
    ↓
Normalization / expansion
    ↓
Trace logic reconstruction
    ↓
Weingarten graph
    ↓
Permutation / pairing engine
    ↓
Cached symbolic result
```

作者特别强调：

- representation-theoretic quantity memoization；
- Weingarten cache；
- dispatch-based backend。

见 Figure 1。

---

## 4.2 Unitary Weingarten calculus

核心公式（Section 2.1）：

\[
\int_{U(d)}
U_{i_1j_1}\cdots U_{i_kj_k}
\bar U_{i'_1j'_1}\cdots \bar U_{i'_kj'_k}\, dU
=
\sum_{\sigma,\tau\in S_k}
\delta_\sigma
\delta_\tau
\mathrm{Wg}_U(\sigma\tau^{-1},d)
\]

其中：

- permutation 来自 symmetric group \(S_k\)；
- Weingarten function 是核心权重。

作者进一步使用 character expansion：

\[
\mathrm{Wg}_U(\sigma,d)
=
\frac{1}{(k!)^2}
\sum_{\lambda\vdash k}
\frac{(f^\lambda)^2\chi^\lambda(\sigma)}
{s_\lambda(1^d)}
\]

见 Section 2.1。

---

## 4.3 Orthogonal / Symplectic engine

对于：

- \(O(d)\)
- \(Sp(d)\)

系统使用 pair partition representation。

关键优化：

\[
\mathrm{Wg}_{Sp}(p,q,d)
=
(-1)^{\mathrm{loops}(p,q)}
\mathrm{Wg}_O(p,q,-d)
\]

即：

> symplectic-orthogonal duality

从而：

- 复用 orthogonal engine；
- 避免单独实现完整 symplectic backend。

见公式 (6)。

---

## 4.4 Symbolic trace logic

这是论文核心工程模块。

输入：

\[
\mathrm{tr}(UAU^\dagger B)
\]

系统自动：

1. 解析 trace graph；
2. 重建 index contraction；
3. 生成 permutation structure；
4. 调用 Weingarten backend。

因此用户无需：

- 手工 index expansion；
- 手工 delta contraction。

这是“高层 symbolic interface”的关键。

---

## 4.5 Symbolic dimension 与渐近展开

作者强调：

\[
\mathrm{Wg}(\sigma,d)
\]
是 \(d\) 的有理函数。

因此系统支持：

- exact symbolic integration；
- Laurent series；
- asymptotic scaling law。

这是区别于“仅 numeric evaluation”的重要点。

---

# 5. 实验分析

## 5.1 架构与功能验证

Figure 1 展示了系统架构与 dispatch flow。

核心发现：

- trace expression 与 scalar expression 走不同 preprocessing pipeline；
- 高频 pattern 可直接 O(1) cache 返回；
- representation-theoretic quantity 被 memoize。

见 Figure 1。

---

## 5.2 Symbolic vs Numeric benchmark

Table 2：



论文比较：

- symbolic-\(d\)
- fixed numeric dimension

的运行时间差异。

核心目标是证明：

> symbolic support 不会导致不可接受的性能退化。

但节选文本未包含具体 benchmark 数值。[需验证]

---

## 5.3 ITensor network averaging

Table 3：

- 测试 Haar random tensor network averaging；
- 比较不同 degree \(k\) 与 dimension \(d\)；
- timing 不包含 ITensor object construction。

作者强调其能够处理：

- 大规模 tensor contraction；
- symbolic tensor averaging。

见 Table 3。

---

## 5.4 与 Haarpy 的性能比较

Table 5：



作者报告：

- IntegrateUnitary.jl 与 Haarpy 在同硬件比较；
- 去除了 JIT/import overhead；
- 使用 repeated median runtime。

表中存在：

\[
\text{speedup}=
\frac{\text{Haarpy runtime}}{\text{IntegrateUnitary runtime}}
\]

但具体倍数在节选中未出现。[需验证]

---

## 5.5 与 RTNI 的比较

Table 6：



论文说明：

- 两边均输出 fully evaluated scalar result；
- 使用 median runtime。

实验目的：

- 验证 Julia implementation 的工程竞争力；
- 说明不依赖 Mathematica 也能获得高性能。

但未给出完整 benchmark 数值。[需验证]

---

# 6. 局限性与风险

## 6.1 部分 workflow 仍需 concrete dimension

论文明确指出以下情况无法完全 symbolic 化：

- higher pure trace moments
  \[
  |\mathrm{tr}(U)|^{2k}
  \]
- HCIZ with SymbolicMatrix；
- 某些 matrix-valued integration。

见 Abstract。

---

## 6.2 小维度极点问题

Weingarten function 在：

\[
d < k
\]

附近会出现 pole / removable singularity。

虽然 evaluate() 会自动化简，但：

- 数值稳定性；
- 特殊维度行为；

仍可能复杂。

见 Section 2.1。

---

## 6.3 非 stable-range SU(d) 情况未完全覆盖

作者指出：

- 当前主要支持 balanced polynomial；
- epsilon-tensor contraction effect 未完全支持。

因此某些小维度 SU(d) 结构可能失效。

---

## 6.4 高阶组合复杂度仍存在

虽然：

- cache；
- JIT；
- memoization；

显著优化了性能，但 permutation growth 本身仍是 factorial 级。

因此超高阶 moment 可能仍不可扩展。[需验证]

---

# 7. 复现与后续问题

## 7.1 复现重点

### （1）Symbolics.jl symbolic-\(d\) pipeline

重点关注：

- rational function simplification；
- removable singularity handling；
- Laurent expansion implementation。

---

### （2）Weingarten cache 机制

Figure 1 提示：

- representation quantity；
- Weingarten data；

被 memoize。

复现时应重点验证：

- cache key；
- permutation canonicalization；
- symbolic expression reuse。

---

### （3）trace reconstruction engine

这是系统最核心的工程模块：

```text
trace polynomial
→ graph reconstruction
→ contraction graph
→ permutation dispatch
```

应重点检查：

- graph encoding；
- contraction ordering；
- symbolic simplification strategy。

---

## 7.2 后续研究方向

### 方向一：GPU / distributed symbolic integration

当前主要依赖：

- Julia JIT；
- CPU symbolic workflow。

未来可探索：

- GPU contraction；
- distributed Weingarten evaluation。

---

### 方向二：非平衡 SU(d) 与 epsilon tensor

论文已指出：

- non-stable-range effect 尚未完整覆盖。

后续可扩展：

- Levi-Civita contraction；
- baryonic invariant；
- gauge-theoretic integral。

---

# 8. 直接证据与待验证项

1. **统一支持多类 compact group 与 ensemble**  
   论文 Abstract 明确列出：
   - \(U(d)\)
   - \(O(d)\)
   - \(Sp(d)\)
   - \(SU(d)\)
   - Gaussian/Ginibre ensemble
   - random pure states
   - unitary \(t\)-design。  
   见 Abstract / Section 3.1。

2. **symbolic trace logic 自动重建 Weingarten graph**  
   Figure 1 caption 明确写道：
   > “Trace expressions are first analyzed by the symbolic trace logic, which reconstructs the Weingarten graph...”  
   见 Figure 1。

3. **symplectic-orthogonal duality 被用于统一实现**  
   公式：
   \[
   \mathrm{Wg}_{Sp}(p,q,d)
   =
   (-1)^{\mathrm{loops}(p,q)}
   \mathrm{Wg}_{O}(p,q,-d)
   \]
   见 Section 2.2 / Equation (6)。

4. **部分 symbolic workflow 仍需 integer dimension**  
   Abstract 明确指出：
   - higher pure trace moments；
   - HCIZ with SymbolicMatrix；
   - direct matrix-valued integration；
   仍需 concrete integer dimension。

5. **[需验证] 性能优势的具体倍数**  
   Table 5 与 Table 6 给出了：
   - Haarpy comparison；
   - RTNI comparison；
   但当前节选未包含具体 runtime/speedup 数值，因此无法确认实际性能提升幅度。

（完）
