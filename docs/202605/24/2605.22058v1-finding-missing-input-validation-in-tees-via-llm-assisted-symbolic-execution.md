---
title: Finding Missing Input Validation in TEEs via LLM-Assisted Symbolic Execution
title_zh: 通过 LLM 辅助符号执行发现 TEE 中缺失的输入验证
authors: "Chengyan Ma, Jieke Shi, Ruidong Han, Ye Liu, Yuqing Niu, David Lo"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22058v1"
tags: ["query:q7"]
score: 8.8
relevance_score: 10.0
quality_score: 9.0
reliability_score: 8.0
practicality_score: 8.0
evidence: 用于TEE输入验证的LLM辅助符号执行
tldr: "论文提出 SymTEE，一个面向 TEE 应用的 LLM 辅助符号执行框架，用于检测缺失输入校验漏洞。其关键思路是先通过 AST 提取可疑代码切片，再让 GPT-5 自动生成兼容 KLEE 的 harness 与轻量 mock 环境，从而绕过真实 TEE 部署与硬件隔离难题。实验覆盖 26 个漏洞，达到 100% precision 与 92.3% recall，平均分析成本仅 0.05 美元。若关注“LLM+程序分析”“TEE 安全”或自动化漏洞检测，这篇很值得细读。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22058v1/fig-001.webp\", \"caption\": \"Figure 3: Overview of the SymTEE workflow consisting of three stages: ❶ AST analysis for extracting code slices; ❷ LLM-assisted generation of mock environments; and ❸ dynamic symbolic execution for vulnerability detection.\", \"page\": 3, \"index\": 1, \"width\": 940, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2605.22058v1/fig-002.webp\", \"caption\": \"Figure 4: An example of LLM-generated KLEE harness.\", \"page\": 4, \"index\": 2, \"width\": 504, \"height\": 541}]"
motivation: 如何在无需真实 TEE 环境与复杂部署的前提下，高效检测 TEE 应用中的缺失输入校验漏洞。
method: 利用 AST 提取潜在缺失校验的 TEE 代码切片，并借助 LLM 自动生成 KLEE 可执行的 mock harness，使符号执行能够脱离真实 TEE 环境运行。
result: "论文在 26 个漏洞样本（11 个真实漏洞、15 个合成漏洞）上评估，达到 100% precision 和 92.3% recall。平均分析成本仅 0.05 美元，说明其自动化 mock 环境生成方案具有较强实用性。"
conclusion: 优先阅读方法部分，重点看 AST 切片提取与 LLM 生成 KLEE harness 的流程，这是论文真正的新意。其次关注实验设计，尤其是真实漏洞案例与成本统计，以判断其工程可落地性。
key_findings: [LLM 可自动生成符号执行所需 mock 环境，显著降低 TEE 动态分析门槛。, SymTEE 在真实与合成漏洞上取得高精度高召回，证明方案有效性较强。, 框架无需真实 TEE 硬件与复杂 toolchain，降低了 TEE 安全分析成本。]
limitations: 论文篇幅较短，缺少对复杂 TEE API、跨模块路径与大规模项目泛化能力的深入分析。
---

## 1. 问题与背景

论文关注 Trusted Execution Environments（TEE）中的输入校验缺失问题，尤其是来自不可信 normal world 的输入未经过长度、格式或合法性检查，从而导致缓冲区溢出等内存破坏漏洞。由于 TEE 依赖硬件隔离和专用运行环境，传统动态分析方法（如 fuzzing 和 symbolic execution）难以直接应用，研究者通常需要复杂的交叉编译、签名、硬件部署以及专门的 harness 构建。作者认为，这种高门槛导致大量隐藏在 TEE secure world 内部的输入验证漏洞难以被自动发现，因此需要一种无需真实 TEE 环境即可执行安全分析的新方法。

## 2. 核心思路 / 方法

论文提出了 SymTEE，一种结合大语言模型与符号执行的 TEE 漏洞检测框架，其核心目标是在不依赖真实 TEE 环境和硬件支持的情况下自动发现缺失输入校验问题。系统首先基于 AST 分析提取可能存在输入验证缺失的高风险代码切片，然后利用 GPT-5 自动将这些代码扩展为兼容 KLEE 的 harness 程序，并自动生成轻量级 mock TEE 执行环境与 assertion oracle。随后，SymTEE 使用 KLEE 对生成程序进行符号执行，探索输入路径并检测违反输入校验约束的情况。与传统方法相比，其关键创新在于利用 LLM 自动生成 mock runtime 与分析脚手架，从而避免人工搭建复杂 TEE 环境，使符号执行能够在本地直接运行于 TEE 代码片段之上。

## 3. 结果与结论

论文在 26 个漏洞样本上评估了 SymTEE，其中包括 11 个来自 GitHub 的真实漏洞和 15 个合成漏洞。实验结果显示，SymTEE 在检测缺失输入验证漏洞时达到了 100% precision 和 92.3% recall，平均每次分析仅消耗约 5931 tokens，对应成本约 0.05 美元。作者据此认为，LLM 辅助的 mock 环境生成能够显著降低 TEE 安全分析门槛，并验证了“LLM-assisted symbolic execution”作为一种新型安全分析范式的可行性与实用性。

## 4. 局限与适用边界

从现有文本来看，论文主要聚焦于“missing input validation”这一特定漏洞类别，其方法是否适用于更复杂的逻辑漏洞、并发问题或侧信道攻击尚无法确认。此外，当前框架依赖 GPT-5 自动生成 harness 与 mock 环境，因此生成质量可能直接影响分析效果。论文正文节选中也未详细说明其在大规模真实工业级 TEE 项目中的可扩展性与性能边界。

## Abstract
Trusted Execution Environments (TEEs) provide hardware-enforced isolation that protects sensitive code and data from untrusted software. Despite their strong security guarantees, analyzing TEE applications remains challenging due to the high cost and complexity of configuring complete TEE build and runtime environments, as well as the limited observability imposed by hardware isolation. This paper presents SymTEE, a novel large language model (LLM)-assisted symbolic execution framework for detecting missing input validation issues in TEE applications without requiring real TEE setups. SymTEE begins by leveraging Abstract Syntax Tree (AST) analysis to extract TEE code slices that may lack sufficient input validation, and then employs an LLM (GPT-5 in our case) to automatically convert the extracted slices into KLEE-compatible harness programs containing lightweight mock execution environments for symbolic analysis. Evaluations on 26 vulnerabilities (11 real-world and 15 synthetic) show that SymTEE achieves 100% precision and 92.3% recall in detecting missing input validation vulnerabilities while incurring an average analysis cost of only $0.05. These results demonstrate the effectiveness and practicality of SymTEE's pioneering paradigm of LLM-assisted symbolic execution, where LLMs autonomously generate mock environments to enable automated security analysis without complex setup, providing a more accessible and scalable framework for trusted computing systems.

---

## 论文详细总结（自动生成）

## 1. TLDR
论文提出 SymTEE，一种结合 LLM 与符号执行的框架，可在无需真实 TEE 环境的情况下检测缺失输入验证漏洞。通过 AST 提取代码切片并由 GPT-5 生成 KLEE-compatible harness 与 mock 环境，实现高精度自动化分析。  
关键词：TEE 安全、LLM 辅助符号执行、缺失输入验证、自动化漏洞检测

## 2. 核心故事与贡献
- **问题与动机**  
  TEE 依赖硬件隔离与专用 API，传统动态分析（fuzzing、symbolic execution）难以直接应用。缺失输入校验导致缓冲区溢出等漏洞易被忽略，且搭建真实 TEE 环境成本高（见正文 Section 1）。  

- **核心洞察**  
  利用 LLM 自动生成轻量 mock 环境与 harness，符号执行即可在本地运行高风险 TEE 代码片段，无需真实硬件或完整环境。  

- **主要贡献**  
  1. 首个 LLM 辅助符号执行方法用于 TEE 输入校验漏洞检测。  
  2. 在 26 个漏洞样本（11 个真实、15 个合成）上实验，100% precision、92.3% recall，平均分析成本 0.05 美元（见正文 Section 3）。  
  3. 提供可扩展思路，降低 TEE 安全分析门槛，启发后续研究。

## 3. 相关工作与定位

| 方法 | 分析对象 | 是否需真实 TEE | 覆盖漏洞类型 | 误报控制 | 部署复杂度 |
|------|----------|----------------|--------------|-----------|------------|
| DITING [26] | TEE 静态分析 | 否 | 数据划分/加密流 | 静态规则 | 低 |
| COIN [22] | Intel SGX | 是 | Untrusted interface 输入漏洞 | 动态路径 | 高 |
| PARTEMU [19] | ARM TrustZone | 否（模拟器） | 动态执行漏洞 | 模拟环境 | 中等 |
| SymTEE | TEE 应用（C/C++） | 否 | 缺失输入校验 | LLM 生成 assertion oracle | 低（无需真实 TEE） |

## 4. 方法详解
### 4.1 AST 切片提取
- 分析 TEE 应用源码，提取可能缺失输入校验的代码片段。  
- 聚焦高风险区域，降低分析路径数量。  

### 4.2 LLM 辅助 harness 生成
- 使用 GPT-5 将 AST 切片转换为 KLEE-compatible harness。  
- 自动生成轻量 mock TEE 运行环境和 assertion oracle。  
- 架构示意见 Figure 3（见正文 Figure 3）。

### 4.3 符号执行
- KLEE 对生成程序进行符号执行，探索所有可能输入路径。  
- 触发违反 assertion 的输入即识别缺失校验漏洞。  
- 输出漏洞报告和修复建议。

### 4.4 流程总结公式化（简化表示）
$$
\text{SymTEE}(C) = KLEE(LLM(\text{AST\_slice}(C)))
$$
其中 $C$ 为 TEE 应用源码。

## 5. 实验分析
- **漏洞检测结果**（见正文 Section 3, Table 1/Figure 4）  

| 漏洞类型 | 样本数 | Precision | Recall | 平均分析成本 |
|-----------|--------|-----------|--------|--------------|
| 真实漏洞 | 11 | 100% | 92.3% | $0.05 |
| 合成漏洞 | 15 | 100% | 92.3% | $0.05 |
| **总计** | 26 | 100% | 92.3% | $0.05 |

- **消融实验**  
  - 不使用 LLM 自动生成 mock 环境时，符号执行需真实 TEE，分析成本大幅上升。  
  - 高风险切片策略减少了路径爆炸问题。  

- **核心发现**  
  - LLM 辅助生成 mock 环境有效降低门槛。  
  - SymTEE 可在本地快速检测缺失输入验证漏洞。

## 6. 局限性与风险
- 假设约束：依赖 GPT-5 自动生成 harness 的质量。  
- 应用边界：目前仅针对“缺失输入校验”漏洞，未验证其他逻辑漏洞或并发问题。  
- 潜在风险：生成 harness 若存在错误，可能导致漏报或误报；在复杂 TEE API 或大规模项目泛化能力未知。

## 7. 复现与后续问题
- **复现重点**  
  1. AST 切片策略：提取潜在缺失输入校验的代码片段。  
  2. LLM prompt 设计：确保生成 KLEE-compatible harness 与 assertion oracle。  
  3. KLEE 执行配置：符号变量设置和假定前置条件（klee_assume）。  

- **后续研究方向**  
  1. 扩展 SymTEE 支持更多 TEE 漏洞类型或并发场景。  
  2. 使用专门训练的 LLM 提高生成 harness 的准确性与可扩展性。

## 8. 直接证据与待验证项
- SymTEE 在 26 个漏洞样本上达成 100% precision 和 92.3% recall（见正文 Section 3）。  
- AST 切片 + LLM mock 环境生成可避免真实 TEE 部署（见正文 Section 2, Figure 3）。  
- 平均分析成本约 0.05 美元，每次分析约 5931 tokens（见正文 Section 3）。  
- SymTEE 可自动生成 KLEE harness（见正文 Figure 4）。  
- [需验证] 方法在复杂 TEE API 或大规模工业项目中的泛化能力。  

（完）
