---
title: "PocketAgents: A Manifest-Driven Library of Autonomous Defense Agents"
title_zh: PocketAgents：一种基于清单驱动的自治防御代理库
authors: "Sidnei Barbieri, Ágney Lopes Roth Ferraz, Lourenço Alves Pereira Júnior"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21694v1"
tags: ["query:q9"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 基于LLM的自主防御代理用于网络安全
tldr: 论文提出 PocketAgents，一种面向网络防御的“manifest 驱动”自治 Agent 库框架，核心目标是让 LLM 参与安全响应时仍保持可验证、可审计、可约束。其方法通过 manifest、prompt、runtime context 三文件定义 Agent，并以类型化边界隔离推理与执行。作者在 Perry 网络攻防测试床上复现 DarkSide 风格攻击，18 次闭环实验中 13 次成功触发有效封禁动作。论文更偏系统架构与安全边界设计，而非模型能力提升，值得关注 LLM Agent 安全治理方向的读者精读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21694v1/fig-001.webp\", \"caption\": \"Fig. 1. PocketAgents runtime. Data-only agents cross a typed boundary before any action is subject to enforcement.\", \"page\": 3, \"index\": 1, \"width\": 1060, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.21694v1/fig-002.webp\", \"caption\": \"TABLE I OUTCOME CLASSES PRODUCED BY THE POCKETAGENTS BOUNDARY.\", \"page\": 3, \"index\": 2, \"width\": 523, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.21694v1/fig-003.webp\", \"caption\": \"TABLE IV MECHANISMS DEMONSTRATED BY THE CURRENT ARTIFACT.\", \"page\": 5, \"index\": 3, \"width\": 522, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2605.21694v1/fig-004.webp\", \"caption\": \"TABLE III OUTCOMES FOR THE 18 EVALUABLE PERRY TRIALS.\", \"page\": 4, \"index\": 4, \"width\": 522, \"height\": 248}]"
motivation: 如何在允许 LLM 自主参与网络防御调查的同时，建立一个可验证、可控且不会直接失控执行防御动作的运行时边界。
method: 通过 manifest 定义 Agent 可观测范围、允许动作与输出 schema，并让所有 LLM 输出先经过共享运行时的类型校验与策略验证，再进入实际防御执行链路。
result: 作者在 Perry cyber arena 中部署了 C2 与数据外传两个 Agent，进行了 18 次 DarkSide 风格闭环攻击实验，其中 13 次生成了通过验证的网络阻断动作并成功遏制攻击。另有 4 次因 schema 校验失败被拒绝执行，体现了类型化边界的约束作用。
conclusion: 优先阅读 System Model and Architecture 与实验部分，前者解释 manifest/runtime/validation 的边界设计，后者展示“LLM 推理—结构化报告—执行拦截”链路如何实现可审计自治防御。若关注 Agent 安全，可重点看其 threat model 与 enforcement boundary 设计。
key_findings: [LLM 不直接调用防御工具，而是只能输出受 schema 约束的结构化报告。, 统一运行时允许不同战术 Agent 共享同一验证与执行框架，提高可扩展性。, 失败案例被明确归因为 schema 校验失败而非黑盒模型错误，提升了系统可解释性。]
limitations: 实验规模较小，仅验证两类 Agent 与 18 次攻击场景，且未评估 prompt injection、日志投毒、模型供应链等现实安全问题。
---

## 1. 问题与背景
企业安全防御面临响应瓶颈，SOC需要处理大量告警且人工响应滞后，导致真实攻击可能快速扩散。虽然大语言模型可以分析遥测数据并给出建议，但其输出若直接驱动防御动作存在安全风险。核心问题在于：如何让LLM参与自动化防御，同时保证其输出可控、可验证且不会直接破坏系统。该问题重要，因为错误或不受控的自动响应可能比攻击本身更具破坏性。

## 2. 核心思路 / 方法
论文提出PocketAgents，一种基于manifest驱动的自主防御代理库，将每个代理封装为manifest、prompt和context三个数据文件，通过统一运行时执行。系统将LLM视为不可信推理组件，其输出必须通过“类型化边界”验证，包括结构合法性、动作是否在许可列表中，以及（设计上）是否基于实际观测到的实体。整体架构将调查（LLM）、验证（确定性规则）和执行（适配器）解耦，确保任何防御动作必须经过严格校验。创新点在于将LLM防御行为抽象为可复用、可比较的“代理单元”，并通过数据驱动方式扩展，而非修改系统代码。

## 3. 结果与结论
在基于Perry网络攻防测试环境的18次闭环实验中，13次成功生成有效阻断动作并遏制攻击，4次因schema验证失败被拒绝，1次产生有效但无需动作的决策。结果表明类型化边界能够将LLM输出转化为可测量、可归因的行为，并区分不同失败类型。论文结论认为，该架构使LLM驱动防御具备可扩展性与可审计性，是实现安全自动化的重要基础。

## 4. 局限与适用边界
论文未评估提示注入、数据污染、模型供应链攻击等现实安全问题，实验范围仅限受控环境与特定攻击场景。部分设计（如实体grounding）尚未在运行时完全强制执行。适用边界主要在防御侧受控环境中，对真实复杂生产环境的泛化能力从现有文本无法确认。

## Abstract
Connecting large language models (LLMs) to defensive enforcement requires more than asking a model whether an attack is happening. A defender must decide which model outputs may change the system state, which outputs must be rejected, and how failures should be recorded. We present PocketAgents, a manifest-driven library of autonomous defense agents. Each agent is installed as three data files: a manifest, a prompt, and a runtime context. The shared runtime gives the agent bounded telemetry access and accepts only typed reports whose requested action appears in the manifest. We implemented PocketAgents on top of a cyber arena (Perry), a cyber-deception testbed, and evaluated two agents, Command and Control and Exfiltration, in 18 closed-loop trials of a DarkSide-inspired attack on a small enterprise topology. Thirteen trials produced validated network-block actions and contained the attack; four failed schema validation; one produced a valid no-action decision. The experiments show that a typed boundary makes LLM-driven defense measurable, extensible, and attributable.

---

## 论文详细总结（自动生成）

# 1. TLDR

《PocketAgents》提出了一种面向网络安全自治防御的“manifest 驱动”Agent 架构：LLM 不直接执行防御动作，而是输出经过 schema 约束的结构化报告，由统一运行时完成验证与执行隔离。论文核心贡献不在于提升检测能力，而在于构建“可验证、可归因、可扩展”的 Agent enforcement boundary。

作者在 Perry cyber arena 上实现了两个 Agent（C2 与 Exfiltration），针对 DarkSide 风格攻击进行了 18 次闭环实验，其中 13 次成功触发合法封禁动作，4 次被 schema 校验拒绝，1 次输出合法 no-action，展示了类型化边界在安全自动化中的治理价值。

**关键词：** LLM Agent、安全边界、Manifest Runtime、Typed Enforcement、Autonomous Defense、SOC Automation

---

# 2. 核心故事与贡献

## 2.1 问题与动机：LLM 能“分析”，但不能直接“执法”

论文切入点非常明确：

- SOC（Security Operations Center）存在严重响应瓶颈；
- LLM 虽然能够理解攻击行为，但若直接调用执行工具，会带来高风险；
- 真正困难的问题不是“识别攻击”，而是：

> 哪些模型输出允许改变系统状态（state-changing action）？

作者明确指出：

- “block suspicious host” 这种自然语言不能直接变成网络封禁；
- 防御系统必须：
  - 校验输出是否合法；
  - 判断 action 是否被允许；
  - 记录失败原因；
  - 保证 enforcement 可追责。

论文强调其核心问题不是“模型能力”，而是：

> 如何建立一个安全的 LLM→Enforcement 边界。

见 Introduction / Section II。

---

## 2.2 核心洞察：把 Agent 当成“受约束调查器”

论文最大的 conceptual shift：

传统 LLM-for-SOC：

- 关注“模型是否能识别攻击”

PocketAgents：

- 关注“调查结果是否能安全跨越 enforcement boundary”

作者重新定义 Agent：

> Agent ≠ 自由工具调用程序  
> Agent = 受 manifest、schema、context 约束的 investigation unit

核心思想：

- LLM 是“不可信推理组件”
- Runtime 才是可信 enforcement controller

因此：

- LLM 只负责 investigation
- Runtime 负责 validation
- Adapter 负责 scoped enforcement

见 Figure 1 / Section II。

---

## 2.3 主要贡献

### （1）提出 manifest-driven autonomous defense runtime

每个 Agent 由三类静态文件构成：

- manifest
- prompt
- runtime context

无需修改框架代码即可安装新 Agent。

见 Figure 1 / Section II。

---

### （2）提出 typed enforcement boundary

核心 admission predicate：

\[
F_M(R,T)=F_{struct}(R,\Sigma)\land[action(R)\in A]\land[entities(R)\subseteq E(T)]
\]

其中：

- \(R\)：Agent report
- \(\Sigma\)：schema
- \(A\)：允许动作集合
- \(E(T)\)：telemetry 中出现的实体集合

其本质是：

> “模型输出必须同时满足结构正确、动作允许、实体可追溯”

见 Equation (1)。

---

### （3）把失败从“黑盒错误”变成“可归因错误”

实验中：

- 13 次 valid block
- 4 次 schema fail
- 1 次 valid no-action

作者强调：

> schema fail 是 runtime reject，而不是 silent failure。

这意味着：

- 系统知道问题来自 parser/prompt；
- 而不是错误执行网络封禁。

见 Table I / Table III。

---

# 3. 相关工作与定位

## 3.1 与传统 LLM-for-SOC 的区别

| 维度 | 传统 LLM 安全工作 | PocketAgents |
|---|---|---|
| 目标 | 攻击识别/分析 | 安全 enforcement |
| 输出 | 自然语言 | Typed report |
| Tool access | 常直接调用 | 不允许直接调用 |
| Enforcement | 弱约束 | Manifest allowlist |
| Failure handling | 黑盒 | 可归因 |
| Runtime validation | 很少 | 核心机制 |
| Telemetry access | 通常开放 | bounded evidence |
| Extension方式 | 改代码 | 数据文件安装 |

---

## 3.2 与 Agent 安全工作关系

论文与以下方向最接近：

- AgentSpec（runtime enforcement）
- IsolateGPT（execution isolation）
- Progent（privilege control）

但差异在于：

| 工作 | 关注点 |
|---|---|
| AgentSpec | 通用 agent runtime enforcement |
| IsolateGPT | 执行隔离 |
| Progent | 权限控制 |
| PocketAgents | SOC autonomous defense + typed enforcement |

见 Related Work references [19][20][21]。

---

## 3.3 与 offensive-agent 工作区别

论文明确区分：

- PentestGPT
- AUTOATTACKER
- autonomous offensive agents

这些工作研究：

> “LLM 如何自主攻击”

而 PocketAgents 研究：

> “LLM 如何被限制后参与防御”

这是明显不同的 threat posture。

见 references [16][17][18]。

---

# 4. 方法详解

## 4.1 系统总体架构

核心 runtime pipeline 如 Figure 1 所示：

```text
Trigger
  ↓
Dispatcher
  ↓
Agent Investigation (LLM)
  ↓
Typed Boundary Validation
  ↓
Action Adapter
  ↓
Environment Change
```

关键思想：

- investigation probabilistic
- validation deterministic
- enforcement scoped

见 Figure 1。

---

## 4.2 Agent 三文件结构

### （1）Manifest

定义：

- trigger class
- action catalog
- output schema
- required fields
- confirmation field
- model backend
- query budget
- result cap

本质：

> Agent capability contract

---

### （2）Prompt

定义 investigation procedure。

注意：

- prompt 不直接定义权限；
- 权限来自 manifest。

这是重要设计点。

---

### （3）Runtime Context

包含：

- trusted subnets
- host roles
- allowed destinations
- expected services

作者强调：

> context 是 first-class input，而不是 hidden prompt prose。

因为：

- C2 traffic 在不同企业环境可能是正常服务；
- exfiltration 可能只是 backup traffic。

见 Section II。

---

## 4.3 Typed Boundary

这是论文核心。

### 当前 enforced 条件

runtime 当前实现：

1. schema parse
2. required fields
3. action allowlist

即：

\[
F_{struct}(R,\Sigma)
\]

与：

\[
action(R)\in A
\]

---

### Grounding（部分实现）

理论上还需：

\[
entities(R)\subseteq E(T)
\]

即：

- Agent 报告中的 IP/host
- 必须出现在 telemetry 中

但论文说明：

- 当前 prototype 只在 post-run analysis 中检查；
- 尚未 fully runtime-enforced。

这是论文一个重要 limitation。

见 Equation (1)。

---

## 4.4 Bounded Telemetry

Agent 不拥有：

- shell access
- full environment visibility
- arbitrary queries

只能访问：

- manifest 允许的 evidence slice。

其目的：

- 降低 attack surface
- 提高 traceability
- 支持 attribution

见 Section II。

---

## 4.5 Outcome Attribution

系统定义六类 outcome（Table I）：

| Outcome | 含义 |
|---|---|
| valid block | 合法封禁 |
| no action | 合法但不执行 |
| schema fail | JSON/schema 错误 |
| unsupported action | action 不在 allowlist |
| grounding fail | 实体不在 telemetry |
| execution fail | enforcement adapter 失败 |

其中：

- 前三类在实验中出现；
- grounding fail 属于 planned。

见 Table I。

---

# 5. 实验分析

## 5.1 实验设置

实验平台：

- Perry cyber arena
- cyber deception environment

攻击：

- DarkSide-inspired attack

Agent：

- Command & Control (C2)
- Exfiltration

实验规模：

- 18 closed-loop trials

见 Section III。

---

## 5.2 主实验结果

见 Table III。

| Outcome | 次数 |
|---|---|
| valid block | 13 |
| schema fail | 4 |
| valid no-action | 1 |

核心结论：

### （1）Typed boundary 确实在工作

4 次 schema fail 被 runtime reject。

这说明：

- LLM 自由输出不会直接触发执行；
- runtime boundary 生效。

这是论文最重要实验价值。

---

### （2）系统不是“逢攻击必封”

存在：

- valid no-action

说明：

- runtime 允许“不执行”
- 不会强制 action

这是安全自动化里非常重要的保守机制。

---

### （3）失败可归因

论文强调：

schema fail ≠ model hallucination

而是：

- parser/prompt/runtime contract mismatch

这让 debugging 可操作。

---

## 5.3 机制验证（Table IV）

论文特别强调：

实验不是 benchmark，而是 mechanism demonstration。

验证点包括：

| 机制 | 是否 demonstrated |
|---|---|
| data-only installation | 是 |
| typed validation | 是 |
| bounded telemetry | 是 |
| reusable runtime | 是 |
| outcome attribution | 是 |

见 Table IV。

---

## 5.4 没有真正的消融实验

论文没有：

- 模型对比
- prompt ablation
- telemetry ablation
- grounding ablation

因此：

- 更偏 systems paper；
- 不属于 ML benchmark 风格。

---

# 6. 局限性与风险

## 6.1 假设约束

论文明确假设：

- dispatcher trusted
- manifest trusted
- enforcement adapter trusted

即：

> Defender-controlled environment

不考虑：

- runtime 被攻破
- policy 被篡改

见 System Model。

---

## 6.2 应用边界

实验仅覆盖：

- 两类 Agent
- 小型 enterprise topology
- DarkSide 风格攻击

无法证明：

- 大规模 SOC 泛化能力
- 多租户环境适配能力
- 实时高吞吐场景表现

---

## 6.3 尚未覆盖的重要安全问题

论文明确排除：

- prompt injection
- memory poisoning
- malicious logs
- LLM provider compromise
- false-positive rate

这些恰恰是 production deployment 的关键风险。

见 Section II。

---

## 6.4 Grounding 尚未完全 runtime enforce

论文承认：

\[
entities(R)\subseteq E(T)
\]

目前只在 post-run evaluation 中验证。

因此：

- runtime 仍可能接受 hallucinated entity。

这是当前架构最重要的未闭环问题之一。

---

# 7. 复现与后续问题

## 7.1 复现重点

### （1）Manifest Schema

复现时需重点确认：

- action catalog
- required fields
- confirmation field
- telemetry scope

因为：

- runtime enforcement 全依赖 schema contract。

---

### （2）Telemetry Boundary

需要严格复现：

- bounded evidence interface
- query budget
- result cap

否则：

- Agent 很容易退化为 unrestricted tool-use。

---

### （3）Runtime Validation 顺序

论文核心在于：

```text
LLM output
  → parse
  → schema validate
  → allowlist check
  → enforcement
```

任何 shortcut 都会破坏论文主张。

---

## 7.2 后续研究方向

### （1）Runtime Grounding Enforcement

当前 grounding 未 fully enforce。

未来需要：

- online entity provenance
- telemetry-linked validation
- cryptographic evidence binding

---

### （2）Prompt Injection Resistant Defense Agents

论文没有处理：

- adversarial telemetry
- malicious log poisoning

未来需要：

- secure retrieval
- evidence trust scoring
- robust telemetry sanitization

---

# 8. 直接证据与待验证项

## 直接证据

1. “The LLM backend is treated as an untrusted reasoning component whose output may be useful but cannot be executed directly.”
   - 见 Section II。

2. Typed boundary admission predicate：
\[
F_M(R,T)=F_{struct}(R,\Sigma)\land[action(R)\in A]\land[entities(R)\subseteq E(T)]
\]
   - 见 Equation (1)。

3. “Thirteen trials produced validated network-block actions and contained the attack; four failed schema validation; one produced a valid no-action decision.”
   - 见 Abstract / Table III。

4. “The agent never calls the enforcement adapter directly.”
   - 见 Figure 1 / Section II。

5. “Prompt injection, memory poisoning, malicious tool descriptions, compromised logs... are outside the measured scope.”
   - 见 Section II。

---

## 待验证项

- [需验证] 实际 runtime schema validator 是否使用 JSON Schema、Pydantic 或自定义 parser。
- [需验证] LLM backend 的具体模型版本与 temperature 配置。
- [需验证] 网络封禁动作是否存在 rollback/recovery 机制。
- [需验证] Perry 中 telemetry latency 是否影响 containment outcome。
- [需验证] grounding failure 在未来 runtime 中如何实现在线 enforcement。

（完）
