---
title: "XAI FL-IDS: A Federated Learning and SHAP-Based Explainable Framework for Distributed Intrusion Detection Systems"
title_zh: XAI FL-IDS：一种基于联邦学习与 SHAP 的可解释分布式入侵检测系统框架
authors: "Mohammad Hossein Gholamrezazadeh, AhmadReza Montazerolghaem"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19448v1"
tags: ["query:q4"]
score: 8.0
relevance_score: 8.0
quality_score: 8.0
reliability_score: 8.5
practicality_score: 7.5
evidence: IDS中用于加密流量的联邦学习和机器学习
tldr: "本文提出了XAI FL-IDS框架，通过联邦学习保护IoT节点数据隐私，并结合SHAP提供可解释的入侵检测。系统在Edge-IIoTset数据集上实现了本地XGBoost训练和中央模型聚合，准确率超过99%，在部分实验中达到100%。该方法兼顾性能和隐私保护，同时提供决策可解释性，值得继续精读以了解其设计细节与实验验证。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19448v1/fig-001.webp\", \"caption\": \"Fig. 3. Server Side of XAI FL-IDS\", \"page\": 6, \"index\": 1, \"width\": 518, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-002.webp\", \"caption\": \"Fig. 6. Average Training Time Per Client of XAI FL-IDS\", \"page\": 7, \"index\": 2, \"width\": 511, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-003.webp\", \"caption\": \"Fig. 7. Average Training Time Per Client of Base Method\", \"page\": 7, \"index\": 3, \"width\": 549, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-004.webp\", \"caption\": \"Table 1: Comparative Summary of Methods, Data, and Performance in Related FL-IDS Work\", \"page\": 3, \"index\": 4, \"width\": 1086, \"height\": 1057}, {\"url\": \"assets/figures/arxiv/2605.19448v1/fig-005.webp\", \"caption\": \"Fig. 1. Data Processing Framework for XAI FL-IDS\", \"page\": 4, \"index\": 5, \"width\": 541, \"height\": 498}]"
motivation: 如何在保证数据隐私的前提下构建可解释的分布式入侵检测系统。
method: 利用联邦学习在各节点本地训练模型，同时通过SHAP分析实现决策可解释性。
result: "在10个客户端和Edge-IIoTset数据集上，XAI FL-IDS整体准确率超过99%，局部甚至达到100%，同时无需传输原始数据。"
conclusion: 优先阅读方法部分，理解XGBoost模型在联邦框架下的训练与聚合机制；随后关注实验与结果分析部分，以评估性能和可解释性效果。
key_findings: ["XAI FL-IDS在分布式IoT环境中实现了高准确率入侵检测，局部节点可达100%。", 通过联邦学习，系统无需传输原始数据，有效保护节点隐私。, 结合SHAP提供了每个决策的特征贡献解释，提高了模型透明性。]
limitations: 实验仅在Edge-IIoTset数据集上验证，缺乏多场景和大规模部署测试。
---

## 1. 问题与背景

论文关注物联网与工业物联网环境中的入侵检测问题，指出传统 IDS 普遍依赖集中式训练，需要各节点上传原始网络数据到中心服务器，这不仅带来通信与系统开销，也会暴露节点隐私。与此同时，大多数基于机器学习或深度学习的 IDS 具有“黑盒”特性，只能给出攻击分类结果，却无法解释模型为何做出该判断。随着 IoT 设备规模快速增长以及网络攻击复杂度上升，如何同时实现隐私保护与可解释的分布式入侵检测，成为该研究的核心动机。

## 2. 核心思路 / 方法

论文提出了名为 XAI FL-IDS 的框架，将联邦学习（FL）与基于 SHAP 的可解释人工智能（XAI）结合，用于构建分布式入侵检测系统。系统由一个中央服务器和 10 个客户端组成，采用 Edge-IIoTset 数据集，并在客户端之间进行类别均衡的数据分配；每个客户端本地使用 XGBoost 模型训练，仅上传模型更新参数而非原始数据，从而降低隐私泄露风险。论文进一步修改了传统联邦聚合机制：在每轮聚合后，系统会选择上一轮表现最佳的模型，并将其广播给所有节点作为下一轮训练基础。与此同时，系统在客户端和服务器侧均引入 SHAP，对每次检测结果中的特征贡献进行解释，使模型不仅能够判断攻击，还能够说明哪些网络特征驱动了该决策。相比仅强调准确率的传统 FL-IDS 方法，该工作同时强化了隐私保护与决策可解释性。

## 3. 结果与结论

实验结果表明，XAI FL-IDS 在入侵检测任务上表现出较高性能，准确率超过 99%，部分情况下达到 100%。论文指出，联邦学习机制有效避免了原始网络数据的集中传输，从而保证了各本地节点网络信息的机密性；而 SHAP 的引入则使系统能够持续输出特征重要性解释，提高模型透明度与可信度。作者认为，该框架能够同时解决现代 IDS 中的隐私保护与模型可解释性问题，从而推动安全、可解释、分布式 IDS 的发展。

## 4. 局限与适用边界

论文指出，目前系统仍可能受到 IoT 节点计算资源有限的问题影响，因此未来需要探索更轻量化的模型与更高效的特征选择方法。从现有文本来看，实验主要基于 Edge-IIoTset 数据集展开，因此其在其他复杂网络环境或真实工业场景中的泛化能力仍有待进一步验证。

## Abstract
An Intrusion Detection System (IDS) is vital in cybersecurity, detecting unauthorized activity across networks. With attacks on network layers increasing, stronger IDSs are needed. Yet most IDSs rely on centralized detection, forcing IoT nodes to ship data to a server, adding overhead and offering no privacy guarantees. Moreover, conventional models focus solely on flagging attacks, without explaining how individual features influence those decisions. This research aims to address these dual limitations by first proposing a solution for privacy preservation and then adding explainability to the new system. We introduce an innovative framework called XAI FL-IDS, which integrates Federated Learning (FL) with Explainable AI (XAI). The XAI FL-IDS system eliminates concerns over data transfer because each node trains its data locally and only sends the necessary update parameters to the server. Additionally, all detections, both at the local node and central server levels, are scrutinized using SHapley Additive exPlanations (SHAP), providing detailed insight into the decision-making process. This system consists of a central server and 10 clients and utilizes the Edge-IIoTset dataset, which is distributed among all clients with careful attention paid to class balancing. On each client, the XGBoost model is executed on local data. The proposed method demonstrates robust efficiency and strong performance in intrusion detection, achieving an accuracy of over 99% and, at times, reaching 100%. By incorporating FL, the confidentiality of the network information on every local node is guaranteed.

---

## 论文详细总结（自动生成）

# 1. TLDR

本文提出 XAI FL-IDS，一个结合联邦学习（Federated Learning, FL）与 SHAP 可解释分析的分布式入侵检测框架，用于解决 IoT/IIoT 环境下 IDS 的“隐私泄露 + 黑盒不可解释”双重问题。系统在 10 个客户端上基于 Edge-IIoTset 数据集进行本地 XGBoost 训练，仅共享模型参数而非原始数据，并通过 SHAP 输出特征贡献解释。实验结果显示，该方法在保持数据本地化的同时，实现了超过 99% 的检测准确率，部分实验达到 100%。

**关键词：** Federated Learning、XAI、SHAP、IDS、IoT、XGBoost、Edge-IIoTset

---

# 2. 核心故事与贡献

## 2.1 问题与动机

论文聚焦 IoT/IIoT 场景中的 IDS（Intrusion Detection System）部署问题，指出现有方案存在两个核心瓶颈：

- **隐私问题：**
  传统 IDS 多采用集中式训练，需要 IoT 节点上传原始网络流量数据到中央服务器，带来：
  - 节点隐私泄露风险
  - 网络传输开销
  - 中央化单点风险

- **可解释性问题：**
  大多数 ML/DL IDS 属于“黑盒模型”，只能输出攻击分类结果，而无法解释：
  - 哪些特征驱动了判定
  - 为什么模型认为该流量是恶意流量

论文引用 IoT 设备规模增长趋势：
- 到 2025 年联网设备预计达到 **700 亿**（见 Introduction）。

---

## 2.2 核心洞察

作者的关键洞察是：

> IDS 不应只“检测攻击”，还应同时满足：
> 1. 数据不离开本地节点；
> 2. 模型决策具备解释能力。

因此论文提出：

- 用 **Federated Learning** 解决数据隐私问题；
- 用 **SHAP** 解释模型预测；
- 用 **best-model aggregation** 替代传统平均聚合策略。

与传统 FedAvg 不同，论文并不对所有客户端参数简单平均，而是：

- 在每轮聚合后，
- 选择“上一轮表现最优模型”，
- 将该模型广播给所有客户端继续训练。

这是本文与已有 FL-IDS 的重要区别（见 Introduction / Section III-C）。

---

## 2.3 主要贡献

### （1）提出 XAI FL-IDS 框架

系统由：
- 1 个中央服务器
- 10 个客户端
组成（见 Abstract）。

每个客户端：
- 本地训练 XGBoost
- 仅上传模型参数
- 不上传原始数据

---

### （2）联邦学习与 SHAP 深度结合

不仅服务器侧做解释：

- 客户端侧检测
- 中央聚合侧检测

均使用 SHAP 分析特征贡献（见 Abstract）。

相比已有工作：
- FEDXAIIDS 只分析聚合结果；
- 本文同时解释局部与全局决策。

---

### （3）高精度 IDS 性能

在 Edge-IIoTset 数据集上：

- 准确率超过 99%
- 部分实验达到 100%

（见 Abstract / Figure 4 / Figure 5）。

---

# 3. 相关工作与定位

## 3.1 与已有 FL-IDS 工作对比

| 工作 | FL | 可解释性 | 模型 | 数据集 | 特点 | 准确率 |
|---|---|---|---|---|---|---|
| Rashid et al. (2023) | ✓ | ✗ | CNN/RNN | Edge-IIoTset | 基础 FL-IDS | 92.49% |
| Albanbay et al. (2025) | ✓ | ✗ | CNN + BiLSTM | CICIoT2023 | CNN+时序特征 | ~99% |
| Bhavsar et al. (2024) | ✓ | ✗ | PCC-CNN | Car-Hacking | Transportation IoT | 99.93% |
| Gutti et al. (2025) | ✓ | 部分 | SHAP + HADA | 多 IoT 数据集 | SHAP 用于鲁棒聚合 | 未强调解释输出 |
| FEDXAIIDS (2025) | ✓ | ✓ | ANN | 未说明 | FL + SHAP | ~88.2% |
| **本文 XAI FL-IDS** | ✓ | ✓ | XGBoost | Edge-IIoTset | 本地解释 + 最优模型广播 | >99% |

（见 Table 1）

---

## 3.2 本文定位

本文定位于：

> “兼顾隐私保护、可解释性与高检测性能的 IoT 分布式 IDS 框架”

其差异化主要体现在：

- 不仅做 FL
- 不仅做 XAI
- 而是同时解决：
  - 分布式训练
  - 特征可解释
  - 高精度检测
  - IoT 隐私保护

---

# 4. 方法详解

## 4.1 整体架构

系统结构：

- 中央 Server
- 10 个 Client
- 本地 XGBoost
- SHAP 解释模块

整体流程（见 Figure 1 / Figure 3）：

1. 数据预处理
2. 数据划分到客户端
3. 本地训练 XGBoost
4. 上传模型参数
5. Server 选择最佳模型
6. 广播到客户端
7. SHAP 输出解释

---

## 4.2 数据集与预处理

### 数据集：Edge-IIoTset

数据集特点（Section III-A）：

- 157,800 条样本
- 63 个特征
- 14 类攻击
- 覆盖：
  - DoS/DDoS
  - Injection
  - Malware
  - MITM
  - Information Gathering

---

### 数据处理流程（Section III-B）

包括：

- Infinite → NaN
- 删除无效特征
- 类别特征数值化
- 用 Mode 填补 NaN
- 特征归一化
- 80/20 train-test split
- Client class balancing

论文特别强调：

> 客户端之间保持类别平衡。

这是因为 FL 在 Non-IID 场景容易退化。

---

## 4.3 联邦学习机制

### 基础 FL 思路

每个客户端：

- 本地训练
- 上传参数
- 不上传原始数据

因此：

- 原始流量不会离开节点
- 提升隐私性

---

### 本文的改进聚合机制

不同于 FedAvg：

本文不是平均所有模型参数，而是：

> 选择上一轮中性能最优模型作为下一轮全局模型。

即：

$$
G_{t+1} = \arg\max_i \text{Perf}(M_i^t)
$$

其中：
- $M_i^t$：第 $t$ 轮客户端模型
- Perf：验证性能

论文未明确说明：
- 使用 Accuracy 还是 Loss 作为选择指标
- 是否有 tie-breaking
- 是否做 weighted selection

因此属于：
- **[需验证] 聚合细节**

---

## 4.4 SHAP 可解释机制

SHAP 基于 Shapley Value：



其中：

- $F$：特征集合
- $S$：特征子集
- $\phi_i$：特征 $i$ 的贡献值

论文将：
- 输入特征视作 coalition member
- 模型输出视作 coalition value

（Section III-D）

---

### SHAP 的用途

论文中 SHAP 用于：

- 分析局部预测
- 解释特征重要性
- 观察不同轮次解释变化

Table 2 与 Table 3：
- 展示 Client 5 在不同轮次的解释输出。

作者指出：

> 随着训练轮数增加，模型对特征的理解更加稳定与细致。

---

# 5. 实验分析

## 5.1 实验设置

| 项目 | 内容 |
|---|---|
| 数据集 | Edge-IIoTset |
| 客户端数 | 10 |
| 本地模型 | XGBoost |
| 数据划分 | Class-balanced |
| 训练方式 | Federated |
| 解释工具 | SHAP |

---

## 5.2 主实验结果

### 检测性能

论文结果：

- Accuracy > 99%
- 部分轮次达到 100%

（见 Abstract / Figure 4）

---

### Log Loss

Figure 5 展示：

- 随训练轮数增加
- Log Loss 持续下降

说明：
- 模型逐渐收敛
- 联邦训练有效

---

### 时间开销

Figure 6 与 Figure 7：

- 比较：
  - XAI FL-IDS
  - Base Method

观察：

- FL 引入额外通信与训练成本
- 但仍保持可接受时间开销

论文没有给出：
- 精确 latency 数值
- bandwidth 成本
- client hardware

因此：
- **[需验证] 系统级部署成本**

---

## 5.3 SHAP 解释结果

Table 2 vs Table 3：

作者比较：
- Round 1
- Round 9

发现：

- 后期 SHAP 输出更稳定
- 特征贡献更明确

说明：
- FL 训练逐渐增强模型判别能力
- 可解释性结果随训练演化

这是本文较有价值的观察。

---

## 5.4 缺失的实验

论文未看到：

- Non-IID robustness 实验
- 恶意客户端实验
- Poisoning attack 防御实验
- 通信复杂度实验
- SHAP runtime overhead

因此实验完整性仍有限。

---

# 6. 局限性与风险

## 6.1 假设约束

### （1）客户端数据较均衡

论文强调：
- class balancing

但真实 IoT 场景：
- 通常高度 Non-IID

因此实验环境可能偏理想化。

---

### （2）依赖 XGBoost

XGBoost：
- 精度高
- 但对 IoT 端设备可能偏重

作者也承认：
- 未来需要 lightweight model。

---

## 6.2 应用边界

### （1）仅验证单一数据集

实验仅基于：
- Edge-IIoTset

缺少：
- Cross-dataset validation
- Real deployment

泛化能力有限。

---

### （2）未验证大规模 FL

当前：
- 10 clients

未测试：
- 数百节点
- 高通信延迟
- 异构边缘设备

---

## 6.3 潜在风险

### （1）最佳模型广播可能导致过拟合

若单客户端模型偶然性能较高：

- 可能主导全局训练
- 削弱联邦泛化

这是本文 aggregation strategy 的潜在风险。

---

### （2）SHAP 计算开销

SHAP 在高维网络流量上：
- 通常较慢

论文未报告：
- 推理阶段额外开销

因此在线部署可行性仍需验证。

---

# 7. 复现与后续问题

## 7.1 复现重点

### （1）客户端数据划分

论文强调：
- class balancing

复现时需关注：

- 每类攻击比例
- IID / Non-IID 设置

否则结果可能显著变化。

---

### （2）最佳模型选择逻辑

论文未详细说明：

- best model metric
- selection timing
- validation split

这是复现关键难点。

---

### （3）SHAP 分析粒度

需要确认：

- 使用 TreeSHAP 还是 KernelSHAP
- Global explanation 还是 local explanation
- SHAP sample 数量

正文未明确。

---

## 7.2 后续研究方向

### （1）轻量化 FL-IDS

未来可探索：

- LightGBM
- TinyML
- Mobile-friendly model

降低 IoT 节点负担。

---

### （2）鲁棒联邦学习

未来可结合：

- Byzantine defense
- Poisoning detection
- Secure aggregation

增强安全性。

---

# 8. 直接证据与待验证项

## 直接证据

1. **系统由 10 个客户端组成，并采用 XGBoost 本地训练**
   - 见 Abstract
   - “This system consists of a central server and 10 clients…”

---

2. **准确率超过 99%，部分达到 100%**
   - 见 Abstract / Figure 4
   - “achieving an accuracy of over 99% and, at times, reaching 100%.”

---

3. **客户端仅上传模型参数，不上传原始数据**
   - 见 Introduction / Section III-C
   - “share only the model updates”

---

4. **SHAP 被用于客户端与服务器两侧解释**
   - 见 Abstract
   - “both at the local node and central server levels”

---

5. **作者提出“最佳模型广播”替代传统聚合**
   - 见 Introduction
   - “the system identifies the best model from the previous round and broadcasts only that superior model”

---

## 待验证项

- [需验证] 最佳模型的具体评价指标（Accuracy / Loss / F1）
- [需验证] 是否使用标准 FedAvg 作为 baseline
- [需验证] XGBoost 参数配置
- [需验证] SHAP 的具体实现方式（TreeSHAP / KernelSHAP）
- [需验证] Figure 4/5 的完整数值结果
- [需验证] 是否存在 IID 与 Non-IID 对比实验

（完）
