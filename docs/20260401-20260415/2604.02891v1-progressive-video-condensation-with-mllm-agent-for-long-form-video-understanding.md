---
title: Progressive Video Condensation with MLLM Agent for Long-form Video Understanding
title_zh: 基于MLLM代理的渐进式视频浓缩用于长视频理解
authors: "Yufei Yin, Yuchen Xing, Qianke Meng, Minghao Chen, Yan Yang, Zhou Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02891v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于加密视频理解的多模态LLM
tldr: 本文针对长视频理解中的计算成本和信息提取问题，提出ProVCA，一种渐进式视频压缩代理，通过多粒度定位关键视频片段和关键帧，实现高效的多模态大语言模型推理，在EgoSchema、NExT-QA和IntentQA等数据集上取得零样本最佳准确率，同时使用的帧数显著减少。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02891v1/fig-001.webp\", \"caption\": \"TABLE I: Comparison with other training-free methods on EgoSchema, NExT-QA, and IntentQA.\", \"page\": 5, \"index\": 1, \"width\": 1076, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-002.webp\", \"caption\": \"TABLE II: Ablation experiments for ProVCA. All results are on the val subset of NExT-QA. Best results are bolded, and\", \"page\": 5, \"index\": 2, \"width\": 1093, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of ProVCA in video understanding based on MLLM.\", \"page\": 3, \"index\": 3, \"width\": 1076, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-004.webp\", \"caption\": \"Fig. 1: Conceptual comparisons of three Video understanding methods based on LLM (MLLM). Existing approaches either (a) directly reason with pre-trained video-based MLLMs at high computational cost, or (b) use LLM agents that convert frames to captions and rely on VLMs (e.g., CLIP) to filter key frames, discarding fine-grained visual cues. In contrast, our ProVCA (c) progressively condenses videos by using an MLLM to narrow down query-relevant content from coarse segments to fine frames, then feeds the selected key frames into the MLLM to generate the final answer.\", \"page\": 1, \"index\": 4, \"width\": 518, \"height\": 356}]"
motivation: 现有方法在长视频理解中要么丢失细粒度视觉信息，要么计算成本过高。
method: ProVCA通过段落定位、片段选择和关键帧精炼三步，逐步筛选关键帧供MLLM推理。
result: "在多个数据集上实现零样本准确率69.3%-80.5%，且使用帧数少于已有方法。"
conclusion: ProVCA有效兼顾计算效率和视觉信息完整性，为长视频理解提供高效方案。
---

## 摘要
理解长视频需要在严格的计算预算下从长序列中提取与查询相关的信息。现有的文本-LLM流水线会丢失细粒度的视觉线索，而基于视频的多模态大语言模型（MLLM）能够保留视觉细节，但其对帧的需求过高且计算成本昂贵。在本研究中，我们旨在利用MLLMs实现高效的视频理解。我们提出了ProVCA，一种渐进式视频浓缩代理，它通过多级别的方式迭代地定位关键视频帧。ProVCA首先采用段落定位模块来识别与查询相关的视频段落，然后使用片段选择模块基于相似性选择重要片段，最后通过关键帧精化模块在这些片段中确定具体的关键帧。通过从粗略的段落到细粒度的帧逐步缩小范围，ProVCA为基于MLLM的推理识别出一小组关键帧。ProVCA在EgoSchema上取得了69.3%的零-shot准确率，在NExT-QA上达到了80.5%，在IntentQA上为77.7%，同时使用的帧数比以往的无训练方法更少。

## Abstract
Understanding long videos requires extracting query-relevant information from long sequences under tight compute budgets. Existing text-then-LLM pipelines lose fine-grained visual cues, while video-based multimodal large language models (MLLMs) can keep visual details but are too frame-hungry and computationally expensive. In this work, we aim to harness MLLMs for efficient video understanding. We propose ProVCA, a progressive video condensation agent that iteratively locates key video frames at multiple granularities. ProVCA first adopts a segment localization module to identify the video segment relevant to the query, then a snippet selection module to select important snippets based on similarity, and finally a keyframe refinement module to pinpoint specific keyframes in those snippets. By progressively narrowing the scope from coarse segments to fine frames, ProVCA identifies a small set of keyframes for MLLM-based reasoning. ProVCA achieves state-of-the-art zero-shot accuracies of 69.3\% on EgoSchema, 80.5\% on NExT-QA, and 77.7\% on IntentQA, while using fewer frames than previous training-free methods.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
长视频理解是计算机视觉和自然语言处理中的重要课题，尤其是在信息提取时需要兼顾计算效率和细粒度的视觉信息。传统的文本-LLM流水线方法由于丢失了视频中的细粒度视觉线索，往往不能很好地处理复杂的长视频内容。另一方面，基于视频的多模态大语言模型（MLLM）能够保留更多的视觉细节，但由于其对视频帧的需求过高，计算成本和内存消耗十分昂贵。因此，本文提出了一种新的方法——ProVCA（渐进式视频浓缩代理），旨在解决在严格计算预算下高效地进行视频理解，尤其是通过使用更少的帧来实现高效推理。

## 2. 论文提出的方法论
### 核心思想
ProVCA通过三步逐渐定位与查询相关的关键信息，从而实现长视频的高效理解：
1. **段落定位（Segment Localization）**：首先通过对视频进行粗粒度的段落定位，确定与查询最相关的时间段。
2. **片段选择（Snippet Selection）**：在定位的段落中，进一步依据片段之间的相似性选择出最重要的片段。
3. **关键帧精化（Keyframe Refinement）**：最终，在选择的片段中进一步精炼出具体的关键帧，以供MLLM进行推理。

### 关键技术细节
ProVCA方法结合了多模态大语言模型（MLLM）的推理能力和逐步筛选策略。通过逐步缩小视频范围，从粗粒度的段落到细粒度的帧，最终减少了视频帧的数量，确保高效且准确的推理。具体的过程通过三个主要模块来实现：
- **段落定位**：根据视频中采样的全局帧，使用MLLM进行语义推理来确定与查询相关的段落。
- **片段选择**：采用基于相似性度量（如文本相似度）选择与查询最相关的片段。
- **关键帧精化**：对选出的片段，使用视觉信息进一步精炼出最相关的关键帧。

## 3. 实验设计
### 使用的数据集
论文使用了三个长视频问答数据集进行评估：
- **NExT-QA**：包含5,440个自然视频和多个多选问题，主要任务为时序、因果和描述性问题。
- **EgoSchema**：一个大规模的以自我为中心的视频理解数据集，包含超过5,000个视频。
- **IntentQA**：专注于意图推理，包含4,303个视频和大约16,000个多选问题。

### 对比方法
论文对比了多个当前最先进的方法，包括基于训练的GPT-4大语言模型（如VideoAgent、VideoTree、LVNet等），并进行了消融实验（Ablation Study）来分析ProVCA各模块的贡献。

## 4. 资源与算力
论文并未明确说明实验所用的GPU型号和数量，也没有给出具体的训练时长。然而，实验使用了**GPT-4o**作为MLLM，且所有实验均为零-shot方式进行，说明实验过程不依赖额外的微调，侧重于现有模型的推理能力和高效推理过程。

## 5. 实验数量与充分性
### 实验数量
- 论文进行了**三组主要数据集**（NExT-QA、EgoSchema、IntentQA）的实验对比，并在每个数据集上分别评估了ProVCA方法的表现。
- 论文还进行了**消融实验**，对ProVCA的三个模块（段落定位、片段选择、关键帧精化）进行了逐一去除的实验，分析其对最终性能的影响。

### 实验充分性
实验设计较为充分，涵盖了不同的数据集和多个对比方法，确保了实验结果的客观性和全面性。消融实验的设计也有助于进一步分析每个组件对整体方法的贡献。

## 6. 论文的主要结论与发现
- **ProVCA方法**在长视频理解任务上取得了优异的成绩，特别是在零-shot情况下，ProVCA在**EgoSchema**（69.3%）、**NExT-QA**（80.5%）和**IntentQA**（77.7%）数据集上的准确率都超过了现有的最先进方法。
- 与传统方法相比，ProVCA使用了**更少的帧数**（如NExT-QA数据集上，平均只使用了4.2帧），同时在保持高推理精度的基础上，大大降低了计算和内存消耗。

## 7. 优点
- **高效性**：通过逐步定位视频中的相关部分，ProVCA显著减少了所需的视频帧数，在保证推理精度的同时提高了计算效率。
- **细粒度的视觉信息保留**：与传统的基于文本的LLM流水线方法不同，ProVCA能够保留视频的细粒度视觉线索，为复杂问题提供更准确的回答。
- **模块化设计**：ProVCA通过段落定位、片段选择和关键帧精化的分层设计，具有更高的可扩展性和适应性。

## 8. 不足与局限
- **视频长度限制**：尽管ProVCA在长视频理解上有显著优势，但其依然受到视频长度的限制，对于更长时间段的视频，如何保持高效性和精度仍然是一个挑战。
- **外部模块依赖**：虽然ProVCA减少了不必要的帧，但依然需要依赖外部的MLLM推理模块，在不同的场景和任务中可能需要进一步优化这些模块的灵活性。

（完）
