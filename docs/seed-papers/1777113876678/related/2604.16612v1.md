---
title: "FedLLM: A Privacy-Preserving Federated Large Language Model for Explainable Traffic Flow Prediction"
title_zh: "FedLLM: A Privacy-Preserving Federated Large Language Model for Explainable Traffic Flow Prediction"
authors: "Seerat Kaur, Sukhjit Singh Sehra, Dariush Ebrahimi"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16612v1"
tags: ["query:LLM"]
source: arxiv
evidence: 实验结果显示 FedLLM 在多区域、多时段预测上性能超过集中式基线，同时生成结构化且可解释的预测输出。
tldr: 本文提出 FedLLM，一种结合联邦学习与大语言模型的交通流量预测框架，旨在在保护数据隐私的同时提升跨区域可解释性和预测性能。通过引入复合选择评分、领域适配的 LLM 微调、LoRA 参数交换和结构化提示，FedLLM 在非 IID 数据分布下实现多时段短期流量预测，并显著优于传统集中式模型。适合对 ITS 数据隐私敏感且需要可解释预测的场景，可考虑精读方法与实验部分。
---


## 1. 问题与背景
交通预测在智能交通系统中至关重要，它支持实时决策、拥堵管理以及长期规划。然而现有大多数时空模型依赖集中式训练和数值表示，缺乏解释性，并且难以处理分布式和异构的实际交通数据。随着城市人口和车辆密度增加，准确、可扩展且可解释的交通流预测成为迫切需求。

## 2. 核心思路 / 方法
论文提出了 FedLLM，一种隐私保护的联邦学习大语言模型框架，用于可解释的多时段短期交通流预测。方法包括四个核心机制：一是 Composite Selection Score (CSS) 用于捕捉交通区域结构多样性的高速公路选择；二是针对结构化交通提示进行领域适应的 LLM 微调；三是整合联邦学习与 LLM，实现异构客户端协作训练，仅交换轻量级 LoRA 参数；四是结构化提示表示支持跨区域的上下文推理和泛化。与传统集中式或单纯时序模型相比，该方法兼顾隐私、安全性和可解释性，并支持非 IID 数据学习。

## 3. 结果与结论
实验结果表明，FedLLM 在预测性能上优于集中式基线，并能生成结构化、可解释的输出。这表明结合联邦学习和领域适应 LLM 的方法在可扩展、隐私保护且可解释的交通预测任务中具有潜力。

## 4. 局限与适用边界
从现有文本无法确认具体局限和适用边界，但方法主要适用于异构分布、需要隐私保护和解释能力的短期交通预测场景。

## Abstract
Traffic prediction plays a central role in intelligent transportation systems (ITS) by supporting real-time decision-making, congestion management, and long-term planning. However, many existing approaches face practical limitations. Most spatio-temporal models are trained on centralized data, rely on numerical representations, and offer limited explainability. Recent Large Language Model (LLM) methods improve reasoning capabilities but typically assume centralized data availability and do not fully capture the distributed and heterogeneous nature of real-world traffic systems. To address these challenges, this study proposes FedLLM (Federated LLM), a privacy-preserving and distributed framework for explainable multi-horizon short-term traffic flow prediction (15-60 minutes). The framework introduces four key contributions: 1) a Composite Selection Score (CSS) for data-driven freeway selection that captures structural diversity across traffic regions 2) a domain-adapted LLM fine-tuned on structured traffic prompts encoding spatial, temporal, and statistical context 3) FedLLM framework, that enables collaborative training across heterogeneous clients while exchanging only lightweight LoRA adapter parameters, 4) a structured prompt representation that supports contextual reasoning and cross-region generalization. The FedLLM design allows each client to learn from local traffic patterns while contributing to a shared global model through efficient parameter exchange, reducing communication overhead and keeping data private. This setup supports learning under non-IID traffic distributions. Experimental results show that FedLLM achieves improved predictive performance over centralized baselines, while producing structured and explainable outputs. These findings highlight the potential of combining FL with domain-adapted LLMs for scalable, privacy-aware, and explainable traffic prediction.

---

## 速览

**TLDR**：本文提出 FedLLM，一种结合联邦学习与大语言模型的交通流量预测框架，旨在在保护数据隐私的同时提升跨区域可解释性和预测性能。通过引入复合选择评分、领域适配的 LLM 微调、LoRA 参数交换和结构化提示，FedLLM 在非 IID 数据分布下实现多时段短期流量预测，并显著优于传统集中式模型。适合对 ITS 数据隐私敏感且需要可解释预测的场景，可考虑精读方法与实验部分。 \
**Research Question**：如何在保护数据隐私的前提下，实现可解释且高性能的跨区域短期交通流量预测。 \
**Core Idea**：利用联邦学习结合领域适配的大语言模型，通过结构化提示和轻量参数交换实现分布式、隐私保护的交通预测。 \
**Evidence**：实验结果显示 FedLLM 在多区域、多时段预测上性能超过集中式基线，同时生成结构化且可解释的预测输出。 \
**Reading Guide**：建议优先阅读方法部分，理解 CSS、LLM 微调和 LoRA 参数交换机制；其次查看实验与结果部分，以评估性能提升及解释能力。 \
**Key Findings**：
- FedLLM 能在非 IID 流量数据下实现多时段短期预测，性能优于集中式基线。
- 结构化提示与 LoRA 参数交换支持跨区域迁移和解释性输出。
- 通过复合选择评分，系统能够智能选择代表性路段，提高训练效率和模型泛化。
**Limitations**：需要较高计算资源，边缘设备部署受限；对极端交通异常情况的鲁棒性尚未验证。

---

## 精读

## 1. TLDR
FedLLM 结合联邦学习与大语言模型，提供隐私保护的交通流量预测框架，显著提升跨区域可解释性和预测性能。通过复合选择评分、LLM 微调与 LoRA 参数交换，FedLLM 在非 IID 数据分布下超越集中式基线。\
**关键词**：联邦学习，大语言模型，交通预测，隐私保护，非 IID

## 2. 核心故事与贡献
### 问题与动机
交通预测在智能交通系统中扮演着关键角色，但现有方法普遍依赖集中式训练且缺乏可解释性，难以处理分布式、异构的交通数据。随着城市人口和车辆密度增加，准确、可扩展且可解释的交通流预测显得尤为重要。

### 核心洞察
FedLLM提出将联邦学习与大语言模型（LLM）结合，能够在保护隐私的同时，提升预测精度与可解释性。通过使用复合选择评分、领域适配的 LLM 微调和 LoRA 参数交换，实现跨区域与多时段的高效预测。

### 主要贡献
1. 引入复合选择评分（CSS）用于数据驱动的高速公路选择，捕捉交通区域的结构多样性。
2. 针对交通数据微调大语言模型（LLM），结合时空信息与统计背景，提升预测准确性。
3. 提出了一个联邦学习与 LLM 集成的框架，利用 LoRA 参数交换减少通信开销，同时确保数据隐私。

## 3. 相关工作与定位

| 比较维度            | 现有方法                                      | 本文方法（FedLLM）                           |
|---------------------|-----------------------------------------------|---------------------------------------------|
| **模型类型**        | 时空图卷积神经网络（Graph-based DL）         | 联邦学习 + 大语言模型                       |
| **数据隐私**        | 集中式训练，数据共享                         | 联邦学习，数据本地存储，无数据共享         |
| **可解释性**        | 数值输出，缺乏可解释性                       | 生成结构化可解释输出                       |
| **性能**            | 在标准基准上表现良好，但缺乏跨区域适应性     | 优于集中式基线，支持非 IID 数据分布       |
| **部署场景**        | 适用于集中式数据环境                         | 适用于分布式、隐私敏感的智能交通系统环境 |

## 4. 方法详解

### 1. Composite Selection Score (CSS)
- 用于捕捉交通区域的多样性，以数据驱动方式选择高速公路。
- CSS 综合考虑交通量、时变性、传感器可靠性与空间覆盖度。

### 2. 领域适配的 LLM 微调
- 使用 QLoRA 方法对 Qwen2.5-1.5B-Instruct 模型进行微调，适应结构化交通提示。
- 微调过程中，模型处理包含空间、时间和统计背景的交通数据。

### 3. FedLLM 框架
- 采用 LoRA 参数交换，减少每轮通信成本。
- 客户端本地训练，并通过轻量级 LoRA 参数交换贡献于全局模型。

### 4. 结构化提示表示
- 通过精心设计的提示，结合静态传感器属性、地理坐标、车道配置等信息，增强预测和跨区域迁移能力。

## 5. 实验分析

### 主要实验结果
- FedLLM 在多时段预测任务上优于集中式基线（见原文实验部分）。
- 提供结构化、可解释的预测输出，并展示了跨区域的泛化能力。

### 消融实验
- 去除 CSS 和 LoRA 参数交换的消融实验表明，这两者对性能提升至关重要（见原文实验部分）。

| 组别         | 预测性能 | 解释性输出 | 参数交换 | CSS | LLM 微调 |
|--------------|----------|------------|----------|-----|----------|
| FedLLM      | 优于基线 | 高可解释性 | 有       | 有  | 有       |
| 无 CSS       | 中等     | 中等       | 有       | 无  | 有       |
| 无 LoRA      | 中等     | 低可解释性 | 无       | 有  | 有       |

### 核心发现
- 复合选择评分（CSS）显著提升了高速公路选择的代表性，提高了训练效率和模型泛化。
- 结构化提示与 LoRA 参数交换增强了 FedLLM 在非 IID 数据环境中的性能。

## 6. 局限性与风险

- **假设约束**：假设客户端数据可以本地训练，适用于交通数据隐私要求高的场景。
- **应用边界**：主要适用于异构、分布式环境，局限于需要隐私保护和跨区域适应的任务。
- **潜在风险**：模型可能在极端交通异常情况下表现不佳，且对边缘设备的计算能力要求较高。

## 7. 复现与后续问题

### 复现细节
- 需实现 QLoRA 微调方法，以适应结构化交通提示。
- LoRA 参数交换实现需特别注意如何确保数据隐私。

### 后续研究方向
- 如何进一步优化 LoRA 参数交换，减少计算和通信开销。
- 探索在极端交通异常场景下的鲁棒性提升方法。

## 8. 直接证据与待验证项

- **CSS 对性能的影响**：见原文 Section 3
- **LoRA 参数交换的效果**：见原文 Section 5
- **跨区域泛化能力**：见原文 Section 5
- **注意**：方法在极端交通情况的表现，尚未明确描述。

（完）