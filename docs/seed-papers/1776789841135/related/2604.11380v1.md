---
title: End-to-end differentiable network traffic simulation with dynamic route choice
title_zh: End-to-end differentiable network traffic simulation with dynamic route choice
authors: Toru Seo
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11380v1"
tags: ["query:LLM"]
source: arxiv
---

## 摘要
Optimization using network traffic flow models require computing gradients of objective functions with respect to model parameters. Conventional approaches rely on numerical differentiation or derivative-free methods that does not scale well with the parameter dimension, or on adjoint methods that require manual derivation for each specific model. This study proposes a novel end-to-end differentiable network traffic flow simulator based on the Link Transmission Model (LTM), incorporating general node models and a dynamic user optimum (DUO) route choice model. We observe that the LTM operates on continuous aggregate state variables (cumulative vehicle counts) through piecewise-linear $\min$/$\max$ operations, which admit subgradients almost everywhere and thus require no smooth relaxation for automatic differentiation (AD). We incorporate the DUO route choice model and its logit extension to explicitly consider endogenous dynamic route choice of travelers while preserving differentiability, because the diverge ratios are continuous functions of per-destination vehicle counts. The resulting simulator computes exact gradients via reverse-mode AD in a single backward pass regardless of the parameter dimension. In order to demonstrate the capability of the proposed model, we solved a dynamic congestion toll optimization problem on Chicago-Sketch dataset with around 2500 links and 1 million vehicles with 15 000 decision variables. The proposed model successfully derived a high quality solution with 10 000 iterations that took about 2 hours, meaning that 1 simulation run and gradient derivation took 0.8 second. The simulator, implemented in Python and JAX, is released as an open-source software named UNsim (https://github.com/toruseo/UNsim).

## Abstract
Optimization using network traffic flow models require computing gradients of objective functions with respect to model parameters. Conventional approaches rely on numerical differentiation or derivative-free methods that does not scale well with the parameter dimension, or on adjoint methods that require manual derivation for each specific model. This study proposes a novel end-to-end differentiable network traffic flow simulator based on the Link Transmission Model (LTM), incorporating general node models and a dynamic user optimum (DUO) route choice model. We observe that the LTM operates on continuous aggregate state variables (cumulative vehicle counts) through piecewise-linear $\min$/$\max$ operations, which admit subgradients almost everywhere and thus require no smooth relaxation for automatic differentiation (AD). We incorporate the DUO route choice model and its logit extension to explicitly consider endogenous dynamic route choice of travelers while preserving differentiability, because the diverge ratios are continuous functions of per-destination vehicle counts. The resulting simulator computes exact gradients via reverse-mode AD in a single backward pass regardless of the parameter dimension. In order to demonstrate the capability of the proposed model, we solved a dynamic congestion toll optimization problem on Chicago-Sketch dataset with around 2500 links and 1 million vehicles with 15 000 decision variables. The proposed model successfully derived a high quality solution with 10 000 iterations that took about 2 hours, meaning that 1 simulation run and gradient derivation took 0.8 second. The simulator, implemented in Python and JAX, is released as an open-source software named UNsim (https://github.com/toruseo/UNsim).

---

## 速览

**TLDR**：本文提出端到端可微交通仿真器，将LTM与DUO路线选择模型结合，利用JAX自动微分在min/max结构上实现近似处处可导，从而在高维参数下高效计算精确梯度，并用于动态拥堵收费优化，在大规模芝加哥路网实验中实现每次仿真+梯度约0.8秒，整体2小时完成1万次迭代，展示良好扩展性，值得深入阅读方法与实验。 \
**Research Question**：如何构建一个可端到端自动微分的动态交通流仿真与路线选择模型，以支持高维交通优化（如拥堵收费）。 \
**Core Idea**：基于LTM的min/max累积流模型结合DUO logit动态路径选择，使整个交通仿真系统在JAX中保持几乎处处可微并支持反向传播精确求梯度。 \
**Evidence**：在约2500条路段、100万车辆、1.5万变量的芝加哥数据上，2小时完成1万次优化迭代。单次仿真+梯度计算约0.8秒。 \
**Reading Guide**：优先阅读LTM与DUO如何被统一为可微结构的建模部分，以及自动微分实现细节，这是方法创新核心；其次看大规模拥堵收费实验验证其工程可扩展性。 \
**Key Findings**：
- LTM与DUO结合实现端到端自动微分，无需手工推导伴随方程。
- 动态路径选择通过连续分流比例保持可导性。
- 在大规模网络上实现高维优化的高效梯度计算与收敛。
**Limitations**：模型依赖LTM与DUO假设，对真实复杂驾驶行为表达能力有限，且实现依赖特定数值结构稳定性。

---

## 精读

本文研究了如何利用网络交通流模型在高维参数空间中进行优化，尤其是在计算目标函数关于模型参数的梯度时遇到的困难。现有方法依赖于数值微分或无导数方法，往往在参数维度较高时无法扩展，或者使用伴随方法需要为每个特定模型手动推导。为了解决这一问题，论文提出了一种新型的端到端可微网络交通流仿真器，它结合了链路传输模型（LTM）和动态用户最优（DUO）路线选择模型，能够在大规模交通优化问题中实现精确的梯度计算，特别是在拥堵收费等动态交通优化任务中具有重要应用。

该研究的核心创新在于将LTM与DUO路线选择模型结合，创建了一个既能捕捉动态路径选择的内生性，又能保持可微性的交通流仿真框架。LTM基于连续的累积车辆数作为状态变量，通过分段线性化的最小值/最大值操作（$\min$/$\max$），几乎在每个地方都有次梯度，因此无需对自动微分（AD）进行平滑放宽处理。DUO模型及其logit扩展被引入，明确考虑了旅行者的动态路径选择，并且由于分流比率是目标路径车辆数的连续函数，因此能够保持模型的可微性。这一方法通过逆向模式自动微分（reverse-mode AD）在单次反向传播中精确计算梯度，解决了高维参数优化中的计算瓶颈问题。

在实验部分，作者通过在芝加哥草图数据集上解决动态拥堵收费优化问题，验证了所提模型的有效性。该数据集包含大约2500条路段和100万辆车辆，总共有15000个决策变量。在实验中，所提模型成功地在大约2小时内完成了10000次迭代，每次仿真和梯度计算仅需要0.8秒，展示了其高效的计算能力。通过消融实验，作者证明了在引入DUO模型和logit扩展后，模型能够在动态路径选择的同时保持可微性，并且反向传播能够准确地计算梯度，显著提高了优化速度和结果的质量。

这项研究的主要优点在于其创新性地将LTM与DUO模型结合，并且成功实现了高效的端到端自动微分，解决了传统方法在高维优化中的扩展性问题。然而，论文也存在一定的局限性，主要是该模型依赖于LTM和DUO假设，可能无法完全捕捉复杂的驾驶行为，且仿真模型的实现稳定性和数值结构也可能影响到实际应用效果。因此，模型在处理更复杂的现实世界交通行为时可能面临挑战。

如果读者想要复现或进一步研究这项工作，最应该关注的实现细节是LTM与DUO模型如何在JAX中实现自动微分，并确保在高维参数下仍能保持计算效率与精度。后续的研究可以进一步探索如何提高模型对复杂交通行为的表达能力，以及如何在更大规模或不同的网络配置下验证其通用性和稳定性。

（完）
