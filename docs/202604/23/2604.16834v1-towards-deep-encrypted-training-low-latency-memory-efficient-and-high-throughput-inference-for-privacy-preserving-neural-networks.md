---
title: "Towards Deep Encrypted Training: Low-Latency, Memory-Efficient, and High-Throughput Inference for Privacy-Preserving Neural Networks"
title_zh: 朝向深度加密训练：低延迟、内存高效和高吞吐量的隐私保护神经网络推理
authors: "Nges Brian Njungle, Eric Jahns, Michel A. Kinsy"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16834v1"
tags: ["query:q4"]
score: 8.1
relevance_score: 8.0
quality_score: 8.5
reliability_score: 8.0
practicality_score: 8.0
evidence: 隐私保护机器学习与神经网络中的加密流量分析相关。
tldr: 本研究旨在提升隐私保护机器学习中同态加密神经网络的推理效率，提出了一种优化算法和管道架构以支持批量处理。通过对ResNet-20和ResNet-34模型的评估，研究显示该方法在处理512个加密图像时，推理时间减少了1.78倍，内存使用降低了3.74倍。这些结果表明该方法在高吞吐量场景下具有显著优势，值得进一步细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16834v1/fig-001.webp\", \"caption\": \"Fig. 1: Transforming multiple inputs into multiple SIMD vectors using channel-by-channel encoding.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 666}]"
motivation: 如何提高同态加密神经网络在批量处理中的推理效率和内存使用。
method: 通过开发优化算法和管道架构，实现同态加密神经网络的批量输入处理以提高推理效率。
result: 对于ResNet-20模型，在处理512个加密图像时，我们的方法实现了8.86秒的推理时间，相比现有设计有显著改进。
conclusion: 若继续精读，建议重点关注方法部分和实验结果部分，以深入理解优化算法的具体实现及其在不同批量处理下的性能表现。
key_findings: [提出了一种新的优化算法以支持同态加密神经网络的批量输入处理。, 在ResNet-20模型上实现了1.78倍的运行时改进和3.74倍的内存使用减少。, 为深度学习模型提供了高效、可扩展的隐私保护解决方案，适用于大规模数据处理场景。]
limitations: 论文主要集中于特定模型（如ResNet），未探讨其他类型神经网络或更复杂场景下的表现。
---

## 1. 问题与背景
Title: Towards Deep Encrypted Training: Low-Latency, Memory-Efficient, and High-Throughput Inference for Privacy-Preserving Neural Networks

URL Source: https://arxiv.org/pdf/2604.16834v1

Published Time: Tue, 21 Apr 2026 00:34:41 GMT

Number of Pages: 14

Markdown Content:
> 1

# Towards Deep Encrypted Training: Low-Latency, Memory-Efficient, and High-Throughput Inference for Privacy-Preserving Neural Networks 

Nges Brian Njungle , Eric Jahns , and Michel A.

## 2. 核心思路 / 方法
we propose an optimized pipeline design that keeps encrypted neural network execution continuously active by maximizing HE resource utilization

## 3. 结果与结论
achieves an amortized inference time of 8.86

## 4. 局限与适用边界
Limitations of Encrypted Neural Networks 

Within the context of HE-based neural networks, a pri-mary approach is single-input processing.

## Abstract
Privacy-preserving machine learning (PPML) has become increasingly important in applications where sensitive data must remain confidential. Homomorphic Encryption (HE) enables computation directly on encrypted data, allowing neural network inference without revealing raw inputs. While prior works have largely focused on inference over a single encrypted image, batch processing of encrypted inputs lags behind, despite being critical for high-throughput inference scenarios and training-oriented workloads.   In this work, we address this gap by developing optimized algorithms for batched HE-friendly neural networks. We also introduced a pipeline architecture designed to maximize resource efficiency for different batch size execution. We implemented these algorithms and evaluated our work using HE-friendly ResNet-20 and ResNet-34 models on encrypted CIFAR-10 and CIFAR-100 datasets, respectively.   For ResNet-20, our approach achieves an amortized inference time of 8.86 seconds per image when processing a batch of 512 encrypted images, with a peak memory usage of 98.96 GB. These results represent a 1.78x runtime improvement and a 3.74x reduction in memory usage compared to the state-of-the-art design. For the deeper ResNet-34 model, we achieve an amortized inference time of 28.14 on a batch of 256 encrypted images using 246.78GB of RAM

---

## 论文详细总结（自动生成）

# 1. TLDR
本文提出一种面向同态加密（HE）神经网络的**批量加密推理优化方法与流水线架构**，专注解决当前HE推理在高吞吐场景下的**内存爆炸与低并行效率问题**。通过改进ciphertext packing与pipeline调度，在ResNet-20/34上实现显著性能提升（最高1.78×加速、3.74×内存下降）。

**Keywords:** HE, PPML, batched inference, ciphertext packing, pipeline optimization, ResNet

---

# 2. 核心故事与贡献

## （1）问题与动机
- 现有HE神经网络主要聚焦**单输入推理（single-input inference）**
- 但现实需求（医疗批量影像、金融批处理）需要：
  - 高吞吐（high-throughput）
  - 批量加密输入处理（batched encrypted inference）
- 当前瓶颈：
  - HE计算开销极高
  - 内存占用巨大（如 prior SOTA 达 **370GB RAM**）
  - 批处理效率低

## （2）核心洞察
作者认为瓶颈不只是HE算法本身，而是：
- ciphertext packing效率低
- pipeline资源未被持续利用（idle slots）
- batch-size与计算资源未统一调度

→ 因此提出：
> 用“连续执行流水线 + ciphertext accumulator”维持HE计算全程高利用率

## （3）主要贡献
- 提出**优化HE batch processing算法**
- 设计**pipeline架构（持续激活网络执行）**
- 引入**accumulator机制**：
  - 跨layer合并ciphertext
  - 控制有效batch size
- 实现ResNet-20/34 HE模型并验证
- 实验结果：
  - ResNet-20：**8.86s/image（batch=512）**
  - 内存降低至 **98.96GB（↓3.74×）**
  - 性能提升 **1.78×**

---

# 3. 相关工作与定位

| 方法/系统 | 处理模式 | HE Scheme | 优化目标 | 局限 |
|------------|----------|-----------|----------|------|
| CryptoNets (2016) | 单输入 | BGV | 首个HE CNN | 深度浅 |
| Faster CryptoNets | 单输入 | BGV | 稀疏优化 | 仍低效 |
| HCNN / Falcon | 单输入 | BFV/BGV | SIMD优化 | 精度/深度限制 |
| Cheon et al. (SOTA batch) | 批处理 | CKKS | 多线程+pipeline | 370GB RAM |
| Orion (2025) | 单输入 | CKKS | DL inference优化 | 不支持batch |
| **本文** | **批处理** | CKKS/OpenFHE | **吞吐+内存优化** | ResNet为主 |

定位总结：
> 本文是 **HE batch inference 从“可行”走向“可扩展”阶段的优化工作**

---

# 4. 方法详解

## 4.1 整体架构（HE Pipeline）
系统核心由三部分组成：

- **Ciphertext Packing Layer**
  - channel-by-channel encoding（见 Fig.1）
  - SIMD slots利用最大化

- **Pipeline Execution Engine**
  - layer-level streaming execution
  - 避免ciphertext idle

- **Accumulator Module**
  - 跨layer合并中间结果
  - 控制batch粒度

---

## 4.2 Ciphertext Packing优化
目标：减少rotation + 提升slot利用率

核心思想：
- 多图像 → SIMD slots并行编码
- 避免逐图单独HE运算

优化点：
- 减少HE rotation操作次数
- 合并同类ciphertext操作
- reuse packed structure

---

## 4.3 Pipeline机制

定义执行方式：
- layer1 → layer2 → layer3连续流动
- 不等待完整batch结束

效果：
- compute + memory overlap
- pipeline saturation（持续占用HE资源）

---

## 4.4 Accumulator机制

作用：
- 合并不同layer intermediate ciphertext
- 控制 effective batch size

关键效果：
- 动态batch control：
  - 小batch → low latency
  - 大batch → high throughput

---

# 5. 实验分析

## 5.1 主结果

| Model | Batch | Latency (avg) | Memory | Gain |
|------|------|--------------|--------|------|
| ResNet-20 | 512 | 8.86s/image | 98.96GB | 1.78× faster |
| ResNet-34 | 256 | 28.14s/image | 246.78GB | significant improvement |

---

## 5.2 对比SOTA

- Cheon et al.：
  - 15.76s/image（batch=512）
  - 370GB RAM
- 本文：
  - 更低latency
  - 显著降低memory（↓3.74×）

---

## 5.3 关键发现

- batch越大 → amortized latency下降
- pipeline稳定性显著影响HE throughput
- memory瓶颈主要来自ciphertext accumulation

---

# 6. 局限性与风险

- **模型依赖性强**
  - 主要验证ResNet-20/34
  - 未验证Transformer等结构

- **资源需求仍极高**
  - 98GB~246GB RAM仍不适合普通部署

- **HE scheme限制**
  - 依赖CKKS/OpenFHE参数调优
  - bootstrapping成本未完全消除

- **batch size依赖性能**
  - 小batch时优势下降明显

---

# 7. 复现与后续问题

## 复现关键点
- ciphertext packing方式（channel encoding）
- pipeline scheduling策略
- accumulator实现细节（跨layer merge）
- HE参数配置（CKKS scale/level）

## 后续研究方向
- 向Transformer / ViT扩展HE batch inference
- 降低memory footprint至 <32GB级别
- 自动化HE pipeline compiler设计

---

# 8. 直接证据与待验证项

## 直接证据

- ResNet-20：
  - 8.86s/image @ batch=512（Abstract）
- 内存优化：
  - 98.96GB vs prior 370GB（Abstract）
- 性能提升：
  - 1.78× runtime improvement（Abstract）
- 方法核心：
  - “pipeline architecture maximizing HE utilization”（Introduction Section）
- batch processing瓶颈描述：
  - single-input dominant paradigm（Section I-A）

---

## 待验证项

- [需验证] accumulator具体数学定义（正文未完全展开）
- [需验证] pipeline调度是否hardware-aware
- [需验证] ResNet-34详细layer-wise latency breakdown
- [需验证] bootstrapping是否被完全避免或延迟执行
- [需验证] CIFAR-100上精度是否与plaintext baseline一致

---

（完）
