---
title: Latent-Condensed Transformer for Efficient Long Context Modeling
title_zh: 用于高效长上下文建模的潜在压缩变换器
authors: "Zeng You, Yaofo Chen, Qiuwu Chen, Ying Sun, Shuhai Zhang, Yingjian Li, Yaowei Wang, Mingkui Tan"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12452v2"
tags: ["query:q3"]
score: 6.8
relevance_score: 6.5
quality_score: 8.0
reliability_score: 7.5
practicality_score: 5.0
evidence: LLM用于改善长上下文建模。
tldr: 本研究提出了一种新的注意力机制——潜在压缩注意力（LCA），旨在解决大语言模型在处理长文本时面临的计算复杂性和内存开销问题。通过将上下文直接压缩到潜在空间中，LCA实现了显著的速度提升和内存节省，同时保持了良好的性能。这一创新方法值得深入研究与应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12452v2/fig-001.webp\", \"caption\": \"Figure 2: Efficiency comparison of different context lengths. (a) presents the first-token generation latency. (b) shows the per-token (interval) latency during decoding. (c) illustrates the GPU memory footprint of KV cache.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2604.12452v2/fig-002.webp\", \"caption\": \"Figure 1: An overview of Latent-Condensed Attention (LCA). The history context is condensed into a compact set of representatives via group-wise conde sation in the latent space: semantic information is aggregated through weighted pooling of CKV , while positional information is preserved via anchor selection from KR. The recent context is retained in full fidelity without condensation.\", \"page\": 4, \"index\": 2, \"width\": 948, \"height\": 478}]"
motivation: 如何有效地处理长文本上下文以提高大语言模型的效率。
method: LCA通过对MLA潜在空间中的上下文进行结构化压缩，实现高效的长文本建模。
result: "实验结果显示，LCA在128K上下文长度下实现了高达2.5倍的预填充速度提升和90%的KV缓存减少，同时保持了竞争力的性能。"
conclusion: 建议优先阅读方法部分以了解LCA的设计理念，以及实验结果部分以评估其性能提升情况。
key_findings: [LCA通过在MLA的潜在空间中直接进行上下文压缩，显著降低了计算复杂度和KV缓存大小。, LCA采用加权池化和锚点选择策略，有效区分语义信息和位置信息，从而提高了长文本处理效率。, LCA可以无缝集成到预训练模型中，无需额外参数，且兼容从头训练。]
limitations: 该方法在多文档问答任务中的表现略逊于基线，显示出在信息集中检索方面的局限性。
---

## 摘要
大型语言模型（LLMs）在处理长上下文时面临显著挑战，原因在于键值（KV）缓存呈线性增长以及自注意力机制的二次复杂度。现有方法通常分别解决这些瓶颈：多头潜在注意力（MLA）通过将 token 投影到低维潜在空间来减少 KV 缓存，而稀疏注意力则减少计算量。然而，稀疏方法无法在 MLA 的压缩潜在结构上原生运行，从而错失联合优化的机会。本文提出了潜在压缩注意力（LCA），直接在 MLA 的潜在空间中对上下文进行压缩，该空间中的表示被解耦为语义潜向量和位置键。LCA 通过查询感知池化分别聚合语义向量，并通过锚点选择保留位置键。这一方法在不增加参数的情况下同时降低了计算成本和 KV 缓存。除了 MLA，LCA 的设计与架构无关，可轻松扩展到其他注意力机制，如 GQA。从理论上，我们证明了长度无关的误差界。实验表明，LCA 在 128K 上下文下可实现最高 2.5 倍的预填充加速和 90% 的 KV 缓存减少，同时保持具有竞争力的性能。

## Abstract
Large language models (LLMs) face significant challenges in processing long contexts due to the linear growth of the key-value (KV) cache and quadratic complexity of self-attention. Existing approaches address these bottlenecks separately: Multi-head Latent Attention (MLA) reduces the KV cache by projecting tokens into a low-dimensional latent space, while sparse attention reduces computation. However, sparse methods cannot operate natively on MLA's compressed latent structure, missing opportunities for joint optimization. In this paper, we propose Latent-Condensed Attention (LCA), which directly condenses context within MLA's latent space, where the representation is disentangled into semantic latent vectors and positional keys. LCA separately aggregates semantic vectors via query-aware pooling and preserves positional keys via anchor selection. This approach jointly reduces both computational cost and KV cache without adding parameters. Beyond MLA, LCA's design is architecture-agnostic and readily extends to other attention mechanisms such as GQA. Theoretically, we prove a length-independent error bound. Experiments show LCA achieves up to 2.5$\times$ prefilling speedup and 90% KV cache reduction at 128K context while maintaining competitive performance.