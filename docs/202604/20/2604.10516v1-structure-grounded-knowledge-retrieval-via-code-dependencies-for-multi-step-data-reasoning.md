---
title: Structure-Grounded Knowledge Retrieval via Code Dependencies for Multi-Step Data Reasoning
title_zh: 基于代码依赖的结构化知识检索用于多步骤数据推理
authors: "Xinyi Huang, Mingzhe Lu, Haoyu Dong"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10516v1"
tags: ["query:profile-1"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 基于结构的知识检索用于LLM任务
tldr: 本文提出SGKR方法，针对多步数据分析中传统基于语义相似度的检索难以提供关键计算知识的问题，将领域知识绑定到函数并构建代码调用依赖图，通过输入输出语义标签定位相关函数，再沿依赖路径检索子图作为LLM生成上下文。实验表明该方法在多个数据分析基准上相比无检索与语义检索均显著提升答案正确率，同时减少上下文冗余，整体思路清晰且工程价值较强，值得进一步细读。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10516v1/fig-001.webp\", \"caption\": \"Figure 2: Framework of SGKR. Framework of the structure-grounded code retrieval system. In the offline phase, code is parsed via AST to build a semantic graph and a function-call dependency graph, while external knowledge is inserted into relevant functions as comments. Extracted sementic input–output (I/O) tags are added as nodes to augment the graph. At inference time, I/O tags from a query guide the retrieval of relevant subgraphs through dependency paths, and the retrieved functions and knowledge are provided to LLMs for reasoning.\", \"page\": 4, \"index\": 1, \"width\": 937, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2604.10516v1/fig-002.webp\", \"caption\": \"Table 1: Accuracy comparison across datasets and average number of retrieved nodes (semantic I/O nodes are not counted).\", \"page\": 6, \"index\": 2, \"width\": 862, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2604.10516v1/fig-003.webp\", \"caption\": \"Table 3: Retrieved knowledge-code nodes by different methods. ✓means the knowledge is selected by the method.\", \"page\": 7, \"index\": 3, \"width\": 939, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.10516v1/fig-004.webp\", \"caption\": \"Figure 1: Comparison among (1) embedding-based similarity between the new task and candidate code snippets, (2) LLM (GPT-5.2) predictions of whether code snippets are required, and (3) ground-truth annotations indicating whether code snippets are actually needed. For similarity computation, we use the openai/text-embedding-3-large model, embedding both function definitions and their natural-language summaries.\", \"page\": 3, \"index\": 4, \"width\": 939, \"height\": 471}]"
motivation: 如何在多步数据分析任务中，利用代码结构与函数依赖关系替代传统语义相似度检索，从而为LLM提供更关键的领域知识。
method: 将领域知识绑定到函数并构建函数调用依赖图，通过I/O语义标签匹配节点，再利用依赖路径检索任务相关的结构化知识子图供LLM使用。
result: 实验在多种多步数据分析基准上显示，SGKR相较于无检索与语义相似度检索基线均显著提升解题正确率，同时减少所需上下文规模。
conclusion: 重点阅读方法部分中知识单元定义与依赖图构建流程，以及实验部分对不同检索策略的对比结果，以理解结构化检索为何优于embedding方法。
key_findings: [函数调用依赖比语义相似度更能刻画数据分析任务所需知识关联。, 将知识绑定到代码函数可显著提升多步计算任务的推理正确率。, SGKR在提升性能的同时还能减少检索上下文冗余信息。]
limitations: 依赖函数级知识建模与依赖图构建质量，对代码覆盖与依赖抽取准确性较敏感，泛化能力未充分验证。
---

## 摘要
在使用大语言模型（LLMs）解决领域特定的数据分析任务时，选择正确的知识至关重要。然而，大多数增强检索的方法主要依赖于词汇或嵌入相似性，而这通常是多步骤推理中所需任务关键知识的一个较弱的代理。在许多此类任务中，相关知识不仅仅是与查询在文本上相关，而是根植于可执行代码及其计算执行过程中所依赖的依赖关系结构。为了解决这一不匹配问题，我们提出了SGKR（结构化知识检索）框架，该框架通过函数调用依赖关系图来组织领域知识。给定一个问题，SGKR提取语义输入和输出标签，识别连接它们的依赖路径，并构建一个任务相关的子图。然后，将相关知识和相应的函数实现作为结构化上下文，供基于LLM的代码生成使用。在多步骤数据分析基准测试中的实验表明，SGKR在无检索和基于相似度的检索基线中，均能持续提高解决方案的正确性，无论是在普通LLM还是编程代理中。

## Abstract
Selecting the right knowledge is critical when using large language models (LLMs) to solve domain-specific data analysis tasks. However, most retrieval-augmented approaches rely primarily on lexical or embedding similarity, which is often a weak proxy for the task-critical knowledge needed for multi-step reasoning. In many such tasks, the relevant knowledge is not merely textually related to the query, but is instead grounded in executable code and the dependency structure through which computations are carried out. To address this mismatch, we propose SGKR (Structure-Grounded Knowledge Retrieval), a retrieval framework that organizes domain knowledge with a graph induced by function-call dependencies. Given a question, SGKR extracts semantic input and output tags, identifies dependency paths connecting them, and constructs a task-relevant subgraph. The associated knowledge and corresponding function implementations are then assembled as a structured context for LLM-based code generation. Experiments on multi-step data analysis benchmarks show that SGKR consistently improves solution correctness over no-retrieval and similarity-based retrieval baselines for both vanilla LLMs and coding agents.

---

## 论文详细总结（自动生成）

## 1. 一页速读

### 1.1 这篇论文在问什么？
如何在多步骤数据分析任务中，通过函数调用依赖构建结构化知识检索，提升大语言模型（LLM）生成代码的准确性，而不是依赖传统的词汇或嵌入相似度检索。

### 1.2 作者的核心结论是什么？
- SGKR通过将知识绑定到函数并构建代码调用依赖图，可以有效捕捉任务关键知识，提升LLM生成代码的正确率。（证据来源：Abstract, 第2章“Structure-Grounded Knowledge Graph”）  
- 与传统基于相似度的检索相比，SGKR在多步数据分析基准上显著减少了所需上下文节点，同时提高了任务解答准确性。（证据来源：Table 1, Figure 2）  
- 函数依赖关系比文本相似度更能反映知识的实际关联性，从而减少遗漏关键计算逻辑的风险。（证据来源：Figure 1, Section 2.3）

### 1.3 这篇论文最值得记住的点
| 常见问题 | 论文给出的回答 |
|---|---|
| 多步数据分析任务中为什么嵌入相似度检索不够？ | 因为关键知识隐藏在代码函数及其调用关系中，文本或嵌入相似性不能保证捕获依赖逻辑。（Section 2.3, Figure 1） |
| 如何定义“知识单元”？ | 知识单元被绑定到具体函数及其实现，函数调用边表示知识之间的依赖。（Section 2.1-2.2） |
| 检索流程如何与LLM结合？ | 通过提取语义I/O标签，沿依赖路径检索子图，将知识和函数作为结构化上下文供LLM生成代码。（Section 3.1-3.2, Figure 2） |
| 相比传统RAG方法的优势？ | 提升正确率、减少冗余上下文、捕获多步推理所需函数依赖。（Section 5, Table 1-3） |

## 2. 问题背景：为什么这篇论文值得读？

### 2.1 具体研究背景
- 多步数据分析任务涉及复杂数值计算、业务规则和结构化数据操作（过滤、连接、聚合、条件变换），直接挑战LLM的代码生成能力。（Introduction）  
- 传统知识检索依赖词汇或嵌入相似度，容易错过功能相关但文本不相似的关键知识。（Introduction, Section 2.3）  
- 提供正确领域知识对提升LLM任务性能至关重要，如BIRD benchmark显示，补充领域知识可显著提高SQL生成准确率。（Introduction）

### 2.2 论文真正想挑战的默认设定
- 默认假设：检索相关知识主要依赖文本或嵌入相似性。  
- SGKR挑战这一假设，提出依赖代码执行结构的知识关联更可靠。

## 3. 论文的核心问题与研究设计

### 3.1 核心问题
- 如何将多步数据分析任务所需的领域知识组织成可被LLM高效利用的形式？  
- 如何通过函数依赖关系而非文本相似性实现知识检索？

### 3.2 研究框架或方法论
- 构建函数调用依赖图，将知识绑定到函数节点（offline phase）。  
- 提取查询的语义输入输出（I/O）标签作为检索锚点。  
- 在依赖图上沿I/O路径进行子图检索，提供给LLM作为结构化上下文（online phase）。  
- 评估方法通过多步数据分析基准比较无检索、embedding检索与SGKR的性能差异。

## 4. 方法拆解

### 4.1 技术细节
- **知识单元定义**：每个函数及其实现附带领域知识，函数调用边定义知识依赖。（Section 2.1-2.2）  
- **语义I/O标签**：标识查询中的数据实体、属性和目标量，并映射到函数输入输出节点。（Section 2.4）  
- **子图检索**：在依赖图上进行广度优先搜索（BFS），识别连接输入输出节点的依赖路径，抽取对应函数及知识。（Section 3.2, Figure 2）  
- **LLM集成**：将检索到的子图转换为结构化上下文，指导代码生成。

#### 算法公式
$$
S^* = \arg\max_{S \subseteq V_{KC}} Acc(q, S)
$$
其中 $V_{KC}$ 为知识-函数节点集合，$Acc(q, S)$ 为在使用节点 $S$ 作为上下文时完成问题 $q$ 的任务性能。（Section 3.1）

### 4.2 与现有方法的区别
- 传统RAG方法依赖embedding相似度或文本匹配，无法捕获多步计算依赖。  
- SGKR利用函数调用图和I/O语义标签实现结构化检索，更能反映任务所需逻辑关系。

## 5. 实验设计与结果

### 5.1 数据集与实验设置
- 多步数据分析基准：DAB STEP、ConvFinQA、FinQA等。（Section 5, Appendix A）  
- 对比方法：无检索、embedding相似度检索、SGKR。  
- 测评指标：解题正确率、平均检索节点数量。

### 5.2 主要结果
- SGKR在所有基准上均显著提高正确率，例如Table 1显示，在DAB STEP上，SGKR相比embedding检索提升约8-12个百分点。  
- 检索节点数量明显减少（Table 1, Figure 2），降低上下文冗余。  
- 函数依赖路径能够覆盖任务所需知识，embedding方法可能遗漏关键函数（Figure 1）。

### 5.3 消融实验与控制变量
- 验证了I/O语义标签和依赖路径在提升检索准确性中的作用（文中Section 5未明确给出具体数值消融，但讨论了Figure 1示意）。  
- 控制变量：使用相同LLM模型对比不同检索策略。

## 6. 优点
- 高效捕获多步计算任务所需知识。  
- 提升LLM生成代码的正确率并减少冗余上下文。  
- 方法直观，工程可落地，易与现有LLM框架结合。

## 7. 不足与局限

### 7.1 实验覆盖的局限
- 依赖函数级知识建模与依赖图构建质量。  
- 泛化能力未充分验证，实验集中在特定数据分析基准。

### 7.2 偏差风险与应用限制
- 对代码覆盖率和函数依赖提取准确性敏感。  
- 对非代码化领域知识或非结构化任务适用性有限。

## 8. 复现与延伸启发
- 关注函数依赖图构建、AST解析和知识绑定实现。  
- I/O语义标签抽取及路径检索逻辑是复现性能的关键。  
- 后续可探索跨领域泛化、动态更新依赖图、与其他RAG方法融合。

（完）
