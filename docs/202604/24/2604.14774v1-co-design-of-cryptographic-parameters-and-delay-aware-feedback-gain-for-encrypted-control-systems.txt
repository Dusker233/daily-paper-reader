Title: Co-Design of Cryptographic Parameters and Delay-Aware Feedback Gain for Encrypted Control Systems

URL Source: https://arxiv.org/pdf/2604.14774v1

Published Time: Fri, 17 Apr 2026 00:42:33 GMT

Number of Pages: 6

Markdown Content:
# Co-Design of Cryptographic Parameters and Delay-Aware Feedback Gain for Encrypted Control Systems 

## Yeongjun Jang 1†

> 1

ASRI, Department of Electrical and Computer Engineering, Seoul National University, Seoul, Korea (Tel: +82-2-880-9053; E-mail: jangyj0512@snu.ac.kr) 

Abstract: Encrypted control employs homomorphic encryption (HE) to protect both the computation and communication stages, making it a promising approach for secure networked control systems. Most existing results pre-design a controller in the plaintext domain and then implement it over encrypted data. However, this can be problematic because HE induces non-negligible communication and computation delays, which typically increase with the security level, potentially de-grading control performance and even destabilizing the closed-loop system. To address this issue, we propose a co-design framework for cryptographic parameters and delay-aware feedback gain. We characterize the encryption-induced delay as a function of the cryptographic parameters and derive a sufficient condition for the existence of a stabilizing delay-aware feedback gain, expressed as a finite set of linear matrix inequalities. This leads to a tractable outer-inner design procedure that searches over cryptographic parameters that satisfy a desired security level and, for each such parameter, seeks a stabilizing feedback gain. 

Keywords: Encrypted control, homomorphic encryption, security, privacy 

## 1. INTRODUCTION 

Networked control systems allow resource-limited de-vices to efficiently outsource computationally intensive tasks to remote servers [1]. However, this architecture requires the transmission of data that may contain sen-sitive information, giving rise to privacy concerns [2]. In particular, communication channels are vulnerable to eavesdropping, and the remote servers may be semi-honest, correctly executing the prescribed protocol while attempting to infer private information from the received data. The leaked information can be exploited to design cyberattacks capable of causing significant physical dam-age, as demonstrated by the incidents in [3, 4]. Recently, encrypted control has emerged as a promis-ing framework for enhancing the security of networked control systems [5-7]. By employing homomorphic en-cryption (HE), which enables arithmetic operations to be performed directly on encrypted data without decryp-tion, it protects both the communication and computa-tion stages. More specifically, sensor measurements are encrypted and transmitted to an untrusted remote server, where the control input is computed over encrypted data and sent back to the actuator for decryption (see Fig. 1). Most related works begin with a controller pre-designed in the plaintext domain and then incorporate HE, while preserving the original control performance [8-10]. This approach, however, may be problematic in practice. Encrypted control systems inevitably introduce non-negligible delay due to the transmission of cipher-texts, whose size is typically much larger than that of plaintexts, as well as the computational burden of encryp-tion, homomorphic evaluation, and decryption. Such de-lays may cause significant performance degradation and even destabilize the closed-loop system [11]. Although [12] presented a method for jointly designing the key          

> †Yeongjun Jang is the presenter of this paper.
> Plant side
> Network
> ZOH Plant Sample
> Ts
> Scale Quantize
> Dec Enc
> Encrypted Controller
> u(t)x(t)
> Dec (u[k])
> ue[k]x[k]
> lx[k]
> r
> k
> x[k]u[k]
> Continuous signal Discrete signal

Fig. 1 Configuration of encrypted control system. length of a cryptosystem and control laws achieving a desired security level and control performance, it did not account for encryption-induced delays. In this paper, we propose a co-design framework for cryptographic parameters and delay-aware feedback gain for encrypted control systems. To this end, we first char-acterize the encryption-induced delay as a function of the cryptographic parameters. Then, for a fixed crypto-graphic parameters, we derive a sufficient condition for the existence of a stabilizing delay-aware feedback gain, given by a finite set of linear matrix inequalities (LMIs). This leads to a tractable outer-inner design procedure that searches over cryptographic parameters satisfying a de-sired security level and, for each such parameter, seeks a stabilizing feedback gain. 

> arXiv:2604.14774v1 [eess.SY] 16 Apr 2026

Notation: Let Z, Z≥0, N, and R denote the sets of in-tegers, nonnegative integers, positive integers, and real numbers, respectively. 

## 2. PRELIMINARIES AND PROBLEM FORMULATION 

2.1. LWE based cryptosystem 

This section briefly reviews the LWE based cryptosys-tem of [13, 14], focusing on its multiplicative homomor-phic property and computational complexity. For a prime number q ∈ N, we consider Zq := 

Z ∩ [−q/ 2, q/ 2) as the space of plaintexts. The modulo operation that maps Z onto Zq is defined as 

a mod q := a −

 a + q/ 2

q



q ∈ Zq , ∀a ∈ Z,

which applies entrywise to vectors and matrices. The secret key sk ∈ ZNq , where N ∈ N is a power-of-two, is drawn uniformly at random from the ternary set 

{− 1, 0, 1}N . Only an entity with access to it can perform both encryption and decryption. The LWE based scheme supports matrix-vector mul-tiplication to be evaluated over encrypted data. Specifi-cally, it employs two distinct encryption algorithms, de-noted by Enc ′ and Enc , for the multiplier and multipli-cand, respectively: 

Enc ′ : Zq → Z(N +1) ×d(N +1)  

> q

=: C′,

Enc : Zq → ZN +1  

> q

=: C,

where d := ⌈log 2 q⌉. The corresponding decryption al-gorithm is denoted by Dec : C → Zq . With a slight abuse of notation, Enc ′, Enc , and Dec are applied entry-wise to vectors and matrices. Then, given M ∈ Zh×lq and 

m ∈ Zlq , there exists a homomorphic multiplication op-eration 1 ⊗ : ( C′)h×l × C l → C h such that [14] 

Dec (Enc ′(M ) ⊗ Enc (m)) ≈ M m mod q. (1) The approximation error in (1) is caused by the “er-rors” injected during encryption, which are fundamental to the security of the LWE based scheme. These errors are typically drawn from a zero mean discrete Gaussian distribution with standard deviation σ > 0. We refer to 

θ = ( N, q, σ ) as the cryptographic parameter set and de-note the corresponding security level by λ(θ). In general, the security level increases with N and σ/q [15], and can be estimated using a tool called the LWE estimator [16]. For the scalar case, i.e., when h = l = 1 , the computa-tional complexities of Enc ′, Enc , Dec , and ⊗ are summa-rized in Table 1 using the big-O notation. It can be seen that the complexities primarily depend on the parameters 

N and q, indicating an inherent tradeoff between security level and computational complexity.      

> 1For simplicity, we omit the dependence of ⊗on hand l.

Table 1 Asymptotic computational complexity of cryptographic operations in the scalar case ( h = l = 1 ).             

> Operation Enc ′Enc Dec ⊗
> Complexity O(N2d)O(N d )O(N d )O(N d log N)

2.2. Problem formulation 

Consider the continuous-time linear plant written by 

˙x(t) = Ax (t) + Bu (t), x(0) = xini , (2) where x(t) ∈ Rn is the state with the initial value 

xini ∈ Rn and u(t) ∈ Rm is the control input. To stabi-lize (2), suppose that a discrete-time static state feedback controller has been designed as 

u[k] = Kx [k], (3) where x[k] := x(kT s) is the state sampled with a sam-pling time Ts > 0 for k ∈ Z≥0, and K ∈ Rm×n is the feedback gain. Ideally, the signal u[k] is fed-back to the plant (2) through a zero-order hold (ZOH), as 

u(t) = u[k], kT s ≤ t < (k + 1) Ts.

Now, we consider a networked control system imple-mentation of (3) in which the control input is computed by a remote server residing in the network layer. In par-ticular, we consider a clock-driven sensor that samples the state periodically, whereas the controller/actuator are event-driven and computes/applies the control input as soon as the required data becomes available. To pro-tect the feedback gain and sampled state against eaves-dropping on the communication channels and the server, we integrate the LWE based scheme introduced in Sec-tion 2.1. The overall configuration is illustrated in Fig. 1. The detailed implementation is as follows. During the offline phase, the gain K is quantized using a scale factor 

1/s ≥ 1, encrypted as 

K = Enc ′

 K

s



mod q



, (4) and stored at the server. At each sampling instant t = kT s

of the online phase, the sensor quantizes the sampled state with a resolution r > 0, encrypts it, and transmits 

x[k] := Enc 

 x[k]

r



mod q



(5) to the server. The server then evaluates u[k] = K ⊗ x[k]

and transmits it back to the actuator, where it is decrypted and rescaled to obtain 

ue[k] := rs · Dec (u[k]) ≈ rs ·

 K

s

  x[k]

r



mod q, (6) where the approximation is due to (1). Indeed, ue[k] can be made arbitrarily close to u[k] by choosing 1/r and 1/s sufficiently large, thereby reducing the precision loss incurred by the rounding operations, t           

> kT s(k+ 1) Ts(k+ 2) Ts
> τ[k]τ[k+ 1]
> ue[k−1]
> ue[k]
> ue[k+ 1]

Fig. 2 Timing diagram of the zero-order held input u(t)

under the total delay τ [k] ∈ [0 , T s).and by choosing q sufficiently large to prevent overflow under the modulo operation. Since the focus of this paper is not on analyzing the effects of quantization or encryp-tion errors, we henceforth make the simplifying assump-tion that 

ue[k] ≡ u[k].

Analyses on the effects of such errors on the closed-loop stability can be found in [8, 17]. Recall that the gain K has been designed for the ideal delay-free case, i.e., when u[k] can be applied exactly at kT s. In the encrypted implementation, however, non-negligible delays arise from the transmission of cipher-texts, whose message size is substantially larger than that of plaintexts, as well as from the time required for en-cryption, decryption, and homomorphic multiplication. Formally, let τ [k] ∈ R denote the total time elapsed from the sampling of x[k] to the computation of ue[k]

at the actuator, which we refer to as the total delay at sampling instant k. For analytical simplicity, we impose the following assumption. 

Assumption 1: The total delay is constant across sampling instants, i.e., τ [k] = τθ for all k ∈ Z≥0, where 

τθ > 0 is a constant determined by the cryptographic pa-rameter set θ and is not assumed to be known a priori. When τθ ∈ [0 , T s), the actuator holds the previous in-put until the current one becomes available, as illustrated in Fig. 2. Specifically, the control input is applied as 

u(t) = 

(

ue[k − 1] , kT s ≤ t < kT s + τθ ,ue[k], kT s + τθ ≤ t < (k + 1) Ts. (7) It is well known that such an input delay can degrade performance and even destabilize the closed-loop system [11, 18]. Motivated by this issue, we propose a co-design framework for the cryptographic parameter set θ and the feedback gain K. Specifically, our goal is to jointly de-termine (θ, K ) such that the following requirements are satisfied: 

(R1) Security requirement : For a desired security level 

λ∗, the condition λ(θ) ≥ λ∗ holds. 

(R2) Delay requirement : The total delay satisfies τθ ∈

[0 , T s).

(R3) Stability requirement : The closed-loop system (2) with the delayed input (7) is stable. As a first step toward the co-design framework, we re-strict our attention to static state-feedback controllers of the form (3). We also note that controller design meth-ods under time-varying delays or delays exceeding the sampling time have been studied in the literature [11, 19]. Extending the proposed framework to incorporate such methods is left for future work. 

## 3. MAIN RESULTS 

3.1. Quantification of the total delay 

We begin by quantifying the total delay τθ as a func-tion of the cryptographic parameter set θ. Under Assump-tion 1, the total delay can be decomposed as 

τθ = τ Comm  

> θ

+ τ Enc  

> θ

+ τ Dec  

> θ

+ τ ⊗ 

> θ

, (8) where the terms on the right-hand-side, in order, represent the delays due to communication, sensor-side encryption, actuator-side decryption, and server-side homomorphic multiplication. Note that the time required for the encryp-tion and transmission of K are excluded from (8), since they are performed during the offline phase. We adopt the following standard communication model [19, 20] to obtain an explicit bound on the com-munication delay. 

Assumption 2: The communication link between the plant and the server has a fixed transmission rate R > 0

and a fixed propagation delay δ ≥ 0.Here, R denotes the number of bits transmitted per unit time, measured in bits per second, and δ denotes the prop-agation delay in seconds, which depends primarily on the distance between the plant and the server. Accordingly, transmitting a message of length L bits requires at most 

L/R + δ seconds. Based on this model, the following lemma characterizes τ Comm  

> θ

.

Lemma 1: Under Assumption 2, the communication delay is upper bounded as 

τ Comm  

> θ

≤ (n + m)( N + 1) dR + 2 δ. (9) 

Proof: The communication delay consists of the trans-mission of x[k] ∈ C n and u[k] ∈ C m. Since each cipher-text in C = ZN +1  

> q

contains N + 1 entries in Zq , and each entry requires at most d = ⌈log 2 q⌉ bits, transmitting x[k]

and u[k] requires at most 

n(N + 1) dR + δ and m(N + 1) dR + δ

seconds, respectively. Summing these two terms yields (9), and this concludes the proof. We next quantify the remaining components of τθ ,namely, the delays due to encryption, decryption, and homomorphic multiplication. The following lemma pro-vides an explicit upper bound. 

Lemma 2: There exist positive constants cEnc , cDec ,and c⊗ such that 

τ Enc  

> θ

+ τ Dec  

> θ

+ τ ⊗

> θ

≤ N d (cEnc n + cDec m + c⊗mn log N ) . (10) 

Proof: By Table 1, there exist positive constants cEnc ,

cDec , and c⊗, independent of θ, such that the execution times of one scalar encryption, decryption, and homo-morphic multiplication are upper bounded by cEnc N d ,

cDec N d , and c⊗dN log N , respectively. Since x[k] ∈ Rn

and u[k] ∈ C m are encrypted and decrypted entrywise, respectively, we have 

τ Enc  

> θ

≤ cEnc nN d, τ Dec  

> θ

≤ cDec mN d. 

Moreover, computing K ⊗ x[k] requires mn scalar ho-momorphic multiplications, and hence 

τ ⊗ 

> θ

≤ c⊗mn N d log N. 

Summing the above inequalities yields (10), and this con-cludes the proof. The constants cEnc , cDec , and c⊗ may depend on the hardware specification and software implementation, such as the choice of HE library and the specific algo-rithms used to realize each cryptographic operation. Although the total delay τθ is unknown under As-sumption 1, Lemmas 1 and 2 provide the following upper bound as a function of the cryptographic parameter θ:

τθ ≤ (n + m)( N + 1) dR + 2 δ

+ N d  cEnc n + cDec m + c⊗mn log N  =: ¯ τθ . (11) 

3.2. Delay-aware gain design 

In what follows, for a fixed cryptographic parameter θ

and feedback gain K, we derive a sampled-data represen-tation of the closed-loop system and analyze its stability. By exactly discretizing the closed-loop system (2) un-der the delayed input (7) at the sampling instants and us-ing the relation ue[k] = u[k] = Kx [k], we obtain 

x[k + 1] = Φ x[k]+Γ 0(τθ )ue[k]+Γ 1(τθ )ue[k − 1] (12) 

= (Φ + Γ 0(τθ )K) x[k]+Γ 1(τθ )Kx [k − 1] ,

where Φ = exp( AT s) and 

Γ0(τθ ) := 

Z Ts

> τθ

exp( A(Ts − h)) dh B, 

Γ1(τθ ) := 

Z τθ

> 0

exp( A(Ts − h)) dh B. 

By defining ξ[k] := [ x[k]⊤, x [k − 1] ⊤]⊤ ∈ R2n as the augmented state, we further obtain 

ξ[k + 1] = ¯A(τθ , K ) ξ[k], (13) where 

¯A(τθ , K ) := 

Φ + Γ 0(τθ )K Γ1(τθ )KIn 0n×n



. (14) Consequently, the stability requirement (R3) reduces to verifying the Schur stability of all matrices in the set 

Ψ( θ, K ) := { ¯A(τθ , K ) | τθ ∈ [0 , ¯τθ ]}.

Algorithm 1 Co-design algorithm 

Input: Model parameters (A, B ), desired security level 

λ⋆, and sampling time Ts

Output: A pair (θ, K ) satisfying requirements (R1)– (R3)  

> 1:

Construct a candidate set Θ of cryptographic param-eters such that λ(θ) ≥ λ∗ for all θ ∈ Θ 

> 2:

for θ ∈ Θ do  

> 3:

Compute ¯τθ in (11)  

> 4:

if ¯τθ ≥ Ts then  

> 5:

Continue  

> 6:

else  

> 7:

Construct a finite candidate set of gains K 

> 8:

for K ∈ K do  

> 9:

if (19) is feasible for some P ≻ 0 then  

> 10:

Return (θ, K ) 

> 11:

end if  

> 12:

end for  

> 13:

end if  

> 14:

end for 

Since this requires checking infinitely many matrices, we derive a tractable sufficient condition in the sequel, ex-pressed in terms of a finite set of LMIs. By the Jordan form representation of A, the matrix 

Γ1(τθ ) can be expressed as [11, Appendix B] 

Γ1(τθ ) = 

> ν

X

> i=1

αi(τθ )Gi, ∀τθ ∈ [0 , ¯τθ ] (15) for some ν ∈ N, where αi : [0 , ¯τθ ] → R are continuous scalar functions and Gi ∈ Rn×m are constant matrices for i = 1 , . . . , ν . Then, with Γ := Γ 0(τθ ) + Γ 1(τθ ) = 

R Ts 

> 0

eA(Ts−h)dhB , which is independent of τθ , we can decompose ¯A(τθ , K ) as 

¯A(τθ , K ) = 

Φ + Γ K 0n×n

In 0n×n

| {z }

> =: M0(K)

+

> ν

X

> i=1

αi(τθ )Mi(K),

where 

Mi(K) := 

−GiK GiK

0n×n 0n×n



, i = 1 , . . . , ν. (16) For each i = 1 , . . . , ν , let 

α− 

> i

:= min    

> τθ∈[0 ,¯τθ]

αi(τθ ), α+ 

> i

:= max    

> τθ∈[0 ,¯τθ]

αi(τθ ). (17) Then, for each η = ( η1, . . . , η ν ) ∈ {− , +}ν , we define 

Mη (θ, K ) := M0(K) + 

> ν

X

> i=1

αηi 

> i

Mi(K). (18) 

Proposition 1: Suppose that a cryptographic parame-ter set θ and a feedback gain K ∈ Rm×n are given. Under Assumptions 1 and 2, if there exists a symmetric matrix 

P ≻ 0 such that 

Mη (θ, K )⊤P M η (θ, K )−P ≺ 0, ∀η ∈ {− , +}ν , (19) then every matrix in Ψ( θ, K ) is Schur stable. 

Proof: By definition, αi(τθ ) ∈ [α− 

> i

, α + 

> i

] for all i =1, . . . , ν . Hence, every matrix in Ψ( θ, K ) belongs to the convex hull of the matrices Mη (θ, K ), i.e., 

Ψ( θ, K ) ⊆ co {Mη (θ, K ) | η ∈ {− , +}ν }. (20) The conclusion then follows from the standard common quadratic Lyapunov argument for convex polytopes; see the proof of [11, Theorem 4.3.1]. 

3.3. Co-design of cryptographic parameters and gain 

The proposed co-design method for the cryptographic parameter set θ and the feedback gain K is formulated as an outer–inner procedure, summarized in Algorithm 1. In the outer loop, the cryptographic parameter set θ is selected from a discrete candidate set, so as to satisfy the security and the delay requirements (R1) and (R2). The candidate set is discrete because the secret key length N

and the modulus q are restricted to powers-of-two and prime numbers, respectively; in practice, such candidates are often precomputed in cryptographic libraries or can be chosen according to standard guidelines [15]. For each fixed candidate θ, the inner loop checks the feasibility of the LMIs in (19) for each feedback gain in a finite candidate set K. The set K is introduced for com-putational tractability and can, for example, be chosen as the set of gains designed for the delay-free case. Conse-quently, any pair (θ, K ) returned by the algorithm sat-isfies the requirements (R1)–(R3). In the present work, we adopt this feasibility based design for simplicity. If an additional performance criterion is imposed, the same framework can be extended to select a more desirable gain among the feasible candidates. Regarding practical applicability, the LMIs in (19) can be efficiently checked using standard semidefinite pro-gramming solvers given K. In principle, one may also attempt to optimize K and P simultaneously. However, the resulting matrix inequalities become nonconvex, and thus, developing suitable relaxations or alternative re-formulations that enable a tractable simultaneous search over K and P is left for future work. 

## 4. SIMULATION RESULTS 

This section provides simulation results to demon-strate the effectiveness of the proposed method through a numerical example. Consider the plant (2) given as 

A =

0 10 0



, B =

01



with the initial condition xini = [1 , 0] ⊤ and sampling time Ts = 25 ms . We empirically set the communication transmission rate to R = 250 Mbit /s and the propagation delay to δ = 2 ms .The encrypted controller was implemented using the Lattigo library [21] with the Brakerski-Gentry-Vaikuntanathan (BGV) variant [22] of the LWE based scheme. The target security level was set to λ∗ = 128 bits Table 2 Candidate parameter sets satisfying (R1) and corresponding upper bounds on total delay. 

log 2 N log 2 q σ ¯τθ [ms ]

θ1 12 109 3.2 19.150 

θ2 13 218 3.2 80.810 

θ3 14 438 3.2 304.225 [15], and three candidate cryptographic parameter sets satisfying the security requirement were selected, which are specified in Table 2. For each candidate parameter set, the upper bound on the computation delay was es-timated as the worst-case value among 100 runtime mea-surements. Then, ¯τθ was obtained by adding this worst-case computation delay to the upper bound on the com-munication delay, as in (11). Among the three candidates, it can be found from Ta-ble 2 that only θ1 satisfies (R2). Hence, for θ1, we de-signed two candidate feedback gains 

K1 = −960 −50  , K2 = −72 −16 .1 ,

via pole placement for the delay-free case. Precisely, K1

and K2 were chosen so that the matrices ¯A(0 , K 1) and 

¯A(0 , K 2) have poles at (0 .20 , 0.25) and (0 .775 , 0.800) ,respectively, thus stabilizing the delay-free closed-loop system. We then tested the LMIs in (19) using cvxpy .While no feasible solution was found for K1, the LMIs were feasible for K2 with one feasible solution given by 

P =



0.740 0.073 −0.101 −0.019 0.073 0.052 −0.004 −0.032 

−0.101 −0.004 0.176 0.013 

−0.019 −0.032 0.013 0.032 

 .

We evaluated the closed-loop performance using the gains K1 and K2 for four different values of total delay 

τθ ; Fig. 3 shows the corresponding state norm ∥x(t)∥.The results show that K2 stabilizes the closed-loop sys-tem for all considered total delays, whereas the perfor-mance of the delay-unaware gain K1 deteriorates as τθ

increases. In particular, the closed-loop system becomes unstable when τθ = 3¯ τθ /4. These results demonstrate both the effectiveness and necessity of the proposed co-design framework. 

## ACKNOWLEDGMENT 

This work was supported by the National Research Foundation of Korea (NRF) grant funded by the Korea government (MSIT) (No. RS-2024-00353032). 

## REFERENCES 

[1] X. M. Zhang et al., “Networked control systems: A survey of trends and techniques,” IEEE/CAA J. Au-tom. Sinica , vol. 7, no. 1, pp. 1–17, 2019. [2] S. C. Anand, A. T. Nguyen, A. M. H. Teixeira, H. Sandberg, and K. H. Johansson, “Quantifying security for networked control systems: A review,” 2025, arxiv:2510.18645 .0 0.5 1 1.5 2 2.5

0

10 

20 

> ∥x(t)∥

(a) τθ = 0 

K1

K2

0 0.5 1 1.5 2 2.5

0

10 

20 

> ∥x(t)∥

(b) τθ = ¯ τθ /4

0 0.5 1 1.5 2 2.5

0

10 

20 

> ∥x(t)∥

(c) τθ = ¯ τθ /2

0 0.5 1 1.5 2 2.5

0

4

8

·10 4

t

> ∥x(t)∥

(d) τθ = 3¯ τθ /4

Fig. 3 Closed-loop state norm ∥x(t)∥ using the gains K1

and K2 for four values of the total delay τθ .[3] K. E. Hemsley and E. Fisher, “History of indus-trial control system cyber incidents,” Idaho National Lab., Idaho Falls, ID, USA, Tech. Rep., INL/CON-18-44411-Revision-2, 2018. [4] J. Slowik, “CRASHOVERRIDE: Reassessing the 2016 Ukraine electric power event as a protection-focused attack,” Dragos, Inc., Hanover, MD, USA, 2019. [Online]. Available: https://pylos.co/wp-content/uploads/2021/02/crashoverride.pdf [5] K. Kogiso and T. Fujita, “Cyber-security enhance-ment of networked control systems using homomor-phic encryption,” in Proc. 54th IEEE Conf. Decision Control , 2015, pp. 6836–6843. [6] J. Kim, D. Kim, Y. Song, H. Shim, H. Sandberg, and K. H. Johansson, “Comparison of encrypted con-trol approaches and tutorial on dynamic systems us-ing Learning With Errors-based homomorphic en-cryption,” Annu. Rev. Control , vol. 54, pp. 200–218, 2022. [7] N. Schl¨ uter, P. Binfet, and M. Schulze Darup, “A brief survey on encrypted control: From the first to the second generation and beyond,” Annu. Rev. Con-trol , vol. 56, 2023, Art. no. 100913. [8] J. Kim, H. Shim, and K. Han, “Dynamic controller that operates over homomorphically encrypted data for infinite time horizon,” IEEE Trans. Autom. Con-trol , vol. 68, no. 2, pp. 660–672, 2023. [9] K. Teranishi, T. Sadamoto, and K. Kogiso, “Input-output history feedback controller for encrypted control with leveled fully homomorphic encryp-tion,” IEEE Trans. Control Netw. Syst. , vol. 11, no. 1, pp. 271–283, 2024. [10] S. Schlor and F. Allg¨ ower, “Bootstrapping guar-antees: Stability and performance analysis for dy-namic encrypted control,” IEEE Control Syst. Lett. ,vol. 8, pp. 2235–2240, 2024. [11] M. B. G. Cloosterman, “Control over communica-tion networks: Modeling, analysis, and synthesis,” Ph.D. dissertation, Dept. Mech. Eng., Eindhoven Univ. Technol., Eindhoven, Netherlands, 2008. [12] K. Teranishi, T. Sadamoto, A. Chakrabortty, and K. Kogiso, “Designing optimal key lengths and control laws for encrypted control systems based on sample identifying complexity and deciphering time,” IEEE Trans. Autom. Control , vol 68, no. 4, pp. 2183-2198, 2023. [13] O. Regev, “On lattices, learning with errors, random linear codes, and cryptography,” J. ACM , vol. 56, no. 6, 2009, Art. no. 34. [14] I. Chillotti, N. Gama, M. Georgieva, and M. Iz-abach´ ene, “Faster fully homomorphic encryption: Bootstrapping in less than 0.1 seconds,” in Proc. Int. Conf. Theory Appl. Cryptol. Inf. Secur. , 2016, pp. 3– 33. [15] M. R. Albrecht et al., “Homomorphic encryp-tion standard,” in Protecting Privacy Through Ho-momorphic Encryption , K. Lauter, W. Dai, and K. Laine, Eds. Cham, Switzerland: Springer, 2021, pp. 31–62. [16] M. R. Albrecht, R. Player, and S. Scott, “On the concrete hardness of learning with errors,” in J. Math. Cryptol. , vol. 9, no. 3, pp. 169–203, 2015. [17] Y. Jang, J. Lee, S. Min, H. Kwak, J. Kim, and Y. Song, “Ring-LWE-based encrypted controller with unlimited number of recursive multiplications and effect of error growth,” IEEE Trans. Control Netw. Syst. , vol. 12, no. 4, pp. 2604–2616, 2025. [18] W. Zhang, M. S. Branicky, and S. M. Phillips, “Sta-bility of networked control systems,” IEEE Control Syst. Mag. , vol. 21, no. 1, pp. 84–99, 2001. [19] K. Liu, A. Selivanov, and E. Fridman, “Survey on time-delay approach to networked control,” Annu. Rev. Control , vol. 48, pp. 57–79, 2019. [20] J. F. Kurose, Computer Networking: A Top-Down Approach , 6th ed, Boston, MA, USA: Pearson, 2013. [21] Lattigo v6. Online: https://github.com/ tuneinsight/lattigo , Aug. 2024, EPFL-LDS and Tune Insight SA. [22] Z. Brakerski, C. Gentry, and V. Vaikuntanathan, “(Leveled) fully homomorphic encryption with-out bootstrapping,” ACM Trans. Comput. Theory ,vol. 6, no. 3, pp. 1–36, 2014.