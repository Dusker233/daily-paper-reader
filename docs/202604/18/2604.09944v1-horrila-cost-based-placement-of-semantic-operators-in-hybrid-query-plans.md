---
title: "Horrila: Cost-Based Placement of Semantic Operators in Hybrid Query Plans"
title_zh: Horrila：基于成本的混合查询计划中语义操作符的放置
authors: "Qiuyang Mang, Yufan Xiang, Hangrui Zhou, Runyuan He, Jiaxiang Yu, Hanchen Li, Aditya Parameswaran, Alvin Cheung"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09944v1"
tags: ["query:q7"]
score: 6.8
evidence: 结合了语义运算符与LLM，涉及流量分析和网络理解
tldr: 本文提出了Horrila，一个针对混合查询计划中语义算子的位置优化器。通过考虑LLM调用和传统关系操作的成本权衡，Horrila通过两个等效保持重写，将混合查询规划简化为语义过滤器的放置问题。实验表明，Horrila在44个语义SQL查询中，能够显著提高查询速度和降低成本，同时保持高输出质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09944v1/fig-001.webp\", \"caption\": \"Figure 4: Illustrating the DP cost model from Algorithm 2 at join node 𝑢. Starting from the input plan (left) where both filters are pushed down, the DP considers pulling SF2 up to 𝑢. (1) Distribute SF1 to 𝐿, leaving 𝑅 unfiltered. (2) Add relational cost of 𝑢, scaled by 𝛼 and reduced by 𝑠1. (3) Place SF2 at 𝑢; the join eliminates unmatched tuples, so the number of distinct 𝑅-tuples at 𝑢.\", \"page\": 6, \"index\": 1, \"width\": 1002, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2604.09944v1/fig-002.webp\", \"caption\": \"Figure 7: Sensitivity to 𝛼 . Left: LLM calls and latency as 𝛼 varies. Right: plan trees under three settings. (a) Large 𝛼 : both filters pushed down, 11 calls but 28.2s. (b) Moderate 𝛼 : SF1 pulled above the top join, 7 calls at 16.7s. (c) Small 𝛼 : both filters pulled up,\", \"page\": 11, \"index\": 2, \"width\": 1003, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.09944v1/fig-003.webp\", \"caption\": \"Figure 1: Three hybrid query plans for Listing 1 (books: 1K rows, reviews: 5K rows). (a) Push-down: filters applied before the\", \"page\": 2, \"index\": 3, \"width\": 990, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.09944v1/fig-004.webp\", \"caption\": \"Figure 5: Hybrid query benchmark: operator composition per query. Each stacked bar shows the count of SP, SF, SJ, 𝜎 , and 1\", \"page\": 9, \"index\": 4, \"width\": 1000, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.09944v1/fig-005.webp\", \"caption\": \"Figure 6: Per-query latency (s) and LLM cost ($) on log scale across the 30-query hybrid benchmark. Subfigures (a, c) show latency; subfigures (b, d) show cost (hatched bars). Bars are shown only for methods with F1 ≥ 0.4 on that query; an ×marks\", \"page\": 9, \"index\": 5, \"width\": 1053, \"height\": 783}]"
motivation: 现有系统在语义算子位置优化上要么忽略要么采用简单启发式方法，未充分考虑成本权衡。
method: Horrila通过基于动态规划的成本模型，优化语义过滤器的位置以平衡LLM调用和关系操作成本。
result: Horrila在五个模式和两个基准测试中的44个查询上，达到了最高1.5倍的加速和4.29倍的成本降低。
conclusion: Horrila在显著降低成本的同时，保持了在六个公开系统中最高的准确性。
---

## 摘要
近年来，数据库系统引入了语义操作符，这些操作符利用大语言模型（LLMs）通过自然语言谓词对结构化数据进行过滤、连接和投影。在实践中，这些操作符通常与传统关系操作符（如等值连接）结合使用，从而生成混合查询计划，其执行成本取决于昂贵的LLM调用和常规数据库处理。一个关键的优化问题是，在查询计划中相对于关系操作符放置每个语义操作符的位置：将其提前放置可以减少后续操作符处理的数据量，但需要更多的LLM调用；将其延后放置可以通过去重减少LLM调用，但会迫使关系操作符处理更大的中间数据。现有系统要么忽略这个放置问题，要么仅应用简单启发式方法而未考虑完整的成本权衡。我们提出了Horrila，一种针对混合语义-关系查询的计划级优化器。Horrila通过两个等价保持的重写将混合查询规划简化为语义过滤器的放置问题。我们证明，在函数缓存下，将所有语义过滤器延迟到最晚的位置可以最小化LLM调用次数，但在复杂的多表查询中，这可能导致关系处理成本占主导。为平衡LLM成本与关系成本，Horrila使用基于动态规划的成本模型，找到使两者加权和最小化的放置方案。在五个模式和两个基准上的44条语义SQL查询中，Horrila实现了最高1.5倍的加速和4.29倍的成本降低，同时保持高输出质量：相较于未优化基线，平均F1为0.85；在SemBench的人类标注参考下，平均F1为0.84。总体而言，Horrila在显著降低成本的同时，在六个公开可用系统中保持了最高精度。

## Abstract
Recent database systems have introduced semantic operators that leverage large language models (LLMs) to filter, join, and project over structured data using natural language predicates. In practice, these operators are combined with traditional relational operators, e.g., equi-joins, producing hybrid query plans whose execution cost depends on both expensive LLM calls and conventional database processing. A key optimization question is where to place each semantic operator relative to the relational operators in the plan: placing them earlier reduces the data that subsequent operators process, but requires more LLM calls; placing them later reduces LLM calls through deduplication, but forces relational operators to process larger intermediate data. Existing systems either ignore this placement question or apply simple heuristics without considering the full cost trade-off. We present Horrila, a plan-level optimizer for hybrid semantic-relational queries. Horrila reduces hybrid query planning to semantic filter placement via two equivalence-preserving rewrites. We prove that deferring all semantic filters to the latest possible position minimizes LLM invocations under function caching, but show that this can cause relational processing costs to dominate on complex multi-table queries. To balance LLM cost against relational cost, Horrila uses a dynamic-programming-based cost model that finds the placement minimizing their weighted sum. On 44 semantic SQL queries across five schemas and two benchmarks, Horrila achieves up to 1.5$\times$ speedup and 4.29$\times$ cost reduction while maintaining high output quality: an average F1 of 0.85 against the unoptimized baseline and 0.84 against human-annotated ground truth on SemBench. Overall, Horrila achieves a significant cost reduction while preserving the highest accuracy among six publicly available systems.