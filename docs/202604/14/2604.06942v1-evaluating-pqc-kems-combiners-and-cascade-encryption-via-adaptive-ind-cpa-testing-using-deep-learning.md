---
title: "Evaluating PQC KEMs, Combiners, and Cascade Encryption via Adaptive IND-CPA Testing Using Deep Learning"
title_zh: 通过使用深度学习的自适应IND-CPA测试评估PQC KEMs、组合器和级联加密
authors: "Simon Calderon, Niklas Johansson, Onur Günlü"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06942v1"
tags: ["query:q3"]
score: 9.0
evidence: 深度学习用于密文分类
tldr: 本文提出使用深度学习对公钥加密及后量子密码学（PQC）密钥封装机制（KEM）、组合加密及级联对称加密进行自适应IND-CPA可区分性测试，通过将加密密文建模为二分类问题并训练神经网络，验证各种混合加密方案的不可区分性，为实现数据驱动的加密实现验证提供了一种实用方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06942v1/fig-001.webp\", \"caption\": \"Table 5: The test validation accuracies and p-values, within the small network setup, for tests on cascade symmetric encryption. The rows correspond to the inner cipher, the columns to the outer cipher.\", \"page\": 15, \"index\": 1, \"width\": 600, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-002.webp\", \"caption\": \"Table 6: The test validation accuracies and p-values, within the big network setup, for tests on cascade symmetric encryption. The rows correspond to the inner cipher, the columns to the outer cipher.\", \"page\": 15, \"index\": 2, \"width\": 600, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-003.webp\", \"caption\": \"Fig. 2: The validation accuracies computed on the validation dataset vs. training epochs for hybridized KEMs.\", \"page\": 14, \"index\": 3, \"width\": 688, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-004.webp\", \"caption\": \"Table 4: Accuracies and p-values for the application of Algorithm 2 to KEMs combined with asymmetric ciphers using combiners of the form k = F (k1, c) ⊕ F (k2, c).\", \"page\": 13, \"index\": 4, \"width\": 571, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-005.webp\", \"caption\": \"Table 3: Accuracies and p-values for the application of Algorithm 1 to single (i.e., non-hybridized) algorithms.\", \"page\": 12, \"index\": 5, \"width\": 613, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-006.webp\", \"caption\": \"Fig. 1: The validation accuracies plotted over training epochs for the KEMs encryption scenario. The validation accuracy is computed on the validation dataset.\", \"page\": 12, \"index\": 6, \"width\": 688, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.06942v1/fig-007.webp\", \"caption\": \"Fig. 3: The validation accuracies plotted over training epochs for the cascade encryption scenario. The validation accuracy is computed on the validation dataset.\", \"page\": 16, \"index\": 7, \"width\": 688, \"height\": 475}]"
motivation: 在混合PQC环境中，验证密文不可区分性具有实际挑战，需要可行的经验验证方法。
method: 将IND-CPA游戏建模为二分类问题，使用深度神经网络训练密文数据以评估可区分性。
result: 实验表明PQC算法、KEM组合及级联加密在所测条件下均未表现出显著可区分性。
conclusion: 深度学习可作为一种自适应、实用且通用的经验工具，用于验证加密方案的不可区分性，补充理论安全分析。
---

## 摘要
确保密文不可区分性是密码学安全性的基础，但在实际实现和混合设置中验证这一特性存在实际挑战。过渡到后量子密码学（PQC），其混合构造结合了经典和抗量子原语，使得经验验证方法变得越来越重要。通过将IND-CPA博弈建模为二分类任务，并使用BCE损失对带标签的密文数据进行训练，我们研究了深度神经网络（DNN）判别器在密文不可区分性上的表现。我们将该方法应用于PQC KEMs，特别测试了用于构建如ML-KEM、BIKE和HQC等示例的公钥加密（PKE）方案。此外，提出了这一DNN建模的一个新扩展，用于经验性地测试混合KEMs的可区分性。我们在PQC KEMs与普通RSA、RSA-OAEP及明文的组合上实现并测试了该方法。最后，通过将DNN IND-CPA分类框架应用于级联对称加密，展示了方法的普适性，其中我们测试了AES-CTR、AES-CBC、AES-ECB、ChaCha20和DES-ECB的组合。在我们关于PQC算法、KEM组合器和级联加密的实验中，没有任何算法或算法组合表现出显著优势（双边二项检验，显著性水平$α = 0.01$），这一结果与理论保证一致，即包括至少一个IND-CPA安全组件的混合方案能够保持不可区分性，并且在考虑的DNN对手模型下没有可被利用的模式。这些实验展示了使用深度学习作为自适应、实用且多功能的经验估算工具，在更一般的IND-CPA设置中评估不可区分性的潜力，能够为实现和组合的验证提供数据驱动的支持，补充分析安全性分析。

## Abstract
Ensuring ciphertext indistinguishability is fundamental to cryptographic security, but empirically validating this property in real implementations and hybrid settings presents practical challenges. The transition to post-quantum cryptography (PQC), with its hybrid constructions combining classical and quantum-resistant primitives, makes empirical validation approaches increasingly valuable. By modeling IND-CPA games as binary classification tasks and training on labeled ciphertext data with BCE loss, we study deep neural network (DNN) distinguishers for ciphertext indistinguishability. We apply this methodology to PQC KEMs. We specifically test the public-key encryption (PKE) schemes used to construct examples such as ML-KEM, BIKE, and HQC. Moreover, a novel extension of this DNN modeling for empirical distinguishability testing of hybrid KEMs is presented. We implement and test this on combinations of PQC KEMs with plain RSA, RSA-OAEP, and plaintext. Finally, methodological generality is illustrated by applying the DNN IND-CPA classification framework to cascade symmetric encryption, where we test combinations of AES-CTR, AES-CBC, AES-ECB, ChaCha20, and DES-ECB. In our experiments on PQC algorithms, KEM combiners, and cascade encryption, no algorithm or combination of algorithms demonstrates a significant advantage (two-sided binomial test, significance level $α= 0.01$), consistent with theoretical guarantees that hybrids including at least one IND-CPA-secure component preserve indistinguishability, and with the absence of exploitable patterns under the considered DNN adversary model. These illustrate the potential of using deep learning as an adaptive, practical, and versatile empirical estimator for indistinguishability in more general IND-CPA settings, allowing data-driven validation of implementations and compositions and complementing the analytical security analysis.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义
- **研究动机**：在密码学中，密文不可区分性（IND-CPA）是安全性的核心要求。然而，在实际实现和混合加密环境中（如后量子密码学（PQC）与经典算法混合），验证这一特性存在实际困难，尤其是组合加密、KEM组合器和级联对称加密的情境。  
- **背景**：
  - PQC算法旨在抵抗量子计算攻击，如Shor算法对RSA和Diffie-Hellman的威胁。
  - 混合加密方法通过组合多种算法，确保只要至少一个组件安全，整体方案即保持安全。
  - 近年来机器学习被应用于密码分析，尤其是用神经网络建模IND-CPA攻击进行密文区分。

## 2. 方法论
- **核心思想**：
  - 将IND-CPA博弈建模为二分类任务，训练深度神经网络（DNN）区分不同类别的密文。
  - 对密文数据使用**二元交叉熵（BCE）损失函数**训练网络，通过训练结果评估密文可区分性。
- **关键技术与算法**：
  - **算法 1**：非混合KEM或级联对称加密的IND-CPA测试
    1. 构建两类明文（随机与固定明文，例如全零）。
    2. 加密生成对应密文并标记类别。
    3. 用DNN训练，BCE作为损失函数，随机梯度下降优化。
    4. 测试网络对密文分类精度是否显著高于随机。
  - **算法 2**：混合KEM的IND-CPA测试（组合器形式 $k = F(k_1,c)\oplus F(k_2,c)$）
    1. 将两个KEM组件的密文拼接形成联合密文。
    2. 分类任务区分共享密钥相同或独立的情况。
    3. 利用DNN训练BCE损失评估是否存在可学习的区分模式。
  - **符号与公式**：
    - BCE 损失：  
      $$
      BCE(Y, \hat{Y}) = -\frac{1}{N}\sum_{i=1}^N [Y_i \log \hat{Y}_i + (1-Y_i)\log(1-\hat{Y}_i)]
      $$
    - 混合KEM XOR组合器公式：
      $$
      k = F(k_1, c) \oplus F(k_2, c)
      $$
- **方法扩展性**：该框架同时适用于KEM组合器和级联对称加密的实验验证。

## 3. 实验设计
- **算法与组合**：
  - 单一KEM：ML-KEM、HQC、BIKE
  - 混合KEM：上述KEM与RSA-OAEP、明文、未填充RSA组合
  - 级联对称加密：AES-CBC、AES-CTR、AES-ECB、ChaCha20、DES-ECB的不同排列组合
- **数据集与样本**：
  - 每类密文训练样本 500,000，验证样本 100,000，测试样本 100,000
  - 明文选择：随机和固定（如全零）
- **Benchmark / 对比**：
  - 使用随机猜测精度50%作为基线
  - 对比不同网络规模（Small Network vs Big Network）
  - 双边二项检验显著性水平 $\alpha=0.01$ 判断分类精度是否显著高于随机
- **指标**：分类准确率与p值，评估网络是否能区分密文类别

## 4. 资源与算力
- **硬件**：1块 Nvidia T4 GPU
- **训练时间**：
  - 每个网络训练时间约 1-3 小时
- **网络结构**：
  - Small Network: 24层中间层，每层100个节点
  - Big Network: 24层中间层，每层600个节点
  - 激活函数：中间层ReLU，输出层Sigmoid

## 5. 实验数量与充分性
- **实验规模**：
  - 单一KEM实验：3种算法 × 2网络规模
  - 混合KEM实验：3种KEM × 3种组合（RSA-OAEP、明文、Plain RSA） × 2网络规模
  - 级联对称加密实验：5种算法不同内外排列组合 × 2网络规模
- **充分性分析**：
  - 训练样本充足，验证和测试独立，避免过拟合
  - 使用统计显著性检验保证实验结果客观
  - 对比网络规模体现方法鲁棒性
  - 实验覆盖了典型IND-CPA安全与非安全算法组合，客观性较高

## 6. 主要结论与发现
- 单一KEM（ML-KEM、HQC、BIKE）均表现为IND-CPA安全，DNN分类精度接近50%
- 混合KEM组合器中，即使加入不安全组件（Plain RSA或明文），整体仍保持不可区分性
- 级联对称加密实验显示组合不削弱IND-CPA安全性
- DNN在所建模的假设下无法学习可利用模式，验证了理论安全性

## 7. 优点
- **方法创新**：
  - 将DNN用于经验性IND-CPA测试，提供自适应、可扩展的分析工具
  - 提出混合KEM组合器的DNN验证方法
- **实验设计合理**：
  - 大规模样本训练，使用两种网络规模检验鲁棒性
  - 严格统计方法验证结果显著性
- **通用性**：
  - 框架适用于PQC KEM、混合KEM及级联对称加密

## 8. 不足与局限
- DNN对手模型限制：
  - 无法访问加密Oracle或公钥信息，可能低估某些攻击风险
- 实验覆盖：
  - 仅测试部分混合组合与经典算法，不包含所有可能的加密场景
  - KEM组合器和级联加密仅用固定函数形式（如XOR或特定F函数）
- 泛化性：
  - 对未来其他类型的加密组合或复杂攻击模型需进一步验证
- 算力限制：
  - 仅使用单GPU训练，未探究大规模多GPU训练对结果的影响

（完）
