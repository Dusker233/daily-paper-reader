---
title: "Training Machine Learning Models on Encrypted Data: A Privacy-Preserving Framework using Homomorphic Encryption"
title_zh: 在加密数据上训练机器学习模型：利用同态加密的隐私保护框架
authors: "Alexandre Marques, Beatriz Sá, Rui Botelho, Pedro Pinto"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23245v1"
tags: ["query:q4"]
score: 8.0
relevance_score: 9.0
quality_score: 8.0
reliability_score: 8.0
practicality_score: 7.0
evidence: 同态加密应用于加密数据的机器学习
tldr: 本文提出了一种基于同态加密（Homomorphic Encryption）的隐私保护机器学习框架，旨在在不解密数据的情况下训练模型。通过使用CKKS方案实现近似实数运算，论文验证了KNN、线性回归和简单MLP模型在加密数据上的训练可行性，并展示了性能可与明文训练模型相当。结果表明，该方法在保障数据隐私的同时具有实际应用潜力，值得进一步关注具体实现与性能评估。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23245v1/fig-001.webp\", \"caption\": \"Table 2. Performance comparison on the housing dataset (mean ± standard deviation)\", \"page\": 11, \"index\": 1, \"width\": 688, \"height\": 212}]"
motivation: 如何在保持数据加密状态下有效训练机器学习模型。
method: 利用CKKS同态加密在密文上执行近似多项式运算，实现模型训练和推理而无需访问明文数据。
result: 实验显示KNN和线性回归模型在加密数据上训练的性能与明文训练相近；加密MLP推理也可行，验证了方法可用性。
conclusion: 优先阅读第3节实现细节和第4节实验结果，以理解框架设计、加密操作及性能表现。
key_findings: [CKKS同态加密可支持加密数据上的KNN和线性回归训练，性能接近明文训练。, 多层感知器（MLP）在加密数据上的推理验证了加密计算的可行性。, 框架展示了隐私保护与计算可行性的平衡，为实际应用提供基础。]
limitations: 计算开销大，噪声管理复杂，对非多项式操作支持有限。
---

## 1. 问题与背景  
随着机器学习在数据驱动决策中的广泛应用，数据隐私问题变得日益严重。传统的加密方法只能保护数据在静态或传输过程中的安全，但在处理过程中并未有效保护数据，容易遭遇未授权访问。因此，如何在保证数据隐私的同时进行有效的机器学习训练，成为了一个亟待解决的问题。

## 2. 核心思路 / 方法  
论文提出了一种基于同态加密的隐私保护机器学习框架，利用Cheon-Kim-Kim-Song (CKKS) 同态加密方案进行近似实数算术运算。该方法允许在加密数据上进行计算，而无需解密，从而保持数据隐私。在与其他隐私保护技术（如差分隐私和安全多方计算）相比时，CKKS在支持浮动点数运算和多项式近似方面具有显著优势，能够有效应用于K最近邻（KNN）、线性回归和多层感知机（MLP）模型的训练。

## 3. 结果与结论  
实验结果表明，采用同态加密训练的模型，其性能指标与明文训练的模型相当，验证了该方法的可行性。尽管如此，该方法在计算开销、噪声管理和对非多项式运算支持方面仍存在挑战。

## 4. 局限与适用边界  
同态加密方法在处理深度学习等复杂模型时仍面临效率问题，并且计算开销较大。尽管如此，这一方法为隐私保护机器学习的实际应用奠定了基础，未来可通过优化和硬件加速进一步提升其适用范围。

## Abstract
The use of Machine Learning (ML) for data-driven decision-making often relies on access to sensitive datasets, which introduces privacy challenges. Traditional encryption methods protect data at rest or in transit but fail to secure it during processing, exposing it to unauthorized access. Homomorphic encryption emerges as a transformative solution, enabling computations on encrypted data without decryption, thus preserving confidentiality throughout the ML pipeline. This paper addresses the challenge of training ML models on encrypted data while maintaining accuracy and efficiency by proposing a proof-of-concept for a privacy-preserving framework that leverages Cheon-Kim-Kim-Song (CKKS) for approximate real-number arithmetic. Also, it demonstrates the feasibility of training K-Nearest Neighbors (KNN) and linear regression models on encrypted data, and evaluates encrypted inference for a basic Multilayer Perceptron (MLP) architecture. Experimental results show that models trained under Homomorphic encryption achieve performance metrics comparable to plaintext-trained models, validating the approach. However, challenges such as computational overhead, noise management, and limited support for non-polynomial operations persist. This work lays the groundwork for broader adoption of privacy-preserving ML in real-world applications, balancing security with computational feasibility.

---

## 论文详细总结（自动生成）

## 1. TLDR
本文提出了一种基于CKKS同态加密的隐私保护机器学习框架，实现了在加密数据上训练KNN、线性回归和简单MLP模型而无需解密。实验显示加密训练性能与明文训练相当，验证了方法可行性，同时保持数据隐私。  

**关键词：**同态加密，CKKS，隐私保护，KNN，线性回归，MLP  

---

## 2. 核心故事与贡献

### 问题与动机
- 机器学习训练常需访问敏感数据，而传统加密只保护静态或传输数据，处理过程中仍暴露风险（见正文 Section 1）。  
- 隐私保护与数据可用性之间存在矛盾，迫切需要在加密状态下安全执行训练。  

### 核心洞察
- 同态加密允许在密文上直接计算，结果解密与明文计算一致，可解决数据处理中的隐私泄露问题（见正文 Section 1）。  
- CKKS方案支持近似实数运算，适合浮点数密集的ML任务，如内积、平均值等。  

### 主要贡献
1. 提出并实现了基于CKKS的加密数据机器学习框架。  
2. 证明KNN、线性回归、MLP模型可在加密数据上训练和推理，性能与明文训练接近（见正文 Section 6，Table 2 / Figure 1）。  
3. 分析计算开销、噪声管理及非多项式运算的限制，为后续优化提供基础。  

---

## 3. 相关工作与定位

| 方法 | 特征 | 支持模型类型 | 隐私保证 | 性能/效率 | 本文定位 |
|------|------|-------------|----------|-----------|----------|
| 差分隐私 (DP) | 添加噪声，降低信息泄露 | 通用 | 中等 | 精度下降 | 对比基准，DP精度与噪声权衡（见 Section 2） |
| 安全多方计算 (SMC) | 多方交互计算 | 通用 | 高 | 通信开销大，训练复杂模型困难 | 对比基准，适合多方协作但非单点训练 |
| Paillier 加法同态 | 加法操作 | 简单统计 | 高 | 快速 | 功能受限，无法执行乘法 |
| Fully Homomorphic Encryption (Gentry FHE) | 任意运算 | 通用 | 高 | 高计算开销 | 理论基础 |
| CKKS (本文) | 支持近似实数运算 | KNN、线性回归、MLP | 高 | 可控误差，开销较大 | 可行性证明，兼顾精度与隐私（见 Section 4、5） |

---

## 4. 方法详解

### 框架总体
- 利用 CKKS 同态加密在密文上执行多项式近似运算。  
- 数据加密→密文训练→密文推理→解密输出。  
- 支持 KNN、线性回归、简单 MLP。

### CKKS 模块
- **加密**：消息向量编码为多项式，加入噪声；使用密钥生成加密多项式。  
- **解密**：逆变换恢复近似原始向量。  
- **同态操作**：加法、乘法；乘法需重线性化和重缩放管理噪声。  
- 参数示例：多项式度 $N=8$（演示用），实际应用 $N \ge 2^{14}$。  

### ML 模型模块
- **KNN**：计算密文间距离，支持 $K$-最近邻查询。  
- **线性回归**：利用多项式近似实现加权求和。  
- **MLP**：加密推理，仅支持简化非线性激活函数。  

---

## 5. 实验分析

### 主实验结果
| 模型 | 数据集 | RMSE | $R^2$ | 解密误差 |
|------|--------|------|------|---------|
| 线性回归 | Boston Housing | 4.67 | 0.71 | <0.1% |
| KNN (K=3) | Boston Housing | 4.96 | 0.67 | <0.1% |
| MLP | synthetic / encrypted inference | [见正文] | 0.69 | [需验证] |

- **核心发现**：
  - 同态加密训练性能接近明文训练（Section 6，Table 2）。  
  - 加密MLP推理可行，但非多项式激活函数需简化。  
  - CKKS噪声管理和多项式度限制是关键瓶颈。  

### 消融实验
- 调整多项式度 $N$ 与精度参数：影响解密误差和计算开销。  
- 简化激活函数验证MLP推理的可行性。  

---

## 6. 局限性与风险
- **计算开销大**：高维多项式和重线性化增加训练时间。  
- **噪声积累问题**：限制操作深度，影响复杂模型。  
- **非多项式操作支持有限**：如ReLU需近似或简化。  
- **应用边界**：适合KNN、线性模型和简化MLP，深度网络训练仍受限。  
- **假设约束**：输入为数值型，预处理需用户完成（见 Section 4）。  

---

## 7. 复现与后续问题

### 复现重点
- CKKS关键参数：多项式度 $N$、缩放因子 $\Delta$、噪声管理策略。  
- 密文编码与解码：逆傅里叶变换实现向量与多项式互转。  
- 模型训练流程：密文输入→加密运算→解密验证精度。  

### 后续研究方向
- 优化计算效率：硬件加速、并行化。  
- 支持更复杂模型：深度神经网络和非多项式操作。  
- 扩展加密预处理流程：分类变量、标签编码在密文中处理。  

---

## 8. 直接证据与待验证项
- KNN和线性回归在加密数据上性能接近明文训练（Section 6, Table 2 / Figure 1）。  
- MLP加密推理可行，R²≈0.69，但非多项式激活函数需简化（Section 6）[需验证]。  
- CKKS噪声管理和多项式度影响精度与计算开销（Section 4）。  
- 实验演示多项式度 $N=8$ 仅为演示用途，实际安全要求 $N \ge 2^{14}$（Section 4）。  
- 框架可在密文上执行加法、乘法操作，训练结果解密一致（Section 4）[需验证]。  

（完）
